# SEVEN GODS 8/31完成披露版・固定バックログ

**作成日：2026-08-15　根拠：Opus実施「8/31完成披露版・総合クオリティ監査」＋CEO最終決定**

**このドキュメントの位置づけ**：2026-08-15時点で、8/31完成披露に向けた改善項目の探索は本監査をもって終了とする。以降は新しい改善項目を探さず、このバックログに固定された項目の実装・検証・本番準備に集中する。まだ実装は開始していない（本ドキュメントは記録のみ）。

---

## 1. 実施フェーズ（CEO承認済み）

### Phase 1（最優先・着手順）✅完了
1. 共鳴7/7・敵ターンバナーのfixed化
2. ミュート解除時のSE一斉再生バグ修正
3. 「開発環境 稼働中」バッジ・技術footer除去
4. 「成長の絆」の誤説明修正＋「未検証」表記削除
5. スコア内訳の英語キー日本語化
6. 神選択画面からホームへの復帰導線追加

### Phase 2 ✅完了
- カード画像のWebP化（201MB→数十MB規模の圧縮）
  - 元PNG56枚は`public/assets/cards/`にすべて保持（削除なし）
  - 512×768・WebP品質0.85で56枚を変換・追加、`cardArt.ts`の参照を全て`.webp`に切替（合計約6.15MB、削減率96.9%）
  - `TutorialOverlay.tsx`・`CardView.tsx`・`RewardOverlay.tsx`・`DeckBuilderScreen.tsx`の4ファイルに`loading="lazy" decoding="async"`を追加（DeckBuilder同時デコード数のピーク抑制）
  - デコードメモリ推定：DeckBuilder最大同時32枚が約192MB→約48MBに削減、OOMリスクを直接緩和
  - 実機確認済み（DeckBuilder・手札・RewardOverlay・TutorialOverlay、コンソールエラー0件）

### Phase 3
- OTOMO進化バナーの新設 ✅完了（共鳴バーストとの時間差表示に修正済み、実機確認済み）
- OTOMO形態効果・共鳴効果の文章表示 ✅完了（決定86。案A＝OTOMO側は童子時効果のみ・神側`resonanceEffects`は見出し直下に1回表示をCEO承認。新規`otomoEffectText.ts`で`damage`/`block`/`heal`/`score`/`buff`/`debuff`/`draw`/`gainAp`を全対応。375px／390px／430px／PCとも文字切れ・はみ出し・重なりなしをCEO実機確認済み。副次的に発見した無関係の既知課題：375pxで`setup-title`の末尾1文字が折り返される表示崩れが残っていた点は、`4308f92`で✅修正済み・本番確認済み（決定87））
- 共鳴／進化／勝敗など重要SEの音量差調整 ✅完了（`otomoEvolve`を0.09→0.13に引き上げ。他は既存値で十分な差があったため据え置き）

### B候補（Phase 1〜3完了後、残り日数を見て判断）
- ErrorBoundary新設（白画面事故の保険）✅完了（実際に例外を発生させての表示確認は未実施）
- 勝利リザルトカードへの金枠追加 ✅完了（実機確認済み、敗北時に誤適用されないことも確認済み）
- 未実装カード4枚（闘志・連撃・見通し・浄めの光）の新規生成 ✅完了（CARD_ART_STYLE_GUIDE.md準拠でCEO生成→512×768 WebPで登録、実機確認済み）

### 延期・凍結
- **恵比寿専用カード4枚（大漁・潮招き・恵比寿顔・福授け）の再生成 → 9月以降へ延期**
- 月蝕綺譚の再調査 → 8/31までは行わない

---

## 2. 総合監査で確認した重大事項（実機・コード確定）

| # | 内容 | 確度 |
|---|---|---|
| ① | デッキ構築画面でカード画像94.5MBを一括ロード（個別画像で4.1秒かかるものあり） | ✅本番URL実測 |
| ② | 共鳴7/7バナーが375px・PCともビューポート外に描画される（`.burst-banner`が`position:absolute`で縦長の`.battle`基準のため） | ✅テスト要素注入で実測 |
| ③ | ミュート解除でバトル中の全SEが一斉再生される（`BattleScreen.tsx:44`の二重ミュート判定が原因） | ✅コード確定 |
| ④ | 本番URL全画面に「開発環境 稼働中」バッジと「React + TypeScript + Phaser 3」footerが常時表示 | ✅本番URL実測 |
| ⑤ | 「成長の絆」の説明文が実データと矛盾（小槌は守り/攻めの説明が実データと完全に逆、鯛丸の力の絆はダメージ0の説明なのに実際はdraw/gainAp） | ✅コード確定 |
| ⑥ | 神選択画面にホームへ戻る導線が無く、一度進むとリロードするまで戻れない（戦績・OTOMO育成画面が2戦目以降デモで見せられない） | ✅本番URL実測 |
| ⑦ | PC幅701〜899pxで「ラウンドを終える」ボタン・手札まで565〜781pxの縦スクロールが必要（決定77・79の`min-width:900px`と決定62〜67の`max-width:700px`の間に未対応の帯域が存在）。**2026-08-17時点でCSS未変更、CEO判断待ち**（決定88） | ✅本番URL実測（701/768/800/899pxで再現確認、要素の重なり・横スクロールは無し） |

---

## 3. 分類結果サマリ

### A：8/31までに必ずやる（Phase 1〜3として上記に反映済み）
共鳴・敵ターンバナーのfixed化／ミュート一斉再生バグ／開発環境バッジ除去／スコア内訳の日本語化／成長の絆の説明修正／カード画像WebP化／OTOMO進化バナー／OTOMO形態効果の文章表示／SE音量の格差付け／神選択のホーム復帰導線

### B：A完了後、時間があれば
ErrorBoundary新設／勝利リザルト金枠／`.game-over-card`のmax-height／APバーのsticky化／敗北時「敵HP残りN」表示／チュートリアルへのOTOMO・スコア項目追加／未実装カード4枚の新規生成／神託の視覚演出

### C：9月以降
共鳴発動の「間」の演出（commit遅延、リスク中のため見送り）／実SE音源への差し替え／**恵比寿専用カード4枚の再生成**／勝利↔報酬の表示順序変更／神選択画面へのOTOMO・専用カード露出／背景アニメーション最適化／**月蝕綺譚の再調査**／**共鳴発動の効果内容（例：敵に25ダメージ・スコア+200）をバトル中の共鳴パネルに常時表示**（2026-08-17、AI社員C発見。現状デッキ構築画面でバトル開始前に1回表示されるのみで対局中は再確認できない。`GodOtomoPanel.tsx`＋`otomoEffectText.ts`のdescribeEffectList流用で実装規模は小さい見込みだが、8/31スコープでは新規実装しない）／**ホーム画面「神を選ぶ」ボタンに確認ダイアログを追加**（決定89。現状は確認なしで仕込んだバトルセーブを即消去するため、8/31は運用上の注意喚起（RELEASE_STATUS.mdステップ3）でカバーし、UI変更は9月以降に見送る）

### D：今回はやらない
D2デイリー加護UI／E2ランキングIF／E3・E4 FARM77連携／A6リプレイ性ループ強化／カードアート56枚の一括統一／多言語対応／アカウント・オンライン機能／七神の世界観テキスト拡充／神専用カードの使用時演出差別化

---

## 4. 検証済み・問題なしと確認された項目（再調査不要）

以下は監査時にコードレベルで確認し、対応不要と判断済み。今後の作業で重複調査しないこと。

- カード連打・ラウンド終了ボタン連打の二重発火（`disabled`制御で防御済み）
- オーバーレイのz-index競合（3 < 9 < 10 < 100で一貫、決定63・77・79で実測済み）
- バトル中のリロードからの復帰、決着後アクションの二重加算防止
- ブラウザバック（SPAのためほぼ無害。RewardOverlay表示中のリロードのみ報酬選択を失うが軽微）
- 全56件のカードアート・アセット参照の欠損（0件）

---

## 5. 8/31直前チェックリストへの反映

- `docs/RELEASE_STATUS.md`の「8/31直前5分チェック」に**localStorageクリアを必須項目として追記済み**（本ドキュメントと同時に対応）
- 2026-08-17（決定88）：localStorage初期化を「本番当日1回だけ・以降は削除しない」運用に変更し、`RELEASE_STATUS.md`の該当節を全面的に再構成（デモ用バトルの仕込み手順・ErrorBoundary発火確認の事前検証を追加）

---

**以降、このバックログに無い新しい改善項目は原則として追加しない。実装はPhase 1から着手し、各Phase完了ごとに検証結果を報告する。**
（例外：2026-08-17、CEO判断によりPC幅701〜899px問題（§2⑦）と共鳴効果常時表示（§3 C）の2件のみ、CEO明示指示のもと追加記録した。CSS・コード変更は行っていない）
