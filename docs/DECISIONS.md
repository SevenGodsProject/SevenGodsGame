# SEVENGODS 決定事項ログ

このファイルは「**何を決めたか**」だけでなく「**なぜそう決めたか**」を残すための記録です。
仕様を変更したときは、上書きせず追記してください。3ヶ月後の自分が最大の受益者になります。

- 最終更新: 2026-08-27（決定113追加）
- 対象バージョン: MVP Ver 0.1

---

## 1. 確定した仕様

### 1-1. ゲームの骨格

| # | 項目 | 決定内容 | 理由 |
| --- | --- | --- | --- |
| 1 | 戦闘方式 | **ハイブリッド型**（7ラウンド以内に敵を撃破 ＋ スコア評価） | 企画書2章「スコアアタック」と8章「敵1体・勝敗判定」を**両方そのまま成立させられる唯一の案**。カード6分類すべてに役割が生まれる |
| 2 | プレイヤーHP | **あり**。敵はプレイヤーを攻撃する | 防御・支援・妨害カードの存在意義が生まれる |
| 3 | 7ラウンド未撃破時 | **敗北扱い。ただし与ダメージ分のスコアは残る** | 「負けても記録が残る」＝再挑戦の動機。スコアアタックの本質 |
| 4 | 敵の方式 | **スクリプト型CPU（行動予告あり）** | 次の攻撃が見えることで「守るか、賭けて殴るか」の読み合いが毎ラウンド発生する。カードを持つCPUは実装量がほぼ倍増するためMVP外 |
| 5 | HP0時の扱い | **即ゲーム終了。そこまでのスコアは残る** | 決定3と一貫させるため |

### 1-2. 神力（AP）システム

| # | 項目 | 決定内容 | 理由 |
| --- | --- | --- | --- |
| 6 | AP供給 | **逓増方式**：R1=2 → R7=8（合計35AP） | 序盤は苦しく終盤に大技という盛り上がりの曲線を作れる。神託カード(3AP)がR3以降しか撃てず、切り札として成立する |
| 7 | AP持ち越し | **なし**（毎ラウンド使い切り、余りは消滅） | 逓増と持ち越しは同じ効果の二重掛け。両方入れると前半3ラウンドが待ち時間になる。また敵の攻撃圧力により「貯める」が罠の選択肢になる |
| 7b | 持ち越しの代替 | **「神力貯蔵」カード**で個別に実現する方針。**ただし導入は Phase 5（デッキ構築実装時）に延期** | 持ち越しをルールではなくプレイヤーの選択にすることで罠にせず楽しさだけを残す。MVPはデッキ構築が無く「選べない」ため設計意図が発揮されず、`core` に持ち越し処理を足すとPhase 1が重くなるため延期 |

**基準レート：1AP = 5ダメージ相当**
すべてのカードの強さはこの物差しで設計する。高コストカードほど効率を良くし、「1APカード連打が最強」になるのを防ぐ。

| カード | コスト | 効果 | 効率 |
| --- | --- | --- | --- |
| 攻撃（小） | 1AP | 5ダメージ | ×1.0（基準） |
| 攻撃（大） | 2AP | 12ダメージ | ×1.2 |
| 防御 | 1AP | 5軽減 | ×1.0 |
| 支援 | 1AP | 5回復 | ×1.0 |
| 妨害 | 2AP | 敵攻撃-5×3ラウンド | ×1.5 |
| 神託 | 3AP | 25ダメージ相当 | ×1.67 |

### 1-3. 共鳴システム

| # | 項目 | 決定内容 | 理由 |
| --- | --- | --- | --- |
| 8 | 共鳴の正体 | **選んだ神とのつながり。ゲージ満タンで神が介入する** | タイトル「SEVEN GODS」とメカニクスが一致する。「神の力を借りる」というコンセプトそのもの |
| 9 | 効果の中身 | **MVPは全神共通の固定効果**（神の一撃：ダメージ＋スコア）。神ごとの個性は将来 | **七神の設定が未決定のままでも実装を進められる**。将来は効果を差し替えるだけで神の個性が出せる |
| 10 | 発動方式 | **自動発動**（満タンになった瞬間に発動し、ゲージは0にリセット） | 実装が軽く、使い忘れが起きない。任意発動は後から追加可能（逆は手戻りになる） |

### 1-4. カード・デッキ

| # | 項目 | 決定内容 | 理由 |
| --- | --- | --- | --- |
| 11 | 初期手札 | **5枚** | R1のAP2に対し選択肢が多く、「選ぶ苦しみ」が生まれる |
| 12 | 毎ラウンドドロー | **2枚** | 5 + 2×6 = 17枚。20枚デッキがR7で尽きかける計算 |
| 13 | 手札上限 | **10枚**（超過分は捨札へ） | 上限なしだと後半に手札が溢れる |
| 14 | デッキ切れ | **捨札をシャッフルして山札に戻す** | R7まで確実に遊べる。「そこで終了」は理不尽 |

### 1-5. 用語・その他

| # | 項目 | 決定内容 | 理由 |
| --- | --- | --- | --- |
| 15 | 「神託」の呼び分け | ゲーム内カード＝**神託**（`oracle`） / 1日7回のシステム＝**託宣**（`divination`） | 同名の別概念はコード上で必ず事故る。企画書5章のカード名を変えずに済む案を採用 |
| 16 | MVPでの神の扱い | **`ebisu` 固定**（決定18で`god_01`から実名に変更）。神選択・デッキ構築はPhase 5 | 七神の設定確定を待たずにMVPを完成させるため |
| 17 | 敵の攻撃力 | **逓増**（R1:6 → R4:10 → R7:16） | プレイヤーのAP逓増と釣り合わせ、終盤の緊張を保つ。将来は「溜めて大技」パターン制へ発展 |
| 18 | 七神・OTOMOの実名／素材 | **公式二次創作素材「SGG Creator Kit v1」を採用**。神は`ebisu/taiyo/sobi/saika/juraku/fukuei/shouren`、OTOMOは神1柱につき専属1体（`taimaru/kozuchi/momokatsu/kotone/juka/haku/shofuku`）、各OTOMOは`spirit/doji/incarnate`の3形態を持つ。決定16のMVP固定神は`ebisu`に変更 | Kitに`RIGHTS.md`（二次創作・商用利用許可、ロイヤリティ不要）付きで正式配布されており利用可能。旧仕様「OTOMO 3体編成ロースター」はKitの実態（神とOTOMOが1対1）と食い違うため、企画書4章の解釈をKit準拠に変更した |
| 19 | OTOMOの形態変化条件と効果 | **共鳴発動のたびに1段階成長する**（精霊態→受肉態→童子）。童子到達後はそれ以上発動しても変化しない。効果は決定9と同じ方針で**全OTOMO共通の固定値**：精霊態は効果なし、受肉態はHP+5、童子はHP+5＋ブロック+8。これがその発動時点の（進化後の）形態の効果として、神の一撃（ダメージ＋スコア）に上乗せで発動する | CEOの実プレイフィードバック「進化の過程がわかりづらい」（形態自体が未実装だったため）に対応。共鳴発動は1ゲームに1〜2回想定（決定8時点の見積り）で、ちょうど3形態のうち2回分の成長に噛み合う。新しい資源やカウンタを増やさず、既存の共鳴発動イベントに乗せることでMVPの実装コストを抑えた。効果は「神＝攻める、OTOMO＝守る」で役割分担し、自動シミュレーターで勝率に大きな影響がないことを確認済み |
| 20 | 七神の性格・世界観 | **七福神モチーフを採用**。各OTOMOの名前（`小槌`→大黒天の打出の小槌、`百勝`→毘沙門天、`琴音`→弁財天の琵琶、`寿鹿`→寿老人の鹿、`ハク`＋兜の「福禄寿」刻印→福禄寿、`笑袋`→布袋の袋）とビジュアルから、伝統的な七福神の生まれ変わりという世界観を確定した。恵比寿：無邪気で食いしん坊の悪ガキ／大耀(大黒天)：戦いを楽しむ豪快な戦術家・姉御肌／蒼毘(毘沙門天)：寡黙で実直な守護者／才華(弁財天)：目立つこと・魅せることが好きなパフォーマー／寿楽(寿老人)：老獪だが中身は子供、人生を楽しむ主義／福永(福禄寿)：怖いもの知らずの冒険家／笑蓮(布袋)：おおらかで面倒見のいい姉貴分。`GodDef`に`motif`（設計メモ、UI非表示）と`tagline`（戦闘画面に表示する一言）を追加した | CEOに確認の上、公式素材（SGG Creator Kit）のOTOMO名・ビジュアルから読み取れる伝統的な七福神モチーフを正式採用。ゼロから世界観を作るより、公式素材との整合性が高く、今後のカード個性・resonanceEffects差別化（決定9の「将来」項目）の土台にもなる |
| 21 | 託宣システムの中身 | 決定15で「1日7回」としていた回数の単位を、MVPでは**「1ゲームにつき7回、かつ1ラウンド1回まで」**に確定（`QuotaProvider`による日次/サーバー版はアカウント基盤ができてから）。使い方は**神力を消費せず、常に同じ3択から選ぶ形式**（ランダム抽選ではない）：①加護の託宣＝HP+3・ブロック+2、②導きの託宣＝カード1枚+神力+1、③天啓の託宣＝敵に3ダメージ+スコア+8（※①加護の中身は決定157で「予告連動ブロック＝max(20, 予告合計×50%)・HP回復なし」へ変更） | 自動シミュレーターで検証したところ、託宣を「AP消費なし・回数制限なし」で実装すると、貪欲AIが全7回を序盤に使い切り平均決着R2台まで難易度が崩壊した。「1ラウンド1回」の上限を追加してもなお、無料の追加リターンである以上ノーリスクで勝率を底上げしてしまうため、選択肢の数値を大きく絞り、その分を敵HP（95→**103**）に上乗せして相殺した。結果、バランス型・全力攻撃型は勝率97.5%（撃破R5〜R7）、防御偏重型は勝率80%・未撃破20%という、「攻めの姿勢を怠ると時間切れの危険が残る」新しい緊張感になった |
| 22 | UI演出・サウンド | 本物の音源・イラストはまだ無いため、**Web Audio APIでその場で合成した効果音**（カード使用・被弾・回復・共鳴発動・OTOMO進化・託宣・勝敗など）と、**HPバー上のダメージ/回復数値ポップアップ**、**カード使用時の縮小フェードアニメーション**、**ミュートボタン**を実装した。カードの見た目自体は引き続き色付き四角のまま | サウンドは資産（mp3等）が用意でき次第 `sound.ts` の中身だけ差し替えれば済む構成にし、呼び出し側（`useBattleSound.ts`）を変えずに済むようにした。判定タイミングを変えない「見せ方だけの遅延」（決定：敵のターン演出と同じ手法）でカード使用アニメーションを実現し、engineには一切手を入れていない |
| 23 | 神ごとのresonanceEffects・OTOMOのeffectsByForm個性化 | 決定9・19で「将来」としていた個性化を実施。恵比寿（`EBISU_RESONANCE_EFFECTS`）と鯛丸の`effectsByForm`は自動シミュレーターの基準値そのものなので**値を変更せず据え置き**。他6神は決定20の性格モチーフに沿って、ダメージ中心の共通効果から一部をブロック・回復・デバフ・ドロー・神力獲得・自己バフ等に振り替えた（例：大耀＝ダメージ特化、蒼毘＝ダメージ+ブロック、才華＝ドロー+神力、寿楽＝ダメージ+敵atkデバフ、福永＝ダメージ+回復、笑蓮＝ダメージ+回復+ブロック）。OTOMO側も同様に神ごとに差し替えた | Phase 5（デッキ構築・神選択）実装前の下準備として、CEOの指示で「後回し」項目から着手。恵比寿以外は現状UIから選べないため未検証（プレイヤーが選べるようになるのはPhase 5から）。数値は`baselineDamagePerAp`等の物差しに厳密に合わせたものではなく仮値で、Phase 5実装時に神ごとのバランスシミュレーションで再調整する前提。データ整合性は`gods.test.ts`で検証（idの重複なし、resonanceEffectsが空でない、神とOTOMOが1対1、spirit形態の効果が空であること） |
| 24 | 神選択・デッキ構築UI（Phase 5） | 決定7bで先送りしていたPhase 5に着手。**選べるカードは「共通28種＋選んだ神の専用カード」のみ**（CEO確認済み。他の神の専用カードは使えない）。**デッキ枚数は決定12の20枚を維持**し、**同じカードは`RULES.deckBuilding.maxCopiesPerCard`（=2）枚まで**編成できる。デッキ構築画面は最初から空ではなく`getRecommendedDeck`によるおすすめ構成（恵比寿はSTARTER_DECKそのまま、他6神は共通カードを攻撃→防御→共鳴→支援→妨害→神託の順にラウンドロビンで20枚拾う機械的な構成）を表示し、そこから+/-で調整する形にした。**永続化（セーブ）はせず、セッション内のみ**（アプリを再読み込みすれば神選択からやり直しになる）。画面遷移は「神選択→デッキ構築→バトル」で、決着後は「同じ構成でもう一度」（同じ神・デッキで再戦）と「神・デッキを選び直す」の2択を用意した | 選択肢が多い割に判断コストが低い項目（デッキ枚数・カードプール範囲・永続化の要否）はCEOに事前確認し、その他（重複上限・おすすめデッキのアルゴリズム）は既存のSTARTER_DECK設計思想を踏襲して実装側で判断した。永続化を見送ったのは、現状セーブ/ロードの仕組み自体がプロジェクトに存在せず、Phase 5単体のスコープを超えるため（決定不変ルール5「セーブデータには必ずversionを持たせる」は将来の永続化実装時に効いてくる）。`GameAction`の`START_GAME`に`deck`フィールドを追加し、`core/data/deckBuilder.ts`の`validateDeck`をplayCard.tsと同じ「UIが事前に絞り込み、engineは不正な構成をバグとして早期に落とす」方針でcreateInitialStateからも呼ぶようにした。テスト50件（新規`deckBuilder.test.ts`7件含む）・型チェック・lint全て通過。Playwrightで実ブラウザ操作を自動化し、神選択→デッキ構築（+/-操作・20/20判定・確定ボタンの有効/無効切り替え）→バトル開始→勝利まで進めて「同じ構成でもう一度」「神・デッキを選び直す」の両導線が正しく動作すること、コンソールエラーが出ないことを確認した |
| 25 | 他6神の専用カード追加 | 決定18時点から恵比寿しか専用カードが無かった（決定24の§3-2で「他6神専用カードの追加」として明記されていた残タスク）状態を解消。**1神につき4枚（恵比寿と同じ枚数）**、全て`rarity: 'rare'`で、決定20の性格モチーフに沿って以下の方向性で設計した：大耀＝リスクを取って火力を伸ばす「玉砕上等」の攻撃＋姉御肌の鼓舞・面倒見（自己バフ`buff`をカード効果として初採用）／蒼毘＝ブロック突出値の防御＋持続の長い妨害（「不動の構え」はブロック13で鉄壁の構え(12)をわずかに上回る）／才華＝ドロー・神力獲得によるコンボ継続（共鳴発動効果と一貫したテーマ）／寿楽＝軽量デバフの持久戦・共鳴と回復のハイブリッド／福永＝リスクを取った攻めと回復・リソース獲得の両立／笑蓮＝攻撃1枚のみ・残り3枚は回復とブロックに寄せた支援特化構成。数値は既存共通カードの効率表（1AP=5ダメージ基準、コスト帯ごとの効率×1.0〜1.67）を物差しに、専用レアらしく既存の同コスト帯カードと同格〜わずかに上回る程度に調整した | カードプールが「共通28種＋専用4種」に増えたことで、`getRecommendedDeck`（決定24）の「共通カードのみ想定」だったラウンドロビン実装だと専用カードが埋もれて採用されない可能性があったため、非恵比寿の神は**専用カードを必ず全部先に採用してから**残り枠を共通カードでラウンドロビン埋めする方式に修正（`deckBuilder.ts`）。カード効果でのbuff初採用（大耀「姉御の号令」）は、gainAp/drawの前例（2026-08-02変更履歴）と同じ「未使用だったEffect種別をカードで初めて使う」パターン。`cards.test.ts`に「各神の専用カードが正確に4枚・全てrareであること」、`deckBuilder.test.ts`に「おすすめデッキが専用カードを必ず全部含むこと」の検証を追加。テスト52件・型チェック・lint全て通過。Playwrightで7神全ての神選択→デッキ構築（おすすめデッキが20/20・専用カード4枚表示・確定ボタン有効）→バトル開始（正しいタグライン表示）までを一括確認し、コンソールエラーが出ないことを確認した。**バランスは自動シミュレーター未検証**（`balanceSim.test.ts`は引き続き恵比寿固定）。実プレイでの検証・数値調整は今後の課題として残る |
| 26 | 他6神のバランス調整（決定25の宿題を解消） | `balanceSim.test.ts`を恵比寿固定から**7神ループ**に拡張（デッキは`getRecommendedDeck`使用）し、初回計測で**蒼毘・寿楽・笑蓮が全戦略で勝率0%**という致命的な状態を検出。原因を2つ特定して修正した。①**`getRecommendedDeck`のラウンドロビンが浅すぎた**：種別を1枚ずつ均等に拾う方式だと20枚に達するまで3巡程度しか回らず、共通攻撃カード8種のうち3種類しか採用されない等、種別ごとの共通カード数の違いを無視して攻撃力が不足していた。→ STARTER_DECKの内訳「攻撃7・防御3・共鳴2→**3**・支援4・妨害2→**1**・神託2」（共鳴を1枚増やし妨害を1枚減らした。共鳴2枚では共鳴ゲージ最大7に届きにくく、防御寄りの神で共鳴発動が一度も起きないままだった）を目標比率とし、専用カードの投入分を差し引いた不足数を種別ごとに共通カードで埋める方式に変更。②**シミュレーターAIの粗さ**：`defensive`戦略が「手札にブロックカードがあれば常に最優先」という極端な設計で、被害が想定されない場面でも無駄にブロックを積み続け、防御色の強い専用カードを持つ神（蒼毘・寿楽・笑蓮）で攻撃機会を潰していた。→ `state.player.block >= 想定被害`なら追加のブロックを見送るよう改善。加えて`draw`/`gainAp`/`buff`/`debuff`等の新しい効果種別を一切評価しないfallbackだったため`utilityOf`関数を新設し、才華・大耀等のリソース系カードも評価されるようにした。この2つの改善だけでは蒼毘・寿楽・笑蓮の`defensive`のみ0%が残ったため、`resonanceEffects`のダメージを個別に引き上げた（蒼毘15→21、寿楽18→23、笑蓮10→21）。笑蓮は唯一の攻撃カード「おおらかな一打」も4→7ダメージに引き上げた。各effect/カードのheal・block・score等の非ダメージ要素は据え置き、コンセプト（各神のプレイスタイル）は変更していない | 恵比寿の数値・デッキ（STARTER_DECK・`EBISU_RESONANCE_EFFECTS`・鯛丸の`effectsByForm`）は自動シミュレーターの基準そのものなので**一切変更していない**（balanced 98%・aggressive 100%・defensive 68%は決定23時点と同一）。調整後は全7神×3戦略（21通り×40シード=840戦）で勝率68%〜100%の範囲に収まり、突出した0%や100%張り付きが解消された（大耀aggressive 80%・才華aggressive 90%等、リスクを取る戦略で相応の敗北が出るのは意図通り）。デバッグ手順：`getRecommendedDeck`の中身を確認するテストコードを一時的に追加し、次に`applyAction`を1戦だけステップ実行してカード選択理由をログ出力する手法で、AIの非効率な選択（HPがほぼ満タンでも回復し続ける等）と真の火力不足を切り分けた（デバッグ用の一時ファイルは調査後に削除）。テスト53件・型チェック・lint全て通過。Playwrightで笑蓮のデッキ構築→バトル開始を再確認し、コンソールエラーなしを確認 |
| 27 | デッキ構成の永続化（決定24§3-2の残タスク） | CEO確認済みの方針：**保存対象は「最後にバトルを始めた神とデッキ構成」のみ**（進行中のGameState／手札・HP・ラウンド等は保存しない。フルセーブは別スコープ）。`src/hooks/deckPreferenceStorage.ts`を新設し、`localStorage`に`{version, godId, deck}`を1件だけ保存する（`sevengods.deckPreference`キー、1スロットのみ＝複数神の構成を同時には覚えない）。デッキ構築確定（バトル開始）時に`saveDeckPreference`で保存し、`DeckBuilderScreen`はその神について保存済みデータがあれば`getRecommendedDeck`の代わりにそれを初期表示する。`version`には決定不変ルール5に従い既存の`RULES.saveVersion`をそのまま流用（将来セーブ形式を変えるときにこの値を上げれば、古い保存データは自動的に無視される） | `localStorage`はブラウザAPIなので、Phaser/Reactに依存しないcore層ではなくReact側の`hooks/`に置いた（不変ルール1）。プライベートブラウジング等で`localStorage`が使えない・壊れたJSONが入っている場合でも遊べるよう、読み書きは全てtry-catchで囲み例外を握りつぶす方針にした。1スロットのみにしたのは、複数神ぶんの構成を覚える価値に対して実装が増える割に合わないと判断したため（別の神を選ぶと前回のスロットは上書きされる。「神を選び直す」→別の神→再度最初の神、と辿ってもおすすめデッキに戻るだけで、データが壊れるわけではない）。テスト58件（新規`deckPreferenceStorage.test.ts`5件含む）・型チェック・lint全て通過。Playwrightで、蒼毘のデッキを「一撃」1→0枚・「剛撃」1→2枚に編成してバトル開始→ページを`reload`→蒼毘を選び直す、という手順で編成がそのまま復元されることを確認した |
| 29 | 進行中バトルのフルセーブ／再開（決定27§3-2の残タスク、§3-2「進行中のGameState自体のフルセーブ」を解消） | CEO確認済みの方針：**保存データがある状態でアプリを開いたとき、「続きから」／「はじめから」の選択画面を挟む**（自動で直行はしない）。`GameState`は関数・Map・Set・classを含まない完全にプレーンなデータ（`core/types/state.ts`）であることを確認済みのため、`JSON.stringify`/`parse`でそのまま往復できると判断し、カスタムシリアライザは作らなかった。`src/hooks/battleSaveStorage.ts`を新設し、決定27の`deckPreferenceStorage.ts`と同じパターン（`version`フィールド、型ガード、try-catchで例外を握りつぶす）で`localStorage`に`{version, state}`を1件保存する（`sevengods.battleSave`キー）。**保存対象は`status === 'playing'`のときだけ**（決着済みの状態を「続きから」で開いてもゲームオーバー画面が出るだけで無意味なため）。`useGameEngine.ts`の`commit`（毎アクション後に呼ばれる唯一の合流点）で自動保存し、決着がついた瞬間（`status`が`won`/`lost`/`finished`に変わった瞬間）に`clearBattleSave`で削除する方式にしたことで、手動の保存ボタンや保存タイミングの選択肢を増やさずに済んだ。`resumeGame(savedState)`を`useGameEngine`に追加し、`state`を直接`setState`する（`state`は生の`GameState`で`useReducer`のラップが無いため、`dispatch`を経由せず直接差し替えられる）。UIは`src/components/setup/TitleScreen.tsx`を新設し、`GameFlow.tsx`の起動時チェック（`loadBattleSave()`の有無）で`godSelect`より先に表示する分岐を追加。「続きから」は`loadDeckPreference`（決定27）で元のデッキ構成も復元し、決着後の「同じ構成でもう一度」ボタンが機能するようにした（`getRecommendedDeck`へのフォールバック付き）。「はじめから」は`clearBattleSave`のみ実行し通常の神選択へ進む | 事前調査（Exploreエージェント）で`GameState`の全フィールドと`seed`/`rngCursor`によるシード付き乱数の再現方法（`createRng(seed, rngCursor)`で毎回作り直す設計、決定不変ルール2と整合）を確認し、シリアライズの安全性を裏取りしてから実装した。CEOには「起動時のUI分岐（選択画面を挟む／自動で直行）」の1点だけを事前確認し、保存タイミング（毎アクション自動保存、cheapなJSONなのでコスト無視できる）やスロット数（決定27に倣い1スロット）は実装側で判断した。新規`battleSaveStorage.test.ts`6件（`deckPreferenceStorage.test.ts`と同じ`MemoryStorage`モックパターンを再利用）を含むテスト64件・型チェック・lint全て通過。Playwrightで、恵比寿でバトル開始→カード使用（手札5→4枚）→`localStorage`に`status: playing`の保存を確認→ページ`reload`→タイトル画面表示→「続きから」でラウンド1・手札4枚・神力1/2・スコア3がそのまま復元されることを確認。別セッションで「はじめから」を選ぶと保存データが削除され通常の神選択に戻ることも確認した。コンソールエラーなし |
| 30 | カードごとの識別アイコン（CEOフィードバック「カードに絵を入れて視覚的にわかるようにしたい」への対応） | CEO確認済みの方針：**実写イラストではなく、カードごとにSVGアイコンを作る**（SGG Creator Kitにはカードイラスト用素材が無く、私自身も本物のイラスト画像は生成できないため）。`src/components/battle/cardIcon.tsx`を新設し、カードIDごとに手作業でアイコンを割り当てるのではなく、**カードの`effects`データから機械的にアイコンを導出する**方式にした（決定不変ルール3「カード効果をハードコードしない」と同じ精神）。`sword`/`shield`/`cross`（heal）/`spiral`（resonance）/`arrowDown`（debuff）/`star`（oracle）等14種のSVG図形プリミティブを用意し、`def.type`と`def.effects`から「主アイコン」（ダメージ量・ブロック量等のしきい値で`sword`→`swordHeavy`→`burst`のように強さも反映）と「副バッジ」（2つ目以降の効果、例：ダメージ+自傷なら`drop`、共鳴+回復なら`spiral`+`cross`）を自動選択する`getPrimaryGlyph`/`getSecondaryGlyph`を実装。`CardView.tsx`の透かしアイコン（旧・タイプ別絵文字）と`DeckBuilderScreen.tsx`のカード一覧（従来アイコン無し）の両方に適用した | カードIDごとの手動マッピング（56枚分）は作業量・記入漏れのリスクが大きく、新カード追加のたびに更新が必要になるため、既存のEffect[]データ駆動の設計思想をアイコンにも延長する方が保守性が高いと判断した。TypeScriptの`switch`で`CardType`を網羅させることで、将来`CardType`が増えた場合はコンパイルエラーで気づける。新規`cardIcon.test.ts`で全56カードがアイコン描画時に例外を出さないことを`react-dom/server`の`renderToStaticMarkup`でスモークテスト（jsdom等のDOM環境が無いプロジェクトのため、Node上で完結するこの方式を選んだ）。テスト65件・型チェック・lint全て通過。Playwrightで蒼毘のデッキ構築画面・戦闘画面の両方でアイコンが正しく描画され、剣（攻撃）・盾（防御）・渦（共鳴）・十字（回復）・爆発（大ダメージ）などタイプ・効果ごとに視覚的に判別できることを確認。コンソールエラーなし |
| 28 | 攻撃シーン演出とカードデザイン刷新 | CEOの実プレイフィードバック「敵や味方の攻撃シーンがほしい」「カードをかっこよくデザインしてほしい」に対応。**攻撃演出**：調査の結果、敵（試練の影）にはビジュアル自体が存在せず、被弾演出もHPバー全体の横シェイクのみだったため、①CSSのみで敵アバター（紫〜黒のグラデーション球＋発光する目、常時ゆらめく`enemy-idle`）を新規に作成、②`useBattleFx.ts`に`godAttackKey`/`enemyAttackKey`を追加（`DAMAGE_DEALT`イベントの`target`で攻撃実行側を判定。共鳴バーストも`godAttackKey`に含めた）、③攻撃実行側は`lunge`で相手側へ突進、被弾側は`hit-shake`に`impact-flash`（明滅）と`slash-fx`（斜めに走る光の斬撃線）を追加した。**カードデザイン**：`cardStyle.ts`の`TYPE_STYLE`に`dark`（背景グラデーション暗端）・`glow`（発光色）を追加し、`CardView.tsx`をタイプ別グラデーション背景＋右下の大きな透かしアイコン＋宝石風コストバッジ＋ホバー時のリフト＆シャイン演出に刷新。神専用カードはその神の`front`立ち絵を背景に薄く敷き、金色の追加グローで特別感を出した | 素材調査の結果、SGG Creator Kitにはカードイラスト・攻撃エフェクト用の画像が一切無く（神・OTOMOの立ち絵のみ42枚）、Phaserも未使用のまま温存されているだけだったため、既存の`useBattleFx.ts`のReact keyトリック（イベントごとにキーを増分し、`key`変更で毎回CSSアニメーションを再生させる）と同じパターンを踏襲し、React+CSSの範囲で実装した（Phaserへの着手は不要と判断）。実装中に2つのバグを検出・修正：①`.card-view-clip`のz-indexが後続の`.card-view > *`セレクタに上書きされてイラスト・アイコン層が前面に出てしまう問題→`:not(.card-view-clip)`で除外して解決、②`GodOtomoPanel`/`EnemyPanel`で同じ親要素内の兄弟同士が初期値0の`key`（`attackKey`と`evolveKey`、`attackKey`と`hitKey`）を共有しReactの「重複key」警告が出る問題→`key={`god-${attackKey}`}`のようにprefixを付けて解決（Playwrightでのブラウザ確認時にコンソールエラーとして検出）。被弾演出は、`.hit-shake`と`.impact-flash`を別クラスのままCSSの`animation`ショートハンドで併用すると、詳細度が同じ場合に後勝ちで一方が丸ごと上書きされるカスケード事故が起きるため、`.hit-shake-flash`という単一クラスに`animation: hit-shake ..., impact-flash ...;`とカンマ区切りでまとめる方式にした。ロジック層（core/engine）は一切変更していないため既存テスト58件は無変更のまま全通過。型チェック・lint全て通過。Playwrightで、カード使用時の神の突進＋敵の被弾（斬撃線＋閃光＋ダメージ数値）、ラウンド終了後の敵の突進＋プレイヤーの被弾、カードのグラデーション・透かしアイコン・専用カードの金枠を実ブラウザで確認し、コンソールエラーがないことを確認した |

---

## 2. 仮の数値（すべて調整可能）

以下はすべて `src/core/data/rules.ts` の1ファイルに集約する。
**紙プロトタイプで実際に遊んだあと、自由に変更してよい。**

| 項目 | 仮の値 | 備考 |
| --- | --- | --- |
| 総ラウンド数 | 7 | 企画書で確定 |
| デッキ枚数 | 20 | 企画書で確定 |
| プレイヤーHP | 30 | 敵の攻撃3回分で死ぬ緊張感 |
| 敵HP | **103** | 当初100→80→95と試算し、決定21の託宣導入に合わせて103に再調整（自動シミュレーションで検証。詳細は変更履歴2026-08-02「バランス調整」）。撃破がR5〜R7に分散し、上手さがスコアに反映される |
| 共鳴ゲージ最大 | 7 | 「7」のモチーフに統一 |
| 共鳴カードの上昇量 | +2 | 満タンまで4枚必要 |
| 共鳴発動効果 | 恵比寿：25ダメージ＋スコア+200／他6神：決定23参照（`gods.ts`の各`resonanceEffects`） | 1ゲーム1〜2回の発動を想定 |
| 託宣の回数 | 7（1ラウンド1回まで） | 決定21で確定。単位は「1ゲーム」（日次/サーバー版は§3-1参照） |

---

## 3. 未決定の項目

### 3-1. 判断を先送りしている（技術的に対応済み）

| 項目 | 状態 | 対応 |
| --- | --- | --- |
| 託宣「1日7回」への変更 | MVPでは決定21により「1ゲームにつき7回」で確定。日次への変更はアカウント基盤ができてから | `QuotaProvider` インターフェースで抽象化し、Local実装とServer実装を差し替え可能にする方針は維持。**サーバー導入の判断をMVP完成後まで先送りできる** |
| AP持ち越し | 「なし」で確定だが、紙プロトタイプ次第で再検討 | `rules.ts` の `carryOver` フラグ1行で切り替えられる形にする |

### 3-2. 後回し（プレースホルダで進行可能）

| 項目 | 今やること | 後でやること |
| --- | --- | --- |
| 七神の性格・属性・世界観 | ✅ **完了**（決定20で七福神モチーフを確定、決定23で`resonanceEffects`・OTOMOの`effectsByForm`、決定25で専用カードも性格に沿って個性化） | なし |
| OTOMO詳細 | ✅ **完了**（決定18で実名・素材・神との対応、決定19で形態変化条件、決定23で神ごとの`effectsByForm`個性化を実装済み） | 実プレイでの検証（決定24で7神とも選べるようになったため、今後の実プレイで数値を確認） |
| 託宣システムの中身 | ✅ **完了**（決定21で選択肢・報酬・回数制限を確定。`core/data/divination.ts`, `core/engine/applyDivination.ts`） | QuotaProviderによる日次/サーバー版（§3-1、アカウント基盤ができてから） |
| カード30〜40種 | ✅ **完了**（共通32種＋恵比寿専用4種＝36種。単純な数値違いでなく複合効果を中心に設計）。STARTER_DECKへの統合、決定24のデッキ構築UIとの連携も完了 | 他6神専用カードの追加も決定25で完了（決定51で共通カードを4種追加し計60種）。報酬カード（勝利後の3択、編成上限+1）は決定43で完了。新カードの追加自体は今後も検討可（新規exclusiveカードの追加は`getRecommendedDeck`が全専用カードを自動採用する仕様上バランス再検証が要るため要注意、決定46参照） |
| 神選択・デッキ構築UI（Phase 5） | ✅ **完了**（決定24。`GodSelectScreen`・`DeckBuilderScreen`・`GameFlow`を新設し、恵比寿固定だった導線を7神選択可能にした。決定25で他6神の専用カードも追加済み。決定27で神・デッキ構成のlocalStorage永続化、決定29で進行中GameStateのフルセーブ／再開も完了） | なし |
| バランス数値 | ✅ 自動シミュレーター（`balanceSim.test.ts`）で恵比寿・他6神・新6敵とも調整済み（決定21：敵HP80→95→103、捨身の一撃・託宣の数値を修正。決定26：7神ループに拡張し`getRecommendedDeck`の目標比率・AI・蒼毘/寿楽/笑蓮の共鳴ダメージを調整、全7神×3戦略が勝率68%〜100%に収束。決定36：7敵ループに拡張し、業斧の鬼将・蒼海の龍神のHPを調整、7神×6敵×3戦略126通りで勝率0%を解消。決定46：`getRecommendedDeck`のTYPE_ORDERバグを修正し、専用カードの推奨コピー数を大耀/才華/福永は2枚・蒼毘/寿楽/笑蓮は1枚の非対称に調整）。**決定45：勝率が高くても選択画面で強さが伝わらない神（才華）がある事例を確認したため、以後は「シミュレーターの勝率」に加えて「選択画面での説得力」も新しい神・カードのたびに必ずチェックする方針を追加** | 生身の実プレイ（紙プロトタイプ／人間のプレイテスト）でのフィードバックに基づく再調整。自動シミュレーターの数値は決定21時点の恵比寿1体のみ人間プレイで裏取り済みで、他6神・新6敵は自動シミュレーターのみで実プレイ未検証 |
| スコア計算式 | ✅ **完了**（damage / combo / apEfficiency / roundBonus / oracleBonus の5項目とも実装済み。GameOverOverlayで内訳表示も完成） | 係数（`RULES.score`）を実プレイのフィードバックで調整 |
| UI演出・サウンド | ✅ **完了**（決定22。Web Audio APIによる合成SEを追加し、ダメージ/回復の数値ポップアップ・カード使用アニメーション・ミュートボタンを実装。決定28で攻撃モーション・被弾エフェクト・カードデザイン刷新、決定30でカードごとの識別アイコン、決定31でプレイヤーアバター表示、決定32で敵7体の専用イラスト、決定33で背景演出・ガラス風UI・カードレアリティ演出、決定34でキャラクター1枚絵の拡大表示・スキル使用時の中央エフェクト・立ち絵`front`の白背景透過、決定35で神選択画面`main`の白背景透過、決定37で全56種のカードイラスト化、決定52-55でホーム/神選択/デッキ構築画面用BGM・戦闘用BGM・勝利/敗北ジングルを実装済み。`docs/bgm-prompts.md`①〜③が全て揃った） | 神の`back.webp`の白背景透過（どこからも参照されておらず未対応のまま） |
| ランキング・アカウント | やらない | サーバー導入時 |
| 多言語対応 | 日本語直書き | 文字列を外部化 |
| 敵AIの行動多様化（Task A4、2026-08-14） | セリフ拡張（3→7種）・敵ターン演出待ち時間の短縮のみ実施予定（ロジック無変更） | **8/31完成披露版には含めない**：①`charge`（溜め→大技の読み合い）を使う敵を現状の2体（銀甲の機工師・乱舞の道化）から増やす、②7敵の攻撃力カーブ（速攻型/耐久型/遅咲き型）をより差別化する数値調整。いずれも`enemies.ts`の戦闘数値・行動構造に触れ、決定21・26・36・57で積み上げた勝敗バランス（`balanceSim.test.ts`）を再び動かすリスクがあるため、CEO判断により8/31完成披露版のスコープからは明示的に除外し、8/31以降の敵AI拡張候補として記録するのみに留める |
| OTOMO育成記録（`otomoBondStorage.ts`、決定70、2026-08-15）の保存単位 | `godId`単位で記録（8/31完成披露版はこのまま現仕様維持。神とOTOMOは決定18により1:1専属のため実害なし） | 21-OTOMO実装時（確認済みの将来仕様、8/31スコープ外）は、1神につき複数OTOMOが存在しうるようになるため、保存キーを`godId`から`otomoId`単位へ移行する。移行時は`sevengods.otomoBond`のバージョン（現在1）を上げ、既存データの安全なマイグレーション or 破棄方針を別途設計する |

---

## 4. 設計上の不変ルール

コードを書くときに守る約束。ここを崩すと保守性が一気に落ちる。

1. **`src/core/` に `import Phaser` と `import React` を書かない**
   ゲームのルールを表示層から完全に切り離す。テスト・リプレイ・将来のサーバー検証がすべてここに依存する。

2. **`Math.random()` を使わない**
   シード付き乱数のみ使用。これを破るとリプレイとバグ再現が永久に不可能になる。

3. **カード効果をハードコードしない**
   `if (card.name === '...')` は禁止。効果は `Effect[]` のデータとして表現する。

4. **調整値は `core/data/rules.ts` に集約する**
   コード中に数値を直接書かない。バランス調整を1ファイルに閉じ込める。

5. **セーブデータには必ず `version` を持たせる**
   将来のアップデートでプレイヤーデータが壊れるのを防ぐ。

---

## 5. 変更履歴

| 日付 | 内容 |
| --- | --- |
| 2026-08-02 | 初版作成。決定事項1〜17を記録 |
| 2026-08-02 | 決定18を追記。SGG Creator Kit v1採用に伴い、七神・OTOMOの実名／素材／対応関係を確定。OTOMOを「3体編成ロースター」から「神1柱につき専属1体・3形態」に仕様変更 |
| 2026-08-02 | 共通カードを8種追加（速攻・渾身の一撃・鉄壁の構え・息吹・神力の泉・見切り・神楽舞・予言）。うち5種（速攻・渾身の一撃・鉄壁の構え・神力の泉・予言）をSTARTER_DECKに組み込み、既存カードの重複枚数を調整して20枚を維持。「神力の泉」(gainAp)・「予言」(draw)で、既存Effect型のうち未使用だった効果種別を初めて使用 |
| 2026-08-02 | 共通カードをさらに13種、恵比寿専用カードを2種追加（計32種、決定事項「カード30〜40種」の目標に到達）。複合効果（攻撃+共鳴、防御+回復など）のハイブリッド型カードを中心に追加し、単純な数値違いの水増しを避けた。うち3種（巫女の舞・大治癒・小さな託宣）をSTARTER_DECKに組み込み、一撃・剛撃・守護の重複枚数をさらに調整して20枚を維持。残り12種は定義のみ先行させ、次の枚数調整パスまたはPhase 5のデッキ構築での採用を検討する。カードID重複・効果空・コスト不正がないことを検証する自動テストを追加 |
| 2026-08-02 | 前回選外だった12種をSTARTER_DECKに統合。デッキ枚数20（決定12で確定）は維持したまま、32種の中から実戦投入する20種を選び直し、**全カード重複なしの単一枚構成**に変更（従来は守護2枚・共振3枚などの重複あり）。結果、一撃・速攻・渾身の一撃・鉄壁の構え・癒し・呪縛・予言・巫女の舞・大治癒の9種がSTARTER_DECKから外れ、ロスター専用（`getCardDef`からは引き続き参照可能。Phase 5のデッキ構築や報酬カード枠での採用を想定）になった |
| 2026-08-02 | バランス調整。簡易AIで大量対戦させる自動シミュレーター（`src/core/engine/balanceSim.test.ts`）を新設し、カード30〜40種化後の難易度を検証したところ、「防御を一切しない全力攻撃」戦略ですら40戦40勝という詰み状態を検出（決定1・3の「7ラウンド以内に倒せなければ敗北」という設計意図が機能していなかった）。原因は(1)「捨身の一撃」が効率×1.8と全カード中最高効率だったこと、(2)カード追加でデッキ全体の火力効率が底上げされ、敵HP80(§2)が実態に合わなくなっていたことの2点。対応として「捨身の一撃」を効率×1.2（自傷3・与9→与6ダメージに変更、剛撃と同格）に修正し、敵HPを80→**95**に変更。再検証の結果、勝率82〜87%・未撃破13〜18%・撃破ラウンドがR5〜R7に分散という、設計意図に近い分布を確認。§2の数値表・enemies.tsのコメントを更新 |
| 2026-08-02 | CEOによる初回実プレイのフィードバックを反映。①難易度「ちょうど良い」（3戦1勝・7ラウンド目撃破）を確認し、上記バランス調整が概ね妥当と判断 ②「敵の予告と着弾が同時に起きているように感じる」という指摘を受け、判定タイミング（同ラウンド内で予告→着弾）は変えずに見せ方だけ変更。`useGameEngine`のEND_ROUND処理に約0.9秒の「敵のターン」演出区間を追加し、その間は入力を止めて`enemy-turn-banner`を表示するようにした（`useGameEngine.ts`, `BattleScreen.tsx`, `battle.css`）③「精霊体からの進化がわかりづらい」という指摘は、OTOMOの形態変化（精霊態→受肉態→童子）が実装されていないことが原因と判明（§3-2に記載の未着手項目そのもの。バグではない） |
| 2026-08-02 | 決定19を追加し、OTOMOの形態変化を実装。共鳴発動（`RESONANCE_BURST`）のたびに`otomo.form`を`OTOMO_FORM_ORDER`に沿って1段階進め、`OTOMO_EVOLVED`イベントを発行（`effects.ts`）。童子到達後は据え置き。ログ表示（`formatEvent.ts`）とポートレートのグロー演出（`useBattleFx.ts`のevolveKey、`GodOtomoPanel.tsx`、`battle.css`の`evolve-glow`）を追加し、ブラウザで精霊態→受肉態への成長を実プレイ確認 |
| 2026-08-02 | OTOMO詳細（効果データ）を実装。`OtomoDef.effects`（未使用の空配列）を`effectsByForm: Record<OtomoForm, Effect[]>`に置き換え、共鳴発動時にその時点の（進化後の）形態の効果を神の一撃に追加で適用するようにした（`effects.ts`のapplyResonance）。決定9と同じ方針でMVPは全OTOMO共通固定値：精霊態は効果なし、受肉態はHP+5、童子はHP+5＋ブロック+8（「神＝攻める、OTOMO＝守る」の役割分担）。ユニットテストを追加し、`balanceSim.test.ts`で勝率に大きな影響がないことも確認 |
| 2026-08-02 | 決定20を追加し、七神の性格・世界観を確定。各OTOMOの名前・ビジュアルから読み取れる伝統的な「七福神」モチーフ（大耀=大黒天、蒼毘=毘沙門天、才華=弁財天、寿楽=寿老人、福永=福禄寿、笑蓮=布袋）を軸に、七神それぞれの性格像とキャッチコピーを設計。`GodDef`に`motif`（設計メモ、UI非表示）と`tagline`（戦闘画面に表示する一言）を追加し、`GodOtomoPanel.tsx`にtaglineを表示するようにした。ブラウザで表示を確認済み |
| 2026-08-02 | 決定21を追加し、託宣システムの中身を実装。神力を消費しない3択（加護＝HP+3・ブロック+2／導き＝カード1枚+神力+1／天啓＝敵に3ダメージ+スコア+8）を`core/data/divination.ts`に定義し、`core/engine/applyDivination.ts`で処理。当初「AP消費なし・回数無制限」に近い設計で自動シミュレーションしたところ、貪欲AIが全7回を序盤に使い切り平均決着R2台まで難易度が崩壊することを検出したため、`state.divination.usedThisRound`で**1ラウンド1回まで**の制限を追加し、選択肢の数値も大きく絞った。それでも残る底上げ分は敵HP（95→103）に転嫁して相殺し、バランス型・全力攻撃型は勝率97.5%、防御偏重型は勝率80%・未撃破20%という分布に着地。UIは`DivinationPanel.tsx`を新設し`BattleScreen.tsx`に組み込み、ブラウザで1ラウンド1回の制限が機能することを実プレイ確認。テスト38件・型チェック・lint全て通過 |
| 2026-08-03 | 決定22を追加し、UI演出・サウンドを実装。Web Audio APIでその場で音を合成する`sound.ts`（`AudioContext`の遅延生成、`OscillatorNode`によるトーン・アルペジオ）と、イベントログから再生タイミングを作る`useBattleSound.ts`を新設（カード使用・被弾・回復・共鳴・OTOMO進化・託宣・勝敗の各SE）。`useFloatingNumbers.ts`＋`FloatingNumbers.tsx`でHPバー上に「-8」「+5」のポップアップを追加し、`EnemyPanel.tsx`/`PlayerPanel.tsx`に組み込み。カード使用は`useGameEngine.ts`に`pendingCardUid`を追加し、END_ROUNDの「敵のターン」演出と同じ「見せ方だけ遅延させる」パターン（約220ms）で縮小フェードアニメーションを実現、engineの判定ロジックには一切手を入れていない。トップバーにミュートボタンを追加。テスト38件・型チェック・lint全て通過、ブラウザでカード演出・ポップアップ・ミュート切り替えを実プレイ確認 |
| 2026-08-04 | 決定23を追加し、神ごとのresonanceEffects・OTOMOのeffectsByForm個性化を実装（決定9・19の「将来」項目に着手）。`gods.ts`の`DEFAULT_RESONANCE_EFFECTS`を`EBISU_RESONANCE_EFFECTS`に改名して恵比寿専用の値として維持しつつ、他6神（大耀・蒼毘・才華・寿楽・福永・笑蓮）には決定20の性格モチーフに沿った異なる効果を設定（例：蒼毘はダメージ+ブロック、才華はドロー+神力獲得、寿楽は敵atkデバフ付き）。`otomo.ts`も同様に`DEFAULT_OTOMO_EFFECTS_BY_FORM`を`TAIMARU_EFFECTS_BY_FORM`として据え置き、他6体のOTOMOにそれぞれ異なる`effectsByForm`を設定。恵比寿・鯛丸は自動シミュレーターの基準値なので数値を変更しておらず、`balanceSim.test.ts`の勝率分布（balanced/aggressive 97.5%、defensive 80%・未撃破20%）は変化なしを確認。新規`gods.test.ts`でid重複なし・resonanceEffectsが空でない・神とOTOMOが1対1・spirit形態の効果が空であることを検証。他6神は現状UIから選択できない（Phase 5未実装）ため実プレイ未検証、数値はPhase 5実装時に再調整する前提の仮値。テスト43件・型チェック・lint全て通過 |
| 2026-08-04 | 決定24を追加し、Phase 5（神選択・デッキ構築UI）を実装。カードプール範囲（選んだ神の専用カード＋共通カードのみ）はCEOに事前確認の上で決定。`core/data/deckBuilder.ts`を新設し、`getCardPoolForGod`（神ごとの選択可能カード一覧）・`validateDeck`（枚数20・カードプール適合・同カード最大2枚を検証）・`getRecommendedDeck`（恵比寿はSTARTER_DECK、他6神は種別ラウンドロビンで機械的に20枚組む）を実装。`GameAction`の`START_GAME`に`deck`フィールドを追加し、`createInitialState.ts`は`STARTER_DECK`の直import・固定使用をやめて`action.deck`を使い、`validateDeck`失敗時はplayCard.tsと同じ方針で早期にthrowするようにした（`testUtils.ts`・`reducer.test.ts`・`balanceSim.test.ts`は明示的に`STARTER_DECK`を渡すよう更新、既存のバランス数値に変化なし）。UIは`src/components/setup/`に`GodSelectScreen.tsx`（7神一覧から選択）・`DeckBuilderScreen.tsx`（種別ごとにカードを+/-で最大2枚まで編成、20/20カウンター、おすすめデッキに戻す／全て外すボタン）を新設し、`GameFlow.tsx`が神選択→デッキ構築→バトルの画面遷移と`useGameEngine`を一括管理する形に整理（`BattleScreen.tsx`は「進行中のゲームを表示するだけ」の役割に単純化し、内部で持っていた`useGameEngine`呼び出しと開始前画面の分岐を除去）。`GameOverOverlay.tsx`の決着後ボタンを「同じ構成でもう一度」「神・デッキを選び直す」の2つに分割。永続化（セーブ）は決定不変ルール5に関わる別スコープのためMVPでは見送り、セッション内のみ。`CardView.tsx`と`DeckBuilderScreen.tsx`で重複していたカード種別の色・アイコン定義は`cardStyle.ts`に切り出して共通化。新規`deckBuilder.test.ts`7件を含むテスト50件・型チェック・lint全て通過。ブラウザ操作の自動化（Playwright、ヘッドレスChromium）で、神選択→デッキ構築（+/-操作・20/20判定に応じた確定ボタンの有効/無効切り替え）→バトル開始→（大耀を選んだ場合に共通カードのみでおすすめデッキが組めること、恵比寿を選んだ場合はSTARTER_DECKがそのまま出ること）→決着→「同じ構成でもう一度」「神・デッキを選び直す」の両導線が機能すること、コンソール/ページエラーが出ないことを確認した |
| 2026-08-04 | 決定25を追加し、他6神（大耀・蒼毘・才華・寿楽・福永・笑蓮）の専用カードを各4枚（計24枚）追加。`src/core/data/cards/{taiyo,sobi,saika,juraku,fukuei,shouren}.ts`をebisu.tsと同じ形式で新設し、`cards/index.ts`の`ALL_CARDS`に統合（カード総数56種）。決定20の性格モチーフに沿って神ごとに異なるプレイスタイルを持たせ、大耀の「姉御の号令」でカード効果としては初めて`buff`種別を使用した。カードプールが増えたことで決定24の`getRecommendedDeck`（共通カードのみ想定のラウンドロビン）だと専用カードが埋もれる恐れがあったため、非恵比寿の神は専用カードを必ず全部先に採用してから残りを共通カードで埋める方式に修正。`cards.test.ts`・`deckBuilder.test.ts`にそれぞれ「各神の専用カードが正確に4枚・全てrare」「おすすめデッキが専用カードを必ず全部含む」の検証を追加。テスト52件・型チェック・lint全て通過。Playwrightで7神全ての神選択→デッキ構築（おすすめデッキ20/20・専用カード4枚表示）→バトル開始までを一括確認し、コンソールエラーなしを確認。バランスは自動シミュレーター（`balanceSim.test.ts`）が引き続き恵比寿固定のため未検証で、今後の実プレイでの調整が必要 |
| 2026-08-04 | 決定26を追加し、他6神のバランス調整を実施。`balanceSim.test.ts`を7神ループに拡張したところ、蒼毘・寿楽・笑蓮が全戦略で勝率0%という致命的な状態を検出。原因は2つ：①`getRecommendedDeck`（決定24）のラウンドロビンが浅く、種別ごとの共通カード数の違いを無視して防御寄りの神の攻撃力が不足していた→STARTER_DECKの内訳を目標比率化し、専用カード投入分を差し引いて種別ごとに埋める方式に変更（共鳴2→3・妨害2→1に調整、共鳴ゲージが7に届きにくかった問題も併せて解消）。②シミュレーターの`defensive`戦略AIが「ブロックカードがあれば常に最優先」という極端な設計で、防御色の強い専用カードを持つ神で攻撃機会を潰していた→既に想定被害以上のブロックがあれば追加のブロックを見送るよう改善し、`draw`/`gainAp`/`buff`/`debuff`を評価する`utilityOf`も新設。これでも蒼毘・寿楽・笑蓮の`defensive`のみ0%が残ったため、`resonanceEffects`のダメージを個別に引き上げ（蒼毘15→21、寿楽18→23、笑蓮10→21、笑蓮の専用カード「おおらかな一打」も4→7）、heal/block/score等のコンセプトは変更せず維持。恵比寿の数値・デッキは自動シミュレーターの基準そのものなので一切変更していない（balanced 98%・aggressive 100%・defensive 68%は変化なし）。調整後は全7神×3戦略が勝率68%〜100%に収束し、突出した0%・100%張り付きが解消された。テスト53件・型チェック・lint全て通過。Playwrightで笑蓮のデッキ構築→バトル開始を再確認しコンソールエラーなしを確認したが、他6神のバランス数値は自動シミュレーターのみの検証で、人間による実プレイでの裏取りは今後の課題として残る |
| 2026-08-04 | 決定27を追加し、神・デッキ構成の永続化を実装（決定24§3-2の残タスク）。保存範囲はCEO確認済みで「神・デッキ構成のみ」（進行中のGameStateは含まない）。`src/hooks/deckPreferenceStorage.ts`を新設し、`localStorage`のキー1つに`{version, godId, deck}`を保存・復元する`saveDeckPreference`/`loadDeckPreference`を実装。バトル開始（デッキ確定）時に`GameFlow.tsx`から保存し、`DeckBuilderScreen.tsx`は同じ神を選んだときの初期表示を`getRecommendedDeck`から保存済みデータ優先に変更。`version`は決定不変ルール5に従い既存`RULES.saveVersion`を流用。読み書きはtry-catchで囲み、`localStorage`が使えない環境でもゲーム自体は問題なく遊べるようにした。新規`deckPreferenceStorage.test.ts`5件（`localStorage`が無いnode環境向けに最小限のメモリ実装でモック）を含むテスト58件・型チェック・lint全て通過。Playwrightで、蒼毘のデッキ編成を変更→バトル開始→ページリロード→同じ神を選び直す、という手順で編成がそのまま復元されることを実ブラウザで確認した |
| 2026-08-04 | 決定29を追加し、進行中バトルのフルセーブ／再開を実装（決定27§3-2の残タスク）。起動時のUI分岐（保存データがあれば「続きから／はじめから」の選択画面を挟む）はCEO確認済み。`src/hooks/battleSaveStorage.ts`を新設し、決定27と同じパターンで`localStorage`に`{version, state}`（`GameState`丸ごと）を保存。`GameState`は関数・Map・Set・classを含まない完全にプレーンなデータであることを確認済みのため`JSON.stringify`/`parse`でそのまま往復させ、`status === 'playing'`のときだけ保存、決着時（`won`/`lost`/`finished`）は`clearBattleSave`で削除する方式にした。`useGameEngine.ts`の`commit`（毎アクション後の唯一の合流点）で自動保存し、`resumeGame(savedState)`で`state`を直接差し替えて再開できるようにした。`src/components/setup/TitleScreen.tsx`を新設し、`GameFlow.tsx`の起動時に`loadBattleSave()`の有無で`godSelect`より先に表示する分岐を追加。新規`battleSaveStorage.test.ts`6件を含むテスト64件・型チェック・lint全て通過。Playwrightで、恵比寿でバトル開始→カード使用（手札5→4枚）→ページリロード→タイトル画面→「続きから」でラウンド1・手札4枚・神力1/2・スコア3がそのまま復元されること、「はじめから」を選ぶと保存データが削除され通常の神選択に戻ることを実ブラウザで確認した |
| 2026-08-04 | 決定30を追加し、カードごとの識別アイコンを実装（CEOフィードバック「カードに絵を入れて視覚的にわかるようにしたい」への対応）。実写イラストではなくSVGアイコンで対応する方針はCEO確認済み。`src/components/battle/cardIcon.tsx`を新設し、カードIDごとの手動マッピングではなく`CardDef.effects`から機械的にアイコン（`sword`/`shield`/`cross`/`spiral`/`arrowDown`/`star`等14種のプリミティブ＋主アイコン/副バッジの2層構成）を導出する`getPrimaryGlyph`/`getSecondaryGlyph`を実装し、`CardView.tsx`の透かしアイコンと`DeckBuilderScreen.tsx`のカード一覧の両方に適用。新規`cardIcon.test.ts`（`react-dom/server`の`renderToStaticMarkup`で全56カードのスモークテスト）を含むテスト65件・型チェック・lint全て通過。Playwrightで蒼毘のデッキ構築画面・戦闘画面の両方でアイコンが正しく描画されることを確認した |
| 2026-08-04 | 決定31を追加し、戦闘画面を「敵 vs プレイヤー」対称レイアウトに再構成（CEOフィードバック「プレイヤーに選択したキャラクターを表示させて」への対応）。神の立ち絵を共鳴パネルからプレイヤーパネルへ移動し、`GodOtomoPanel.tsx`はOTOMO＋共鳴ゲージ専用に、`PlayerPanel.tsx`に神の立ち絵・タグライン・攻撃モーションを追加。ロジック層は無変更のため既存テスト65件は無変更のまま全通過。型チェック・lint全て通過。Playwrightでlungeアニメーションとコンソールエラー無しを確認した |
| 2026-08-04 | 決定32を追加し、敵のバリエーション化を実装。CEOがChatGPTで生成した7体分のキャラクターイラスト（鬼将軍・怨霊・機巧蜘蛛・魔獣・龍神・堕天使・道化師）を1枚のシートから切り出し、`public/assets/enemies/`に配置。`EnemyDef`に`art`フィールドを追加し、既存の「試練の影」（`balanceSim.test.ts`が検証する基準）は数値を一切変えず立ち絵だけ追加、他6体はHP・攻撃パターンが異なる新規デザイン（決定4で型のみ存在していた`charge`＝溜め行動を初めて実際に使用する敵も含む）。`pickEnemyId(seed)`で`state.seed`から決定論的に敵を選ぶ方式（`Math.random`不使用）。新規`enemies.test.ts`4件を含むテスト69件・型チェック・lint全て通過。Playwrightで複数の敵が正しいイラストとステータスで選ばれることを確認した |
| 2026-08-04 | 決定33を追加し、UI全面刷新の第一弾を実装（CEOから「人気スマホカードゲーム相当のデザインに」という詳細要件を受領）。新規イラストなしでCSS/アニメーションだけで実現できる範囲（背景の粒子・魔法陣風オーラ、ガラス風UIフレーム、カードのレアリティ縁取りグロー、ボタン/数値演出の強化、700px以下でのレスポンシブ縦積み）を実装。カードレアリティの4段階化（要件の一部）は既存データ構造（決定25と紐づく）を変更する別の意思決定になるため今回は見送り、既存3段階のまま見た目を用意した。ロジック層は無変更のため既存テスト69件は無変更のまま全通過。型チェック・lint全て通過。Playwrightで3画面（神選択・デッキ構築・戦闘）すべての表示とコンソールエラー無しを確認した |
| 2026-08-04 | 決定34を追加し、キャラクター1枚絵の拡大表示とスキル演出を実装。敵・プレイヤーのアバターを200×240pxの縦長ポートレートに拡大し、カード使用時にタイプ色のアイコンが中央で弾ける`.cast-flash`演出を追加。拡大に伴い目立つようになった立ち絵の白背景を、敵イラスト7枚は.NET `System.Drawing`、神の立ち絵7枚はブラウザCanvas API（WebP非対応だったため）でそれぞれ透過PNG化して差し替えた。ロジック層は無変更のため既存テスト69件は無変更のまま全通過。型チェック・lint全て通過。Playwrightで白背景が解消され立ち絵がパネルに自然に溶け込むこと、カード使用時の演出とログ更新が正常でコンソールエラーが出ないことを確認した |
| 2026-08-04 | 決定35を追加し、決定34で手つかずだった神選択画面の`main.webp`も同じ手法（ブラウザCanvas APIでの白背景透過）で処理し、`public/assets/gods/<id>/main.png`に差し替えた。CEOから「あなただけで進められるものを先に進めて」という指示のもと確認なしで実施。ロジック層は無変更のため既存テスト69件は無変更のまま全通過。型チェック・lint全て通過。Playwrightで神選択画面の7枚全てが白背景なしで表示されることを確認した |
| 2026-08-04 | 決定36を追加し、決定32の新6敵のバランス検証を実施。`balanceSim.test.ts`を7敵ループに拡張し7神×6敵×3戦略126通りを検証したところ、蒼海の龍神（HP125）が笑蓮に対し全戦略で勝率0%、業斧の鬼将（HP108）が蒼毘・寿楽・笑蓮のdefensive戦略で未撃破100%という決定26と同型の問題を検出。カード・AIには手を加えず敵のHPのみ調整（業斧の鬼将108→100、蒼海の龍神125→103）して再検証し、126通り全てで勝率0%を解消した。ロジック層のうちenemies.tsのHP数値のみ変更で、他は無変更のため既存テスト（新規`balanceSim.test.ts`のテストケース1件追加を含む）70件全通過。型チェック・lint全て通過 |
| 2026-08-08 | 決定45を追加し、神選択画面のUX改善一式（CEOの実プレイフィードバック複数件への対応）と、今後の神・カード追加時の恒久チェック項目を追加した。**①レイアウト**：7枚のカードグリッドが「6枚+1枚」の偏った並びになっていた問題を、`display:grid`（`auto-fill`）から`display:flex; flex-wrap; justify-content:center`に変更して解消（4+3の中央揃えに）。**②起動導線**：起動時に必ず表示する`HomeScreen.tsx`を新設し、決定29の`TitleScreen.tsx`（保存データがあるときだけ表示）の役割を統合。「神を選ぶ」→「難易度を選ぶ」の2ステップ構成に変更（従来は難易度選択が神選択より先に表示され、CEOから「タイトルは神を選ぼうなのに難易度が先に出て分かりにくい」と指摘があったため）。ミュート・チュートリアルボタンを画面ごとの重複配置から`App.tsx`のグローバルヘッダーに一本化。**③プレイスタイルの可視化**：CEOから「カード能力を見ても才華を選ぶ理由が分からない」と指摘を受けた。`balanceSim.test.ts`で確認したところ才華は勝率85〜100%と実際には弱くなく、**「弱い」のではなく「選択画面で強さが伝わらない」ことが原因**と判断（ダメージ12という数字だけでは、ドロー+神力による“回転力”の価値が直感的に伝わらない）。対応として`GodDef`に`archetype`（攻撃型/防御型/支援型/技巧型/バランス型、tagline・motifと同じ手動割り当て）を追加してバッジ表示し、`resonanceEffects`＋OTOMOの`effectsByForm`から機械的に集計した4軸ステータスバー（攻撃・防御・回復・特殊）を`godStyle.ts`に新設。「特殊」の中身が数字だけでは分からないという追加指摘に対し、`describeSpecial()`で「カード+3枚・神力+4」のような内訳テキストを特殊バーの下に表示。さらに`countGodsByArchetype()`で7神中その型が1体だけの神（才華＝技巧型、蒼毘＝防御型）に「・唯一」を自動付与し、独自性を訴求した。防御・回復が0の神を見て「被弾時に不利では」という懸念にも、共鳴ボーナスと通常の共通カードは別物であることを明記した凡例文を追加して対応。**④実プレイでの裏取り**：才華で自分自身をプレイし（試練の影・恵比寿基準の敵で検証）、「アンコール」（神力+2・スコア+30）→浮いた神力で「魅惑の舞」（共鳴+3・ドロー1）と繋げるコンボが実際に気持ちよく機能することを確認。**才華の数値自体は変更していない**（バランスシミュレーターと実プレイの両方で問題なしと判断したため）。**⑤恒久チェック項目**：今回の一件を踏まえ、今後神・カードを追加するときは`balanceSim.test.ts`の勝率だけでなく「選択画面での説得力（数字の意味が一目で伝わるか）」も必ず確認する、という方針をここに明記する。ロジック層（`core/`）は`GodDef.archetype`という表示専用フィールドの追加のみで、engine本体・カード効果・共鳴・バランス数値は無変更。テスト88→89件（`gods.test.ts`にarchetype妥当性の検証を追加）・型チェック・lint全て通過。ブラウザで、ホーム画面→神選択（4+3のバランスの取れたグリッド、才華の「技巧型・唯一」バッジと内訳表示）→難易度選択の一連の流れ、グローバルヘッダーのミュート・遊び方ボタンがどの画面でも機能することを確認した |
| 2026-08-08 | 決定46を追加し、決定45で見送った「専用カードの比率を上げて神ごとの違いをもっと感じられるようにする」施策を、原因を突き止めた上で安全な形で実現した。CEOから重ねて「共通カードが多すぎてどの神を選んでも差別化しづらい」と指摘を受け、「そのまま進めて」の指示のもとで再調査した。**発見①（アルゴリズムのバグ）**：専用カードを2枚ずつ採用するよう試したときに全滅した蒼毘について、1戦だけ実際にステップ実行して追ったところ、デッキから`神託`・`予言`（`oracle`タイプ、1AP当たりの価値が高いカード）が完全に消えていることを発見した。原因は`getRecommendedDeck`の種別ごとの充填ループが、専用カードの投入で早い種別（`attack`・`guard`等）が目標数を超過したとき、その超過分を予算チェックせずに後続の種別へそのまま繰り越し、`TYPE_ORDER`の末尾（`oracle`）に予算が全く残らず静かに0枚になる、というバグだった。`TYPE_ORDER`に`oracle`を`attack`の直後へ繰り上げ、充填ループに残り予算のクランプ（`Math.min(need, remainingBudget)`）を追加して修正した。**発見②（純粋なバランス問題）**：アルゴリズム修正後に全神一律2枚で再検証すると全滅は解消したが、**蒼毘・寿楽・笑蓮（決定26で「防御・支援寄りの専用カードを持つ神」と名指しされた3神）だけ`defensive`戦略が全敵で勝率0%のまま**残った。再度ステップ実行すると、この3神は専用カードの大半が非ダメージ（防御・回復・妨害）のため、コピーを増やすほど「防御札を優先し、余った神力だけ攻撃に回す」という`defensive`AIの意思決定パターンが防御にAPを使い切ってしまい、7ラウンドで倒し切れなくなることを確認した。これはアルゴリズムのバグではなく専用カードの中身そのものに起因する実質的な問題と判断し、**この3神だけ1枚のまま据え置き、残る4神（大耀・才華・福永、恵比寿はSTARTER_DECK固定のため対象外）を2枚に引き上げる非対称な方式**にした（`REDUCED_EXCLUSIVE_COPY_GODS`）。結果、専用カードの採用率は大耀・才華・福永が8/20＝40%、蒼毘・寿楽・笑蓮は4/20＝20%のまま（ただし`maxCopiesPerCard`は変えていないため、プレイヤーが手動でこの3神も2枚まで編成すること自体は引き続き可能）。`balanceSim.test.ts`で7神×7敵（trialを含む）×3戦略のいずれも勝率0%・未撃破100%が発生しないことを確認（蒼毘defensive 0%→93%、寿楽defensive 0%→85%、笑蓮defensive 0%→95%に回復）。デバッグ用の一時テストファイル（3本）は調査後にすべて削除した。`deckBuilder.test.ts`のテストを新しい非対称コピー数に合わせて更新。テスト89件・型チェック・lint全て通過。ブラウザで大耀（専用4種とも2/2）・蒼毘（専用4種とも1/2、手動で2まで増やせる）のデッキ構築画面と20/20判定を確認した | 決定45の時点では「専用カードの比率を上げる」こと自体を見送っていたが、CEOが同じ課題を重ねて指摘したため「そのまま進めて」の指示を字面通りに再現するのではなく、最初の失敗（全滅）の**真因を最後まで特定してから**安全な範囲で実現する方針にした。1回目の失敗原因（アルゴリズムのバグ）と2回目の失敗原因（純粋なバランス問題）は全く別物で、前者を直さずに「専用カードの効率が低いから」という誤った仮説だけで思考を止めていたら、本来直せたはずのバグを見逃したまま「この施策自体を諦める」という誤った結論に至っていた。**教訓**：「そのまま進めて」であっても、実装のたびに`balanceSim.test.ts`で検証し、0%が出たら安易に全体を差し戻す前に、1戦ステップ実行で真因を切り分けること（決定26のデバッグ手法の再確認） |
| 2026-08-08 | 決定47を追加し、決定44「OTOMOの成長：守りの絆／力の絆」のうち、これまで`balanceSim.test.ts`が未対応（自動シミュレーター未検証）だった「力の絆」を検証した。決定46の作業中に「守りの絆は7神×3戦略×複数敵で検証済みだが、力の絆は一度もシミュレーターを通していない」ことに気づき、CEOに「次に何を進めるか」を提案した際にA案（この検証）として採用された。`playOneGame`/`summarizeWinRate`に`otomoGrowthPath`引数（省略時`guardian`）を追加し、既存の呼び出し元は無変更で済むようにした上で、新規テストケースで7神×3戦略＝21通りを「力の絆」固定・試練の影（trial、決定21・26で人間プレイ裏取り済みの基準）で検証。結果、**21通り全てで勝率0%は発生せず**（75%〜100%の範囲に収まり、多くが97.5%以上の「易しすぎ」寄りだが、これは決定36で「守りの絆」でも見られた許容範囲内のパターンと同型）、数値調整は不要と判断した。決定44時点で「数値は既存のeffectsByFormと比べて控えめに抑えた仮値」としていた力の絆の各OTOMO効果は、この検証結果からは弱すぎることも壊れていることも無く、少なくとも自動シミュレーターの基準では健全だと確認できた。ロジック層は無変更（テストファイルのみ）。テスト89→90件・型チェック・lint全て通過 |
| 2026-08-06 | 決定37を追加し、決定30のSVGアイコンから実イラストへの全56種カード差し替えを完了。CEOがChatGPT（Web）でカード4枚を2×2または横並びの1シート画像として生成し、PowerShell/System.Drawingで4分割（各コマ362×543px相当、2:3比率）→1024×1536にリサイズして`public/assets/cards/`へ配置、`src/core/data/cardArt.ts`の`CARD_ART`（`CardDefId`→パスの対応表）に登録する方式。`CardView.tsx`は`getCardArt(def.id)`が返す絵があればそれをカード全面に表示し、無ければ従来のタイプ別グラデーション＋透かしアイコン（決定30）にフォールバックする設計のため、バッチ単位で段階的に差し替えを進められた。生成プロンプトは`docs/card-art-prompts.md`に集約し、スタイルの土台（ちびキャラ・和風文様・金の装飾線・種別ごとのテーマカラー背景）を1回だけ共有した上で、共通28種（攻撃・防御・共鳴・支援・妨害・神託の7＋1バッチ）→神専用28種（7神×4枚、7バッチ）の順に1バッチ＝4枚ずつ生成。神専用カードは各神のモチーフ（例：寿楽＝寿老人風で亀を連れる、笑蓮＝布袋風でおおらかな包容）を軽く連想させる程度に留め、神の顔そのものの一致は求めていない。共通カードは決定32・34の神/敵ポートレートと異なり背景込みでカード全体の絵として使う方針（CEOと実物サンプルで2026-08-05に確定）のため、白背景の透過処理は不要だった。全56カードが`CARD_ART`に登録済みであることをテストで検証（一時テストで確認、常設化はせず）。型チェック・テスト70件・lint全て通過 |
| 2026-08-08 | 決定44を追加し、OTOMOの成長選択を実装。相棒視点の批評で「OTOMOの成長に選択肢がなく、神のデッキ構築ほど自分で育てる楽しさがない」と指摘した項目で、決定43（報酬カード）に続けて着手した。**設計方針**：決定19「共鳴発動のたびに自動で1段階成長する」という発動タイミング・自動発動そのもの（決定8・10）は一切変更せず、バトル開始前に「守りの絆」（決定19・23の既存`effectsByForm`、変更なし）／「力の絆」（決定44で新規追加した代替`powerPathEffectsByForm`、攻め寄りの方向性）のどちらかを選ぶ方式にした。共鳴発動の途中で選択を挟む案（ラウンド中に立ち止まって選ばせる）も検討したが、GameStateに「選択待ち」という新しいフェーズ・GameActionを追加する必要があり決定8の設計判断（自動発動で実装を軽く保つ）を部分的に覆すことになるため見送り、決定39（難易度）・決定43（報酬カード）と同じ「バトル開始前に選び、`START_GAME`アクションで運ぶ」パターンに揃えた。**内容**：`OtomoDef`に`powerPathEffectsByForm`を追加し、7体それぞれに攻め寄りの代替効果を新規に書いた（例：鯛丸＝draw+gainAp、小槌＝既存が攻め型のため逆に堅守型、百勝＝寡黙な守護者があえて前に出る「先陣型」等、各OTOMOの性格・モチーフに沿わせた）。数値は既存の`effectsByForm`と比べて控えめに抑えた仮値（決定21・26と同じ「弱すぎる方から始める」方針）。`GameState`に`otomoGrowthPath: GrowthPath`（'guardian'|'power'）を追加し、`effects.ts`の`applyResonance`がこれを見てどちらの効果テーブルを参照するか切り替える。`START_GAME`アクションの`otomoGrowthPath`は省略可・デフォルト'guardian'のため、既存の呼び出し元・テスト・自動シミュレーター（`balanceSim.test.ts`）は無変更のまま、これまでの検証済みバランスと完全に一致する。UIは`DeckBuilderScreen.tsx`に選択UIを追加（そのOTOMOの名前付きで「（OTOMO名）の成長：守りの絆／力の絆」と表示）。決定不変ルール5に従い`RULES.saveVersion`を2→3に上げ（`GameState`に必須フィールドが増えたため）、既存の保存データは自動的に無効化される。新規`gods.test.ts`・`effects.test.ts`・`reducer.test.ts`のテストを追加。テスト84→88件・型チェック・lint全て通過。Playwrightで実際に恵比寿を選び「力の絆」を選択→バトル開始→共鳴発動→OTOMOが受肉態に成長する際、守りの絆の効果（HP回復）ではなく力の絆の効果（カードを1枚引く）が正しく発動することをログで確認した（コンソールエラーなし） |
| 2026-08-08 | 決定43を追加し、報酬カード（メタ進行）を実装。CEOに「次は何を進めるか」と聞かれ、PMとして「OTOMOの成長選択」（決定19の検証済みバランスに手を入れる必要があり再検証コストが高い）より「報酬カード」（既存ルールに追加するだけでリスクが低い）を優先する判断を提示し、CEOの「相棒にある程度判断を任せる」という指示のもと実装した。**方式**：勝利後（`status === 'won'`のみ、敗北・未撃破は対象外）に3択のカードが提示され（`state.seed`から決定論的に選出、`Math.random`は不使用）、選んだカードは以後そのデッキ構築で編成上限が2→3枚に増える（永続、`src/hooks/rewardStorage.ts`でgod単位・カード単位に記録）。デッキ枚数20・専用カードプール等の既存ルールには一切触れない設計にしたことで、`RULES`の不変値を変えずに済んだ。**実装**：`deckBuilder.ts`の`validateDeck`に`bonusCopies: Map<CardDefId, number>`を追加（省略可・デフォルト空Mapのため既存呼び出し元は無変更で済む）、`START_GAME`アクションにも同様に`bonusCopies`を追加（省略可）。`createInitialState.ts`でこれを使い、UIと同じ基準でデッキを再検証する。`DeckBuilderScreen.tsx`は`loadRewardBonuses(godId)`で自分の神のボーナスを読み込み、上限表示・+/-ボタン・バリデーションすべてに反映（ボーナスがあるカードには「報酬で上限+N」のバッジを表示）。新規`RewardOverlay.tsx`（`GameOverOverlay`と同じ全面オーバーレイパターン）で3択UIを実装し、`BattleScreen.tsx`で`state.status === 'won'`かつ未選択のときに`GameOverOverlay`より先に表示する。**バグを1件、実プレイ検証で発見・修正**：実装直後に自分でブラウザ操作して勝利まで実際にプレイしたところ、報酬カードのイラストが表示されない不具合を発見。原因は`.reward-card > * { position: relative; z-index: 1; }`というwildcardルールが、クリップ層自身（`.reward-card-clip`、本来`position: absolute`のまま保つ必要がある）にも適用されてしまい、`position: absolute`が上書きされて画像が潰れていたため——決定28で一度踏んだのと全く同じ落とし穴（`.battle-arena-glow`・`.deck-builder-card-clip`では`:not()`で正しく除外していたが、この`RewardOverlay`だけ同じ対応を忘れていた）。`:not(.reward-card-clip)`を追加して解決。**教訓**：この手のwildcardパターンを新しいコンポーネントに複製するときは、`:not()`除外を機械的にコピーし忘れないこと。またこの発見自体が「実装して終わりではなく実際にブラウザで最後までプレイして確認する」ことの価値を示した一例（型チェック・自動テストではこの種のCSS描画バグは検出できない）。新規`rewardStorage.test.ts`6件、`deckBuilder.test.ts`・`reducer.test.ts`に決定43関連のテストを追加。テスト73→84件・型チェック・lint全て通過。Playwrightで実際に恵比寿を操作して7ラウンド目に勝利し、報酬カード（剛撃）を選択→神選択からやり直し→デッキ構築画面で「報酬で上限+1」バッジと3枚目の編成が可能になっていること→その構成でバトル開始が通ることまで一連の流れを確認した |
| 2026-08-08 | 決定42を追加し、決定41で保留していた笑蓮の立ち絵差し替えを完了。他6体と比べて露出・構図の方向性が大きく異なる点をCEOに確認したところ、「運営（＝CEO自身）が配布している画像なのでそのまま採用したい」と明確な回答があったため、他6体と同じ手順（webp→ブラウザCanvas APIで白背景を閾値アルファ抜き→480px縮小→PNG保存）で処理し、`public/assets/gods/shouren/front.png`・`main.png`を差し替えた。これで神7柱全員の立ち絵差し替えが完了。ロジック層は無変更のため型チェック・テスト・lintへの影響なし。Playwrightで神選択画面の7体全てに新イラストが表示され、コンソールエラーが出ないことを確認した |
| 2026-08-08 | 決定41を追加し、決定40のCSS演出強化に続けて、CEOがChatGPTで生成した実アセット（背景・スキルエフェクト・神6柱の立ち絵）を組み込んだ。加えてCEOの批評依頼（相棒視点でのテストプレイ）から見つかった改善点のうち、確認不要で完結する2件も実装した。**①戦闘背景**：`docs/battle-fx-prompts.md`のプロンプトで生成された1672×941の環境イラストをJPEG化（`public/assets/backgrounds/arena.jpg`、約300KB）し、`.battle-main`の背景として設定。決定40のCSSグラデーション（`.battle-arena-glow`）は残しつつ不透明度を下げ、実イラストの上に重なるアクセントの光として役割を変えた。**②スキルエフェクト**：6種（攻撃/防御/共鳴/支援/妨害/神託）を1枚のシートで黒背景生成→6分割・縮小して`public/assets/fx/cast-*.png`に配置。`cardStyle.ts`に`CAST_FX`を追加し、`.cast-flash`に`mix-blend-mode: screen`で重ねることで、黒背景を透過処理なしでそのまま合成できるようにした（決定34で苦労した白背景の透過処理が不要な設計を最初から意図した）。決定40で作った6種のタイプ別キーフレームは、アイコンとエフェクト画像の両方に適用するよう拡張。**③神の立ち絵差し替え**：CEO確認済みの方針（既存のSGG Creator Kit立ち絵を新イラストに差し替える）に沿って、`docs/god-portrait-prompts.md`のプロンプトで生成された7柱のうち6柱（恵比寿・大耀・蒼毘・才華・寿楽・福永）を採用。生成物はwebp形式でSystem.Drawingが読み込めない（決定34と同じ制約）ため、ローカルファイルを一時的に`public/tmp/`へ配置しVite経由でブラウザから読み込み、canvas上で白背景を閾値アルファ抜き＋480px縮小してPNGダウンロードする手順（決定34のfront.webp処理と同じロジック）を6体分実行し、`front.png`・`main.png`の両方を同じ新イラストに置き換えた。**笑蓮のみ据え置き**：生成された絵が他6体と方向性が大きく異なり（他はアクション主体・露出控えめ、笑蓮のみ露出が高く扇情的な構図）、ゲーム全体のトーン（少年少女向けカードゲーム）と不釣り合いだったため、私の判断で組み込みを見送り、CEOに再生成の意向を確認する形にした（既存のSGG Creator Kit版のまま）。**④改善点2件**：CEOへの実プレイ批評で指摘した「神力の使い残しペナルティが分かりにくい」に対し、`useBattleFx.ts`に`apPenaltyKey`/`apPenaltyAmount`を追加し`SCORE_GAINED(apEfficiency, 負の値)`イベントを検知して画面上部にトースト表示するようにした。「敵を選んでテストできない」に対し、`useGameEngine.ts`に`?enemy=oni`等のURLクエリで`pickEnemyId`のシード選択を上書きできる開発用バックドアを追加（`ENEMY_IDS`のキー名と一致しない場合は通常どおりシード選択、決定不変ルール2は維持）。ロジック層はAPペナルティ検知とURL上書きのみで、engine本体・カード効果・共鳴・託宣は無変更。テスト73件・型チェック・lint全て通過。Playwrightで蒼毘の実プレイを通し、新背景・新オーラ・新神ポートレート・新スキルエフェクト・敵の掛け声のいずれもコンソールエラー無しで動作することを確認した |
| 2026-08-07 | 決定40を追加し、決定39の直後にCEOが実プレイで見つけたフィードバック5件に対応。①**デッキ構築画面のカードイラスト欠落**：決定37・38で`CardView.tsx`（戦闘中の手札）にはフルアートを適用したが、`DeckBuilderScreen.tsx`は独自にカード見た目を実装しており`getCardArt`を一度も呼んでいなかったため、56枚とも常にSVGアイコン止まりだった（CEO発見の実バグ、キャッシュ起因ではないことをブラウザで確認済み）。`CardView.tsx`と同じ「クリップ層→フルアート→下部スクリム」パターンを移植して解消。②**戦闘画面が単色で単調**：`index.css`の`body`背景に色相を追加（紫単色グラデーションから4方向・4色のradial-gradientへ）、`battle.css`に`.battle-arena-glow`（味方側＝緑・敵側＝紫を基調にした「せめぎ合うアリーナ光」、決定28と同じ「クリップ層をwildcardで前面固定」パターンで実装）を追加。③**神ポートレートの臨場感不足**：既存のSGG Creator Kit素材の解像度自体は変更できないため、`.enemy-avatar-wrap`/`.player-avatar-wrap`の背後に回転するオーラの輪（`conic-gradient`、プレイヤー側は緑×金、敵側は紫×赤）をCSSで追加し「力を纏っている」感を補った。④**カード使用エフェクトの単調さ**：`.cast-flash`が全カード共通の同一キーフレーム（中央でポップして消えるだけ）だった点を、`カードtype`ごとに動きの質そのものが異なる6種のキーフレームに分離（攻撃＝斜めに斬り込む、防御＝下からせり上がって構える、共鳴＝回転しながら渦を巻く、支援＝ふわっと浮かび上がる、妨害＝上から叩きつける、神託＝星が弾けるように回転拡大）。⑤**敵にセリフがない**：`EnemyDef`に`battleCries: string[]`を追加し、7敵分のセリフを性格に沿って新規に執筆（例：業斧の鬼将＝豪快な咆哮、藍花の怨霊＝陰湿な呟き、乱舞の道化＝おちゃらけた挑発）。ラウンドごとに`(round - 1) % battleCries.length`で順番に表示する吹き出しUIを追加（`Math.random`は使わず決定不変ルール2に準拠）。ロジック層は敵の掛け声データ追加のみでengine本体は無変更。新規`enemies.test.ts`1件を含むテスト73件・型チェック・lint全て通過。Playwrightで蒼毘のバトルを3ラウンド分実際にプレイし、デッキ構築画面のフルアート・敵の掛け声のラウンド切り替え・カード使用エフェクトの違い・オーラ演出・背景の色味変化のいずれもコンソールエラー無しで動作することを確認した |
| 2026-08-07 | 決定39を追加し、CEOから依頼された「優先度の高い土台固め」のうち、コード側で完結する2項目（難易度選択・初回チュートリアル）を実装。**難易度選択**：`RULES.difficulty`に`easy`/`normal`/`hard`の3プリセット（敵HP倍率・敵攻撃力倍率・プレイヤー最大HP補正のみ）を追加し、`normal`は倍率1・補正0で決定21・26・36が検証した数値と完全に一致させた（既存70件のテストは無変更のまま全通過で裏取り）。`GameState`/`START_GAME`アクションに`difficulty`フィールドを追加（アクション側は省略可・省略時`normal`とし、既存の呼び出し元・テストを一切変更せずに済ませた）。`createInitialState.ts`でプレイヤー最大HP・敵最大HPに倍率を適用し、`round.ts`の`nextEnemyAction`で毎ラウンドの敵の予告ダメージにも同じ倍率をかける（予告と実際の着弾が常に一致するよう、予告作成時点で丸め込み済みの値をそのまま`state.enemy.intent`に保存する設計）。選択UIは`GodSelectScreen`に追加し、神を選ぶ前に3択から選ぶ（デフォルト`ふつう`）。GameStateの構造が変わるため決定不変ルール5に従い`RULES.saveVersion`を1→2に上げ、旧バージョンの神・デッキ構成／進行中バトルの保存データは自動的に無効化される（読み込み時のバージョン一致チェックが既存であるため追加コード不要）。**初回チュートリアル**：`src/hooks/tutorialStorage.ts`（`localStorage`の単純なON/OFFフラグ、ゲームデータではないため`version`は持たせない）と`src/components/TutorialOverlay.tsx`（目的・神力・敵の予告・共鳴ゲージ・託宣の5項目を要約表示するモーダル）を新設し、`GameFlow.tsx`が初回起動時（未読フラグ）に自動表示、神選択画面・戦闘画面の両方に設置した「？」ボタンでいつでも再表示できるようにした。ロジック層は`round.ts`・`createInitialState.ts`の難易度反映のみで、カード・共鳴・託宣・スコア計算式は無変更。新規`reducer.test.ts`2件（`normal`未指定時が既存基準値と一致すること、`easy`/`hard`が敵HP・敵攻撃力・プレイヤー最大HPだけを規定通りの倍率で変えること）を含むテスト72件・型チェック・lint全て通過 |
| 2026-08-06 | 決定38を追加し、決定37リリース直後のCEOフィードバック2件に対応。①カードのフルアートが`.card-view-illustration{height:96px}`という固定の薄いストリップに収まっていたため、1024×1536のイラストのうち上部96pxしか表示されず「イラストが途中で途切れている」状態だった。`CardView.tsx`を、イラストをカード全面の背景（`.card-view-clip`内、position:absolute inset:0、object-fit:cover）として敷き、名前・効果文（`.card-view-body`）はカード下部に自身の背景グラデーション（透明→暗）を持たせて可読性を確保する構成に変更。カードサイズも128×150→132×190に拡大し、フルアートが映える比率にした。②戦闘画面のレイアウトを「敵・共鳴・神」の3列横並びから、敵を上段フル幅、神とOTOMO/共鳴を`.ally-row`という2列の下段、その直下に手札という縦積みの構成に変更（CEO要望「敵を上、味方を下、味方の下にカード」）。JSX上も`hand`を`battle-log`/`DivinationPanel`より前に移動し、見た目の並び通りに操作導線も上から下へ追った形にした。③OTOMOの進化ポートレート（`.portrait img`）を120×120→84×84に縮小。ロジック層は無変更のため型チェック・テスト70件・lint全て通過。Playwrightで恵比寿の再開セーブを使い、フルアートの手札・敵上段/味方下段のレイアウト・縮小したOTOMOポートレートを実ブラウザで確認した |
| 2026-08-04 | 決定31を追加し、戦闘画面を「敵 vs プレイヤー」対称レイアウトに再構成（CEOフィードバック「プレイヤーに選択したキャラクターを表示させて」への対応）。従来はプレイヤー側パネルにキャラクター画像が無く、神の立ち絵は中央の共鳴パネルにのみ表示されていた。CEO確認済みの方針：**神の立ち絵を共鳴パネルからプレイヤーパネルへ移動**し、EnemyPanel（敵アバター＋HP）と対になるレイアウトにする。`GodOtomoPanel.tsx`から神の立ち絵・タグライン・`godId`/`attackKey`propsを削除しOTOMO＋共鳴ゲージ専用にし、`PlayerPanel.tsx`に神の立ち絵（`god.art.front`）・タグライン・攻撃時のlungeアニメーション（既存の`god-lunge`キーフレームをそのまま再利用）を追加。`BattleScreen.tsx`の配線を変更しただけで、`useBattleFx.ts`（`godAttackKey`等のイベント→キー変換ロジック）は無変更。新規CSS`.player-avatar-wrap`/`.player-avatar`を追加（`.enemy-avatar`と対になる円形アバター）。ロジック層は無変更のため既存テスト65件は無変更のまま全通過。型チェック・lint全て通過。Playwrightで、蒼毘を選択→バトル開始→カード使用時に神の立ち絵にlungeアニメーションが再生されること、コンソールエラーが出ないことを確認した。**今回はレイアウト変更のみ**で、CEOから別途要望のあった「敵キャラのビジュアルバリエーション」「人気スマホゲーム相当のUI刷新」は、実イラスト素材（CEOがChatGPT/Gemini等で生成予定）が揃ってから着手する方針で合意（詳細は次回以降のCEOとの調整・決定を待つ） |
| 32 | 敵のバリエーション化（決定31で保留した「敵キャラのビジュアルバリエーション」への対応） | CEOがChatGPT（Web版）で7体分のキャラクターイラスト（鬼将軍・怨霊・機巧蜘蛛・魔獣・龍神・堕天使・道化師、いずれもダークファンタジー調の詳細なイラスト）を1枚のシート画像として生成し、Downloadsフォルダ経由で提供。CEO確認済みの方針：**ステータス（HP・攻撃パターン）も異なる新しい敵を複数作る**（見た目だけの差し替えではない）。シート画像は.NET `System.Drawing`（PowerShell経由、ImageMagick等は未インストールのため代替）で7枚に切り出し、`public/assets/enemies/<slug>/art.png`に配置。`EnemyDef`に`art: string`フィールドを追加（神・OTOMOと違い1枚のみ）。既存の「試練の影」（`enemy_01`）は**数値を一切変更せず**（`balanceSim.test.ts`が引き続きこの敵の数値で検証しているため）、立ち絵（堕天使のイラスト）だけを追加。他6体（`enemy_02`〜`enemy_07`）は新規デザインで、鬼将軍＝標準やや強化、怨霊＝遅咲き型（後半急伸）、機巧蜘蛛＝技巧型（`charge`を2回使い大技を溜める）、魔獣＝速攻型（低HP高火力）、龍神＝耐久型（最高HP125）、道化師＝攪乱型（`charge`混じりの不規則パターン）とした。`charge`（決定4で「将来用」として型のみ存在していたが未使用だった敵行動）を初めて実際に使用。敵の選択は`core/data/enemies.ts`の`pickEnemyId(seed)`が`state.seed`文字列を単純ハッシュしてローテーションから決定論的に選ぶ方式（`Math.random`不使用、決定不変ルール2に準拠）。`useGameEngine.startGame`が`enemyId: ENEMY_IDS.trial`の直接指定をやめ`pickEnemyId(seed)`を使うよう変更。`GameAction.START_GAME`は元々`enemyId`を明示的に受け取る設計だったため、core側（reducer・型）は無変更で済んだ | シート画像1枚から7枚を切り出す際、キャラクターの一部（武器やチェーン等の装飾）がわずかに隣のセルへはみ出す箇所があったが、実際の表示サイズ（円形アバター、直径120px）では視認できない程度のため許容した。「試練の影」の数値を絶対に変えなかったのは、決定21・26で人間のプレイ検証を経て積み上げたバランス調整を無効化するリスクを避けるため（他6体は`balanceSim.test.ts`未対応であることを明記し、決定25と同じ「今後の課題」として扱う）。新規`enemies.test.ts`4件（id重複なし・HP/actions/artの健全性・`pickEnemyId`の決定論性とロースター内一致）を含むテスト69件・型チェック・lint全て通過。Playwrightで、`localStorage`をクリアして複数回バトルを開始し、双牙の魔獣（HP85・次の攻撃9）・藍花の怨霊（HP95・次の攻撃3）・乱舞の道化（HP92・次の攻撃4）とそれぞれ異なる敵が選ばれ、対応するイラストが円形アバターに正しく表示されること、コンソールエラーが出ないことを確認した |
| 33 | UI全面刷新（決定31・32で保留していた「人気スマホカードゲーム相当のUI」への対応、第一弾） | CEOから「Slay the Spire・Balatro・Marvel Snap・Legends of Runeterra・Shadowverse: Worlds Beyond等のような、より豪華で追力のあるデザイン」という詳細な要件（背景演出・敵/味方エリアの強化・ガラス風UIフレーム・カードのレアリティ演出・スキル演出・レスポンシブ対応）を受領。**新規イラスト無しでCSS/アニメーションだけで実現できる範囲**（背景・UIフレーム・カード演出・ボタン・数値演出・レスポンシブ）を今回のスコープとし、キャラクター1枚絵の拡大表示やスキル使用ごとの専用VFXは次回以降に持ち越した。実装内容：①`index.css`の`body`に、`::before`（漂う光の粒子、6種のradial-gradientをアニメーション）と`::after`（中央でゆっくり回る魔法陣風のconic-gradientオーラ）を追加し、`#root`を`z-index:1`で前面に固定するダークファンタジー背景システム。②`.panel`/`.battle-topbar`/`.divination-panel`/`.battle-log`/`.god-select-card`/`.deck-builder-card`を、半透明グラデーション＋`backdrop-filter: blur()`＋内側ハイライト＋外側グローの「ガラス風」に統一。③`.end-round-button`等のボタンをグラデーション＋グロー＋ホバー時の浮き上がりに強化。④`cardStyle.ts`に`RARITY_STYLE`（common/rare/legendごとの縁取り色）を新設し、`CardView.tsx`・`DeckBuilderScreen.tsx`のカードに既存のタイプ別配色へ重ねる形でレアリティの輪郭グローを追加。⑤`.floating-number`（ダメージ/回復数値）を15px→22pxに拡大し、currentColorでの発光を追加。⑥`.battle-main`に`max-width:700px`のメディアクエリを追加し、3カラムを1カラムへ縦積みするレスポンシブ対応。`prefers-reduced-motion: reduce`で背景アニメーションを止める配慮も追加 | 「カードのレアリティを4段階（コモン/レア/エピック/レジェンド）に」という要件は、現状のカードデータが`common`/`rare`（+未使用の`legend`）の3段階で決定25（神専用カードは全て`rare`）と紐づく設計判断のため、今回は既存の3段階のまま見た目だけ用意し、カードの再分類（どのカードをepic/legendにするか）は別途CEOに確認する方針にした（バランス・カテゴリ分けに関わる決定であり、CLAUDE.md運用ルールの「既存の決定事項を覆す提案は確認してから」に該当するため）。「画面を揺らす」「スキルごとに中央で派手なエフェクト」は、既存の`hit-shake-flash`（決定28）で被弾演出は既に持続しているため過剰演出になるリスクがあると判断し、今回は数値の拡大・発光に留めて全画面シェイクの追加は見送った。ロジック層（core/engine）は一切変更していないため既存テスト69件は無変更のまま全通過。型チェック・lint全て通過。Playwrightで、神選択・デッキ構築・戦闘の3画面すべてで背景の粒子/オーラ、ガラス風パネル、グロー付きボタンが表示され、カード使用時のブロック取得・ログ更新が正常に動作し、コンソールエラーが出ないことを確認した |
| 34 | キャラクター1枚絵の拡大表示・スキル演出の強化（決定33の続き） | CEOの追加要望「キャラクター1枚絵の拡大表示・スキル演出の強化を進めて」に対応。**キャラクター拡大**：`EnemyPanel`/`PlayerPanel`のアバターを84〜120pxの円形アイコンから200×240pxの縦長ポートレート枠（角丸18px）に拡大し、`.battle-main`のグリッド比率を`1fr 1.2fr 1fr`→`1.15fr 0.9fr 1.15fr`に調整、`.battle`のmax-widthも960px→1120pxに拡張した。中央のOTOMOポートレートも72px→120pxに拡大しバランスを取った。**スキル演出**：カード使用時（`pendingCardUid`がセットされている約220msの間）、そのカードのタイプ色でカードアイコン（決定30の`CardIcon`を再利用）が画面中央で弾けるように拡大表示される`.cast-flash`エフェクトを`BattleScreen.tsx`に追加した。**白背景の透過処理**：拡大に伴い、敵イラスト（決定32）とプレイヤー（神）の立ち絵（決定18のSGG Creator Kit素材）に元々あった白背景の矩形が非常に目立つようになったため、両方とも透過処理を行った。敵イラスト7枚は.NET `System.Drawing`（PowerShell経由）で白に近い画素のアルファ値を0に落とす処理をして`public/assets/enemies/*/art.png`を上書き。神の立ち絵（`front.webp`）は`System.Drawing`がWebPを読み込めなかったため、**ブラウザのCanvas API**（`drawImage`→`getImageData`で同じロジックを実行→`toBlob`でダウンロード）で7神分を個別に透過PNG化し、`public/assets/gods/<id>/front.png`として追加、`gods.ts`の`art.front`のみこのPNGを指すよう変更した（`main`/`back`は引き続きwebpのまま、影響範囲を今回必要な部分に限定） | キャラクター表示を拡大する提案自体は前回（決定33）から続く一連の「人気カードゲーム相当のUI」要望の一部だが、CEOから「基本的に開発に必要な確認はOKにするので今後確認不要」と明言されたため、実装方針の細部（ポートレートのサイズ・角丸か円形か等）は確認を挟まず判断した。透過処理でChromeの自動ダウンロードが2つ目以降サイレントにブロックされる問題に遭遇し（タブが一時的に応答不能になる副作用もあった）、新しいタブを1神につき1つ使い切る方式に切り替えて回避した。ロジック層は無変更のため既存テスト69件は無変更のまま全通過。型チェック・lint全て通過。Playwrightで、敵・プレイヤーどちらの立ち絵も白い矩形なしにパネルへ自然に溶け込んで表示されること、カード使用時にログ・スコア・共鳴ゲージが正しく更新されコンソールエラーが出ないことを確認した |
| 35 | 神選択画面`main`の白背景透過（決定34の残タスク） | 決定34では戦闘画面で大きく表示する`front.webp`のみ透過処理し、神選択画面（`GodSelectScreen`）で使う`main.webp`は手つかずのままだった。CEOから「あなただけで進められるものを先に進めて」と明確な指示があったため、確認を挟まず同じ手法で処理した。手順は決定34と同一（ブラウザCanvas APIで白系画素のアルファ値を0に落とし、新しいタブ1枚につき1神ずつダウンロード→`public/assets/gods/<id>/main.png`として保存）。`gods.ts`の`art()`ヘルパーの`main`もこのPNGを指すよう変更。`back.webp`はプロジェクト内のどこからも参照されていないため、今回も対応を見送った | 決定34で判明した「Chromeは1タブにつき自動ダウンロードを1回しか許可せず、2回目以降は無エラーで静かにブロックする」という制約に従い、今回も新しいタブを1神ごとに使い切る方式でそのまま進めた（7神とも1回で成功）。ロジック層は無変更のため既存テスト69件は無変更のまま全通過。型チェック・lint全て通過。Playwrightで、神選択画面の7枚のサムネイルすべてが白い矩形なしにダーク背景へ自然に溶け込んで表示されることを確認した |
| 36 | 新6敵（決定32）のバランス検証（決定32§理由欄「バランス未検証」の解消） | CEOから「あなただけで進められるものを先に進めて」との指示を受け、`balanceSim.test.ts`を拡張して決定32の新6敵（試練の影を除く）を検証。7神×6敵×3戦略＝126通りを1行サマリーで出力し、勝率0%（詰み）または97.5%以上（易しすぎ）の組み合わせを自動検出する新テストを追加した。初回計測で**`蒼海の龍神`（当初HP125）が笑蓮に対して全戦略で勝率0%**という致命的な状態、および**`業斧の鬼将`（当初HP108）が蒼毘・寿楽・笑蓮のdefensive戦略で未撃破100%**（7ラウンド以内に倒し切れない）という決定26と同型の問題を検出した。原因はどちらも敵のHPが高すぎたことで、カード効果やAIには手を加えず**敵のHPだけ**を調整（`業斧の鬼将`108→100、`蒼海の龍神`125→103＝trialと同値）して再検証したところ、126通り全てで勝率0%が解消された（97.5%以上の「易しすぎ」表示は多数残るが、これは決定26で検証済みの恵比寿/6神×trialの基準でも同様に見られるパターンで、7敵に持たせた難易度のばらつき自体は許容範囲と判断し、あえて手を加えなかった）。`藍花の怨霊`・`銀甲の機工師`・`双牙の魔獣`・`乱舞の道化`の4体は初回計測から0%が一切無く、調整不要だった | 決定21・26の「0%または100%張り付きは基本的な設計意図（7ラウンド以内に倒せなければ敗北、攻めないと時間切れの危険が残る）が機能していないサイン」という判断基準をそのまま踏襲した。カード・AIではなく敵のHPだけを動かしたのは、CEOから明確に「あなただけで進められるもの」の範囲として敵のバランス検証を切り出されており、神やカードの数値（decision 26で既に人間のプレイ検証込みで調整済み）を再び動かすとその裏取りが無効になるため。1体だけ残る単一戦略のdefensive未達（例：蒼毘×蒼海の龍神×defensiveが90%止まり等）はゼロではないため許容範囲内とし、108→100/125→103という比較的大きい下げ幅で一度に解消させた（小刻みな調整を繰り返すより、trialで既に実プレイ裏取り済みの103という値に寄せる方が安全だと判断） |
| 2026-08-10 | 決定48を追加し、「実プレイ・フィードバック基盤」（CEOが優先度で選択）としてアプリ内フィードバック機能を実装。§3-2の技術的に対応可能な項目が全て完了済みだったため、AskUserQuestionで次の優先度を確認したところ「実プレイ・フィードバック基盤」を選択。さらに中身を2段階で絞り込み（配布・ホスティングはVercel/Netlify等のCEO側アカウントが必要になるため保留、CEOが選んだのは「アプリ内フィードバック機能」のみ）。**設計**：外部サービス（フォーム・API）は使わず、自由記述コメント＋自動添付のプレイ状況要約（画面・神・難易度・ラウンド・スコア・OTOMO形態）を1つのテキストにまとめ、`navigator.clipboard`でコピーするだけの機能にした。送信先（Discord・LINE等）は問わず既存の連絡手段に委ねる設計で、サーバー・アカウント一切不要。`src/components/feedback/feedbackSnapshot.ts`に`computeSnapshot`（`GameFlow`の`setupScreen`/`godId`/`difficulty`とGameStateから要約を組み立てる純粋関数）・`describeSnapshot`・`formatFeedbackText`を実装し、Reactから分離してテスト可能にした。`FeedbackOverlay.tsx`はtutorial.css（決定39/47）と同じ「フルスクリーンオーバーレイ＋カード」構造を踏襲し、`navigator.clipboard`が使えない環境向けに失敗時は読み取り専用テキストエリアへフォールバックする。ミュート・チュートリアルボタンと同じ「Appヘッダーに一本化」方針（決定45）に倣い、`GameFlow.tsx`が`useEffect`で画面遷移・ラウンド進行のたびに`onSnapshotChange`を呼び、`App.tsx`側でスナップショットstateを保持してヘッダーの3つ目のアイコンボタンから開く構成にした。ロジック層（`core/`）は無変更。新規`feedbackSnapshot.test.ts`7件を含むテスト90→97件・型チェック・lint全て通過。Playwrightで、ホーム画面（「ホーム・ふつう」）→恵比寿を選択しバトル開始→戦闘中（「バトル中・恵比寿・ふつう・R1・スコア0・OTOMO精霊態」）とスナップショットが画面遷移・ラウンド進行に追随して正しく更新されること、コメント入力→クリップボードコピーで「コピーしました✓」に切り替わること、コンソールエラーが出ないことを確認した |
| 2026-08-10 | 決定49を追加し、決定48直後にCEOへ実プレイ評価＋改善提案を提示したところ「ハイスコア・自己ベストの記録機能」を選択したため実装。決定43の`rewardStorage.ts`と同じ方針（hooks配置・`RULES.saveVersion`とは独立した専用バージョン）で`src/hooks/recordStorage.ts`を新設し、神ごとに`bestScore`・`bestScoreDifficulty`・`wins`・`losses`・`finished`・`fastestWinRound`を`localStorage`に永続化。`useGameEngine.ts`の`commit`が決着（won/lost/finished）のたびに`recordGameResult`を呼び、この1戦で自己ベストを更新したかどうかを`newBest`として返す（`isEnemyTurn`/`pendingCardUid`と同じ「決着時にリセットするフラグ」パターン）。`GameOverOverlay.tsx`に「✨ 自己ベスト更新！」バッジ、`GodSelectScreen.tsx`の各神カードに「自己ベスト◯◯（△勝）」を追加（未挑戦の神は非表示、記録済みのみ表示）。新規`recordStorage.test.ts`8件を含むテスト97→105件・型チェック・lint全て通過。**実プレイ検証で見つけた既存バグを2件、この場で修正**：①`.game-over-overlay`に`z-index`が未指定だったため、`.enemy-panel`/`.ally-row`（z-index:1）・`.burst-banner`（z-index:5）等の戦闘演出要素がその上に描画され、決着後も背景が暗転せず操作できるように見える状態だった（DOM上は正しく存在し内容も正しいため型チェック・自動テストでは検出不可能、Playwrightで実際に勝利までプレイして初めて発覚）。`.reward-overlay`（z-index:9、決定43）より上になるよう`z-index:10`を指定して解消。②`BattleScreen.tsx`の`.ap-penalty-toast`（`key={fx.apPenaltyKey}`）と`.burst-banner`（`key={fx.burstKey}`）が、どちらも`.battle`の直接の子として素の数値をそのままkeyに使っていたため、AP効率ペナルティのトーストと共鳴バーストのバナーが同じラウンドで両方0から1へ切り替わるとReactの「duplicate key」警告が発生する状態だった（自動化した複数ラウンドの実プレイ中に発見。決定28で一度対処した「兄弟要素が同じkeyの初期値から始まる」バグと同型で、今回は別の2つのfxカウンターが偶然同じ値を取るケース）。`key={`ap-penalty-${fx.apPenaltyKey}`}`／`key={`burst-${fx.burstKey}`}`とプレフィックスを付けて解消し、3戦分のPlaywright自動プレイで警告が再発しないことを確認した。両修正ともロジック層（`core/`）は無関係でCSS/JSXのみ |
| 2026-08-10 | 決定50を追加し、決定49直後にCEOから提示した改善提案の残り2件（狭い画面幅での確認・BGM生成プロンプト作成）を「残りのタスクを進めて」の指示で両方実施。**①狭い画面幅での確認**：`resize_window`ツールがこの環境のディスプレイ制約で使えなかったため、同一ウィンドウ内に`width:390px`の`<iframe>`を注入し、その中でアプリを独立して読み込ませる手法で代替した（media queryはiframe自身のビューポート幅で評価されるため、実機同等の検証になる）。ホーム／神選択（2ステップとも）／デッキ構築／戦闘画面（手札・託宣パネル・エンドラウンドボタンまで）を390px幅で一通り操作し、`document.documentElement.scrollWidth > clientWidth`によるオーバーフロー自動検知も併用して確認したところ、**決定33で用意した700px以下のブレークポイントは正しく機能しており、修正が必要な問題は見つからなかった**（それまで一度も実際の狭幅ビューポートで検証されたことが無かった箇所なので、「問題なし」を確認できたこと自体に価値がある）。**②BGM生成プロンプト作成**：`docs/bgm-prompts.md`を新設し、`docs/battle-fx-prompts.md`等と同じ運用（CEOが外部ツールで生成→パスを伝えてもらい組み込みはこちらで行う）で、ホーム/神選択/デッキ構築用・戦闘用・勝敗ジングル（各5〜10秒）の3種のプロンプトを用意した。**この回で初めて「ChatGPT/Geminiでは音楽ファイル自体は生成できない」という制約に触れる**ため、Suno/Udio/ElevenLabs等の専用ツールが必要である旨を明記し、CEOがアカウントを持っていなければ保留してよいと注記した。技術要件（インストゥルメンタルのみ・ループ前提でイントロ最小化・mp3書き出し）と、実装時の組み込み方針（`<audio>`＋既存ミュートボタンとの連動、画面遷移でのトラック切り替え）もメモとして残した。ロジック層・既存コードへの変更は無し（①は検証のみ、②はドキュメントのみ）。テスト105件・型チェック・lintは前回から変更なしで全て通過 |
| 2026-08-13 | 決定51を追加。CEOから「続きを進めて」の指示を受け、まず自動テスト105件・型チェック・lint・実プレイ（才華で数ラウンド）をブラウザで確認して新規バグが無いことを確かめた上で、`§3-2`の技術対応可能項目が決定50までで尽きていたため次の優先度をAskUserQuestionで確認し、「新カード・新敵の追加（自分だけで完結）」を選択（新敵は`EnemyDef.art`が必須フィールドでCEO提供の立ち絵が無いと追加できないため、今回は見送りその旨を明記。決定30のSVGアイコン自動導出のおかげで新カードは素材不要で追加できることを見送りの根拠とした）。**共通カードを4種追加**（`src/core/data/cards/common.ts`、闘志／連撃／見通し／浄めの光。damage+gainAp・draw+gainAp・heal+debuffという、既存の共通カードにはまだ無かった組み合わせを選んだ）。`getRecommendedDeck`の種別ごとの目標枚数（`TARGET_TYPE_COUNT`）は既存カードだけで既に満たされているため、これらはどの神のおすすめデッキにも自動採用されず、デッキ構築画面で手動編成したときのみ選べるロースター専用カードになる（2026-08-02の「定義だけ先行させる」運用と同じ）。`getRecommendedDeck`が新カードを含まないことを`deckBuilder.test.ts`に追加したテストで明示的に固定し、`balanceSim.test.ts`にも変化が無いことを確認した。**あわせて、新カードの1枚（闘志＝buff(self,atk)）を実装する過程で発見した既存の実バグを修正**：決定25で大耀専用カード「姉御の号令」に`buff(target:'self', stat:'atk')`を初採用した際、`effects.ts`の`case 'damage'`が常に`applyDamage(state, effect.target, effect.amount)`という素の数値で敵にダメージを与えており、`state.player.buffs`を一度も参照していなかった（＝カードの説明文「攻撃力を上げる」に反し、実際は何も起きない死んだ効果だった）。`round.ts`が敵の`atk`デバフを敵自身の攻撃力に反映する処理（決定4）と対になる、プレイヤー側の対応する処理が存在していなかったのが原因。`case 'damage'`で`target === 'enemy'`のときだけ`sumBuff(state.player.buffs, 'atk')`を加算するよう修正（自傷ダメージや`applyDamage`本体は無変更、既存の`applyDamage`単体テストへの影響もゼロ）。`effects.test.ts`に3件（バフが敵ダメージに乗ること／自傷には乗らないこと／大きな負のバフでもダメージが0未満にならないこと）を追加。ブラウザで大耀を実際に操作し、「闘志」使用後に基礎ダメージ4の「一心不乱」が6ダメージになることを目視でも確認した。テスト105→109件（内訳：effects.test.ts+3、deckBuilder.test.ts+1）・型チェック・lint全て通過、`balanceSim.test.ts`で大耀を含め0%勝率の組み合わせが新たに発生していないことも確認済み |
| 2026-08-13 | 決定52を追加。決定51直後にCEOへ`docs/bgm-prompts.md`の使い方（Suno等の操作手順）を説明したところ、CEOがすぐに①ホーム・神選択・デッキ構築画面用BGM（「七福神の冒険」、約4分・5.79MB・199kbps）を生成して渡してくれたため組み込んだ。`public/assets/bgm/home.mp3`に配置し、新規`src/components/battle/bgm.ts`（決定22の効果音合成`sound.ts`とは別ファイル。役割が違う——こちらは実音源ファイルを`<audio loop>`でそのまま鳴らすだけの薄いラッパー）を追加。ブラウザの自動再生制限（ユーザー操作前は`play()`が拒否される）に対応するため、拒否されたら最初のクリック/キー入力を待って再試行する仕組みを入れた。`GameFlow.tsx`に`engine.state`の有無（＝バトル中かどうか）を真偽値化した`useEffect`を追加し、ホーム／神選択／デッキ構築の間だけ`playTrack('home')`を鳴らし、バトル開始で`stopBgm()`する（②戦闘用BGM・③決着ジングルは未生成のためバトル中は従来通り無音）。`App.tsx`の既存ミュートボタン（決定45）が`setSoundMuted`と並べて`setBgmMuted`も呼ぶよう1行追加し、効果音とBGMが連動してミュートされるようにした。曲の長さが`docs/bgm-prompts.md`で依頼した1〜2分の目安より長い（約4分）が、`<audio loop>`前提なので長尺のままループさせても動作上の問題はないと判断し、トリミング等はしなかった（この環境にはffmpeg等の音声編集ツールが無く、仮にトリミングが必要でも実行できないため）。**実際に音が鳴ることの確認について**：`fetch`のHEAD/Rangeリクエストでファイル自体はVite開発サーバーから正しく配信されている（200/206、正しいcontent-type・content-length）ことは確認できたが、Claude in Chrome拡張が操作するタブは`document.visibilityState`が常に`'hidden'`を返す環境だと判明し、`<audio>`要素の実際の読み込み・再生（`readyState`が`HAVE_NOTHING`のまま進まない）をこのツール経由では検証できなかった（ブラウザが非表示タブのメディア読み込みを抑制する既知の挙動で、`resize_window`と同じ「この自動化環境固有の制約」に該当すると判断）。型チェック・lint・テスト109件（無変更）は全て通過、コンソールエラーも無し。**実際に音が鳴るかどうかは、CEOご自身の通常のブラウザタブでの確認をお願いした**（ループの継ぎ目の違和感の有無・SFXとの音量バランスも合わせて）。→ CEOが実機で確認し「完璧、すばらしい」とのフィードバックを得て、①は実プレイでも確定 |
| 2026-08-13 | 決定53を追加。決定52の①確定を受けてすぐCEOが②戦闘用BGM（「七神の戦場」、約5分11秒・7.13MB・191kbps）を生成して渡してくれたため組み込んだ。`public/assets/bgm/battle.mp3`に配置し、`bgm.ts`の`TRACKS`に`battle`を追加。`GameFlow.tsx`の`useEffect`を「バトル中なら`stopBgm()`」から「バトル中なら`playTrack('battle')`、そうでなければ`playTrack('home')`」に変更し、ホーム⇄バトルの往復でトラックが自動的に切り替わるようにした（③決着ジングルはまだ未生成のため、勝敗時に一瞬だけ挟む演出は次回以降）。曲の長さは①同様、依頼した1〜2分より長い（約5分）が同じ理由でそのままループ採用。型チェック・lint・テスト109件（無変更）は全て通過。音が実際に鳴るかはこの環境のブラウザ操作ツールでは検証不可（決定52で判明した`document.visibilityState`常時`hidden`の制約）なため、サーバー側の配信確認（`curl`でHTTP 200・`audio/mpeg`・正しいファイルサイズ）のみ行い、CEOご自身の確認待ち。→ CEOが実機で確認し「大丈夫」とのフィードバックを得て、②も実プレイで確定。①②とも完了、残るは③決着ジングルのみ |
| 2026-08-13 | 決定54を追加。CEOから2つのパスが届いたが、`七神の戦場.mp3`（Temp配下）は既に組み込み済みの決定53のbattle.mp3と**バイト単位で完全に同一サイズ（7,476,270バイト）**だったため、誤って再送された同じファイルと判断し取り込まなかった（新しい別音源であれば生成のたびにファイルサイズが変わるはずで、ここまで一致するのは同一ファイルの可能性が極めて高いという判断。もし実際は差し替え意図だった場合は再度知らせてもらう前提）。もう一方の`七神の勝利.mp3`（4分39秒・6.76MB・193kbps）を③勝利ジングルとして`public/assets/bgm/victory.mp3`に組み込んだ。**Sunoの生成物が依頼した5〜10秒よりずっと長い**（①②と同じ傾向）ため、この環境にmp3をトリミングするツールが無い代わりに、**JS側で「9秒再生したらフェードアウトして止める」仕組み**を`bgm.ts`に追加（`playJingle()`。自然に短ければ`ended`イベントで先に止まる）。ジングルが鳴っている間はループ中のBGMをいったん一時停止し、ジングル終了（自然終了 or 9秒フェード）で元の音量のまま再開する。呼び出し箇所は`useBattleSound.ts`の`GAME_ENDED`イベント（勝利時のみ。敗北ジングルはまだ音源が無いため`JINGLE_TRACKS`に`victory`しか定義していない——音源が届いたら`defeat`を1行追加するだけで済む設計）。**既知の軽微な未対応ケース**：勝利直後9秒以内に「同じ構成でもう一度」を押した場合、`GameFlow.tsx`の`inBattle`真偽値が勝敗をまたいでtrueのまま変化しないため②のBGMが自動再開されず、ジングルの9秒タイマーが切れるまで無音になる（`engine.state`のnull/非nullでのみBGM画面遷移を判定している設計上の隙間。発生条件が狭く実害も軽微なため今回は許容し、コメントで明記するに留めた）。型チェック・lint・テスト109件（無変更）は全て通過。音の実確認はこの環境では引き続き不可のため、サーバー配信確認（`curl`でHTTP 200・正しいファイルサイズ）のみでCEOの実機確認待ち |
| 2026-08-13 | 決定55を追加。CEOが敗北ジングル`七神の残響.mp3`（4分12秒・6.16MB・194kbps、他4曲とファイルサイズが一致しないことを先に確認し誤送でないことを裏取り済み）を生成・送付。`public/assets/bgm/defeat.mp3`に配置し、`bgm.ts`の`JINGLE_TRACKS`に`defeat`を1行追加、`useBattleSound.ts`の`GAME_ENDED`の敗北分岐に`playJingle('defeat')`を追加するだけで組み込み完了（決定54のコメントで予告した通り「1行追加」で済んだ）。決定54で作った9秒フェードアウト機構がそのまま流用できるため`bgm.ts`本体の追加変更は無し。これで`docs/bgm-prompts.md`の①〜③が全て揃い、BGM関連の§3-2項目は音源面で完了（残るは実プレイでのCEO確認のみ）。型チェック・lint・テスト109件（無変更）は全て通過。サーバー配信確認（`curl`でHTTP 200・正しいファイルサイズ）のみ行い、CEOの実機確認待ち。→ CEOが実機で確認し「OK」とのフィードバックを得て、③敗北ジングルも実プレイで確定。`docs/bgm-prompts.md`①〜③、全て実プレイ確認済みで完了 |
| 2026-08-13 | 決定56を追加。CEOの「お願いします」（自分だけで進められる範囲に一任）を受け、§3-2が技術的にほぼ完了したこのタイミングで、決定51で見つけた「buffが保存されるだけで一度も読まれていなかった」バグと同じ穴が他にも無いか、Exploreエージェントに全カード（common+7神専用）・神/OTOMOのresonanceEffects・敵のactionsのtext/データ整合性、および全Effect種別が実際に消費されているかを横断監査させた。**結果：他に同種のバグは無し**（全カードのtextとeffectsは数値・対象とも完全一致）。ただし`StatKey`の`'def'`が2026-08時点でどのカード・神・OTOMOからも一度も使われておらず、消費するコードも存在しないことを確認——「今は誰も使っていないので実害は無いが、将来誰かが`stat:'def'`のカードを作った瞬間に決定51と同じ穴に落ちる」という潜在的な罠だったため、`src/core/types/effect.ts`の`StatKey`定義に警告コメントを追加した（コードの実装は変更していない、ドキュメントのみ）。あわせて、決定51-55で追加した内容（新カード・BGM・ジングル）が実際のプレイフローで問題を起こさないかを、大耀で1戦丸ごと自動プレイして確認：勝利→報酬カード選択→決着オーバーレイ→勝利ジングルのフェードアウト完了まで、コンソールエラー・警告が一切発生しないことを確認した（`console.error`/`console.warn`/`window.onerror`/`unhandledrejection`を全て監視した上でゼロ件）。型チェック・lint・テスト109件（無変更）は全て通過 |
| 2026-08-13 | 決定57（調査、数値変更はまだ）。CEOに実プレイベースでゲーム評価・改善提案を依頼され、大耀（勝利）・蒼毘（未撃破）・笑蓮（防御札を避けて未撃破）で実プレイし、「防御・支援寄りの神は無防備に近いプレイでもHP0の敗北がほぼ起きない」と報告したところ、CEOから「もう少し発生しうる方が緊張感が出る」との回答。数値を勘で動かす前に、`balanceSim.test.ts`にdifficultyパラメータを追加（`playOneGame`/`summarizeWinRate`、決定47のotomoGrowthPath同様の追加方式）し、**それまで一度もシミュレーション検証されていなかった`easy`/`hard`の勝敗分布**を7神×3戦略×2難易度で実測した（決定39は倍率計算の正しさをPlaywrightで一度確認しただけで、大量試行によるバランス検証は未実施だった）。**判明した実態は「敗北率が低い」という単純な話ではなく、戦略によって症状が真逆の構造的な問題**：hardの`aggressive`は既に才華93%・大耀45%・福永40%が敗北するほど致死的な一方、hardの`defensive`は蒼毘・笑蓮が**未撃破100%（敗北0%・勝利0%）の完全な膠着状態**になっていた（CEOに報告した2回の実プレイは、たまたまこの「膠着」側のサンプルだった）。この構造を踏まえ、**`enemyAtkMultiplier`を単純に上げる案を試算**（hard 1.15→1.30に一時的に変更してシミュレーション→即座に元に戻す、rules.tsへのコミットはしていない）したところ、予想通り**aggressive側の敗北率がさらに悪化する一方（才華93%→100%、大耀45%→70%、福永40%→63%）、defensive側の膠着率はほぼ変化しなかった**（蒼毘・笑蓮とも未撃破100%のまま）。理由は、defensive AIが「必要な分だけブロックする」設計（決定26）のため、被ダメージが増えてもブロックカードで追随してしまい、行動パターン自体は変わらないため。**結論：「敵の攻撃力を上げる」は的外れな対策**で、むしろ「防御に全振りすれば絶対に安全」という状態そのものを崩す方向の変更が必要——これは既存のEffect種別だけでは対応が難しく設計判断が要るため、CEOに実測データと共に選択肢を提示し相談する。`balanceSim.test.ts`のdifficulty対応自体は恒久的な拡張として残した（今後の難易度調整のたびに数値で検証できる）。型チェック・lint・テスト110件（新規決定57テスト1件追加）全て通過。
| 2026-08-14 | 決定58を追加。8/31完成披露ロードマップのTask A1（難易度バランス修正）として、決定57で「CEOに改めて相談する」とされていた膠着問題（蒼毘・笑蓮のhard×defensiveで未撃破100%）を再検討。今回は数値を動かす前に、hard×全神(7)×全戦略(3)×全敵(7)＝147通り（各40シード、一時分析スクリプトで実測し分析後に削除）を計測し、決定57時点では未計測だったtrial以外の6敵も含めて調査した。**新たに判明した事実は2点**：①膠着（蒼毘・寿楽・笑蓮のdefensive戦略、双牙の魔獣を除く6敵で発生）は、終了時の敵残りHPが平均2〜16%という僅差の「時間切れ」であり、決定57のコメントが示唆した「防御に全振りすれば一切攻撃しない」という構造的ロックではなかった。②膠着・敗北が発生する全21組み合わせについて、**同じ神・同じ敵でbalanced戦略が必ず73%以上（多くは88〜100%）の勝率を記録**しており、「合理的な戦略では絶対に勝てない」という意味での詰みは0件だった（最弱は才華×双牙の魔獣で、最善戦略のdefensiveでも50%＝五分五分の接戦であり詰みではない）。CEOはこの結果を受け、**数値変更は行わずTask A1をクローズ**する判断をした（「Hardだから難しいのは問題ではない、合理的な戦略で勝敗が成立しない状態こそが問題」という完成披露版の方針に、実測データが既に合致しているため）。`rules.ts`・`effects.ts`・`round.ts`への変更は一切無し。代わりに`balanceSim.test.ts`に**再現防止テストを1件追加**：hard×全神×全敵の147通りについて、3戦略中の最善勝率が50%を下回る組み合わせが存在しないことをアサーションで保証する（しきい値50%は今回実測した最弱値に一致。将来のカード追加・数値調整で「詰みが無い」状態が壊れたら、このテストが失敗して検知する）。防御アーキタイプ（蒼毘・寿楽・笑蓮）がdefensive戦略でhard時に高確率で膠着する特性自体は、「堅実だが決め手に欠ける」という**意図されたゲームデザインの一部**としてテストコード内のコメントに明記し、修正対象から外した。テスト110→111件、型チェック・lint全て通過。
| 2026-08-14 | 決定59を追加。Task A2（スコア係数チューニング）として、normal×trial・7神×3戦略のスコア内訳（damage/combo/apEfficiency/roundBonus/oracleBonus）を実測（60シード）。**oracleBonusの実体を調査した結果、`rules.ts`の天啓の託宣(+8×最大7回)だけでなく、`gods.ts`の各神の共鳴発動に埋め込まれた`score`効果（恵比寿200・他5神170・笑蓮150、いずれも決定9/23由来）が主要因と判明**。その上で7神のスコア期待値を比較したところ、**才華が他6神より約10〜20%高く、笑蓮が低い**という偏りを確認したが、共鳴スコア定数自体は150〜200の狭い範囲に収まっており、才華の突出は共鳴定数ではなく**コンボスコアの蓄積しやすさ（draw/gainAp主体のカードで1ラウンドに多くの安いカードを連打できるアーキタイプ設計）**が原因と特定した。両神とも勝率自体は健全（balanced/defensiveで88〜100%）であることも確認。CEOはこの結果を「才華＝技巧型・コンボ型、笑蓮＝支援型・単発火力低めというアーキタイプ設計を素直に反映した意図的な差」と判断し、**`rules.ts`のスコア係数・`gods.ts`の神固有値のいずれも変更せずTask A2をクローズ**する判断をした。あわせて、aggressive戦略は「勝率は低めだが勝利時スコアはbalanced/defensiveより高い」というハイリスク・ハイリターンの形を保っていることも確認し、これも意図された設計として維持。`rules.ts`・`gods.ts`・`effects.ts`への変更は一切無し。代わりに`balanceSim.test.ts`に**再現防止テストを2件追加**：①勝利時平均スコアが敗北時平均スコアの1.5倍を常に上回ることを保証、②aggressive戦略の勝率がbalancedより低いこと（リスク）と、aggressive勝利時の平均スコアがbalanced勝利時平均の0.9倍を下回らないこと（リターン）を保証。CEOの明示的な指示により、**「7神のスコアを均一化する」方向のテストはあえて書いていない**（それ自体が今回の判断に反するため）。テスト111→113件、型チェック・lint全て通過。
| 2026-08-14 | 決定60を追加。Task A3（カード効果・神託・OTOMO共鳴の手応え監査）として、①Exploreエージェントによる全60カード・3神託・7神resonanceEffects・14 OTOMO効果テーブル（7体×2成長パス）のtext⇔effects静的監査、②一時分析スクリプトによる同範囲の直接発動・GameState反映確認（`applyAction`/`applyEffects`を実際に呼び、予測デルタと実測デルタを数値で突き合わせ）、③共鳴を意図的に連続発動させてdoji形態到達・doji効果発動を直接確認、の3方向で監査を実施（分析用スクリプトは削除済み）。**結果：決定51/56と同種の「効果が保存されるだけで消費されない」バグの再発はゼロ件**。全60カード・3神託・7神共鳴・14 OTOMO効果テーブルとも、テキストの記述通りに効果がGameStateへ反映されることを確認した。`StatKey.def`は決定56から変化なく引き続き未使用（新規混入なし）。`cardArt.ts`未設定の4枚（決定51の第三弾カード）は意図的なSVGフォールバック仕様であり対応不要と判断。**唯一の実質的な発見は「効果は正常だが表示が分かりにくい」UI課題**：`PlayerPanel.tsx`/`EnemyPanel.tsx`のバフバッジと`formatEvent.ts`のイベントログが、buff/debuffのステータス名を`'atk'`/`'def'`という内部キーのまま日本語UIの中に表示していた（大耀「闘志」等のバフカード使用時に発生）。CEOはこれを「完成披露時の初見プレイヤーの分かりやすさに直接影響する」としてA3の範囲内で修正することを決定。**修正内容**：既存の`src/components/setup/godStyle.ts`の`STAT_LABEL`（`{atk:'攻撃力', def:'防御力'}`、神選択画面の特殊効果説明で既に使われていた）を`export`し、`PlayerPanel.tsx`・`EnemyPanel.tsx`（`STAT_LABEL[b.stat]`、`Buff.stat`は`StatKey`型で保証済みのため直接インデックス）・`formatEvent.ts`（`GameEvent.stat`は将来の拡張性のため素の`string`型のため、未知の値は元の文字列にフォールバックする`statLabel()`ヘルパー経由）の3箇所で再利用する形に統一した。`godStyle.ts`は`core/types`のみに依存し`battle/`側への依存が無いため、循環依存は発生しない（確認済み）。Effectロジック・GameState・`rules.ts`・カード/神/OTOMOの数値は一切変更していない、表示のみの最小修正。新規`formatEvent.test.ts`（3件：atk表示・def表示・未知stat文字列のフォールバック）を追加。他のUI発見（②神託「導きの託宣」の選ばれにくさ、③カードアート未設定4枚）はCEOの判断でバグではなく現仕様維持とし、今回は変更していない。テスト113→116件、型チェック・lint全て通過。
| 2026-08-14 | 決定61を追加。Task A4（敵AI・7ラウンドテンポ調整）の分析（7敵の行動パターン・平均決着ラウンド5.6〜6.2R・UI固定待機時間9.4〜11.6秒の実測、および`charge`使用敵が7体中2体のみという単調さの指摘）を踏まえ、CEOは4つの変更候補のうちB（戦闘セリフ拡張）とC（敵ターン演出待ち時間短縮）のみを承認し実施した。**A（chargeを使う敵の追加）とD（攻撃カーブの数値差別化）は`enemies.ts`の戦闘数値・行動構造に触れ、決定21・26・36・57の勝敗バランスを再び動かすリスクがあるため、8/31完成披露版のスコープからは明示的に除外し、8/31以降の敵AI拡張候補として§3-2に記録するのみに留めた**（実装なし）。**B**：全7敵の`battleCries`を各3種→7種に拡張（`src/core/data/enemies.ts`）。既存3種は一切変更・削除せず、各敵の性格・戦闘タイプ（試練の影＝試す者、業斧の鬼将＝猛々しい武人、藍花の怨霊＝怨念、銀甲の機工師＝機械的、双牙の魔獣＝獣性、蒼海の龍神＝神性、乱舞の道化＝不気味な道化）に沿った4種を追加。7種にした理由：既存の巡回ロジック（`(ラウンド-1)%length`、無変更）と`RULES.totalRounds=7`を組み合わせると、理論上どんなに長い戦闘でも同じセリフが2回出ることが無くなる（3種だと平均決着ラウンド5.6〜6.2Rで既に2周目に入っていた）。`actions`・`maxHp`等の戦闘数値・行動構造は一切変更していない。**C**：`ENEMY_TURN_REVEAL_MS`を900ms→700ms に短縮（`src/hooks/useGameEngine.ts`の定数1箇所のみ）。`CARD_PLAY_REVEAL_MS`(220ms)との比率を約3.2倍に保ち、カード1枚の演出より明確に長い「間」を維持しつつ、7ラウンド分積み重なる待機時間を短縮（平均6.2Rの戦闘で約1.24秒短縮）。charge/attackによる時間分岐は追加しなかった（判定ロジックへの分岐追加は8/31までのリスク対効果が見合わないと判断）。判定ロジック・敵行動・スコア計算には一切触れていない。テスト116件（無変更）・型チェック・lint全て通過。
| 2026-08-14 | 決定62を追加。Task B1（神選択・デッキ構築画面のレスポンシブ対応）として、375px／390px／430pxの3幅でGodSelectScreen・DeckBuilderScreenをブラウザ実測（`resize_window`が使えない環境のため決定50と同じiframe注入方式。Chrome既定の縦スクロールバーが幅測定に約19px分の誤差を生むことも確認し、`scrollbar-width:none`をiframe内に注入して実効幅を補正した上で計測）。**結果：3幅とも横スクロール発生ゼロ、コンテナからのはみ出しゼロ、神選択・デッキ構築とも操作に支障なし、「この構成でバトル開始」ボタンまで正常に到達可能**と確認できたため、**「レスポンシブ対応として修正が必要な構造的問題は存在しない」とCEOが判断し、`setup.css`を含め一切のコード変更を行わずTask B1をクローズ**した。**着手前の静的解析では`.deck-builder-toolbar`/`.deck-builder-actions`に`flex-wrap`が無いことを理由に375pxでのオーバーフローを予測していたが、実測ではボタン文字列の実際のレンダリング幅が想定より狭く、375pxでも24〜33px・390pxで25〜48px・430pxで48〜88pxの余白を保って収まっており、この初期仮説は実測によって明確に否定された**（静的なコード読み取りだけでは実際のフォントレンダリング幅を正確に予測できないことを示す一例。`flex-wrap`は追加しない）。GodSelectScreen・DeckBuilderScreenは`.god-select-grid`（flex-wrap）・`.deck-builder-cards`（CSS Grid `repeat(auto-fill, minmax(150px,1fr))`）・`.difficulty-select`/`.growth-path-select`（flex-wrap）といった可変ユニットで組まれており、**専用のmedia queryが無くても375〜430pxで自然に折り返す設計になっていた**ため、決定33由来の既存`@media (max-width:700px)`（`.home-hero`専用）を拡張する必要も無かった。**B3（タッチ操作最適化）へ移管した2件**：①`.deck-builder-card-stepper button`が26×26pxで、Apple HIG(44pt)・Material Design(48dp)の推奨を下回り、かつデッキ編成で頻繁に操作する主要UIであるため、タップ領域拡大をB3で検討する。②GodSelectScreenの`.god-stat-label`/`.god-stat-value`/`.god-stat-note`が9〜10pxと小さく、B3でスマートフォン実機の可読性として再評価する（この時点では数値変更しない）。**8/31以降へ回した項目**：`.deck-builder-toolbar`内ボタンの2行折返しの見た目改善、`.deck-builder-toolbar`/`.deck-builder-actions`の余白をより厚くする根本対応（いずれも現状動作に支障なし）。**参考記録（B1スコープ外）**：`App.css`の`.app-header`（"SEVEN GODS"タイトル・"開発環境稼働中"バッジ）が375pxで2行折返しになる現象を確認したが、`setup.css`範囲外かつ操作上の問題がないため今回は変更しない。テスト116件（無変更）・型チェック・lint全て通過。
| 2026-08-14 | 決定63を追加。Task B2（バトル画面レスポンシブ対応）として、カードバトル画面全体（敵/プレイヤー/OTOMOパネル・手札・神託UI・共鳴ゲージ・イベントログ・操作UI）を375/390/430pxでブラウザ実測。**横方向の破綻（横スクロール・要素のはみ出し・重なり・文字切れ）は3幅とも一切発見されなかった**（`body{overflow-x:hidden}`に隠された「見えない横はみ出し」も各要素の`getBoundingClientRect`実測で確認したが該当なし）。一方、**実際に対局を最後まで進めて決着させたところ、スマホ縦方向で重大な問題を発見した**：`.game-over-overlay`・`.reward-overlay`が`position: absolute; inset: 0`で、ビューポートではなく`.battle`（スクロール可能な戦闘画面全体、スマホでは単一カラム表示により2500px超に達する）を基準に中央配置されていたため、プレイヤーが画面下部（「ラウンドを終える」ボタン付近）までスクロールした状態で対局を終えると、勝敗カードが現在のビューポートより上（見えない位置）に表示され、スクロールして戻らない限り気づけない状態だった。コードベース内には既に正しいパターン（`TutorialOverlay`・`FeedbackOverlay`がどちらも`position: fixed`でビューポート基準）が存在していたため、**`.game-over-overlay`・`.reward-overlay`の`position`を`absolute`から`fixed`に変更**（`inset: 0`は維持、既存のz-index・レイアウト・ロジックは無変更）。修正後、375px／390px／430px／PC幅（1280px）の4条件で、①ページ最下部までスクロールした状態で決着させてもオーバーレイがビューポート内に完全表示されること、②オーバーレイが画面全体を覆うこと（`overlayCoversFullViewport`）、③`.app-header`より前面に表示されること（`elementFromPoint`で確認）、④オーバーレイ内ボタンが正しく操作可能なこと、⑤背景部分をクリックしても背面の要素ではなくオーバーレイ自身がヒットすること（誤操作防止）、⑥横スクロールが発生しないこと、を全てブラウザ実測で確認した。**ページ総丈（約2500px超）そのものの圧縮・再設計は大規模な変更になるため8/31以降のバックログとし、今回は着手しない**。`GameOverOverlay.tsx`・`RewardOverlay.tsx`のロジック、`GameState`、スコア計算、戦闘ロジック、カードサイズ、共鳴ゲージ、手札レイアウト、`battle-topbar`等、position以外のCSSは一切変更していない。テスト116件（無変更）・型チェック・lint全て通過。
| 2026-08-14 | 決定64を追加。Task A5（勝敗演出・リザルト完成度向上）として、実際に勝利2回・敗北2回をブラウザでプレイし（未撃破は3回試行するも簡易ヒューリスティックでは再現できず敗北止まり、コードのスタイル定義で代替確認）、GameOverOverlay/RewardOverlayのUXを調査。**調査結果**：横方向の破綻はなし。敗北時「スコア-10」という負のスコア表示を実測（極端な検証プレイでのみ発生と推測）。**最大の発見**は、勝敗間の視覚差別化が状態文字の色のみで、カード枠・ボタン装飾（金色）が勝敗問わず完全に同一だったこと。スコア内訳（damage/combo/apEfficiency/roundBonus/oracleBonus）が数値の羅列のみで「なぜこの点数か」「次にどう伸ばすか」が伝わらないこと。GameOverOverlay/RewardOverlayに登場アニメーションが無く瞬時表示だったこと。CEOは5つの改善候補のうち①敗北時の視覚的差別化②スコア内訳の分かりやすさ改善③登場アニメーションの3件を採用し（④神/難易度/ラウンド表示は8/31以降、⑤マイナススコア表示変更は現仕様維持で不採用）、以下を実装した。**①**：`GameOverOverlay.tsx`の`.game-over-card`に`game-over-card-${status}`のクラスを追加し、`battle.css`で`lost`（敵の攻撃力デバフ等で既存使用の`#e5484d`系）と`finished`（既存の`#7f8ab8`系）にのみボタン・カード枠の装飾を上書き（`won`は無変更、既存の金色を維持）。**②**：`rules.ts`（perDamage/comboPerExtraCard/unusedApPenalty/defeatBonus/perRemainingRound）・`round.ts`・`playCard.ts`・`effects.ts`のapplyVictory・A2で確認済みのoracleBonus発生源（天啓の託宣＋神/OTOMOの共鳴score効果）を実装コードで再確認した上で、5項目に一行の短い補足（「与えたダメージ」「1ラウンドに2枚以上でUP」「神力を余らせると減点」「早く倒すほどUP」「神託・共鳴の発動でUP」）を追加。特に神力効率は「使い切るとUP」ではなく常に0以下の減点方式（`unusedApPenalty`）であるため「余らせると減点」という正確な表現に補正した（CEO提示の例文をそのまま使わず、コード確認の上で修正）。**③**：`overlay-fade-in`（背景0.25秒フェード）・`overlay-card-pop-in`（カードfade+scale 0.35秒）のkeyframesを新設し、`.game-over-overlay`/`.reward-overlay`と`.game-over-card`/`.reward-card-panel`に適用。`prefers-reduced-motion`は、`index.css`の既存方針（環境演出の`body::before/::after`のみ無効化、`hit-shake`等の個別フィードバック演出は対象外）を確認した上で、本演出も既存のバトル内演出群と同じ扱い（対象外）とした。B2で修正した`.game-over-overlay`/`.reward-overlay`の`position:fixed`は変更していない。**副次的な重要発見**：`RewardOverlay`の内容（報酬カード3択）が長くなるケースで、パネル高さがビューポートを超えると`position:fixed`化（決定63）以降は`overflow-y:auto`等のスクロール機構が無いため「見送る」ボタンや下部のカードが完全に操作不能になることを375px実測で発見した（`TutorialOverlay`/`FeedbackOverlay`は`max-height:85vh; overflow-y:auto`を既に備えており、`.reward-card-panel`/`.game-over-card`にはこの対処が無い）。**今回のタスク範囲（登場アニメーションのみ）を超えるため修正はせず、次回以降の課題として記録するに留めた**。375px／390px／430px／PC幅（1280px）で実測し、勝利＝金色維持・敗北＝赤系トーン・未撃破＝グレー系トーンの3状態が視覚的に区別できること、スコア補足が読めること、375pxでも横スクロール・はみ出しが発生しないこと、`position:fixed`が維持されビューポート中央に表示されること、ボタンが操作可能で背面への誤操作が無いこと、PC表示に問題が無いことを確認した。UIコンポーネントの自動テストはこのプロジェクトの既存の慣習（React Testing Library等を導入していない）に合わせ追加しなかった。テスト116件（無変更）・型チェック・lint全て通過。
| 2026-08-15 | 決定65を追加。**A5検証中に発見した「RewardOverlayがスマートフォンで操作不能になるバグ」への独立した修正**（決定64の書き換えではなく別決定として記録）。**発見条件**：決定63（Task B2）で`.reward-overlay`を`position:fixed`化した際、内側の`.reward-card-panel`に高さの上限・スクロール機構が無かったため、報酬カード3枚の内容次第でパネル高さがビューポートを超えるケースが発生し、375px実測で「見送る」ボタンおよび3枚目のカードが完全に到達不能（`overflow:visible`のため、はみ出した領域は不可視かつクリック不能）になることを確認した。**原因**：`TutorialOverlay`（`.tutorial-card`）・`FeedbackOverlay`（`.feedback-card`）は`max-height:85vh; overflow-y:auto`を備えていたのに対し、`.reward-card-panel`にはこの対処が無かった（B2時点では`position:absolute`が`.battle`の巨大な高さの中に埋もれる形だったため、ページ全体のスクロールで到達できてしまい問題が表面化していなかった）。**採用した最小修正**：`.reward-card-panel`に既存のTutorial/Feedbackと全く同じ`max-height:85vh; overflow-y:auto`をそのまま追加（数値を新規に決めず、既存パターンをそのまま流用）。`position:fixed`（決定63）はそのまま維持し、変更していない。報酬カードの3択構造・カード内容・報酬ロジック・「見送る」処理・GameState・スコア計算・A5で追加した装飾/補足/登場アニメーションは一切変更していない。**GameOverOverlayの同種問題有無も確認**：`.game-over-card`は375×480という極端に低いビューポートでも常に十分な余白を残して収まっており（カード高400px、最も厳しい条件でも上下38pxの余白）、同じ問題は再現しなかったため、`.game-over-card`には変更を加えていない（「問題が無いなら不要な変更をしない」という方針を遵守）。**ブラウザ検証結果**：375px（実測clientWidth356、パネルmax-height563.55px＝85vh、`panelScrollHeight804px > panelClientHeight562px`によりパネル内部スクロールが有効化されることを確認、スクロール後「見送る」ボタンが`elementFromPoint`でヒットし操作可能になることを実証）／390px・430px・PC幅1280px（いずれもパネル内容がmax-height以下に収まり、`panelScrollable:false`＝不要なスクロールバーが出ないことを確認）。RewardOverlay→GameOverOverlayへの遷移も正常（`game-over-card-won`が正しく適用され、A5の装飾も維持）。テスト116件（無変更）・型チェック・lint全て通過。**2026-08-15朝、CEOが実機で①RewardOverlayのスクロール到達性を確認し、問題なし。正式採用。**
| 2026-08-15 | 決定66を追加。Task B3（タッチ操作最適化）として、B1で発見済みだった2件をブラウザ実測で再検証し最小修正した（夜間自律開発、CEO事前承認範囲内の低リスクUI改善として実施）。**①`.deck-builder-card-stepper button`（26×26px）**：数値を機械的に44/48pxへ変更せず、375〜430pxでブラウザ実測して安全な最大値を決定する方針を徹底した。実測の結果、カード幅（151〜179px）に対し`.deck-builder-card-body`の内側にはボタンサイズを拡大しても十分な余白があることが判明し（375px時点で40pxボタンでも左右各10pxの余白を確認）、**26px→40px**に拡大（Apple HIG 44pt・Material Design 48dpには届かないが、実測で確認できた安全な範囲内で最大限拡大した値）。375/390/430/1280pxいずれも`.deck-builder-card-stepper`が`.deck-builder-card-body`からはみ出さないこと、ボタンが引き続き正しく動作すること（0/2→1/2への加算を実機相当幅で確認）を実測済み。**②`GodSelectScreen`のステータス表示**（`.god-stat-label`/`.god-stat-value`10px、`.god-stat-note`9px）：`.god-stat-row`の固定24px幅の値欄について、実際にレンダリングされている全28行（7神×4軸）の最大値（32）を含めてフォントサイズ増加後も一切はみ出さないことを実測確認した上で、**10px→11px（label/value）・9px→10px（note）**に微増。カード幅220px・grid-template-columns（28px 1fr 24px）は無変更。両修正ともデッキ構築・カード数値・戦闘ロジック・GameStateには一切触れていない、CSS（`setup.css`）のみの変更。テスト116件（無変更）・型チェック・lint全て通過。**2026-08-15朝、CEOが実機で②ステッパーボタンの大きさ・操作感、③神選択画面のステータス表示を確認し、いずれも問題なし。正式採用。**
| 2026-08-15 | 決定67を追加。Task B4（スマホ実機/エミュレータ通し確認）を実施（夜間自律開発）。今夜実施したB1〜B3・A5・RewardOverlay修正（決定62〜66）を統合した状態で、375px幅においてホーム→神を選ぶ→才華選択→難易度確認→デッキ構築→バトル開始→カード使用＋神託使用＋ラウンド終了を7ラウンド分実施→勝利（ラウンド7）→RewardOverlay「今回は見送る」→GameOverOverlay（`game-over-card-won`・スコア内訳補足5件とも正しく表示）→「神・デッキを選び直す」→GodSelectScreenへ正常復帰、という一連の流れを実際に最後まで操作して確認した。`window.onerror`・`unhandledrejection`・`console.error`・`console.warn`を全て監視した上で**エラー・警告ゼロ**を確認。390px・430pxでも同一状態（GodSelectScreen）での横スクロール発生ゼロをスポット確認した。**新たな問題は発見されなかった**ため、コード変更は無し（`docs/DECISIONS.md`への記録のみ）。テスト・型チェック・lintは前回（決定66）から変更なし。
| 2026-08-15 | 決定68を追加。夜間自律開発（CEO一時離席、ブラウザ操作系ツール使用禁止の指示下）として、`src/core/`配下でテストファイルが存在しないソースファイルを静的に棚卸しした。ほとんどは他のテストから間接的に検証されている、または純粋なデータ/型定義でテスト不要と判断したが、**`src/core/rng/seededRandom.ts`（不変ルール2「Math.random()を使わない」の中核実装）に直接のテストが一件も無いことを発見**。特に「同じseed+cursorから再開すると、続けて呼んだ場合と完全に同じ数列になる」性質は、決定29（バトル状態のセーブ・再開＝「続きから」機能）が全面的に依存する前提だが、これを直接検証するテストが存在しなかった。既存ロジックは一切変更せず、新規`src/core/rng/seededRandom.test.ts`（8件：決定論的な同一性、seed違いでの非同一性、next()/nextInt()の値域、shuffleの非破壊性、callCountの正確性、**決定29が依存するcursor再開の一致性**、cursor省略時とcursor=0の同一性）を追加。テスト116→124件、型チェック・lint全て通過。ブラウザ検証は不要（純粋関数のユニットテストのみ）。

**同決定68内、追加試行の記録（暴走防止ルールにより撤回）**：`status:'finished'`（7ラウンド未撃破、決定3）への遷移を直接検証するテストも無かったため追加を試みたが、3回試行しても成立せず、ルール「同じ問題への修正を3回試しても解決できない場合、それ以上変更を重ねない」に従い**撤回した**（`reducer.test.ts`は変更前の状態に復元、テスト数は変わらず124件のまま）。試行内容：①恵比寿の初期デッキ＋ブロック優先プレイ→ラウンド5で敗北。②同条件＋回復も優先→やはり敗北。③防御アーキタイプの蒼毘＋そのおすすめデッキ＋ブロック/回復優先（攻撃札は一度も使わない）→それでも敗北。**推測される原因（CEO判断待ち）**：決定57/58で確認済みの「防御一辺倒での膠着（未撃破）」は**hard難易度限定で再現する現象**であり、normal難易度では蒼毘/寿楽/笑蓮であっても最終的に攻撃札を交えるAIでない限り敵を倒せず、単純な「攻撃札を一切使わない」ヒューリスティックでは生存もできない、という可能性が高い。`finished`状態への直接ユニットテストを追加する場合は、hard難易度＋蒼毘/寿楽/笑蓮のいずれかを使う必要があると推測されるが、これは新たな設計判断（今回のCEO不在中の安全な自律実装の範囲を超える）と判断し、これ以上の試行はせず次の安全なタスクへ進んだ。
| 2026-08-15 | 決定69を追加。夜間自律開発の継続として、`src/core/engine/buffs.ts`（`sumBuff`・`tickBuffs`。決定51で見つかった「バフが保存されるだけで消費されない」バグと同じ仕組みの中核だが、直接のテストが無かった）に新規`buffs.test.ts`（8件：同stat内の複数バフ合計、デバフ（負値）の合計、対象stat以外を無視すること、該当バフ無しで0を返すこと、`tickBuffs`の残りラウンド減算、0になったバフの除去、複数バフのうち期限切れのみ除去、元配列の非破壊性）を追加。純粋関数のみのテストのためブラウザ検証は不要、既存ロジックは無変更。テスト124→132件、型チェック・lint全て通過。

**同決定69内、追加試行の記録（撤回）**：`cardArt.ts`（カードID→イラストパス対応表）についても、登録パスが実際に`public/`配下に存在するかを確認するテストを試みたが、Node.jsの`fs`/`path`/`url`モジュールを使う実装が`npx tsc -b --noEmit`でエラーになった（`tsconfig`が`core/`を環境非依存に保つ設計のためNode型定義を含んでおらず、本プロジェクトのどのファイルもNode APIをimportしていない）。`tsconfig`の変更は設定ファイルへの変更であり今回の安全な自律実装の範囲を超えると判断し、テストファイルを削除して撤回した（テスト数は132件のまま変わらず、型チェック・lintを再度クリーンな状態で確認済み）。

**同じ決定57、CEOが「防御一辺倒の膠着を崩す」を選択した後の追加調査（実装→検証→撤回）。** 2つの案を実装して試した。①`RULES.difficulty.hard`に`piercePercent`（ブロックで軽減できない貫通ダメージ割合）を追加し、`effects.ts`に新設した`applyEnemyAttack`（`round.ts`の敵攻撃処理だけで使用、`applyDamage`本体・カードの自傷ダメージには無関係）で反映。20%→40%と試したが、蒼毘・笑蓮の未撃破100%は**一切動かなかった**。②`piercePercent`だけでは崩せなかったため、`playerHealMultiplier`（hardでの回復量減衰、`effects.ts`の`heal`ケースに適用）も追加し併用（貫通30%＋回復50%）したが、**それでも未撃破100%は不変**。1戦をステップ実行して原因を特定：防御AIは被弾後「HPが60%未満なら回復」という判断（決定26のpickCard）で、増えたAP（毎ラウンド+1、7ラウンド目で8AP）を使って回復カードを複数枚重ね掛けし、1ラウンドで大きく削れたHPもほぼ満タンまで戻していた。**貫通・回復減衰のどちらも「割合」で削る方式だったため、APが多いほど回復量も比例して増える防御特化ビルドには原理的に追いつけない**（削った分だけ、多いAPで取り返されるため）。一方で両案とも、balanced/aggressive等の**無関係な戦略の生存率を巻き添えで悪化させていた**（例：恵比寿balanced 83%→68%勝率に悪化）。CEOが求めていた「膠着の解消」には効果ゼロ、副作用だけがあるという明確な失敗だったため、`rules.ts`・`effects.ts`・`round.ts`への変更は全て撤回し、テストも決定57の計測テスト（`normal`/`hardの勝敗率を出す恒久テスト）だけを残してデバッグ用の一戦トレーステストは削除した。型チェック・lint・テスト110件、変更前と完全に同じ状態に復元済み。**次に必要なのは「回復量を割合ではなく絶対量で頭打ちにする」といった構造的な仕組み**（例：1ラウンドの回復合計に上限を設ける）で、既存のEffect種別の延長では対応できない設計判断のため、CEOに改めて相談する |
| 2026-08-15 | 決定70を追加。Task C1（OTOMO経験値・親密度トラッキング）として、CEO指示に従い実装前に加算タイミング・二重加算リスクを設計として整理した上で最小実装した。**設計判断（加算タイミング）**：「勝利時のみ」ではなく**決着（won/lost/finished）全般**で加算する方式を採用。理由は、OTOMOの共鳴発動はラウンド中に勝敗と無関係に起こりうるため、敗北した対局でもOTOMOと共に戦った成長実感を反映するのが「育成」の趣旨に合うと判断したため（`recordStorage.ts`の戦績カウント＝勝敗記録とは別軸の指標として設計）。**共鳴発動回数の取得元**：`GameEvent`ログを個別カウントする方式（useRef等の累積カウンタ）は採用せず、**決着時点の`state.otomo.form`（spirit/incarnate/doji）から導出**する方式にした。理由：①OTOMOの`form`は`START_GAME`時に必ず`'spirit'`から始まる（reducer.test.ts既存テストで保証済み）ため、決着時点のformはその対局1回分の成長を過不足なく表す。②これにより対局をまたいだ累積カウンタを一切持たずに済み、後述の二重加算・再開・リロード問題が設計上そもそも発生しなくなる。既知の近似として、共鳴がdoji到達後さらに発動した場合の3回目以降は反映されない（過小評価はあっても過大評価はしない安全側の設計として許容）。**二重加算対策**：`useGameEngine.ts`の`commit()`内、既存の`recordGameResult`と全く同じ決着ブランチ（`result.state.status !== 'playing'`）に`recordOtomoBond(result.state)`を追加する形とした。この分岐は、GameStateが`'playing'`から決着状態へ遷移した瞬間の1回しか実行されない（reducer.tsが決着後の追加アクションを例外で弾ぶことは`reducer.test.ts`の「throws once the game is already over」で既に保証済み）ため、同じ対局結果が2回加算される経路は存在しない。**セーブ再開時**：`resumeGame`は`commit()`を経由せず`setState`のみを行うため、再開そのものは加算のトリガーにならない。再開後に決着すれば、上記と同じ理由で1回だけ加算される。**ブラウザ更新（リロード）時**：`commit()`が一切呼ばれないため、更新そのものが加算を引き起こすことはない。**再戦時**：`startGame`が発行する`START_GAME`の結果は`status:'playing'`となり`commit()`のif分岐（`saveBattle`）に入るため、決着ブランチ（加算処理）には入らず、前回分の再加算は発生しない。**実装**：新規`src/hooks/otomoBondStorage.ts`（`recordStorage.ts`と同じパターン：`sevengods.otomoBond`キー、`version:1`、try/catch＋構造チェックで壊れたデータは空レコードにフォールバック、godId単位で記録）。保存する値は`battlesPlayed`（決着対局数）・`resonanceCount`（各対局の到達形態に応じたポイント、spirit=0/incarnate=1/doji=2の累計）・`dojiReached`（doji到達回数）の3つで、いずれも表示専用の生データ（Lv換算等の加工はC2側に委ねる設計）。`useGameEngine.ts`は`commit()`の決着ブランチに`recordOtomoBond(result.state)`を1行追加したのみ。**GameState・core/engine・カード効果・共鳴効果・rules.tsは一切変更していない**（戦闘バランスに影響なし、表示専用データの追加のみ）。**テスト**：新規`otomoBondStorage.test.ts`12件（未記録時の空レコード、playing状態は非加算、到達形態別のポイント加算、勝敗問わず同ルールで加算されること、神ごとの独立記録、localStorage不能時に例外を投げないこと、壊れたJSON／バージョン不一致／形不正データそれぞれのフォールバック、および「関数自体は呼べば増える＝二重加算防止は呼び出し側の1回性に依存する設計である」ことを明示するテスト）を追加。テスト132→143件、`npx tsc -b --noEmit`・`npx oxlint .`とも全て通過。ブラウザ検証は不要（純粋なストレージ関数のユニットテストのみ、UIはC2で実装予定）。**2026-08-15、CEO承認。ただし`resonanceCount`はC2のUI表示名として「累計共鳴回数」を使わないこと（実体は形態到達ベースの近似値であり、共鳴発動の実回数と一致しない）と指示。「共鳴成長ポイント」等、実データと矛盾しない名称をC2で採用する。保存単位`godId`は8/31完成披露版では現仕様維持とし、21-OTOMO実装時に`otomoId`単位へ移行するバックログを§3-2に記録した** |
| 2026-08-15 | 決定71を追加。Task C2（OTOMO育成画面）として、`otomoBondStorage.ts`（決定70）の記録データを表示する画面を新設した。**表示名の遵守**：CEO指示（決定70フォローアップ）どおり、`resonanceCount`は「累計共鳴回数」ではなく**「共鳴成長ポイント」**として表示し、実体（決着時点の到達形態ベースの近似値）と矛盾しない名称に統一した。**Lv・進捗バーの設計**：`resonanceCount`をLvへ変換する計算はC1では持たせず（C1報告時点の設計どおり）、新規`src/components/setup/otomoGrowthDisplay.ts`の純粋関数`computeOtomoGrowthDisplay`としてC2側に実装した。1Lvあたり3ポイント（`POINTS_PER_LEVEL`）を仮の閾値とし、この値は戦闘バランスに一切関与しない演出専用の定数のため`rules.ts`には置かず、不変ルール4（調整値の集約先）の対象外と判断した。**絆テキスト**：`battlesPlayed`・Lv・`dojiReached`から「まだ出会ったばかり」「共に歩み始めた相棒」「息の合った相棒」「固い絆で結ばれた相棒」の4段階を返す簡易ロジックを実装（現時点では7 OTOMO共通の汎用文言。神ごとの個性に沿った専用テキストは今回のスコープ外とし、将来の拡張候補として残す）。**画面配置**：`HomeScreen`に「OTOMOとの絆を見る」ボタン（新規`HeartIcon`、`icons.tsx`）を追加し、`GameFlow.tsx`の`SetupScreen`に`'otomoGrowth'`を追加して新規`OtomoGrowthScreen.tsx`へ遷移する構成にした（`GodSelectScreen`・`DeckBuilderScreen`の既存の画面遷移・stateには触れていない）。画面内は7神ぶんのOTOMOをカード一覧表示し、各カードにOTOMO名・所属神・Lv・進捗バー・共鳴成長ポイント・絆テキスト・（プレイ済みなら）対局数と童子到達回数を表示する。OTOMOの絵は常に童子（doji）形態のイラストを使用（現在の到達状態ではなく「成長の到達点」を見せる意図、`GodSelectScreen`が常に`god.art.main`を表示するのと同じ「代表イラストを出す」方針を踏襲）。CSSは`.god-select-grid`/`.god-select-card`/`.god-stat-track`等の既存の視覚言語を踏襲しつつ、専用クラス（`.otomo-growth-*`）として新設し、既存画面のクラスは一切共有・変更していない。`feedbackSnapshot.ts`の`SnapshotInput['setupScreen']`と`FeedbackSnapshot['screen']`にも`'otomoGrowth'`/`'OTOMO育成'`を追加し、この画面滞在中もフィードバック機能の状況表示が壊れないようにした（既存の型定義の拡張のみ、他の分岐は無変更）。**GameState・core/engine・カード効果・共鳴効果・戦闘スコア計算・rules.tsは一切変更していない**（表示専用画面の追加のみ、戦闘バランスへの影響なし）。**ブラウザ実測**：iframe注入方式で375px／390px／430px／PC幅（1280px）の4条件において、7枚のOTOMOカードが横スクロール・はみ出しなく表示されること、7枚とも画像が正しく読み込まれること（`naturalWidth`実測）、「ホームへ戻る」ボタンでホーム画面へ正しく戻ること、`window.onerror`・`console.error`ともにゼロであることを確認した。さらに実際に恵比寿で対局を1回最後まで進め（カードを一切使わずラウンド5で敗北、`reducer.test.ts`の既存の「ブロックしなければ敗北する」ケースと同じ挙動）、`useGameEngine.commit()`→`recordOtomoBond`→`localStorage`→本画面の再読み込みまでの一連の流れが実データで正しく動作することを確認した（`sevengods.otomoBond`に`{"battlesPlayed":1,"resonanceCount":0,"dojiReached":0}`が保存され、画面に「共に戦った対局 1回・童子到達 0回」「共鳴成長ポイント 0（次のLvまで 3）」「共に歩み始めた相棒」と正しく反映されることを確認。カードを使っていないためOTOMOのformはspiritのままで`resonanceCount:0`は期待どおり）。検証用に生成したlocalStorageのテストデータは確認後にクリアした。**テスト**：新規`otomoGrowthDisplay.test.ts`6件（未プレイ時の初期値、レベル境界のポイント計算、Lv到達による絆テキストの切り替わり、doji到達とLv5以上の組み合わせでの最上位テキスト、`pointsPerLevel`の固定値）を追加。`feedbackSnapshot.test.ts`にも1件追加（`otomoGrowth`画面の`screen`ラベル確認）。テスト143→150件、`npx tsc -b --noEmit`・`npx oxlint .`とも全て通過 |
| 2026-08-15 | 決定72を追加。Task D1（QuotaProvider Local実装）として、日付ベースの利用回数管理を行う独立モジュールを新規実装した。CEO指示により実装前に7項目（日付の注入方法／ローカルタイムとタイムゾーン／リセット条件／同日中の残数管理／破損データ・version管理／端末時計の不正操作／将来Server差し替え設計）を整理した上で着手した。**日付の注入方法**：`core/`のシード付き乱数（不変ルール2）と同じ思想を時刻にも適用し、`Date.now()`/`new Date()`を無条件に呼ぶのは各関数の引数`now`のデフォルト値としてのみに限定した。`getState(featureKey, limit, now?)`・`consume(featureKey, limit, now?)`はいずれも`now`を明示的に渡せる設計で、テストは全てモック不要で決定論的な`Date`を直接渡している。**ローカルタイム・タイムゾーン**：`dateKeyOf(now)`は`toISOString()`（UTC基準）ではなく`getFullYear`/`getMonth`/`getDate`（ローカルタイムゾーン基準）で'YYYY-MM-DD'を作る。UTC基準だとタイムゾーンによっては実際の現地日付と異なる日でリセットされてしまうため。月末→翌月・年末→翌年・うるう年の繰り上がりは独自の暦計算を持たず`Date`オブジェクト自身の計算に委譲した。**リセット条件**：スケジューラ等は使わず、`getState`/`consume`が呼ばれるたびに現在の`dateKey`と保存済みレコードの`dateKey`を比較する遅延リセット方式（既存の`recordGameResult`等と同じ「呼ばれた時点で判定する」設計思想）。**同日中の残数管理**：featureKeyごとに`{dateKey, used, lastSeenAtMs}`を保存し、`remaining = max(0, limit - used)`で算出。`limit`はQuotaProvider自身は知らず、呼び出し側が都度渡す設計にした（将来「1日7回」以外の上限を持つ機能が増えても、このモジュール自体に手を入れずに済む）。**破損データ・version管理**：`recordStorage.ts`と全く同じ方針（`sevengods.quota`キー、`version:1`、try/catch＋構造チェックで壊れたデータ・version不一致は空データへフォールバック＝実質「今日はまだ未使用」扱いになる安全側のデフォルト）。**端末時計の不正操作**：`lastSeenAtMs`（これまでに観測した`now`の最大値）を各レコードに保持し、観測した`now`がそれより5分（`ROLLBACK_TOLERANCE_MS`）を超えて過去だった場合は時計の巻き戻しとみなし、`dateKey`が変わって見えても日次リセットを発生させない設計にした。**明示的に許容する範囲**：この対策は「時計を過去に戻して何度もリセットを稼ぐ」手口のみを防ぐものであり、「時計を未来に進めて即座に翌日の割り当てを得る」手口は、サーバー時刻という信頼できる基準を持たないローカル実装である以上、原理的に防げないため今回は許容する（コード内コメントとして明記）。この制限こそが`QuotaProvider`をインターフェースとして独立させた理由でもあり、将来サーバー時刻を基準にする`ServerQuotaProvider`（同一インターフェースを実装）に差し替えれば、呼び出し側のコードを変えずに解消できる。**将来Server実装への差し替え設計**：`getState`/`consume`は将来のServer実装（API呼び出しが必要で本質的に非同期になる）を見据え、Local実装の時点から`Promise`を返す形にした（`LocalQuotaProvider`は同期処理を`Promise`で包むだけ）。**実装ファイル**：新規`src/hooks/quotaProvider.ts`（`QuotaProvider`/`QuotaState`/`QuotaConsumeResult`の型定義＋`dateKeyOf`純粋関数）・`src/hooks/localQuotaProvider.ts`（`createLocalQuotaProvider(): QuotaProvider`、localStorage実装）。**今回実装していないもの（D2待ち）**：デイリー加護のUI・実際の`featureKey`（例：託宣用の値）の決定、`GameState`・`core/engine`・カード・OTOMO・共鳴・スコア計算への反映は一切行っていない。`GameFlow.tsx`等の既存画面からもまだ呼び出していない（このモジュールはまだどこからも import されていない、文字どおり独立したモジュールとして完結させた）。**テスト**：新規`quotaProvider.test.ts`5件（`dateKeyOf`の基本ケース・タイムゾーン基準・月末→翌月・年末→翌年・うるう年）、新規`localQuotaProvider.test.ts`13件（CEO指定の9項目＝初回／同日消費／上限到達／翌日リセット／月末→翌月／年末→翌年／破損データ／version不一致／localStorage利用不能に加え、時計巻き戻しの検知・許容幅内の誤検知なし・featureKeyごとの独立管理を追加）で計18件を追加。テスト150→167件、`npx tsc -b --noEmit`・`npx oxlint .`とも全て通過。ブラウザ検証は不要（UIから未接続の純粋なストレージ・ロジックモジュールのみ） |
| 2026-08-15 | 決定73を追加。Task E1（自己ベスト・記録画面の新設）として、`recordStorage.ts`（決定48フォローアップ）に既に保存されている戦績データを可視化するだけの画面を新設した。**新しいランキングロジック・スコア計算・保存フィールドは一切追加していない**（`GodRecord`型・`recordStorage.ts`は無変更、CEO指示どおり既存データの可視化に限定）。**調査結果**：`recordStorage.ts`は`bestScore`・`bestScoreDifficulty`・`wins`・`losses`・`finished`・`fastestWinRound`の6フィールドを神ごとに保持済みだったが、既存の`GodSelectScreen`は`record.bestScore > 0`のときだけ「自己ベスト◯◯（◯勝）」という一行を各神カードの隅に添えるのみで、6フィールド中`bestScore`と`wins`の2つしか画面に出ていなかった（`bestScoreDifficulty`・`losses`・`finished`・`fastestWinRound`は保存されているが未表示だった）。**実装**：新規`RecordScreen.tsx`で、CEOが候補に挙げた5項目（自己ベスト・勝利数・敗北数・未撃破数・最速撃破ラウンド）に、既存データの一部である`bestScoreDifficulty`を自己ベストの補足として添えた構成にした（新規データではなく、`bestScore`に付随する既存フィールドの表示強化のため候補5項目の範囲内と判断）。「情報過多にならないように」との指示を踏まえ、7神を並べる形は`OtomoGrowthScreen`（決定71）と同じカードグリッドの視覚言語を踏襲し、専用クラス（`.record-*`）として新設（既存クラスとは非共有）。1件も対局していない神は数値の羅列ではなく「まだ記録がありません」の一言に切り替える分岐を入れ、未プレイの神ばかりのときに0だらけの表になるのを避けた。`fastestWinRound`が`null`（未勝利）の場合は「—」表示。**画面配置**：`HomeScreen`に「戦績を見る」ボタン（新規`TrophyIcon`、`icons.tsx`）を追加し、`GameFlow.tsx`の`SetupScreen`に`'record'`を追加して新規`RecordScreen.tsx`へ遷移する構成にした（`OtomoGrowthScreen`・`GodSelectScreen`等の既存の画面遷移・stateには触れていない）。`feedbackSnapshot.ts`にも`'record'`/`'戦績'`を追加（決定71のOTOMO育成画面追加時と同じ理由：この画面滞在中もフィードバック機能の状況表示が壊れないようにするための型拡張のみ）。**GameState・core/engine・カード・OTOMO・共鳴・スコア計算・ランキング通信のいずれにも触れていない**（表示専用画面の追加のみ）。**ブラウザ実測**：iframe注入方式で375px／390px／430px／PC幅（1280px）の4条件を確認。加えてCEO指定の3パターン（①空データ＝localStorage未保存、②1神のみ記録あり＝恵比寿だけ実データを注入、③7神すべて記録あり＝各神に異なる値を注入）を375pxで実測し、①は7枚とも「まだ記録がありません」表示・②は恵比寿のみ実データ表示で残り6枚は空表示・③は7枚とも正しい値が表示されること、いずれも横スクロール・カードのはみ出しが発生しないことを確認した。`window.onerror`・`console.error`ともにゼロ。検証用に注入したlocalStorageのテストデータは確認後にクリアした。**テスト**：`feedbackSnapshot.test.ts`に1件追加（`record`画面の`screen`ラベル確認）。`RecordScreen.tsx`・`recordStorage.ts`自体への新規ロジックは無いため（既存の`loadGodRecord`をそのまま呼ぶだけ）、新規ユニットテストファイルは追加していない（ブラウザ実測でCEO指定の3パターンを直接確認する方針を取った）。テスト167→168件（`feedbackSnapshot.test.ts`の追加分のみ）、`npx tsc -b --noEmit`・`npx oxlint .`とも全て通過 |
| 2026-08-15 | 決定74を追加。Task C3（OTOMO Lv到達時の演出的ご褒美）として、C1（決定70）・C2（決定71）で実装済みの育成記録・Lv計算を活かし、対局終了時にLvが実際に上がった場合だけ短い演出バッジを出す機能を実装した。**表示場所の調査**：候補4つ（対局終了後／RewardOverlay後／GameOverOverlay内／OTOMO育成画面へ戻った時）を検討し、**GameOverOverlay内**を採用。理由：①`newBest`（決定48フォローアップ）と全く同じ場所・同じ仕組みで実装でき、新規オーバーレイ・新規ナビゲーションstateが一切不要な最小変更。②RewardOverlayは`state.status==='won'`の時しか表示されないが、OTOMOは決定70の設計により敗北・未撃破でも育つため、RewardOverlayに紐付けると敗北時のLvアップを見逃す（C1の設計意図と矛盾する）ため除外。③OTOMO育成画面への遷移待ちは、プレイヤーがその画面を見ない限り体験自体を逃すため除外。**発動条件・二重表示対策**：`recordOtomoBond`（`otomoBondStorage.ts`）の戻り値を`void`から`{prevRecord, nextRecord}`（更新前後の記録）に変更（`recordGameResult`が`isNewBest`を返すのと同じ設計思想）。`useGameEngine.ts`に新規state`otomoBondChange`を追加し、`commit()`の決着ブランチで1回だけセットする（`newBest`と全く同じ1回性の保証：reducer.tsは決着後の追加アクションを例外で弾ぶため、同じ対局結果が2回セットされる経路は無い）。`startGame`/`resumeGame`/`resetGame`のいずれでも`otomoBondChange`をnullにリセットする（`newBest`と同一パターン）ため、①再戦時に前回の演出が残らない、②リロード時（`commit()`を経由しないため）に誤って表示されない、の両方を構造的に保証する。**Lv比較ロジック**：新規`otomoGrowthDisplay.ts`の`detectOtomoLevelUp(prevRecord, nextRecord)`（純粋関数）が、`computeOtomoGrowthDisplay`（決定71）でprev/nextそれぞれのLvを算出し、上がっていれば`{prevLevel, nextLevel}`を、変わらなければ`null`を返す。`nextLevel - prevLevel`をハードコードで+1と決め打ちしていないため、将来`POINTS_PER_LEVEL`や1戦あたりの獲得量が変わって複数Lv一気に上がるケースが発生しても正しく「Lv.X → Lv.Y」と表示できる（今回の数値では、1戦の最大獲得ポイントが2・1Lvに必要なポイントが3のため、2<3により1戦で2Lv以上一気に上がることは数学的に起こらないことも確認済み）。**表示**：`GameOverOverlay.tsx`に`otomoLevelUp`プロパティを追加し、`newBest`バッジのすぐ下に「💠 絆Lv UP！ {OTOMO名} Lv.{X} → Lv.{Y}」を表示。新規keyframesは作らず、決定64で既に使っている`overlay-card-pop-in`（fade+scale、過剰なバウンド無し）をそのまま再利用（CEO指示「既存の安全なパターンを再利用」に対応）。演出時間は0.5秒（CEO指定の目安0.5〜1.5秒の下限、戦闘テンポを止めないよう短めに設定）。`BattleScreen.tsx`が`engine.otomoBondChange`から`detectOtomoLevelUp`とOTOMO名（`getOtomoDef`）を組み立てて`GameOverOverlay`へ渡す。**禁止事項の遵守**：Lvによる戦闘補正・カード性能・共鳴効果・OTOMO効果・GameState・core/engine・スコア計算はいずれも変更していない（`otomoBondStorage.ts`・`otomoGrowthDisplay.ts`・`useGameEngine.ts`・`BattleScreen.tsx`・`GameOverOverlay.tsx`・`battle.css`のみ変更、新規ライブラリ追加なし）。**テスト**：`otomoBondStorage.test.ts`に2件追加（戻り値のprev/next検証、playing状態ではprev===nextを返すこと）。`otomoGrowthDisplay.test.ts`に`detectOtomoLevelUp`のテスト5件追加（Lv上昇時／変化なし時／prev・next同一時／Lv低下時にもnullを返すこと／resonanceCount+0でLv据え置き時）。テスト168→175件、`npx tsc -b --noEmit`・`npx oxlint .`とも全て通過。**ブラウザ実測**：375px・PC幅（1280px）の両方で、恵比寿のOTOMO育成記録をLv2直前（resonanceCount=2）に事前投入した状態から実際に対局をプレイし、共鳴発動でOTOMOが受肉態に到達→決着→GameOverOverlayに「💠 絆Lv UP！ 鯛丸 Lv.1 → Lv.2」が実データで正しく表示されること、`localStorage`の`sevengods.otomoBond`が`resonanceCount:3`（Lv2相当）に正しく更新されていることを確認した。さらに①同条件で「同じ構成でもう一度」を押した直後（新しい対局の1ラウンド目）にバッジ・オーバーレイが残っていないこと、②バトル途中でブラウザをリロードし「続きから」で再開した直後にもバッジ・オーバーレイが出ないこと、③共鳴を一切発動させずに対局を終えた場合（resonanceCount+0でLv据え置き）にバッジが表示されないこと、④375px・PC幅どちらでも横スクロール・要素のはみ出しが発生しないこと、⑤`window.onerror`・`console.error`がゼロであること、を全て確認した。検証用に投入したlocalStorageのテストデータは確認後にクリアした |
| 2026-08-15 | 決定75を追加。Vercel本番公開URL（`https://seven-gods-game.vercel.app/`）の公開確認を実施し、正式に完了とした。トップ画面表示・神選択→デッキ構築→バトル開始の一連の導線・375px幅での崩れ無し・主要画像23点＋BGM4曲の404無し（全て200）・コンソールエラー無し（アプリ起因分）を確認済み。運営への第2週進捗報告・Vercel開発版共有は完了。検証用に投入したlocalStorageのテストデータは確認後にクリア済み |
| 2026-08-15 | 決定76を追加。F1（8/30 Freeze前統合リグレッション）準備として唯一の明確な自動テスト空白だった`status:'finished'`（7ラウンド終了・未撃破、決定3）への遷移を、コード変更無しで直接検証できる形で再現・テスト追加した。**過去3回（決定68追記）の失敗**は、AI戦略を実プレイ相当にシミュレートして「未撃破になるまで7ラウンド回す」確率的な再現方法だったため成立しなかった。今回は方針転換し、`round.ts`のコードから遷移条件そのものを特定した上で、**その条件を満たす状態を直接組み立てて`applyAction`に1回渡すだけ**の決定論的な再現に切り替えた。**遷移条件の特定結果**：①`END_ROUND`アクションの`finishRound`（`round.ts`）で判定される。②`endRound.ts`の処理順は「敵のターン（`runEnemyTurn`、プレイヤーへの攻撃を先に解決）→ラウンド終了処理（`finishRound`）」の順。③`finishRound`は`if (next.status !== 'playing') return`を`if (next.round >= RULES.totalRounds)`より先に評価するため、**同じEND_ROUND呼び出し内で敵の攻撃によりプレイヤーが力尽きていれば（lost）、finishedには絶対に上書きされない**（＝lostがfinishedより優先される、勝利は仕組み上PLAY_CARD時点で確定しfinishRoundを待たないため「won vs finished」という優先順位の分岐自体はEND_ROUND内には存在しないことも確認した）。④よって`finished`が成立する条件は「ラウンド7の`END_ROUND`時点でプレイヤーが生存しており、かつ敵をまだ倒せていない（status依然`'playing'`）」の1点のみ。⑤`RULES.totalRounds`（7）はdifficultyに関わらず共通のため、この遷移条件自体に難易度差は無い（難易度が変えるのは「その条件に到達しやすいかどうか」という発生確率のみで、遷移ロジック自体は同一）。**再現方法**：既存の`startTestGame()`（`testUtils.ts`）で作った通常の初期状態を、`recordStorage.test.ts`等で既に使われている「状態を直接spreadで組み立てる」パターンを踏襲して`round: RULES.totalRounds`・`phase: 'playerTurn'`・`status: 'playing'`・`player.hp: 999999`（rules.ts側の具体的な攻撃力とは無関係な安全マージン、数値検証が目的ではないため意図的に大きい値にした）・`enemy.intent: null`（ラウンド1の古い予告が残らないよう再計算させる）に上書きし、`applyAction`へ`END_ROUND`を1回渡すだけで100%決定論的に`finished`が再現できることを確認した。AI戦略・特定の神・特定の難易度への依存は無い。**優先順位の確認テストも追加**：同じ状態から`player.hp`を1に変えるだけで、ラウンド7の敵攻撃（`trial`のR7攻撃力15、`enemies.ts`）により`lost`になり`finished`に上書きされないことを別テストで直接確認した（`won`側は前述の理由でEND_ROUND内に到達しえないため対象外と判断）。**変更内容**：`src/core/engine/reducer.test.ts`に2件追加のみ。`GameState`仕様・coreロジック・数値（`rules.ts`・`enemies.ts`）はいずれも無変更、一時的なデバッグコードも残していない。テスト175→177件、`npx tsc -b --noEmit`・`npx oxlint .`とも全て通過。ブラウザ検証は不要（純粋なreducerユニットテストのみ）。**F1チェックリストへの反映（評価）**：`status:'finished'`は「CEO実機確認が必須（既知のギャップ）」から**「自動テストで直接担保＋ブラウザ表示のみ確認」へ格下げ可能**と判断する。遷移ロジック自体は今回のテストで100%決定論的に担保されたため、F1で残る作業は「GameOverOverlayが`finished`状態を正しく表示するか（文言・スタイル・スコア表示）」というUI描画確認のみであり、これは既存のブラウザ自動操作の手法（iframe注入）で対応可能。実際に「未撃破になるまで人力で対局を進める」作業はF1当日不要になった |
| 2026-08-15 | 決定77を追加。CEO実機リハーサルで発見された「PC幅で『ラウンドを終える』ボタンに到達するまで毎ラウンド縦スクロールが必要」というUX課題への最小修正。**調査結果**：`.battle`は単純な縦積み（`flex-direction:column`）で、`.battle-main`（敵/自分/OTOMOパネル）単体が800px前後を占めるため、PC幅（1280px）でもビューポート高800〜1000pxで463〜663pxのスクロールが常に必要だったことを実測確認。375〜430pxのスマホでは元々スクロールが前提の操作性でありB2〜B4（決定62〜67）で問題視されなかったため、**PC特有の課題**と判断した。**案の比較**：`position:sticky`と`position:fixed`をブラウザ実機注入で検証したところ、いずれもボタンの本来位置が画面外にあるためスクロール量ゼロの時点で画面下部に浮き、`.battle-main`内のプレイヤー立ち絵と重なることを確認（`elementFromPoint`で実証）。ボタンを上部へ再配置する案は誤操作リスク増加・大規模レイアウト変更に近くなるため不採用。CEOの判断により**`position:fixed`を「常設の操作バー」として意図的に見せる方針**を採用。**実装**：`src/components/battle/battle.css`の末尾に`@media (min-width: 900px)`ブロックを追加し、`.end-round-button`を`position:fixed; left:50%; bottom:20px; transform:translateX(-50%); z-index:3`とし、既存の金色パネル系と同じ`backdrop-filter:blur(8px)`を追加、`.battle`に`padding-bottom:90px`を追加してページ末尾のコンテンツが隠れないようにした。ホバー時の`transform`も上書きし中央寄せが崩れないようにしている。**JSX（`BattleScreen.tsx`）・`GameState`・core/engine・カード効果・AP・ラウンド進行ロジックは一切変更していない**（`disabled`制御・`onClick`はBattleScreen.tsx側で無変更のため論理的に不変）。**ブラウザ実測**：1280×800／1280×1000のいずれもスクロール量ゼロでボタンが完全に可視・クリック可能で、実際にクリックしてラウンドが正しく進行することを確認。375／390／430pxは`position:static`・`.battle`の`padding-bottom:0px`のまま（メディアクエリの範囲外）で、`bodyScrollHeight`が変更前の実測値（375pxで2255px）と完全一致することを確認し、**スマホは一切影響を受けていない**ことを実証した。GameOverOverlay（z-index:10）が固定ボタンより確実に前面に来ることを`elementFromPoint`で実証（`elementFromPoint`がオーバーレイ自身を返す＝ボタンは背後に隠れクリック不能になる）。RewardOverlay（z-index:9）も同一のCSS機構（固定ボタンより高いz-index）のため同様に機能する設計だが、今回のブラウザ検証では敗北ルートのみ確認できた（コードレベルでの確認に留まる）。横スクロール・コンソールエラーはいずれの幅でも発生しなかった。**テスト**：CSSのみの変更のためテストケースの追加は無し。テスト177件（無変更）、`npx tsc -b --noEmit`・`npx oxlint .`とも全て通過 |
| 2026-08-15 | 決定78を追加。8/31 UI/UX最終監査で発見したP0①「共鳴ゲージのラベル文字が未充填部分（濃紺背景）でほぼ判読不能」を修正。`.resonance-gauge-label`の`color`を`#05060d`（ほぼ黒）から、既に`.hp-bar-label`で実績のある`color:#e8e9f3; text-shadow:0 1px 2px #000`パターンに統一しただけ（`battle.css`1箇所のみ、JSX・GameState・core/engine・共鳴ロジックは無変更）。同クラスにスマホ向けの上書き定義は存在しないため、変更は全幅共通で適用される。**ブラウザ実測**：本番URLで実際に共鳴0/7（開始直後）・2/7・5/7・6/7（満タン直前）・発動後0/7リセットの各状態を再現し、いずれもラベルが明瞭に読めることを確認。`npm test`（177件）・`npx tsc -b --noEmit`・`npx oxlint .`とも全て通過 |
| 2026-08-15 | 決定79を追加。8/31 UI/UX最終監査で発見したP0②「PC幅でバトル画面を開くと、手札はおろか共鳴ゲージすら初期表示で画面外」を修正。`.battle-main`（敵/自分/OTOMOパネルの縦積み）単体で800px前後を占め、決定77で解決したのはボタン到達性のみで、その手前の手札・共鳴ゲージの可視性は未解決だったことをブラウザ実測で確認済み。3案（A:縦圧縮のみ／C:手札をposition:fixed常時表示／B:PC限定で敵とプレイヤー+OTOMOを横2カラム化）を実機・座標計算で比較し、C（および A+Cの素朴な組み合わせ）は固定手札バーが敵HPバーを覆い隠す致命的な問題を確認したため却下。**採用**：B案。`src/components/battle/battle.css`の既存`@media (min-width:900px)`ブロックの直後に新規ブロックを追加し、`.battle-main`を`display:grid; grid-template-columns:1fr 1fr`（左＝敵、右＝プレイヤー+OTOMO/共鳴を縦積み）に変更、アバターを200×240px→150×180pxに調整、`.battle-main .panel`のpadding/gapを最小限圧縮した。JSX・GameState・core/engine・カード効果・AP・ラウンド進行は無変更（DOMの兄弟順はそのまま、CSSで列配置しているのみ）。決定77の固定「ラウンドを終える」バーはそのまま維持。**ブラウザ実測・座標計算**：1280×1000相当は敵HP・プレイヤーHP・共鳴ゲージ・手札・ラウンド終了ボタンまで完全にスクロール0で収まることを確認。1280×800相当は手札下端のみ約28pxはみ出す（キャラクターを不自然に縮めてまで完全1画面化はしないというCEO方針の範囲内として許容）。実測できたローカル環境（1034×515、極端に低いビューポート）では敵HP・プレイヤーHPはスクロール0で確認でき、手札までのスクロール量は従来（約950px）から大幅に削減されることを確認。横スクロールなし、カード操作（AP消費・ラウンド進行）が引き続き正常に動作すること、コンソールエラーがゼロであることを確認。CEO実機確認によりB案を正式承認。z-index・オーバーレイ関連CSSには一切触れていないため、Reward/GameOverOverlayとの競合は構造上発生しない。375/390/430pxは今回のCSSが全て`@media (min-width:900px)`内に限定されているため無変更（メディアクエリの範囲外という構造的な保証。今回の検証環境では実機resize不可のためスクリーンショットでの再確認はできていない）。`npm test`（177件）・`npx tsc -b --noEmit`・`npx oxlint .`とも全て通過 |
| 2026-08-15 | 決定80を追加。CEO承認済み`FINAL_BACKLOG_8-31.md` Phase 1（①〜⑥）の実施。本セッション開始前に完了・コミット済みだった`e55c846`「phase1 polish and bug fixes」・`09be490`「Phase1(6) god select -> home return」の内容を、本ドキュメントへの記載漏れを埋める形で追記する（コード内には既に「決定80（Phase 1）」という参照コメントが7箇所存在しており、番号自体はコード側が先に使っていたものに本ドキュメントを合わせた。決定1〜79・当該2commitのhashやコード内容はいずれも変更していない）。**①共鳴7/7・敵ターンバナーのfixed化**：`.burst-banner`/`.enemy-turn-banner`を`position:absolute`→`fixed`に変更（`.battle`が縦長要素のため、absoluteのままだと基準が`.battle`全体になり画面外に描画されるバグがあった。決定63のGameOverOverlay/RewardOverlayと同じ修正）。**②ミュート解除時のSE一斉再生バグ修正**：`muted`propを`GameFlow`→`BattleScreen`→`useBattleSound`と経由させ「ミュート中はlogを`[]`に見せる」二重防御をしていたが、これが原因でミュート解除の瞬間に`useBattleSound`内部の`seenCount`が0にリセットされ、その対局の全SEが一斉再生されるバグを引き起こしていた。`sound.ts`側の`mutedフラグのみ`で完結させ、`muted` propの受け渡し自体を撤去。**③「開発環境 稼働中」バッジ・技術footer除去**：`App.tsx`から`<span className="app-badge">開発環境 稼働中</span>`と`<footer>React + TypeScript + Phaser 3</footer>`を削除。**④「成長の絆」の誤説明修正**：`GROWTH_PATH_OPTIONS`の説明文が「守り＝HP回復・ブロック」「力＝ダメージ・妨害」という断定的な表現で、OTOMOごとの実際の`effectsByForm`/`powerPathEffectsByForm`と矛盾するケース（例：小槌は守り/攻めの説明が実データと逆）があったため、7体共通で矛盾しない方向性のみの表現に変更（正確な実効果表示はPhase 3「OTOMO形態効果・共鳴効果の文章表示」で対応予定、2026-08-15時点で未着手）。**⑤スコア内訳の英語キー日本語化**：`formatEvent.ts`に`SCORE_REASON_LABEL`（`damage`→ダメージ等）を追加し、`SCORE_GAINED`イベント表示の内部キーそのままの露出（例：「スコア+200（oracle）」）を解消。**⑥神選択画面からホームへの復帰導線追加**：`GodSelectScreen`に「‹ ホームへ戻る」ボタンを追加し、`GameFlow`の`onBack`配線・`resetGame()`後の`savedBattle`再読み込みを行い、一度神選択に進むとリロードするまで戻れなかった問題を解消。①〜⑤は`e55c846`、⑥は`09be490`（コード内コメントでは「決定80フォローアップ：Phase1⑥」と表記）。テスト・型チェック・lintの結果は各commit時点のものを参照 |
| 2026-08-15 | 決定81を追加。CEOが約6時間離席する間の自律作業として、`FINAL_BACKLOG_8-31.md`のB候補「ErrorBoundary新設」を実装した。新規`src/components/ErrorBoundary.tsx`（Reactクラスコンポーネント。フックには同等機能が無いためこの1ファイルのみ例外的にクラスを使用）を追加し、`main.tsx`で`<App />`を包んだ。描画中の例外を捕捉した場合、日本語の簡単な案内文とリロードボタンのみを表示し、**状態の復旧は一切試みない**（白画面事故の保険という目的の範囲に限定）。既存の画面・状態管理・GameState・core/engineはいずれも無変更。`npm test`（179件、既存から無変更）・`npx tsc -b --noEmit`・`npx oxlint .`とも全て通過。ページロード時点でのコンソールエラー0件は確認したが、実際に例外を発生させてフォールバック画面自体を表示させる実機確認はまだ行っていない |
| 2026-08-15 | 決定82を追加。同じく自律作業として、`FINAL_BACKLOG_8-31.md`のB候補「勝利リザルトカードへの金枠追加」とPhase3「OTOMO進化バナーの新設」を、既存パターンの流用のみで実装した。**勝利リザルト金枠**：`.game-over-card`は`game-over-card-${status}`でstatus別クラスが付くが、`lost`/`finished`用の上書きスタイルはあるのに`won`用が無く、カード枠自体は常に既定の紺色のままだった（ボタンのみ既存の金色`#ffd166`グラデーションで祝祭感を出していた）ことをコードで確認。`.game-over-card-won`に同色のボーダー＋グローのみを追加した（`-lost`/`-finished`と同型）。**OTOMO進化バナー**：`.burst-banner`/`.enemy-turn-banner`（いずれも決定80、`position:fixed`化済み）と同型の`.evolve-banner`を新設し、`z-index:7`（6と9の間、決定48フォローアップのz-index順序を維持）とした。表示テキストは新しいラベル定義を作らず、既存の`formatEvent()`のOTOMO_EVOLVEDケース（`OTOMOが◯◯に成長した！`）をそのまま再利用。`useBattleFx.ts`の`BattleFx`に`evolveForm`を追加し、`OTOMO_EVOLVED`イベントの`form`をバナー表示用に伝搬した（`apPenaltyAmount`と全く同じ既存の伝搬パターン）。**変更範囲**：`battle.css`・`useBattleFx.ts`・`BattleScreen.tsx`のみ。GameState・core/engine・カード効果・共鳴・スコア計算・OTOMOの実効果はいずれも無変更。`npm test`（179件）・`npx tsc -b --noEmit`・`npx oxlint .`とも全て通過。ページロード時点でのコンソールエラー0件は確認したが、実際に勝利・OTOMO進化のシーンを再生してのビジュアル確認はまだ行っていない |
| 2026-08-15 | 決定82フォローアップを追加。CEO実機確認により、決定82のOTOMO進化バナーが共鳴バーストバナーと完全に重なって読めなくなる不具合が発覚（両方とも`fx`の同一state更新で同時にマウントされる設計だったため）。共鳴バースト→OTOMO進化の順で時間差表示するよう修正した（commit `2b764ab`）。`.burst-banner`に`onAnimationEnd`ハンドラを追加し、その発火（＝共鳴バーストの表示が実際に終わった瞬間）をトリガーに進化バナー用のローカルkeyを起動する方式（`setTimeout`不使用、CSSのanimation-durationとJS側の遅延秒数を別々に持たないための設計）。`.burst-banner`のanimation-durationを1.4s→0.9s、`.evolve-banner`を1.4s→1.0sに短縮（総演出時間 約1.9秒、CEO指示）。変更範囲は`BattleScreen.tsx`・`battle.css`のみ、`core/engine`・`useBattleFx.ts`のイベント検知ロジックは無変更。`npm test`（179件）・`npx tsc -b --noEmit`・`npx oxlint .`とも全て通過。CEO実機確認により、共鳴バースト→OTOMO進化の順で重なりなく表示されることを確認済み |
| 2026-08-16 | 決定83を追加。`FINAL_BACKLOG_8-31.md` Phase 2「カード画像軽量化・OOM対策」を実施（commit `7f53f25`）。元の1024×1536 PNG（56枚・計約200MB）をブラウザCanvas API（`toBlob('image/webp', 0.85)`）で512×768・WebP品質0.85に一括変換し、`cardArt.ts`の参照を全て`.webp`に切り替えた（56枚合計約6.15MB、削減率96.9%）。元PNG56枚は`public/assets/cards/`にそのまま保持し削除していない（将来の再変換・高解像度化のソースとして温存）。512×768を選んだ理由：手札(132px)・デッキ構築(150〜190px)・報酬画面(140px)いずれの実表示幅もDPR3倍まで512pxで鮮明にカバーできる一方、デコード後メモリ（フォーマット非依存、幅×高さ×4byte）は元の約1/4（6.0MB→1.5MB/枚）に削減され、デッキ構築画面で最大32枚同時デコード時のOOMリスクを直接緩和する。`TutorialOverlay.tsx`・`CardView.tsx`・`RewardOverlay.tsx`・`DeckBuilderScreen.tsx`の4ファイルの`<img>`に`loading="lazy" decoding="async"`を追加し、DeckBuilder同時デコード数のピーク抑制も行った。GameState・core/engine・カード効果・rules.tsは無変更（画像参照とロード属性のみ）。push前監査（2026-08-16、本ドキュメント外）で`cardArt.ts`の全参照パスと`public/assets/cards/`実ファイルの1:1突合を行い、欠落・孤立ゼロを確認済み |
| 2026-08-16 | 決定84を追加。`FINAL_BACKLOG_8-31.md` B候補「未実装カード4枚（連撃・闘志・見通し・浄めの光）の新規生成」を実施（commit `25463f2`）。`CARD_ART_STYLE_GUIDE.md`（2026-08-15付、設計案）の型に基づくプロンプトでCEOが生成したマスター画像を、既存56枚と同じCanvas API手順（512×768・WebP品質0.85）で変換し、`cardArt.ts`に`card_common_attack_09`（連撃）・`card_common_support_06`（闘志）・`card_common_support_07`（見通し）・`card_common_hinder_05`（浄めの光）として登録した。既存56枚と異なり、新規PNGマスターは中間生成物としてプロジェクトに保存していない（生成→変換のみ）。カード効果・コスト・rules.tsは無変更（画像追加のみ）。push前監査（2026-08-16）で4枚の`CARD_IDS`（`cards/common.ts`）と`cardArt.ts`のID一致、および本番URL（Vercel）での実表示・404無しを確認済み |
| 2026-08-16 | 決定85を追加。`FINAL_BACKLOG_8-31.md` Phase 3「共鳴／進化／勝敗など重要SEの音量差調整」を実施（commit `eb7e674`）。共鳴／OTOMO進化／勝敗という「重要SE」が、カード使用等の頻出SEと比べて音量差で強調されていなかった（特に`otomoEvolve`がheal等の些末なSEと同じ音量0.09で埋もれていた）ことをCEOが指摘。頻出SE（`cardPlay`/`cardDrawn`/`block`/`resonanceGain`等）は据え置き、重要SEのうち唯一浮いていた`otomoEvolve`のみ、既に十分大きい`resonanceBurst`/`victory`と同じ0.13に引き上げる最小変更とした（`defeat`は元々0.11で十分距離があるため据え置き）。`sound.ts`1行のみの変更、GameState・core/engineは無変更 |
| 2026-08-16 | 決定86を追加。`FINAL_BACKLOG_8-31.md` Phase 3「OTOMO形態効果・共鳴効果の文章表示」を実施（commit `5c24a5b`）。決定80④で「守り＝HP回復・ブロック」「力＝ダメージ・妨害」という断定的な説明が一部のOTOMOで実データと矛盾していた（例：小槌は守り/攻めが逆）ため方向性のみの表現に留めていたが、今回`otomo.ts`/`gods.ts`の実データから実効果を文章化して表示するようにした。**実装前調査**：OTOMO形態効果は`otomo.ts`の`effectsByForm`/`powerPathEffectsByForm`、共鳴効果（神側）は`gods.ts`の`resonanceEffects`が唯一の情報源。既存の`describeSpecial()`（`godStyle.ts`）は`buff`/`debuff`/`draw`/`gainAp`の4種のみ対象（`damage`/`block`/`heal`は数値バー担当、`score`は完全に未表示という穴があった）。**CEO承認案（案A・最小表示）**：3形態すべては出さずOTOMO側は童子（最大成長）時の効果のみ、神側`resonanceEffects`は成長方針セレクター見出し直下に1回だけ、各絆カード内はその絆の童子時効果のみを表示する方針で承認。**実装**：新規`src/components/setup/otomoEffectText.ts`に純粋関数`describeEffectList()`を追加し、`damage`/`block`/`heal`/`score`/`buff`/`debuff`/`draw`/`gainAp`/`resonance`（型上存在するが実データ未使用）を全対応、未知の`kind`が来ても`null`を返し読み飛ばす安全設計とした。`DeckBuilderScreen.tsx`の`GROWTH_PATH_OPTIONS`から固定`description`を廃止し、「共鳴発動：〜」（見出し直下）・「童子まで育つと：〜」（各絆カード内）を動的表示するよう変更。`setup.css`に`.growth-path-resonance`のみ新規追加（折りたたみ・ツールチップ等の新規UI要素は追加していない）。`core/engine`・`GameState`・`otomo.ts`・`gods.ts`の数値・型はいずれも無変更（既存`Effect[]`データを読むだけ）。`npm test`（179件、既存から無変更）・`npx tsc -b --noEmit`・`npx oxlint .`とも全て通過。**ブラウザ実測**：CEOが375px／390px／430px／PC幅の全てで文字切れ・枠はみ出し・重なり・横方向のレイアウト崩れ無しを確認済み。push前監査（2026-08-16）・本番URL（Vercel）実機確認（恵比寿・大耀・笑蓮の3神）でも、共鳴発動文言（`score`効果を含む笑袋のケースも含む）が事前算出値と完全一致することを確認した。**副次的に発見した既知課題（未修正）**：375px幅で`setup-title`（「恵比寿のデッキを編成しよう」等）の末尾1文字が次行に折り返される表示崩れをCEOが発見。Phase 3の変更とは無関係の既存事象のため、別途調査・対応する |
| 2026-08-16 | 決定87を追加。決定86で発見された「375px幅で`setup-title`の末尾1文字が次行に折り返される」課題を修正（commit `4308f92`）。原因は神名の文字数（例：「恵比寿のデッキを編成しよう」）と375px幅時のフォントサイズ・余白の組み合わせで、見出し全体の描画幅が僅かにビューポート幅を超えていたこと。`setup.css`に`@media (max-width:380px){.setup-title{font-size:26px}}`を追加し、375px幅限定でフォントサイズのみを縮小する最小修正とした（380pxをしきい値にしたのは既存の375/390/430pxブレークポイント設計（決定62）に合わせ、390px以上には影響させないため）。JSX・`GameState`・core/engine・カード効果・数値はいずれも無変更。**ブラウザ実測**：375px／390px／430px／PCの全幅で「恵比寿のデッキを編成しよう」を含む全7神の見出しが1行に収まることを確認し、390px以上のレイアウトに変化がないことも確認済み。`npm test`（179件、無変更）・`npx tsc -b --noEmit`・`npx oxlint .`とも全て通過。commit `4308f92`は`origin/master`へpush済み・Vercel本番URLへの反映も確認済み（2026-08-16）。`docs/RELEASE_STATUS.md`・`docs/FINAL_BACKLOG_8-31.md`の「未修正」表記が本決定の記録漏れにより取り残されていたため、2026-08-17に本決定と同時に訂正した |
| 2026-08-17 | 決定88を追加。CEO就寝中の夜間自律作業（コード変更・push禁止、docs整理と読み取り専用調査に限定）として、以下4点を実施した。**① 375px修正のdocs記録漏れ訂正**：決定87参照、`RELEASE_STATUS.md`・`FINAL_BACKLOG_8-31.md`の「未修正」表記を修正済みに更新。**② PC幅701〜899px帯域の再現調査（コード変更なし）**：`battle.css`の`.end-round-button`固定化（決定77）・`.battle-main`2カラム化（決定79）がいずれも`@media (min-width:900px)`限定、スマホ対応（決定62〜67）が`@media (max-width:700px)`限定であることを確認した上で、iframe注入による実測（CSSピクセル幅701／768／800／899pxで検証、スクロールバー分の誤差を較正した上で目標幅ちょうどになるよう調整）で、いずれの幅でも「ラウンドを終える」ボタンに到達するまで565〜781pxの縦スクロールが必要になることを再現確認した（横スクロール・要素の重なり・画面外はみ出しは無し。純粋にスクロール量のみの問題）。`min-width:900px`の実際の閾値は、Chrome既定の縦スクロールバー幅（実測4px、決定62の19px想定とは異なる値）により、ウィンドウ実幅900〜903pxのごく狭い範囲でも同様に未対応であることも判明したが、影響範囲が4px分と軽微なため今回は701〜899pxの主要課題と切り分けて記録するに留めた。**CSSは変更していない**。最小修正案（未適用）：`battle.css`の`@media (min-width:900px)`（`.end-round-button`用・`.battle-main`用の2箇所）を`@media (min-width:701px)`に変更し、決定62の`max-width:700px`としきい値を隙間なく接続する案が最小変更だが、決定79の2カラムグリッドは1034px以上でのみ実機検証されており、701〜899pxという より狭い幅での見た目（アバター・パネルの圧縮度合い）は未検証のため、適用前にCEO承認と実機確認が必要と判断し、`FINAL_BACKLOG_8-31.md` §2⑦にCEO判断待ちとして記録するに留めた。**③ 本番当日運用フローの再構成**：`RELEASE_STATUS.md`の「8/31直前5分チェック」に含まれていたlocalStorage全削除の指示を、CEO判断により「本番当日1回だけ実施し、以降は削除しない」運用に変更（デモ用に進行中バトルを仕込んでおく手順と、本番直前に誤って`sevengods.battleSave`を消してしまう事故を両立させるための整理）。ErrorBoundary（決定81で実装のみ・発火未確認のまま残っていた）の発火確認手順も、本番デモ機やセーブデータに触れない方法（別環境での一時的なコード書き換え→確認→即座に元に戻す、コミットしない）として追加した。**④ README刷新の準備**：既存READMEがViteテンプレートのままでSEVEN GODS本体の説明を一切含んでいないことを確認（詳細は本決定と同日のREADME刷新作業を参照）。**変更ファイル**：`RELEASE_STATUS.md`・`FINAL_BACKLOG_8-31.md`・`DECISIONS.md`（本決定・決定87）・`README.md`のみ。`src/`配下・`GameState`・core/engine・カード/神/OTOMO/敵/神託の数値はいずれも無変更。作業後に`npm test`・`npx tsc -b --noEmit`・`npx oxlint .`を実行し、結果は当該コミットメッセージ・CEOへの報告に記載する |
| 2026-08-17 | 決定89を追加。決定88の運用フロー（ステップ2でデモ用バトルを仕込み、ステップ3で以降localStorageを削除しない）を夜間の読み取り専用エージェントに検証させたところ、重大な見落としを検出した。ホーム画面の**「神を選ぶ」ボタン**（`HomeScreen.tsx`、`home-cta-primary`）は`onClick`が`GameFlow.tsx`の`onStartFresh`に直結しており、押した瞬間に**確認ダイアログ無しで`clearBattleSave()`が実行され、仕込んだバトルセーブが即座に消去される**ことをコードで確認した（`battleSaveStorage.ts`の`clearBattleSave`）。しかも同じ`home-cta-row`内で隣に並ぶ「続きから」ボタン（`home-cta-secondary`、地味な枠線のみ）より、「神を選ぶ」の方が金色グラデーション＋グロー付きで視覚的に目立つデザインになっている（`setup.css`の`.home-cta-primary`／`.home-cta-secondary`）ため、本番当日に誤って「神を選ぶ」を押してしまうリスクが高いと判断した。**対応**：コード（ボタンの見た目・確認ダイアログの追加等）は一切変更せず、`RELEASE_STATUS.md`の「ステップ3」に「ホーム画面に戻った場合は『神を選ぶ』を絶対に押さず、必ず『続きから』を押す」という運用上の注意書きを追記するのみとした。ボタンに確認ダイアログを追加する等の恒久対応は、UIへの変更を伴うため8/31スコープ外・CEO判断待ちとして`FINAL_BACKLOG_8-31.md`には今回追加していない（運用の注意喚起のみで当面のリスクは回避可能なため）。`GameState`・core/engine・カード効果・バランス数値はいずれも無変更 |
| 2026-08-17 | 決定90を追加。8/31完成披露版に向けた最終ブラッシュアップ監査（AI社員7名・5名の2回の並列調査）でC7〜C-8として整理された改善候補のうち、Opus（サブエージェント）による最終設計レビューでGO判定を受けた4件を、CEO承認のもと1件ずつ実装・検証・単独commitした。本決定はそのうちC-7「cast-flash尺の不一致解消」（commit `86825cf`）。**現状**：`useGameEngine.ts`の`CARD_PLAY_REVEAL_MS`（220ms）に対し、`battle.css`のタイプ別`cast-flash-pop-*`演出（attack=0.4s、guard/resonance/hinder=0.45s、support/oracle=0.5s）が最短でも45%地点で強制終了し、6タイプ中4タイプ（guard/resonance/support/oracle）は`opacity:1`の最大到達時刻（最大でsupport/oracleの250ms地点）に達する前に強制アンマウントされていることをOpusのコード読解で確認した。**対応**：`CARD_PLAY_REVEAL_MS`を**220ms→280ms**に変更（6タイプ全ての最大ピーク到達時刻250msを上回る最小のキリのいい値）。同時に`ENEMY_TURN_REVEAL_MS`（決定61）のコメント「約3.2倍」を実態に合わせ「約2.5倍」に更新（`ENEMY_TURN_REVEAL_MS`自体は700msのまま変更せず、決定61の「カード1枚の演出より明確に長い間を維持する」意図は2.5倍でも保たれると判断）、`.card-view-playing`のCSSアニメーションも0.22s→0.28sに整合させた。**副作用検証**（Opusのコード読解＋実機）：敵ターン処理は二重待機なし（`PLAY_CARD`と`END_ROUND`の待機は別経路）／カード連打はreducer側のuid存在チェックにより安全（実機で同一カードへの3連続クリックがAP消費1回・ログ1件のみであることを確認）／ラウンド終了ボタンは既存の`isPlayerTurn`ガードで従来どおり操作不能／RewardOverlayの表示タイミングへの影響なし／SEはdispatch時点発火という既存仕様のまま変わらないため新たなズレは生じないが、入力から着弾までの総遅延が220ms→280msへ+60ms悪化する（1戦約18枚使用として累計+約1.1秒、決定61で短縮した1.24秒の枠内）。**変更ファイル**：`useGameEngine.ts`・`battle.css`のみ。`core/engine`・`GameState`・カード効果・`rules.ts`の数値は無変更。`npm test`（179件、無変更）・`npx tsc -b --noEmit`・`npx oxlint .`とも全て通過。ローカルdevサーバーでの実機確認（攻撃／神託／共鳴タイプのカード使用、演出が途中で消えないことをスクリーンショットで確認、コンソールに新規のアプリ側エラーなし）を実施済み |
| 2026-08-17 | 決定91を追加。4件のうちC-1「OTOMOポートレートへのオーラ枠追加」（commit `86668bf`）。**現状**：`GodOtomoPanel.tsx`の`.portrait-otomo`（JSX側は既にクラス付与済みの未スタイルフック）が84×84の素の正方形で、隣の神/敵アバターの`enemy-avatar-wrap::before`/`player-avatar-wrap::before`（決定40）による額装（conic-gradientのオーラ・`portrait-aura-spin`14s回転）が無く、OTOMOだけ見劣りしていた。**対応**：JSXは無変更のまま、`battle.css`に`.portrait-otomo`（`position:relative`）と`.portrait-otomo::before`（オーラ本体）を追加。既存の`portrait-aura-spin`keyframeをそのまま再利用し新規keyframesは作らず、サイズは既存のアバター200px/オーラ270px＝1.35倍という比率をOTOMOの84pxに当てはめた114px、色はOTOMO紫（`#c39bff66`、既存のbuffバッジ・進化グローと共通の色）と共鳴金（`#ffd16655`、`player-avatar-wrap::before`と同値）とした。**変更ファイル**：`battle.css`のみ（JSX変更ゼロ）。`core/engine`・`GameState`・素材画像は無変更。`npm test`（179件）・`npx tsc -b --noEmit`・`npx oxlint .`とも全て通過。**ブラウザ実測**（ローカルdevサーバー）：375px／390px／430px／PC(900px)いずれも横スクロールの増加なし（`scrollWidth-innerWidth`が装飾追加前後で不変）、computed styleで114px・`position:absolute`・`z-index:-1`・`portrait-aura-spin`14sが設計どおり適用されていることを確認、神・敵・OTOMOの3体を並べてOTOMOだけ浮いて見える状態が解消されたことを視覚確認した |
| 2026-08-17 | 決定92を追加。4件のうちC-2「サムネイル額装統一」（commit `ab77445`）。**現状**：`.god-select-card img`（神選択）・`.god-select-recap img`（難易度選択の神再確認カード）・`.otomo-growth-card img`（OTOMOとの絆）の3箇所が装飾ゼロの素の正方形画像だった（他の画面のサムネ・カード・神/敵/OTOMOポートレートは決定28・33・34・37・40・91で既に額装済みのため対象外とし、重複装飾はしていない）。**対応**：3セレクタに、`.card-view-exclusive`（battle.css、決定37以降の専用カード装飾）のoutlineレシピと`.portrait img`のbox-shadowグローを値そのまま流用して追加（`outline: 1px solid #ffd16655; outline-offset: -2px; box-shadow: 0 0 20px -4px #ffd16655;`。outline-offsetのみ、`.card-view-exclusive`の-4pxから、56〜120pxという小さめのサムネ向けに-2pxへ調整）。`object-fit:cover`のimgにはinset box-shadowが効かない（コンテンツに隠れて見えない）ためinset方式は不採用とし、下端グラデーション・archetype別の色分けは今回のスコープ外としてCEO承認のうえ見送った。**変更ファイル**：`setup.css`のみ。素材画像・`core/engine`・`GameState`は無変更。`npm test`（179件）・`npx tsc -b --noEmit`・`npx oxlint .`とも全て通過。**ブラウザ実測**：375px／390px／430px／PC(900px)の3画面（神選択・難易度再確認・OTOMOとの絆）いずれも横スクロールの増加なし（`scrollWidth-innerWidth`不変）、二重枠に見えないこと・金枠が強すぎないことを視覚確認した |
| 2026-08-17 | 決定93を追加。4件のうちC-8「AP（神力）表示のゲージ化」（commit `924cf99`）。**現状**：`.battle-topbar`の「神力 X / Y」がテキスト表示のみで、カード側は既にコスト珠として視覚化されているのに対しAP残量だけが数字比較を要する非対称があった。**対応**：既存の「神力 X / Y」テキストは1文字も変えず、その直下に`.battle-topbar-ap`（列flex）で6pxの`.ap-gauge`／`.ap-gauge-fill`を追加。`.resonance-gauge`のtrack/fill/transitionという考え方のみ再利用し、決定71が明文化した「視覚言語は踏襲しつつクラスは共有しない」慣習に従いクラスは新設、色は共鳴ゲージ（金〜オレンジ）と誤認しないよう青系（`#7fb2ff`/`#4d88e5`）にした。ゲージ幅はテキスト幅に自動追従（固定pxを持たない）させることで横幅の増加をゼロにした。`apRatio`の計算は`GodOtomoPanel`の共鳴比率計算と同じガード式（`max>0 ? min(1, current/max) : 0`）。**変更ファイル**：`BattleScreen.tsx`・`battle.css`のみ。`core/engine`・`GameState`・AP計算ロジック・カード効果・`rules.ts`は無変更。`npm test`（179件）・`npx tsc -b --noEmit`・`npx oxlint .`とも全て通過。**ブラウザ実測**（375／390／430／701／768／800／899px・PC900px+の8幅）：`.battle-topbar`は`flex-wrap:nowrap`のため全幅で折り返しなし、横スクロールの増加なし。ゲージのON/OFFを同一状態で切り替える方式で高さへの影響を実測した結果、topbar高さ・701〜899px帯のオーバースクロール量・PC 1280×800相当の手札下端はみ出し量のいずれも、全シナリオで一律**+9px**（事前のOpus試算と完全一致）の増加のみで、それ以外の悪化（重なり・クリップ・誤認）は確認されなかった。CEOはこの実測結果をもって採用（GO）と判断した |
| 2026-08-17 | 決定94を追加。8/31完成披露版に向けたVisual Polishの最終1件として、CEO判断により「神託パネルの改善」のみを実施（commit `6b452dc`）。C-3「勝利リザルトへの神/OTOMOポートレート追加」と「共鳴ゲージの助走演出」は9月以降候補として維持し、今回は実装しないことをCEOが明示した。**現状**：`DivinationPanel.tsx`の神託3択（加護・導き・天啓）が、ゲーム内で唯一「画像もアイコンも一切ない文字だけの角丸ボタン」だった。**事前調査**：`divination.ts`の3種類の効果内容、`cardIcon.tsx`の`GlyphIcon`（決定30の資産、`TutorialOverlay.tsx`で実績あり）、`useGameEngine.ts`の`divine()`（`dispatch`を即実行するのみで`playCard`のような`pendingCardUid`/`CARD_PLAY_REVEAL_MS`遅延パイプラインに乗っておらず、選択時に競合しうる既存演出が無いことを確認）を確認したうえで着手した。**対応**：`DIVINATION_GLYPHS`（`['shield', 'cards', 'star']`、`DIVINATION_CHOICES`の並び順に対応）を新設し、加護＝護りの意匠で`shield`、導き＝効果文の主軸「カードを1枚引き」に対応させ`cards`、天啓＝神託(oracle)タイプの共通カードと同じ意匠で`star`を割り当てた。アイコン枠のスタイルは`tutorial.css`の`.tutorial-section-visual`/`.tutorial-section-glyph`（`background:#ffffff0d`・`border:1px solid #ffd16633`・`color:#ffd166`という金×ガラス系トークン）を値そのまま流用し、新規SVG・新規色・新規keyframesは一切追加していない（常時アニメーションなし）。`.divination-choice`は`flex-direction:column`から`row`に変更し、name/textは新設`.divination-choice-body`（column）に包んだ。**変更ファイル**：`DivinationPanel.tsx`・`battle.css`のみ。神託の効果・文言・`divine()`のロジック・`core/engine`・`GameState`・`rules.ts`は無変更。`npm test`（179件）・`npx tsc -b --noEmit`・`npx oxlint .`とも全て通過。**ブラウザ実測**：3択が盾/カード/星のアイコンで瞬時に区別できること、文字が引き続き読めること、アイコンが既存トークン流用のため強すぎないこと、実際にクリックしてログ・スコア・「このラウンドは使用済み」の無効化表示まで正常動作し既存演出との競合が無いことを確認。375px／390px／430px／PC(900px)いずれも横スクロールの増加なし（`scrollWidth-innerWidth`=-15pxで全幅共通、変化なし）、コンソールエラー0件だった |
| 2026-08-17 | 決定95を追加。決定94の「今回は実装しない」対象だった2件のうち「共鳴ゲージの助走演出」をCEO判断により追加実施（commit `9edee30`）。**現状**：共鳴ゲージが0〜4/7の間ずっと静的なプログレスバーのままで、満タン（自動発動）直前の高揚感を演出する仕掛けが無かった。**対応**：`GodOtomoPanel.tsx`に`getResonanceStage(value)`（`value>=6`で`'resonance-gauge-high'`、`>=5`で`'resonance-gauge-mid'`、それ未満は`''`の純粋関数、表示専用の分岐）を新設し、`.resonance-gauge`をラップする`.resonance-gauge-wrap`にクラスとして付与。5/7で静的な金の輪、6/7でより強い発光＋脈動を追加した。脈動は`.battle-arena-glow`と全く同じ既存keyframes`arena-glow-breathe`（`opacity:0.5→0.8→0.5`のみ）を流用し、新規keyframesは追加していない。発光は`.resonance-gauge-wrap::after`という装飾レイヤーにのみ適用し、ゲージ本体（fill・「共鳴 X/7」の文字）の不透明度は常に1のまま可読性を損なわない設計にした。共鳴は`applyResonance`が満タン到達と同一トランザクション内で即座に0へリセットする既存仕様（決定8・10）のため、UI上「7/7」が実際に描画されることは無く、本演出と既存`burst-banner`の競合は構造的に発生しない。**実装中に発見・修正した不具合**：`.god-otomo-panel`が`align-items:center`を指定しているため、幅未指定の新規ラッパーがshrink-to-fitで0pxに潰れる問題を実機確認で発見し、`.resonance-gauge-wrap`に`width:100%`を追加して同一commit内で解消済み。**変更ファイル**：`GodOtomoPanel.tsx`・`battle.css`のみ。`core/engine`・`GameState`・共鳴ロジック・数値・発動条件・`rules.ts`は無変更。`npm test`（179件）・`npx tsc -b --noEmit`・`npx oxlint .`とも全て通過。**ブラウザ実測**：0/7・1/7はクラス無し・幅523pxで変更前と完全に同一の見た目、5/7で静的な発光、6/7でより強い発光＋脈動を確認し、「共鳴6/7」の文字が常に明瞭に読めることを確認した |
| 2026-08-18 | 決定96を追加。決定94で「C-3（勝利リザルトへの神/OTOMOポートレート追加）は9月以降候補として今回は実装しない」とした判断をCEO承認により撤回し、C-3を実装（commit `203531b`）。**撤回の理由**：運営から「素材のベタ張り禁止（画像を貼るだけの単調な演出を避け、既存アセットをもっとリッチに見せる）」という8/31完成披露版に向けた新しい優先要件が示され、勝利という最も感情が動く瞬間に神とOTOMOの姿が一切映らないのはこの要件に反すると判断したため。Opusによる最終設計レビュー（実装前に別途実施、コード変更・commit禁止の条件下でGameOverOverlay.tsx／BattleScreen.tsx／battle.css／神・OTOMO画像取得方法を確認）でGO判定・具体的な最小差分・低ビューポート対策が確定してから着手した。**対応**：`GameOverOverlay`に`godId`/`otomo`（`BattleScreen`が既に保持している`state.godId`/`state.otomo`をそのまま渡すのみ）を追加し、`status === 'won'`のときだけ`.game-over-portraits`（神の`art.front`＋OTOMOの`otomoDef.art[otomo.form]`）を表示する。**勝利限定の理由**：決定64「敗北・未撃破では祝祭感を出さない」方針を維持するため、`lost`/`finished`では従来どおり非表示。**進化状態の正しさ**：`otomo.form`をそのまま`art[form]`に渡すだけで、直前の共鳴発動で`spirit`→`incarnate`等に進化していた場合も進化後の姿が表示される。これは`applyResonance`がOTOMOの形態更新を撃破・勝利判定より先に同一トランザクション内で確定させる既存実装（決定8・10）により、Reactが進化前の中間状態をレンダリングする経路が構造的に存在しないため。**新規アセット**：画像は一切新規作成せず、既存の`art.front`（`art.back`は白背景未処理のため不使用）と既存のOTOMO形態別画像のみを使用。**低ビューポート対策**：`.game-over-card`に`max-height: 85vh; overflow-y: auto;`を追加。既存の`.game-over-card-won`（決定82の金枠仕様）・回転オーラ（決定91のC-1）は流用せず、新規keyframesも追加していない。**変更ファイル**：`GameOverOverlay.tsx`・`BattleScreen.tsx`・`battle.css`のみ。`core/engine`・`GameState`・勝敗判定・スコア計算・カード効果・`rules.ts`は無変更。`npm test`（179件）・`npx tsc -b --noEmit`・`npx oxlint .`とも全て通過。**ブラウザ実測**：恵比寿でのプレイを複数回行い、(1)共鳴未発動勝利で精霊態のOTOMOが正しく表示、(2)ラウンド中盤の共鳴発動で受肉態へ進化した直後に勝利した場合も受肉態のOTOMOが正しく表示、(3)敗北時はポートレート非表示、(4)PC幅(1358px)で横スクロールの増加なし（`scrollWidth-innerWidth`=-15px）、(5)神の`art.front`・OTOMOの現在形態画像とも`naturalWidth>0`で読み込み成功しconsoleエラー0件、を確認した。**正直な申告（未確認・代替確認で済ませた項目）**：375×480ピクセル単位でのライブスクリーンショット確認は、ブラウザ自動化環境の制約（Vite HMRによる予期しない全体リロードでセーブ済み勝利状態が失われる／`resize_window`ツールが`window.innerWidth`実測値に反映されない既知の制約）により、CEOの明示的な許可（「無理に何十回もプレイせず、コード構造＋可能な代表ケース確認で安全側に判断」）に基づき打ち切った。代わりに次の代替確認を行った：実際にPC幅(1358×594px)で勝利画面をレンダリングした状態で`.game-over-card`の`scrollHeight`(553px)が`85vh`のクランプ値(504.9px)を上回りoverflow-y:autoが実際に作動していることを確認し、さらに`card.scrollTop`を最大値まで動かして`.game-over-actions`（ボタン行）がクランプ内に完全に収まることを実測した。これにより「クランプで隠れた分はスクロールで到達できる」という機構自体は実機で動作確認済みだが、375×480という具体的な画面サイズでの見た目・タップ操作性のピクセル単位の確認はできていない。同様に375/390/430pxでの横スクロール増加なしの確認も、静的アセット（神7柱の`art.front`・OTOMO7体×3形態＝計28ファイル）が全て存在することのファイルシステム確認と、追加した`.game-over-portraits`のCSS（固定px、実効幅約180px）が既存のボタン行・スコア内訳など375pxで検証済みの要素より明らかに狭いことの構造確認に留め、ライブスクリーンショットでの再現はできていない。次回ブラウザ検証が可能なタイミングで375×480の実機確認を行うことを推奨する |
| 2026-08-18 | 決定97を追加。「第二次完成フェーズ・第1波」として、AI社員6部門＋Opus統合レビューによる監査（Game Feel/Design/Identity/Score）のP0判定4件を実装（commit `66d679f`・`9a17561`・`bc82818`・`36752e9`・`77b5076`）。**P0-1（`66d679f`）**：`.cast-flash`（カード使用演出）を`position:absolute`→`fixed`に変更。決定80が`.burst-banner`等をfixed化した際に本要素だけが取りこぼされていたための是正で、モバイルで`.battle-main`が画面外にスクロールした状態でもカード演出が視界に入るようになった。**P0-2（`9a17561`）**：`.ap-penalty-toast`（神力使い残しペナルティ通知）も同じ理由でfixed化。**P0-3（`bc82818`→`77b5076`）**：敵攻撃を完全/一部ブロックした際の無反応を解消。`useBattleFx.ts`の敵攻撃モーション発火を`DAMAGE_DEALT{target:'self',amount>0}`から`ENEMY_ACTED{kind:'attack'}`起点に変更し、ブロック結果に関係なく敵の突進モーションが再生されるようにした。既存データ`DAMAGE_DEALT.blocked`（従来未消費）を使い、`useFloatingNumbers.ts`に新規`kind:'block'`のフローティング表示を追加。**表記は「軽減N」を採用**：当初`🛡{N}`（アイコン付き）としたが、既存の残ブロック量バッジ（PlayerPanel/EnemyPanelの`.badge-block`、同一の🛡アイコン・同一の色`#7fb2ff`）と見た目が完全に一致し、「今残っている量」なのか「今回吸収した量」なのか一瞬で区別できないというOpusレビューの指摘をCEOが承認し、既存のバトルログ（`formatEvent.ts`）が同じ意味（`DAMAGE_DEALT.blocked`）を「（N軽減）」と表現していた言葉をそのまま流用して「軽減N」（アイコンなし、色`#7fb2ff`は維持）に変更した（`77b5076`）。**P0-4（`36752e9`）**：`gods.ts`の`resonanceEffects`（神ごとに個性化済み）をバトル中`GodOtomoPanel`に常時表示。既存`describeEffectList()`（決定86、DeckBuilderScreen.tsxで使用中）をそのまま再利用し、実データの複製をしていない。**4件とも`src/core/`・GameState・`rules.ts`・カード効果・敵AI・デッキ構成・スコア式は無変更**（`git diff`で`src/core/`配下の差分ゼロを確認、Opusも独立に再確認済み）。**テスト**：179テスト全通過・`tsc`/`oxlint`クリーン（各commitで実行）。**レスポンシブ検証**：375/390/430/800/1280pxで実機確認、横スクロール増加・文字切れなし。7神全てで共鳴効果表示が`gods.ts`の実データと完全一致。完全ブロック／一部ブロック／通常被弾の3ケースを実際の対局で比較し、「-N」（left:50%）と「軽減N」（left:30%）が重ならず表示されることを確認。**Opus最終レビュー**：ゲームロジックのバグ・回帰は0件、core/engine領域への逸脱もゼロと判定。P0-1・P0-2はGO、P0-3・P0-4は条件付きGOと判定され、P0-3の🛡表記混同（→軽減N採用で解消）とP0-4のPC高さ増加（後述）を残存事項として指摘した。**残存する既知事項**：①PC 1280×800幅で、P0-4により手札下端の画面外はみ出しが実測+23px増加した（笑蓮＝7神中最長文での最悪ケース）。CEOは「神ごとの戦い方の違いが伝わらない」という実プレイ課題への効果を優先し、375/390/430pxで破綻がないこと・PCでも操作不能にはならないことを確認したうえで許容・採用（レイアウト変更は今回見送り）。②今回の実測で、決定79時点（2026-08-15）で「約28pxはみ出す」と記録されていたPC 1280×800の手札下端はみ出しが、P0-4適用前の時点で既に**約41px**まで悪化していたことが判明した。決定93（AP逓増ゲージ追加）等、決定79以降に積み上がった変更が原因と推定されるが、**これはP0-4とは無関係の既存事象**であり、今回は修正せず、今後の検討対象として記録するに留める（P0-4適用後は約64px）。 |
| 2026-08-19 | 決定98を追加。「第三次完成フェーズ」として、CEO実プレイで残っていた本質課題（①攻撃偏重、②神ごとの本当の戦術差、③スコア設計）をSonnet3並列調査＋Opus最終レビューで再調査した（コード・数値変更なしの調査フェーズ）。**①攻撃偏重という初期仮説は、勝敗・スコアの両面でそのままでは成立しなかった**。勝敗軸：hardでのaggressive一辺倒（才華93%・大耀45%・福永40%が敗北）はむしろ致死的で、**balanced戦略が勝敗上は健全**（決定58の実測と整合）。スコア軸：Opusの算術検算により、`roundBonus`の撃破ラウンド差（最大300点）は早期撃破で失う`combo`・`oracleBonus`の機会損失とほぼ相殺されることが判明し、5戦略×7神×3難易度4200試合の実測でも「aggressiveが常に最適」とは言えなかった（詳細な実測ログは`balanceSim.test.ts`決定98計測テストに記録、撃破ラウンド別平均スコアはR4=838.8/R5=814.1/R6=800.3/R7=743.2で、単調減少はするが理論上の300点差ほどではない）。**②問題の本体は、攻撃の優遇ではなく、防御・回復・HP維持等の判断がスコアへ一切反映されないこと**。`effects.ts`の`block`/`heal`処理に`addScore`呼び出しが無いことをコードで確認済み（`ScoreState`にHP・ブロック関連フィールドも存在しない）。生存に不可欠な判断が可視化されないため、体感として攻撃偏重に見えていた。**現行勝利スコアは約670〜903点に集中**（決定59の実測と整合、7神×3戦略の勝利時平均）。追加で以下を確認：**オーバーキルが満額加点される**（`effects.ts`の`applyDamage`は敵の残りHPでdamageをクランプせず、スコアには`dealt`満額を加算）。**共鳴ゲージの超過分が無言で切り捨てられる**（満タン到達時に`value:0`へ即リセットし、超過分を次に持ち越さない）。**笑蓮「笑って許す」(heal4+block4)と寿楽「長生きの知恵」(heal4+block4)の効果が完全一致**している（typeがsupport/guardと異なるのみ）。**笑蓮の専用カードにcommonの下位互換が2件存在**（「福袋」heal11＜共通「息吹」heal12、「懐の深さ」block11＜共通「鉄壁の構え」block12）。前者は既存コメントで意図明記済み、後者は決定98で同じ意図（決定25の支援特化コンセプト）によるものとCEOが確認し、コードコメントを追記した（`shouren.ts`、数値・Effect・CardDef.textは無変更）。恵比寿もSTARTER_DECK固定で専用カード比率20%であり、「防御寄りの神ほど専用色が薄まる」という調査時点の仮説は誤りだったことも判明（恵比寿は攻撃型のため）。神選択画面の「得意戦法」文言（決定97のGOD_TACTICS）はOpus検算の結果、スコア構造と矛盾しないことを再確認し、修正不要と判断した。**8/31ではスコア式・ScoreState・カード数値・敵数値・core/engineを一切変更しない**。安全な範囲として、`balanceSim.test.ts`へ勝利試合のみを対象にした「最終スコア×撃破ラウンド×神×難易度」相関の計測テスト（アサーションなし、既存の決定58・59再現防止テストは無変更）を追加し、将来のランキング設計用データとして残した。**ランキング向けスコア再設計は9月の最優先候補とする**。9月に検討すべき論点は、共鳴回数のスコア化（既存`RESONANCE_BURST`イベントで計測コストほぼゼロ、超過分切り捨ての可視化と連動可能）・オーバーキル減点（`effects.ts`の1行修正で対応可能だがcore/engine変更のため8/31対象外）・笑蓮の専用カード数値是正・「笑って許す」/「長生きの知恵」の重複解消・block/heal未評価の是正方針、の5点。**変更ファイル**：`balanceSim.test.ts`（計測テスト追加のみ）・`shouren.ts`（コメント追加のみ）・本決定の3件。`npm test`（184件、既存183件は無変更・決定98の計測テスト1件を新規追加）・`npx tsc -b --noEmit`・`npx oxlint .`とも全て通過 |
| 2026-08-23 | 決定99を追加。「v4おすすめデッキ確定」「BattleMiniResult」「敵タイプ表示強化」「datenshi刷新」の4施策を1コミット（`1856ea5`）でまとめて記録漏れになっていたため追記。**STEP1/STEP1-B（v4おすすめデッキ）**：恵比寿・蒼毘・才華・寿楽の4神に固定の`V4_RECOMMENDED_DECKS`（`deckBuilder.ts`）を導入し、専用カードが埋もれる/バランスが不安定になる既存の汎用ラウンドロビン方式の弱点に対応した。才華はhard×双牙の魔獣での回帰（最善戦略でも勝率20%前後まで低下）を検出し、守護（ironStance）・受け流し（parry）への差し替えで70〜85%まで回復させた。大耀・福永・笑蓮は既存の汎用ロジックのまま（笑蓮のみ専用カードコピー数1枚の既存非対称ルールを維持）。**STEP2-B（BattleMiniResult）**：カード使用結果（ダメージ・ブロック・回復・共鳴・バフ）を`BattleHud`直下にトースト表示するコンポーネントを新設。CEO実プレイ指摘「カードしか見えず攻撃シーンが見えない」への対応。既存`useBattleFx`のイベントログのみを情報源とし、新規ロジック・保存フィールドは追加していない。**STEP3-A（敵タイプ表示強化）**：`EnemyDef`に`typeLabel`/`typeDescription`/`visualType`を追加し、tier別intentアイコン（⚔/💥/🔥/⚡）・CSSのみの演出差別化（終盤急上昇の発光・溜めの発光・突進速度差）を実装。全て既存のHP・行動テーブルから導出するのみで、新規の敵メカニクス・数値変更は無い。**datenshi刷新**：チュートリアル敵の`art.png`（元画像シートの隣接キャラの滲みが混入していた）を新規生成の単体・透過済み`art.webp`に差し替え（旧PNGはロールバック用に保持、他6体は無変更）。**変更範囲**：`src/core/`のGameState・reducer・カード効果・敵数値・saveVersionは無変更。198→199テスト全通過、`tsc`/`oxlint`クリーン |
| 2026-08-23〜24 | 決定100を追加。karakuri（commit `87b92a8`）・doukeshi（`a4aa9e3`）・oni/juuma（`5b1e1b7`）の4体を、datenshi（決定99）と同じ「Reference Standard」方式（既存イラストの破片混入・発光ムラ等を修正した単体・透過済みWebPへの最小差分置き換え。新規デザイン生成なし、数値・AI無変更）で本番の`art.webp`へ切り替え、7体中5体（datenshi・karakuri・doukeshi・oni・juuma）のビジュアル刷新が完了した。**なぜ**：各敵とも複数のSTEPで候補検証・CEO承認を経た上での最小変更方式を踏襲。onryo・ryujinの2体は今回のスコープに含めず、9月以降の課題として据え置いた（8/31完成披露版の目的は新機能追加ではなく既存の仕上げであり、5/7の刷新で視覚的一貫性は概ね確保されていると判断したため）。**結果**：`src/core/`のGameState・reducer・敵数値・AI・saveVersionは無変更。各commitで`npm test`・`tsc`・`oxlint`確認済み |
| 2026-08-24 | 決定101を追加。共鳴7/7カットイン（案A・横スライド）を新設し、7神共通機能として本番反映した（commit `6834da0`）。共鳴ゲージが7/7に到達すると、神のキービジュアルが画面右へスライドインするカットイン演出を挟んでから既存の「神の一撃」バナー・OTOMO進化バナーへつながる。**経緯**：STEP-R1で設計・実装前監査（暗転200ms→スライド400ms→静止300ms＝計900ms、既存`fx.burstKey`を信号源とし`useBattleFx.ts`は無変更）を実施、STEP-R2で蒼毘のみのプロトタイプ実装・実機QA、STEP-R3で全7神へ横展開（`isSobi`分岐を削除し全神共通の`cutin→burst-banner→evolve-banner`経路に統一）。笑蓮のみ`900×900`の正方形・横臥ポーズが他6神（`675〜720×900`の縦長ポートレート）と構図が大きく異なるため、`.resonance-cutin-image[data-god='shouren']`に`aspect-ratio:3/4; object-fit:cover; object-position:20% 23%`を適用し個別トリミング（実画像のグリッド計測で顔中心≈x29%,y23%と確認した上で決定。元画像アセット自体は無変更）。STEP-R4で最終回帰監査（7神データ対応・発火条件・MiniResult抑制・入力ブロック・console・test）を行い本番反映した。**変更ファイル**：`BattleResonanceCutin.tsx`（新規）・`BattleScreen.tsx`・`battle.css`の3ファイルのみ。`core/engine`・GameState・カード/敵/共鳴数値は無変更。199/199テスト全通過（テスト件数自体の増減なし）、`tsc`/`oxlint`クリーン。実機QA：PC・入力ブロック・BattleMiniResult抑制・cutin→burst→evolveの順序・他神回帰（才華で確認）・本番URLでのconsole error 0件を確認済み。**未確認事項（正直な申告）**：375/390/430pxの実機目視確認のみ未達成。自動化環境の`resize_window`ツールが実ビューポート幅に反映されない既知の制約（決定96と同種の制約）のため、PC幅での実機確認・計算値（clamp式）の検証に留めた。CEO＋相棒による実機またはDevTools端末エミュレーションでの確認をP0として別途記録した |
| 2026-08-24 | 決定102を追加。8/31提出版に向けた総合監査（STEP-FINAL-A、コード変更なしの調査フェーズ）を実施し、残タスクをP0/P1/P2に再分類した。ランキング（RankingProviderIF）・OTOMO FARM77連携（`QuotaProvider`のUI接続含む）・多言語対応・Phaser演出・onryo/ryujin敵アセット刷新・daily加護UI（D2）・スコア再設計（決定98で列挙した5点）は、いずれも8/31提出版のスコープ外として維持することを再確認した。**なぜ**：これらは全て`docs/RELEASE_STATUS.md`・`docs/FINAL_BACKLOG_8-31.md`で既に「今回はやらない」と決定済みの項目であり、8/31の目的（壊れない・スマホで遊びやすい・見栄えがよい・バランスが破綻していない・READMEが整っている・本番で安心して触れる）に照らして新規実装は不要と再判断した。監査の過程で、v4おすすめデッキ・敵5体刷新・共鳴カットイン（決定99〜101が対応する内容）が`DECISIONS.md`に一切記録されていなかったことが判明し、本ドキュメント更新（決定99〜102、および`README.md`・`docs/RELEASE_STATUS.md`の同時更新）で解消した。**結果**：コード・CSS・画像の変更は無し（ドキュメントのみ）。最新commit・テスト199件・敵5体刷新済み・共鳴カットイン完成（375/390/430px実機確認のみP0として残存）の状態を各ドキュメントに反映した |
| 2026-08-24 | 決定103を追加。決定101・102で唯一残っていたP0（共鳴7/7カットインの375px／390px／430px実機目視QA）を完了・確認した（STEP-P0-2）。**確認方法**：自動化環境の`resize_window`制約を回避するため、CEO＋相棒自身のChrome DevTools Device Toolbarで375px／390px／430pxへ手動でビューポート幅を変更し、目視確認した。QA用の共鳴7/7到達状態は、STEP-R2〜R4と同じ手法（`localStorage`の`sevengods.battleSave`を一時的に直接編集し、共鳴値を6/7・共鳴を+2する共通カード「共振」を手札に注入）で、開発サーバー（`localhost:5176`）上に安全に作成した。本番セーブ・既存ユーザーデータには一切触れておらず、注入した検証用データはQA終了後に`localStorage.removeItem`で撤去済み。コード・CSS・画像・commit・pushは一切行っていない。**確認結果**：CEOからの報告により、顔切れ・画像サイズ・「〇〇、共鳴発動！」の文字の水平中心・HUD/handとの競合・横スクロール・体感時間の6項目とも、375px／390px／430pxのいずれも「特段問題なし」を確認した。**結論**：共鳴7/7カットイン（決定101）は、375/390/430px／PCの全幅で実機確認が完了し、STEP-FINAL-A（決定102）で唯一残っていたP0が解消された。**変更ファイル**：`docs/RELEASE_STATUS.md`（「残るP0」から本項目を除去、スマホ対応状況・Vercel公開URL・本番通しリハーサルの各節を確認済みに更新）・本決定のみ。`src/`・GameState・core/engine・カード/敵/共鳴数値は無変更 |
| 2026-08-24 | 決定104を追加。「敵Intent危険度強調」（STEP-UX4／STEP-UX4-B）を実装・QA・本番反映した（commit予定：`feat: emphasize dangerous enemy intents`）。**背景**：STEP-UX1の監査で、敵の予告（`enemy.intent`）はテキスト情報としては既に十分（威力・攻撃内容・tier文言・絵文字）だったが、危険度が視覚的に一目で伝わらない（「普通の攻撃」「少し危険」「これは対策すべき」の区別がテキストを読むまで分からない）ことが課題だった。**事前調査（処理01）**：表示生成は`formatEnemyIntent()`、tier判定は`getIntentPowerTier(amount)`（閾値10/15、カード側`getEnemyDamagePowerTier`と共通）＋`intent.kind==='charge'`が既に存在しており、`normal`/`strong`/`huge`/`charge`の4段階は追加の`GameState`なしに導出可能と確認した上で着手した。**対応**：`cardStyle.ts`に表示専用の純粋関数`getIntentTierClass(intent)`を追加（`formatEnemyIntent`の文言・`core/engine`・敵AI・敵数値は無変更）。`EnemyPanel.tsx`・`BattleHud.tsx`の`.intent`要素にクラスを適用。`battle.css`に4クラスを追加：normal＝クラス無し（現状のまま）、strong＝赤文字bold（枠・グロー無し）、huge＝赤`outline`＋静的`text-shadow`（パルス無し）、charge＝金`outline`＋`text-shadow`＋既存keyframe`arena-glow-breathe`を`::after`のみに適用した呼吸パルス（最上位のみpulseというCEO方針）。padding/borderを使わず`outline`/`text-shadow`/絶対配置`::after`のみに限定し、`.battle-hud`の高さ（`.battle-mini-result`の`top:58px`依存）に影響しない設計とした。**QA（STEP-UX4-B）**：`resize_window`が`window.innerWidth`に反映されない既知の制約（決定96・101と同種）を、同一originのiframe埋め込み（`/?enemy=karakuri`をiframeのCSS幅375/390/430/1280pxで読み込む一時ページ、QA後削除）で回避し、375px／390px／430px／**真の1280×800**の4条件全てで実機相当のライブDOM確認ができた（従来の「1280×800は未確認」という正直な申告が今回は不要になった）。**主な確認結果**：4tier（normal/strong/huge/charge）とも文字切れ・横スクロール増加なし。charge時の懸念事項だった「既存`.enemy-avatar-charging`（敵ポートレートの金色glow、静止）」と「新規Intent側の呼吸パルス（`::after`のみ）」の二重演出は、位置が離れている（ポートレート上部／Intentはパネル下部）ことと、パルスするのが新規要素の`::after`層のみであることを実測で確認し、二重に過剰な点滅にはなっていないと判断した。STEP-FINAL-D2基準（enemy-avatar 173×207・OTOMO 100×100・`.enemy-panel{justify-content:center}`）も真の1280×800で維持を確認した。`npm test`（199件）・`npx tsc -b --noEmit`・`npx oxlint .`とも全て通過、コンソールエラー0件。**変更ファイル**：`cardStyle.ts`・`EnemyPanel.tsx`・`BattleHud.tsx`・`battle.css`のみ。`GameState`・`core/engine`・reducer・`saveVersion`・カード/敵数値・敵AI・Intent文言・OTOMO/共鳴効果は無変更。**次候補**：敵攻撃演出（lunge速度等）をIntent tierに連動させる案はSTEP-UX5として今回は着手せず分離した。**副次的に判明した記録漏れ（今回は対応せず記録のみ）**：STEP-UX2〜UX3-Bで実装・本番反映済みの「7OTOMOバーストリアクション」（`GodOtomoPanel.tsx`の`otomo-reacting`/`otomo-reaction-label`、commit `592a122`）が本ドキュメントに一度も記録されていないことに気づいた。本決定のスコープ外のため今回は追記せず、CEO確認のうえ別途記録することを推奨する |
| 2026-08-24 | 決定105を追加。決定104で記録漏れとして指摘した「7OTOMOバーストリアクション」（STEP-UX2〜UX3-B、commit `592a122`）を補完記録する（STEP-UX5 PART A）。**何を決めたか**：OTOMOが共鳴発動時に実際に効果を発揮した場合のみ、名前＋効果テキストを0.5秒だけポップさせる短い反応演出を、蒼毘の百勝1体のプロトタイプ（STEP-UX2、CSS修正はSTEP-UX2-C）から全7 OTOMOへ横展開した（STEP-UX3）。**演出仕様**：`scale 1.14`・上方6px・紫drop-shadow・burst-banner表示と同じタイミング（`cutinBurstBannerKey`をトリガーに再利用）で発火し、`onAnimationEnd`で自動的に消える。効果が空（例：spirit形態で発動効果を持たない回）の場合は「助けてくれた」という嘘を表示しないよう、そもそもリアクション自体を発生させない設計とした。**なぜ**：OTOMOは常時表示されているが「今この瞬間、自分の効果で助けてくれた」という手応えが無かったため、既存の常時アニメーションを増やすのではなく発動した瞬間だけの短い反応を追加する方針とした。**実装**：効果テキストの算出は新規ロジックを作らず、既存`describeEffectList()`（決定86。`otomo.effectsByForm`/`powerPathEffectsByForm`を育成方針で切り替える、`DeckBuilderScreen.tsx`と共通のパターン）をそのまま再利用した。7 OTOMOとも全く同じロジック・同じCSSで、神ごとの個別分岐は持たない。**GameState・core/engine・reducer・OTOMO効果の数値は一切変更していない**（表示専用のローカルstateのみ）。**QA**：STEP-UX2-Bでラベルの2文字省略（ellipsis）バグを発見し、STEP-UX2-Cで`max-width`・2行`line-clamp`方式に修正。STEP-UX3で全7 OTOMOの効果文言・形態・育成ルートをコードから監査した上で横展開し、STEP-UX3-Bで7神↔7OTOMOの対応関係最終監査・取り違え確認を行い、問題なしとして本番反映した。`npm test`・`npx tsc -b --noEmit`・`npx oxlint .`は各STEPで通過。**変更ファイル**：`GodOtomoPanel.tsx`・`BattleScreen.tsx`・`battle.css`のみ。**結果**：commit `592a122`にて`origin/master`へpush済み・Vercel本番反映済み |
| 2026-08-27 | 決定106を追加。「STEP-SCORE1：スコア再設計 仕様監査＋新評価軸設計」を実施した（コード変更なしの監査・シミュレーションフェーズ）。**何を決めたか**：新スコア式の採用ではなく、以下6点を判断として確定した。①現行スコアには構造的課題がある ②block/heal/resonance等を評価軸へ単純追加するだけでは解決しない ③試算した「案B」では神格差がむしろ悪化した ④aggressive/defensiveの既存リスク・リターン構造も試算で崩れた ⑤8/31版へのスコア再設計導入は見送る ⑥9月に神別バランス調整とスコア評価軸をセットで再設計する。**なぜ**：CEO実プレイで「スコアが800点台付近に集約しているように感じる」との指摘（STEP-UX7-VERIFY）があり、7神×3難易度×3戦略＝63条件×各300run＝**18,900試合**の一時シミュレーション（`src/core/engine/__tmp_scoreRedesign.sim.test.ts`、作業後完全撤去）で検証した。**現行スコアの実測**：全試行mean734.2／median750.0／std170.5／range144〜1339、勝利時mean777.1／median767.0／std121.9／range368〜1339。800〜899点は全試行22.4%・勝利時25.3%、1000+は全試行4.7%・勝利時5.3%、1200+は約0.2〜0.3%、1500+は0件——CEOの体感は概ね裏付けられた。**構造的問題**：`roundBonus`＋`oracleBonus`で勝利時スコアの**約74.4%**を占め（damage14.1%・combo11.6%）、block／healはスコアに**0点**（`effects.ts`の該当処理に`addScore`呼び出しが無い）、resonance burst回数自体も未スコア化（burst時のscore効果のみoracleBonusへ混入）、overkillは敵残りHPでクランプされず満額加点、difficultyに明示倍率が無い。神別では防御/回復寄り3神（蒼毘・寿楽・笑蓮）が他4神より勝利時平均で**約17.5%低く**、才華957.8（最高）〜寿楽664.8（最低）の格差（比1.44倍）を確認した。**新評価軸の監査**：有効ダメージ（overkill除外）・実際に防いだblock量・実際に回復したheal量・resonance burst回数・combo・remainingHP・round efficiencyが有望と判定。blockは「獲得量」でなく「実際に防いだ量」、healは「実回復量のみ」、overkillは「加点しない」方向が有力で、いずれも既存イベントから実装難易度は低いと確認。resonanceは「BURST発動回数」中心が有力だが既存oracleBonusとの重複整理が必要。**案B試算**（`perEffectiveDamage:1.2`／`comboPerExtraCard:9`／`unusedApPenalty:2`／`defeatBonus:180`／`perRemainingRound:70`／`perBlockAbsorbed:1.5`／`perHealActual:1.5`／`perResonanceBurst:40`／`perSurvivalHpPercent:40`／`difficultyMultiplier:{easy:0.95,normal:1.0,hard:1.1}`——**あくまでsimulation用の仮係数であり採用値ではない**）の結果：勝利時mean833.0／median846.1／std171.8／range377.2〜1634.5、1000+13.3%／1200+2.6%／1500+0.2%と分散は広がったが、**神格差は1.44倍→1.47倍へ悪化**し、**aggressive/defensiveの既存リスク・リターン構造が意図せず逆転**するケースを確認した。「評価軸を増やすだけでは神格差もリスク・リターン構造も自動的には改善しない」ことが今回の最重要な発見。**8/31判断**：スコア再設計の8/31版導入は**見送り（NO）**。理由は神格差悪化・aggressive/defensive構造崩れ・既存回帰テストへの影響・神別数値調整まで含めると追加2〜3日以上必要・8/31提出版の安定性優先の5点。`docs/RELEASE_STATUS.md`・`docs/FINAL_BACKLOG_8-31.md`が既にスコア再設計を9月以降スコープとしていた判断を、今回のシミュレーションが裏付けた。**9月方針**：STEP-SCORE2は単純な係数確定ステップではなく「神別バランス＋スコア評価軸の共同最適化」として設計し直す。候補順は①block/heal評価の安全性再検証 ②resonanceとoracleBonusの重複整理 ③才華のcombo/resonance優位の扱い確認 ④蒼毘・寿楽・笑蓮の神別数値/専用カード調整 ⑤その後にdifficulty倍率を含む最終スコア係数確定。**変更ファイル**：本決定のみ。`src/`・`core/engine`・`rules.ts`・ScoreState・カード/敵/共鳴数値・既存テストは無変更。`npm test`（199件）・`npx tsc -b --noEmit`・`npx oxlint .`とも全て通過、一時シミュレーションファイルは撤去済みで`git status --short`は本決定作業前からの`.claude/settings.local.json`差分のみ |
| 2026-08-27 | 決定107を追加。「STEP-SCORE2-A：七神七道＋スコア共同最適化」の設計・大規模シミュレーションを実施した（コード変更なしの設計・監査フェーズ）。**何を決めたか（仮係数の本番採用ではない）**：①現行スコアは実質的に「カード使用枚数」を強く評価しており、神ごとの火力・防御・回復・妨害の個性がスコアに十分反映されていない ②七神の現状差別化は平均**4.2/10**で、初手からの差は全神0（`GodDef`型にstat系フィールドが存在せず`createInitialState.ts`が`godDef`を参照しないため、HP・AP・手札・共鳴MAX・託宣は完全に同一） ③完全共通式（案A方式）では神格差が悪化する ④神ごとに完全別式を持つC方式は保守性・敵依存の暴発・ランキング適性に問題がある ⑤**「共通基礎スコア＋神別Skill Bonus」のB方式を次STEPの採用候補として進める** ⑥52,500試合のシミュレーションで現行神格差**1.589**→案B**1.076**まで縮小することを確認した ⑦**ただし仮係数は採用値ではなく、蒼毘1柱のプロトタイプで実装検証してから確定する** ⑧七神七道はスコア設計と一体で進める（決定106の「神別数値とスコア評価軸の共同最適化」方針の具体化）。**なぜ**：決定106で「評価軸を単純追加するだけでは神格差もリスク・リターン構造も自動改善しない」ことが実証されたため、スコア式側と神データ側の双方から同時に検証する必要があった。**シミュレーション規模**：総**52,500試合**（試練の影＝7神×3難易度×5戦略×420run＝44,100／銀甲の機工師4,200／双牙の魔獣4,200。戦略は既存のbalanced/aggressive/defensiveにcombo・resonanceの2種を追加）。**現行スコアの実測（勝利時）**：mean 812.0／median 779／std 184.8／range 378〜2249／800台26.7%／1000+ 8.5%。神格差**1.589倍**（才華1096 vs 寿楽690）、god×難易度×戦略のセル最大格差**2.860倍**。**根本原因**：才華−寿楽の差406点のうち**oracleBonusが+291（71.7%）・comboが+95（23.4%）で約95%がoracle＋combo由来**、damageは全神ほぼ固定（敵HPが固定のためdamage scoreが107〜113に収束）。comboとoracleBonusはいずれもカード使用枚数に強く比例し、使用枚数比1.73が神格差1.589にほぼ一致する。相関でもburst回数0.866・獲得AP0.843・追加ドロー0.813に対し、実効ダメージ0.142・block吸収0.272・実回復−0.016・**debuff−0.350（負）**であり、妨害を積むほどスコアが下がる構造だった。**七神七道の設計方向（今後のprototypeで調整可能な設計方針であり、数値確定ではない）**：恵比寿＝速攻「早く倒すほど高得点」／大耀＝爆発火力「強く殴る」／蒼毘＝鉄壁「敵の大技を防いで勝つ」／才華＝手数コンボ「多く使いながら無駄なく倒す」／寿楽＝妨害「敵を弱らせて勝つ」／福永＝リスク＆リターン「危険域から生還する」／笑蓮＝耐久回復「傷つかずに勝つ」。**案Bの結果**：勝利時mean 810.5／std 125.4（現行184.8から約3割減）／神格差はtrial **1.076**・銀甲の機工師1.101・双牙の魔獣1.171／戦略差1.251→**1.149**／セル最大格差**2.860→1.931**。A/B/C比較で案Bが全指標で最良だった。**案A・案Cを採用しない理由**：案A（現行式＋低係数追加）は神格差が1.589→**1.637へ悪化**した（才華がblock吸収・burst回数でも最上位のため、追加軸でも才華が最も取るため）。案C（skill bonusを大きくする方式）は上限なしでは敵依存で暴発し（銀甲の機工師で蒼毘が1305点まで伸び神格差**1.790**）、上限を付けると格差1.237と案Bより悪化するため、「敵依存の暴発」か「格差再拡大」のどちらかを必ず引き受ける構造だった。**重要な新発見3件**：(1)**バースト到達性に構造的格差**——デッキ内resonance総量が恵比寿・才華13／大耀・寿楽9／**蒼毘・福永・笑蓮7**（＝ゲージMAXちょうど）で、蒼毘等は共鳴札を全て引き全て使ってようやく1BURST。防御神の共鳴にblock15が付いているのにその共鳴に最も到達しにくいという逆転構造。(2)**童子(doji)形態は現行の1戦内では全神到達不能**（3BURST＝21resonance必要 vs デッキ最大13）。にもかかわらず神選択画面のステータスバー（`godStyle.ts`の`computeGodStats`）はdoji値で描画しており、選択画面の表示と実プレイ体験が乖離している。(3)**spirit形態のOTOMO効果は`applyResonance`の処理順序上、実質的に発動しない**（先に形態が進化してから効果が適用されるため）。**OTOMOについて**：蒼毘（神block＋OTOMO block）・才華（神draw＋OTOMO draw）・福永（神heal＋OTOMO heal）・笑蓮（神heal＋OTOMO heal）のように神の特徴を補完するより同じ効果を重複させており、力の絆も7体中5体のincarnateがdamage4〜5程度で似ている。**OTOMOは再設計候補だがSTEP-SCORE2-Bでは触らず、七神七道確定後に「神を補完する存在」として別STEPで再設計する**。**神別の最小調整候補（記録のみ・採用確定ではない）**：笑蓮＝専用カードのコピー数1→2、専用2枚（福袋heal11＜共通息吹heal12、懐の深さblock11＜共通鉄壁block12）の完全下位互換の解消／蒼毘・福永・笑蓮＝おすすめデッキのresonance札+1〜2／寿楽＝3AP札を軽量札へ置換（GOD_TACTICS「軽いカードを次々に重ね」と実デッキ総AP38・平均1.90という7神中最重の構成が矛盾しているため）／才華＝数値nerfは現時点では行わず、スコア式側で有利さを吸収する方針。**初期stat**：案Bで神格差1.076まで改善できるため、HP差・初期AP差・passive等の`GameState`/`core`変更を伴う初期stat差は**現段階では追加しない**（費用対効果が低い）。**Resonance方針**：七神個性の中核へ昇格させる価値はあるが、**BURST回数への直接加点は禁止**（案Aで相関が0.904まで悪化することを実測）。先にresonance到達性の格差を是正する。**difficulty**：現段階の推奨候補はeasy 0.95／normal 1.0／hard **1.05**（**採用値ではない**）。1.1は素のhard/easy比1.083と重畳して1.140になり、神別のhard適性差（恵比寿0.899〜福永1.318）を増幅する可能性が確認された。**仮係数の位置づけ**：STEP-SCORE2-Aで算出した係数群（godSkillの神別係数、上限260、非勝利時×0.25、oracleWeight 0.25、perRemainingRound 120 等）は**すべてSTEP-SCORE2-Bのプロトタイプ候補であり、採用値・正式値ではない**。蒼毘プロトタイプで検証してから確定する。なおシミュレーション中に、指示仕様のperRemainingRound 60では「遅く倒すほど高得点」に反転すること（R4撃破859 vs R7撃破944）、oracleWeightを0にすると神託・予言・小さな託宣・天啓の託宣・全神の共鳴score効果が死にカードになること、skill bonusを非勝利時にも満額付与すると防御型の「未撃破farming」が成立すること、の3点が判明しており、これらはプロトタイプで必ず検証する。**exploit監査**：高リスク0件、中リスク3件（resonance farming 0.446／combo spam 0.310／AP farming 0.311、いずれも現行の0.866／0.699／0.843から低下）、低リスク7件。**次STEP**：**STEP-SCORE2-B「蒼毘1柱プロトタイプ」**。`ScoreState`拡張・godSkill bonus・上限260・非勝利時×0.25・block absorbed・huge attack defense・exploit耐性を**蒼毘のみで検証**し、いきなり7神へ横展開しない。蒼毘を最初に選ぶ理由は、52,500試合で唯一暴発した軸（実吸収ブロック）を持つ最高リスク神であること、勝率64.6%で7神最低かつblock吸収効率24%と問題が多面的であること、デッキ内resonance合計7というburst到達性の欠陥も併せ持つため**スコア式側と神データ側の両方の介入を同時に検証できる唯一の神**であること、の3点。**変更ファイル**：本決定のみ。`src/`・`core/engine`・`GameState`・`rules.ts`・ScoreState・カード/神/OTOMO/敵の数値・既存テストは無変更。`npm test`（199件）・`npx tsc -b --noEmit`・`npx oxlint .`とも全て通過、一時シミュレーションharnessは撤去済みで`src/`配下に未追跡ファイルが残っていないことを確認済み |
| 2026-08-27 | 決定108を追加。「STEP-SCORE2-A REDTEAM：案B反証監査とスコア再設計順序の変更」を実施した（コード変更なしの反証監査フェーズ）。**なぜREDTEAMを行ったか**：決定107では七神七道・「共通基礎＋神別Skill Bonus」（案B）・神格差1.589→1.076・蒼毘prototype先行を有力候補としたが、「平均値が公平でも、ランキング上位帯・敵多様性・exploit・将来拡張まで公平とは限らない」ため、既存52,500試合の生データを事後再計算・再分析する形で反証目的の監査を行った（新規対戦simulationは0試合、本番コード変更なし）。**主要結果①上位帯ランキング問題**：案Bの平均神格差1.076は上位帯では維持されず、P90=1.164→P95=1.225→**P99=1.341**と上位ほど拡大。**上位1%の79.8%が才華**（うち才華/combo/hardの1セルだけで66.7%）、1000点到達率は才華13.3% vs 蒼毘0.6%で**約22倍差**。平均神格差だけではランキング公平性を評価できないと判断した。**②cap260問題**：神別Skill Bonusのcap260が平均を人工的に揃えており、各神の上位10%試合では蒼毘**81.6%**・寿楽46.9%・恵比寿46.8%がcapに張り付いていた。cap∞では格差1.005、cap200では1.118と、**公平性がcap設定に依存**することを確認。cap260は正式採用値として扱わない。**③隠れハンディキャップ化**：基礎スコアが弱い神ほどSkill Bonus平均が大きい構造（才華：基礎約635→bonus約21／蒼毘：基礎約435→約175／笑蓮：基礎約453→約207）で、神別bonusが「神らしい上手さの評価」ではなく「平均を揃える隠れハンディキャップ」になっていた。最小二乗フィットによる神別係数調整は正式設計として採用しない。**七神七道の個別問題**：恵比寿＝速攻コンセプトは維持するが、godSkillの早期撃破評価と共通基礎perRemainingRoundが同一変数を二重評価（速度由来がスコアの42.1%、corr 0.869）。大耀＝爆発火力は維持するが、**bonusの81.5%がoverkill由来**で「基礎からoverkillを除外する」設計思想と矛盾——overkill依存軸は再設計対象。蒼毘＝鉄壁は維持するが、trialでは「大技半減」軸の寄与が**わずか0.2%**（trialの15+攻撃はR7の1回のみ）で、cap張り付き81.6%と併せ測定器自体が歪んでいる。才華＝手数コンボは維持するが、支配の原因は神別bonusではなく**共通基礎に残ったcombo/oracle依存**。寿楽＝妨害は維持するが、debuffだけ「適用量」評価でblock/healの「実際に効いた量」原則と不整合（無駄debuffでも加点可能、corr(debuff量,撃破R)=0.765）——将来は「有効debuff量」の評価方法を検討。福永＝リスク＆リターンは維持するが、**intentional damage exploitを確認**：被弾量Q1→Q4でスコア約717→929、意図的に多く被弾すると**約+212点（約+30%）**になるため、現在の被弾連動bonus方式は採用しない。笑蓮＝無傷耐久は維持するが、bonusの74.5%がほぼ一定の残HP率項で定数的な下駄になっており、完全ブロックも神固有スキルとしての識別力が弱い（火力神の大耀でも平均1.35回発生する副産物）。**敵依存問題**：神別bonusの敵間変動率は大耀1.45・才華1.39・福永1.39等で、神ごとに最適なfarm敵が存在する（恵比寿/蒼毘/福永→魔獣、他→trial型）。今後予定の「敵選択UI」と組み合わせると**敵選択機能そのものがランキングスコアfarm exploitになる危険**があるため、敵選択UI実装前にランキング式の敵依存耐性を確認する。**最重要結論＝共通基礎式の問題**：案Bの本当の病巣は神別Skill Bonusだけでなく、**共通基礎スコアに残存する「カード使用枚数バイアス」（combo×9＋oracle×0.25）**である。神別bonusを完全に外し共通基礎だけにすると神格差**1.462**・**上位1%は才華100%**となり、単純な「ランキング＝共通基礎」方式も現時点では成立しない。したがって**神別レイヤーを設計する前に共通基礎式を再設計する**。**Battle Score＋Mastery二軸案の扱い**：却下ではなく「現在の共通基礎式のままでは成立しない」が正確。基礎式修正後は、Battle Score（全神共通のランキング評価）＋Mastery（その神らしい戦い方の評価）の二軸方式を**有力候補として再比較する**（最終採用は未決定）。**決定107から維持するもの**：七神七道というCore Fantasy（恵比寿＝速攻／大耀＝爆発火力／蒼毘＝鉄壁／才華＝手数コンボ／寿楽＝妨害／福永＝リスク＆リターン／笑蓮＝無傷耐久。REDTEAMで否定されたのは方向性ではなく測定方法）／52,500試合simulationの知見／**「実際に効いた量」を評価する原則**（blockは獲得量でなく実吸収量、healは表示値でなく実回復量、debuffは付与量でなく実際に軽減したダメージ等への換算を検討）／スコアと神個性を共同最適化する必要性／単純な評価軸追加だけでは解決しないという判断／本番実装前にsimulationで検証する方針。**決定107から覆すもの**：蒼毘prototype先行（cap張り付き81.6%＋trialで大技軸寄与0.2%のため測定器として歪んでおり撤回）／cap260／非勝利×0.25を含む仮係数群の採用候補扱い／最小二乗フィットされた7神別係数／現在の共通基礎へそのまま神別Skill Bonusを載せる実装順序。これらは正式採用値ではなく、REDTEAMで問題が確認されたsimulation仮説として扱う。案Bは完全失敗ではなく、**方向性から得られた知見は維持しつつ、REDTEAMにより実装順序と測定方式を修正した**という位置づけ。**新しい順序**：第1段階＝神別prototypeではなく**共通基礎スコア式をsimulationで再設計**、第2段階＝神別評価方式（小God Skill方式 vs Battle Score＋Mastery二軸方式 vs ハイブリッド）を再比較して最終方式を決定。神別prototypeが必要になった場合の現時点の第1候補は寿楽（有効debuff計測という唯一エンジン側の新計測を要する最難問のため）だが、**寿楽prototype自体もまだ候補であり正式決定ではない**。**次STEP＝STEP-SCORE2-B改（BASE）**：共通基礎スコア式の先行再設計。重点項目はcomboカード枚数バイアスの圧縮／combo逓減または上限方式の比較／oracleWeight再設計／overkillの基礎からの除外維持／実効ダメージ評価／テンポ単調性の回復（全神で「基本的には早く倒すほど高得点」になるか）／defensive・aggressive・balanced等の戦略成立性／上位1%構成比／敵依存性／exploit確認。**目標は共通基礎単体で神格差1.2前後以下への縮小だが、1.2は正式採用基準ではなく次STEPの探索目標値**。**変更ファイル**：本決定のみ。`src/`・`core/engine`・GameState・ScoreState・カード/神/OTOMO/敵データ・CSS・テスト・saveVersionは無変更。新規simulation・係数調整・prototype実装も行っていない。commit・push・deployも行っていない |
| 2026-08-27 | 決定109を追加。「STEP-SCORE2-B改（BASE）：共通Battle Score再設計」を実施した（コード変更なしの設計・大規模評価フェーズ）。**何を決めたか（BASE-D係数の本番採用ではない）**：①決定108の「神別Skill Bonusを先に入れるのではなく共通Battle Score自体を先に直す」方針が正しかったことを実測で確認した ②候補式**BASE-Dが現時点の最有力候補** ③神別補正ゼロの共通式のみで、平均だけでなくP90・P99・上位1%構成まで大きく改善できた ④Battle Score＋Mastery二軸方式が基礎修正後の有力候補になった ⑤**ただしBASE-Dの仮係数はまだ採用値ではない** ⑥次はMastery設計を先に行い、Battle Scoreとの役割分担を確定してから本番prototypeへ進む。**評価規模**：合計**84,000試合分**（既存52,500試合の事後再計算＋補完31,500試合。補完はper-roundカード枚数・oracle発生源内訳等の厳密計測用。一時ハーネスは完全撤去済み、本番コード変更なし）。**新発見3件**：(1)**oracleバイアスの最大原因はscoreカードそのものよりBURST時score効果**（才華−寿楽のoracle差の内訳：burst由来約190点 vs カード由来約79点）。(2)damageは敵HPで飽和しやすいが、「全神にほぼ共通」かつ「敵が強いほど自然に高い」ため、公平なBattle Score基礎項として有効（敵強度ボーナスとして機能）。(3)カード枚数バイアスを除去すると上位支配神は才華→大耀へ移る——**単一神支配の原因は一神だけの数値ではなく、combo/oracle vs tempoの係数バランスにある**。**BASE-D候補式の構成（いずれも正式採用値ではない）**：combo＝逓減方式（各Rで2枚目+12／3枚目+8／4枚目+4／5枚目以降+3。才華のコンボ個性を残しつつ枚数暴走を抑え、threshold方式ほど個性を消さない）／oracle＝**score効果（oracle score・BURST score）をBattle Scoreから完全除外**する方向を採用候補とする（神託が与えるdamage・draw・AP・heal等の実効果は他項へ自然に反映されるため、神託自体の弱体化ではない）／damage＝実効ダメージ×1.2、**overkillはBattle Scoreで無得点**（大耀の「爆発火力」個性はMastery側で表現する候補）／tempo＝線形係数でなく**逓減テンポ表**（勝利時：基礎300＋R6+90／R5+170／R4+240／R3+290。R4&gt;R5&gt;R6&gt;R7の単調性を保ちつつ早期撃破神のテール暴走を抑制）／block・heal＝**共通Battle Scoreには入れない**（低係数でもintentional damage等の逆インセンティブ発生を確認。将来Mastery側の有力評価軸）／survival＝勝利時のみ残HP率×30（敗北・未撃破では加点なし、intentional damageを直接得点化せず、神間レンジ小）／unused APペナルティ＝**廃止候補**（実測寄与が極小）／difficulty＝乗算でなく**加算方式**（勝利時hard+30／normal 0／easy-20。乗算は神ごとのhard適性差をそのまま増幅するため）／enemy normalization＝**現時点では採用しない**（正規化により逆に「最弱敵を選ぶのが最適」となるケースを確認。BASE-Dでは7神中6神で最タフのtrialが最高得点側になり敵farm問題は大きく緩和）。**BASE-Dの結果**：平均神格差**1.141**／P90神格差**1.126**／P99神格差**1.098**、上位1%最大神比率**大耀47%**（構成：大耀47／才華39／恵比寿11）。**前回（決定108）は上位ほど神格差が拡大していたが、今回は1.141→1.126→1.098と上位ほど縮小**する構造に反転した。分布（勝利時）：mean 806／median 807／std 62／P90 887／P95 903／P99 939／1000+は0.011%／最高1027。**score scale目安候補（ゲーム内正式表現としては未確定）**：800＝普通の勝利／900＝上手い勝利／950+＝非常に上手い／1000＝事実上のパーフェクト。**exploit監査**：stall不成立（未撃破max506＜勝利P10 722）／intentional damage直接得点源なし／block farming不可／heal farming不可／enemy farming大幅緩和／difficulty farmingはランキング上位ではhard優位方向に整列／comboは上位1%で48%を占めるがEVでは最下位の高リスク戦略のため監視対象とし現時点では即exploit判定しない。**残る課題**：(1)勝利時std62で分布がかなり圧縮されており、Battle Scoreだけでは「上達の見え幅」が小さい可能性（Mastery層の設計に依存） (2)上位1%のcombo48%・hard59%集中が残る (3)蒼毘の平均が最下位747（ただし本STEPでは神データ側の調整を行っていない） (4)resonance到達性13/9/7問題・doji到達不能・spirit効果dead code等（決定107・108）は未解決のまま。**Battle Score＋Mastery二軸方式**：BASE-Dにより共通Battle Scoreが改善したため、Battle Score（全神共通・総合ランキング用）＋Mastery（七神七道＝神らしいプレイの評価）の二軸方式が現実的な有力候補になった。**Masteryは現時点ではBattle Scoreへ加算しない方向を有力候補とする（ただし正式決定ではなく、次STEPで比較検証する）**。**小God Skill Bonus**：BASE-Dだけで平均1.141・P99 1.098まで改善できたため、Total Scoreへ神別Skill Bonusを加える必要性は大きく低下した。現時点では「不要の可能性が高い」と記録する（Mastery設計後に最終決定）。**次STEP＝STEP-SCORE2-C-MASTERY**：「七神七道Mastery設計＋Battle Scoreとの二軸UX監査」。Masteryを何で測るか／S・A・B・C等のgradeか0〜100か／神別ランキングへ使うか／Battle Scoreへ加算するか否か／結果画面での見せ方／初心者理解／実績・神縁への流用、を設計し、その結果を見てからBASE-Dの本番prototypeへ進む。**変更ファイル**：本決定のみ。`src/`・`core/engine`・GameState・ScoreState・rules.ts・カード/神/OTOMO/敵データ・CSS・テストは無変更。評価時の品質ゲート（`npm test` 199件・`npx tsc -b --noEmit`・`npx oxlint .`全通過、一時ハーネス撤去済み）確認済み。commit・push・deployは行っていない |
| 2026-08-27 | 決定110を追加。「STEP-SCORE2-C-MASTERY：Battle Score＋七神七道Mastery二軸設計」を実施した（コード変更なしの設計・監査・シミュレーションフェーズ。補完60,900試合、うち52,500試合は決定109のraw.jsonと1:1一致を照合済み）。**主要判断**：①Mastery導入価値は**8/10**——BASE-D候補では勝利時std62まで分布が圧縮されており、Battle Scoreだけでは表現しにくい「神ごとの上達の見え幅」をMasteryが補完できる（決定109の残課題(1)への回答）。②**Battle Score（全神共通の戦闘技術・ランキング評価）とMastery（七神七道をどれだけ体現したかを示す神技評価）を完全分離する方向を採用候補とする**。③**MasteryをBattle Score Totalへ加算しない方針を正式採用候補とする**——加算すると決定108で確認した隠れハンディキャップ・敵依存フィット・神格差再拡大・exploit誘因が再発するため。④小God Skill BonusをBattle Scoreへ加算する方式（MODEL-B）は評価3/10で**不要方向と判断**。⑤**最終推奨構造はMODEL-D**＝Battle Score＋Mastery完全分離＋Battle Score神別ランキング（神別ランキングはBattle Scoreの神別フィルタで実現し、Mastery自体はランキング得点にしない）。⑥MasteryのUI表現は**S/A/B/CのGrade方式**を推奨（内部では生値を保持してよいが、ユーザーには原則Gradeを表示。0〜100の直接表示は現時点では不要）。⑦Masteryはリアルタイム常時表示しない——結果画面で「神技評価 大耀：A」のように1行表示し、詳細は展開式とする方向（「Mastery稼ぎ」への誘導を避ける）。**七神七道Mastery候補**：恵比寿「一本釣り」＝1枚あたり実効ダメージ（ただしBASE-Dのtempo評価との二重評価が強く**7神中唯一の要再設計候補**、prototypeは最後に回す）／大耀「爆発」＝1R最大実効ダメージ÷敵maxHP（**7神中もっとも健全**：識別1位・反stall・故意被弾なし・難易度中立）／蒼毘「鉄壁」＝実際に吸収したblockを基準とする吸収率（空積みblockは評価しない。被弾と負相関−0.58＝被弾を罰する良設計）／才華「奏舞」＝1R最大カード使用枚数（P90で才華11 vs 他神4〜6の圧倒的識別。単純spam化を監視）／寿楽「無力化」＝debuff適用量ではなく**実際に軽減したdamage**を評価（敵依存が7神中最大のため要注意）／福永「逆転」＝危険HP域から勝利・回復して逆転するプレイ（**被弾量そのものは絶対に得点化しない**＝intentional damage exploitの禁止）／笑蓮「無傷」＝完全HP勝利・高HP勝利など中心（笑蓮68.9% vs 次点47.2%で識別良好。単純な残HP率の定数下駄化を避ける）。※当初設計7案のうち4案（恵比寿の神託使用数＝撃破Rとcorr+0.94の純stall指標／才華の3枚以上R継続＝福永と識別不能・stall+0.69／寿楽の軽減絶対量＝stall+0.79・機工師で全難易度S=0%／笑蓮の無傷R数＝蒼毘が1.8倍でblock神の指標化）が**実測で否定され、上記の最終案に差し替えた**。**Mastery設計原則（シミュレーションから得た原則として記録）**：実効量を評価する／比率型・binary型を優先（絶対量型よりstall相関を0.79→0.33等に低減し、蒼毘では故意被弾の符号を+0.14→−0.58に反転させることを実証）／必要に応じて難易度ゲート／intentional stall・intentional damage・spamを評価しない／敵依存を可能な限り抑える／Battle Scoreと同じ行動を二重評価しない。**シミュレーション結果**：Mastery S達成率＝校正後約3.2〜7.2%、A達成率＝約24〜28%。位置づけ候補：S=非常に上手いプレイ／A=上手いプレイ／B=その神らしいプレイをある程度できた／C=参加・未達成寄り。**ただしGrade閾値はまだ正式採用値ではない**。**敵依存問題**：機工師について寿楽・笑蓮・福永で構造的なMastery難度差を確認（機工師の溜めラウンド構造により、機会数の単純な正規化では完全解消できないことを実測で確認）。対策は**絶対閾値＋必要に応じた難易度・敵ゲート**を候補とする（敵別閾値の増加は保守コストになるため最小限にする）。**測定可能性**：7指標すべて既存イベントのみで計測可能（新規core/engineイベント不要。寿楽の実軽減量は`ENEMY_INTENT_SET.amount`と`ENEMY_ACTED.amount`の差分そのもの）。ただし恒久実装は、`resumeGame`がイベントログをリセットする（決定29のセーブ再開との衝突）ため、GameState内にMasteryStateを持つ方式（saveVersion影響あり）を推奨。**Masteryの将来用途**：Battle Scoreへ加算せず、神別熟練度表示・実績・称号・神縁EXP・Daily条件・Story条件/演出への利用候補とする。神縁等の報酬はPay-to-Winにならないよう表示・演出・称号等を中心とする方向。**BASE-Dとの関係**：MasteryはBattle Scoreへ加算しないため、決定109のBASE-D候補のシミュレーション結果（平均神格差1.141／P90 1.126／P99 1.098／上位1%最大神比率47%）を**Mastery導入によって破壊しない**。現時点ではBASE-D側をMasteryのために修正する必要なし。**ただしBASE-D自体はまだ「simulation検証済み最有力候補」であり正式採用値ではない**ことを維持する。**prototype方針（次STEP候補＝STEP-SCORE2-D-PROTO、今回は着手しない）**：対象は(1)BASE-D prototype実装 (2)MasteryState骨格 (3)大耀Masteryのみprototype。**最初のMastery対象は大耀**——指標が最も健全／敵依存が比較的小さい／exploitリスクが低い／MasteryState配線検証に適しているため（決定108の教訓「壊れた計測器で検証しない」の適用）。prototype順候補：大耀→寿楽→蒼毘・笑蓮→才華・福永→恵比寿。**未解決として記録（今回解決していない）**：恵比寿Mastery再設計／寿楽の敵依存／蒼毘S達成率調整（7.2%）／才華S達成率調整（7.1%）／Mastery Grade正式閾値／MasteryState詳細型／saveVersion対応／resonance到達性13/9/7問題／doji到達不能／spirit効果dead code／OTOMO再設計。**変更ファイル**：本決定のみ。`src/`・`core/engine`・GameState・ScoreState・rules.ts・カード/神/OTOMO/敵データ・CSS・テストは無変更。品質ゲート（`npm test` 199件・`npx tsc -b --noEmit`・`npx oxlint .`全通過、一時ハーネス`__tmp_mastery.sim.test.ts`撤去済み・`src/`未追跡ファイルゼロ）確認済み。commit・push・deployは行っていない |
| 2026-08-27 | 決定111を追加。「STEP-SCORE2-D/D2 prototype CEO実プレイ承認 — BASE-D＋神技評価＋表示スケールUX」。simulation・testだけでなく**CEO自身が実ゲームをプレイし「体感として改善した」ことを確認した**点が本決定の核心（未commitのprototype実装。commitは別途判断）。**1. BASE-D prototype（STEP-SCORE2-D-PROTO）**：決定109のBASE-D候補を本番コードへprototype実装（ScoreState v4化・saveVersion 3→4・v3セーブは破棄せずmigrationで読み込み継続・recordStorageは新フィールド`bestBattleScore`追加で旧ベスト温存）。検証：230 tests PASS／tsc clean／oxlint clean／console error 0／**21,000試合のsimulation回帰**（本番エンジン実行）で mean 806.8・P90 887・P99 943・平均神格差1.137・P99神格差1.108——決定109のsimulation候補値と軽微な差のみで整合し、実装バグを示唆する系統的乖離なし。**これによりBASE-Dは「simulation上のみの候補」から「実装prototype＋回帰検証を通過した採用有力候補」へ一段階昇格した。ただしまだ正式最終採用値と断定せず、今後の実プレイで微調整余地を残す（永久確定値ではない）**。**2. Mastery prototype**：大耀のみ「神技評価 大耀「爆発」」を実装。指標＝1ラウンド最大実効ダメージ÷enemy.maxHp（S≥0.58/A≥0.45/B≥0.37、prototype閾値）。**Battle Scoreとは完全分離でTotalへ一切加算しない**（コード上・テスト上・21,000試合の数値上の三重で分離を保証）。**3. 神技評価UX改善（STEP-SCORE2-D2a）**：CEO実測「Mastery A/Sって何かわからない」への対応として結果画面を3行構成へ——「神技評価 A（見事）」＋小hint「スコアとは別の、その神らしい戦い方の評価」／「大耀「爆発」— 1ラウンドで敵HPの47%を一気に削った！」／「Sまであと12%」。Grade和語＝S神業・A見事・B堂々・C修行中。**C評価時は「あと○%」を出さず「大耀らしい爆発力を、さらに高めよう」等の励まし表現**（圧対策）。S時は「神業達成！」。**4. 神選択画面**：大耀のみ「神技「爆発」：1ラウンドで大ダメージを与えるほど評価UP」の1行を追加（戦闘前に狙いが分かるように）。**5. 表示スケール（STEP-SCORE2-D2b）**：**内部バランスは一切変更せず**、damage/HP/block/heal（＋atk系バフ・Battle Score）を**表示のみ×10**（新設`displayScale.ts`、4桁以上カンマ区切り）。例：内部damage20→表示200、内部enemyHP103→表示1,030、内部スコア957→表示9,570。**AP×1・resonance×1・round×1・combo枚数×1は倍率対象外**（特に共鳴0〜7の「7」モチーフは不可侵として維持）。**6. 表示×10の技術判断**：内部値そのものは×10しない。理由＝84,000試合simulation結果の維持／丸め粒度を変えない／saveVersion追加変更不要／recordStorage・ranking内部値不変／将来display倍率を変更可能。finalScale13案（内部係数変更）は不採用。**7. BURST表示の訂正**：UX-AUDIT監査中の「BURST 2,100〜3,200」は誤記で、正しくは**内部21〜32→表示210〜320**。prototypeでは正しく実装され実機で確認済み（大耀burst「敵に320ダメージ」表示）。**8. CEO実プレイ結果（実機UX acceptance）**：CEOが大耀を実プレイし、Battle Score **9,570**・神技評価**C（修行中）**・「1ラウンドで敵HPの36%を一気に削った」という結果を確認のうえ、FB①「ダメージ表示気持ちいい」②「大技に感じる」③「スコアアタックとして目標感が増した」④「神業の意味がわかる」⑤「大耀の遊び方が想像できる」⑥「もう一戦やりたくなる」を得た。**9. 今回のUX判断**：CEO実プレイ結果から、表示スケール×10＝採用方向で承認／神技評価3行UX＝採用方向で承認／大耀「爆発」Mastery prototype＝成功判定。Battle Score＋神技評価の二軸目標（「高スコアを狙う」＋「その神らしいS評価を狙う」）が2種類の再挑戦動機を実際に作れる可能性が確認された。**10. 品質結果（D2完了時）**：test **315件**全通過／tsc 0／oxlint 0／console error 0／内部バランス変更なし——**同一のセーブ状態で内部スコア732→表示7,320、内部764→表示7,640と、表示だけが変化することを直接比較で確認**（内部不変の実証）。**11. カード文言整合**：55カード＋託宣2種の数値表示をdisplay scaleへ書き換え。新設`cardTextScale.test.ts`が全カード・全託宣について「effect値×倍率が本文に存在するか」を機械照合（63件全通過）し、「effect 12なのに本文80」型の書き換え事故を恒久検知する。**12. 最重要残課題**：旧スコア仕様の「スコアをN得る」「スコア+N」等の文言が、カード本文・託宣・共鳴効果説明に残存している。BASE-Dではoracle score／BURST scoreをBattle Scoreへ直接加算しないため、**表示と実挙動が一致しない箇所が存在する。次STEP最優先で整理する（ランキングゲームとして虚偽表示を残してはいけない）**。**13. その他残課題（今回解決していない）**：GodSelect旧形式bestの注記不足／localhost recordsにQA由来の大耀記録が存在／恵比寿Mastery再設計／寿楽Mastery敵依存／残り6神Mastery／resonance到達性13/9/7／doji到達不能／spirit効果dead code／OTOMO再設計。**14. 次STEP＝STEP-SCORE2-E**：最優先は「旧score仕様の表示・効果・文言整合監査」——「スコアをN得る」「スコア+N」・oracle score・BURST score・神説明・カード本文・共鳴効果文言・Tutorial・result・tactics・deck builder・GodSelect等を全件監査し、BASE-D導入後に虚偽化したスコア関連表示を完全整理する。その後、寿楽Mastery prototype→残りMastery展開へ進む。**15. 今回確定方向として記録**：BASE-D prototype成功／表示×10／神技評価3行UX／Battle Scoreと神技評価の完全分離／Mastery Total非加算／大耀「爆発」prototype成功。**変更ファイル**：本決定のみ（docs記録）。prototype実装自体は未commitの作業ツリー上にあり、コード・テスト・CSSは本決定作業で一切変更していない。commit・push・deployも行っていない |
| 2026-08-27 | 決定112を追加。「STEP-SCORE2-F2：旧score仕様廃止＋B'戦闘効果への正式置換」。**B'採用判断**：BASE-D（決定109）で無効化された旧`kind:'score'`効果の代替について、STEP-SCORE2-E監査→F1（84,000試合のPLAN-A/B/C比較）→F2-PHASE1A（B'追試37,800試合）→PHASE1B（アンコール変種37,800試合）→PHASE2（本番prototype実装＋回帰検証16,800試合）を経て、**B'構成をsimulation・prototype・実機QA通過済みの採用構成として確定した**。**B'構成**：恵比寿BURST=[damage25, gainAp2]（一本釣り→追撃。旧「最高スコア」identityから「速攻」へ。GOD_TACTICSも「大きな一撃で素早く仕留める、扱いやすい速攻型」へ更新）／大耀=[damage36]（7神最大の純火力一本、神技「爆発」と整合）／蒼毘=[damage21, block20]／**才華=旧score170の補償なし**（旧oracle/score優位がランキング偏重の主因だったため。手数エンジンで個性は十分）／寿楽=[damage23, atk debuff-6/3R]／福永=[damage22, heal6, gainAp1]（窮地からの反撃資源）／笑蓮=[damage21, heal12, block12]。カード：アンコール=[gainAp2, draw1]／喝采=[draw1, block2]／福授け=[heal6]／天啓=[damage4]／**笑袋・力の絆=incarnate[damage5]・doji[damage5, heal3]**（「何も起きないburst」だった完全dead効果を解消。F2で発火482/482を実証）／神託・予言・小さな託宣=旧score効果を削除し既存の実効果のみ維持（神託は素の8.33dmg/APでトップ効率のため補償不要）。**アンコール判断（PHASE1B）**：A=[gainAp2,draw1]／B=[gainAp3]／C=[gainAp1,draw1]を各12,600試合で比較した結果、**「draw循環が才華テール集中の原因」という仮説は反証された**——Bでは上位1%才華53.1→56.0%・上位5% 31.1→39.5%・才華hard勝率67.0→78.3%へむしろ悪化し、**真の燃料はdrawではなく過剰なAP供給**だった。Cは公平性を改善するが才華勝率79.8%まで低下しnet−1APの「使って損」カード化する。よって**A=[gainAp2, draw1]を採用**し、「もう一手動ける」という才華・アンコールの体験を優先した。**才華上位集中の扱い**：B'の上位1%才華率53.1%について、50%未満を絶対条件とはしない。旧案の84%超から大幅改善しており、これ以上の圧縮は才華の面白さを損なうことがPHASE1Bで確認されたため許容し、将来的には決定110 MODEL-DのBattle Score神別ランキングを最終的な受け皿とする。**旧score仕様廃止**：旧score effect **16件→0件**（カード6・託宣1・BURST7・OTOMO2）。ユーザー向け旧score虚偽表示（「スコアをN得る」「スコア+N」等、現在のBattle Scoreで加点されない旧仕様表示）**0件**。新設の恒久テスト`scoreLegacyIntegrity.test.ts`（22件）で再発を防止。dead code（formatEventの旧reason label、useBattleFxのapPenalty残骸）も撤去した。**resonance設計原則**：score→resonanceへの機械的置換は不採用（F1 PLAN-Cでburst回数+50%・doji率暴騰・才華テール83%超・hard緊張感低下・Mastery Grade崩壊を実測）。**BURST・OTOMO効果へresonanceを追加しない**ことを設計原則として維持し、恒久テストで全神・全OTOMO・全形態を横断保証。**Battle Scoreとの関係**：BASE-Dのスコア式自体（effective damage・combo逓減・victory base・tempo・survival・difficulty bonus・finalScale・overkill除外・unused AP廃止）は一切変更していない。B'は戦闘効果の変更であり、Battle Score評価式の変更ではない。MasteryのTotal非加算も維持。**大耀Mastery**：BURST damage36でもS率4.61%と目標帯3〜5%に収まったため、prototype閾値S≥0.58は現時点では変更不要とデータで判断。**最終回帰結果**：実装後simulation計16,800試合（main 12,600＋power絆4,200）で、win rate 85.56%／mean 821.3／P90 902／P99 960／平均神格差1.140／P99神格差1.112／上位1%構成=才華53.1%・大耀23.9%・恵比寿17.7%／大耀Mastery S率4.61%。さらに**PHASE1AのB' simulationと12,600行を行単位照合して不一致0**——事前simulationのB'と本番実装B'が決定論的に一致することを証明した。**実機QA**：恵比寿=250ダメージ＋神力+2（burst後に神力6/5とmax超過し「一本釣り→追撃」の体験が成立）／大耀=360ダメージ表示／才華=アンコールで神力+2＋draw1（コスト込みnet±0APの「もう一手」体験）／375px=横はみ出し0。**品質**：337 tests全通過（315＋整合22）・tsc 0・oxlint 0・console error 0・意図しない変更なし。**未解決（今回解決していない）**：残り6神Mastery／寿楽Mastery敵依存問題／恵比寿Mastery再設計／蒼毘・才華Grade最終校正／resonance到達性13/9/7／doji到達不能問題／spirit dead code／OTOMO全面再設計／神別ランキング本体／Daily／Story／神縁／backend。**表現上の注意**：B'戦闘効果はsimulation＋prototype＋実機QAを通過した採用構成として記録するが、**BASE-D全体を永久不変の最終値とするものではなく**、Masteryの残り6神やGrade閾値の確定でもない。**変更ファイル**：本決定のみ（docs記録）。実装はSTEP-SCORE2-F2-PHASE2として未commitの作業ツリー上にあり、本決定作業ではコード・テスト・CSSを一切変更していない。commit・push・deployも行っていない |
| 2026-08-27 | 決定113を追加。「STEP-SCORE2-G1 寿楽Mastery「無力化」— 敵依存問題の検証とJ-G方式採用方向」（HEAD=`67ec57f`基準、コード変更なしの設計・検証フェーズ。寿楽prototypeはまだ実装しない）。**検証規模**：**19,250試合**（寿楽×全7敵×3難易度×5戦略15,750＋7神識別力検証3,500）＋保存生データ（敵攻撃ごとのraw/actualペア）からの事後再計算。候補J-A（総実軽減率）／J-B（有効攻撃軽減率×ガード有無2種）／J-C（危険攻撃無力化率×2種）／J-D（Best Play型×2種）／J-F（危険攻撃平均軽減）／追加設計のJ-G・Hybridを比較。**重要な反証**：①**J-D Best Play型は棄却**——満点飽和42.8%・combo戦略で常時約0.992・軽量debuff約4.2本の重ね掛けで満点を購入可能＝「上手い無力化」ではなく作業化。②**J-B無ガード型も棄却**——S相当到達の39.4%が「raw<10の弱攻撃だけを100%軽減」するweak-hit exploit。③J-Aは寿楽識別力1位だが機工師S率0%・spam相関+0.61のため最終案にしない。④**神識別力問題の発見**：J-A以外のほぼ全候補で蒼毘（見切り・一喝のdebuff保有）が寿楽を上回った——「寿楽のMastery」を蒼毘の方が上手く取れる指標は採用不可という選定基準が確立。**J-G採用方向（最有力prototype候補）**：**「攻撃等重み平均軽減率」**＝各敵攻撃について`(rawDamage−actualDamage)÷rawDamage`を計算し**攻撃1回ごとに同じ重みで平均**（charge除外。raw=`ENEMY_INTENT_SET`素値、actual=`ENEMY_ACTED`実値）。**敵ID補正なし・敵HP補正なし・敵別係数なし**。目的＝大技1発の金額支配を防ぐ／攻撃回数による支配を防ぐ／長期戦・stall依存を抑える／実際に敵攻撃へ作用したdebuffだけを評価する。**J-G主要結果**：識別力=寿楽1位0.714で2位蒼毘の×1.54／stall相関**+0.27**（全候補最低）／Battle Score相関**−0.22**（二重評価なし・独立軸として健全）／被damage相関**−0.71**（被弾を罰する＝intentional damage不成立）／debuff回数相関+0.54（空撃ちspamは実軽減が発生せず無得点のため、相関は寿楽の様式そのもの）／weak-hitはraw≥10のstrong-hitゲート（既存の強打tier普遍閾値）で遮断／future enemy耐性=高（敵別閾値不要）。**Grade候補（正式最終閾値ではなくprototype検証用初期値）**：B≥0.55／A≥0.80／S＝base≥0.90かつ「raw≥10の強攻撃を50%以上無力化した実績1回以上」の行動ゲート、さらにeasyはS上限をAとする難易度ゲート方向。**S率と敵間公平のトレードオフ（重要判断）**：S希少性と敵間公平性は**単純な閾値調整では両立しない**ことが閾値スイープで実証された（S希少化→怨霊/龍神へ再集中し敵間格差がむしろ悪化）。確認されたフロンティア＝希少性優先：約7.7%・敵格差約97倍／バランス：約11.8%・約24倍／フロア優先：約13.1%・約15倍。したがって**「全神のS率を必ず1〜5%へ揃える」ことを絶対条件にはしない**——Masteryはランキング得点ではなく神ごとのプレイスタイル評価であり、大耀S率4.61%と寿楽S率が完全一致する必要はない。ただし寿楽約11.8%を正式採用する意味でもなく、prototype＋CEO実プレイ後に再評価する。**機工師問題**：S率0%は部分解消（HybridでS約9.1%）したが依然として寿楽の最難敵。これは正規化不足ではなく「**charge構造とdebuff戦術のゲーム構造上の相性**」である可能性が高く、敵ID別ハンディキャップで無理に均一化しない。**enemy farming（最大の残課題）**：怨霊・龍神でS取得率が高い傾向が残る。対策として敵別閾値表は大量導入せず、将来のBattle Score神別ランキング（決定110 MODEL-D）・敵別実績・Daily固定条件で吸収する方向。Mastery単体で全敵を完全同難度にすることを目的にしない。**UX方向**：結果画面候補＝「神技評価 A（見事）／寿楽「無力化」— 敵の攻撃を平均80%削いだ！／Sには強打を半分以下に抑えよう」、S時「強打を封じ、神業の無力化！」、C時「寿楽らしい搦め手を、さらに磨こう」（C圧対策）。表示値と内部指標は数学的に一致させる。**大耀との設計関係**：大耀=「最大瞬間火力」／寿楽=「敵攻撃全体への無力化品質」。Battle Score非加算・%ベース・既存戦闘データ由来・神らしいプレイ評価の原則は共通だが、寿楽のみeasy S gateとS行動条件を持つ**意図的非対称**（七神を無理に同じ計測式へ押し込まないため）。**実装予測**：MasteryState追加候補=attackCount／reductionRateSum／strongNeutralized。core event追加不要（runEnemyTurn内でraw/actual両方が取得可能）。saveVersionはv4→v5候補（default-fill migrationで旧saveを破棄しない方向）。**未解決（今回解決していない）**：S率11.8%の妥当性／enemy farming／機工師との構造的不利／S条件0.90・50%の正式値／A・B正式閾値／easy S gateの最終判断／実プレイでの理解度／寿楽Masteryの楽しさ／残り5神Mastery／resonance到達性13/9/7／doji到達不能／spirit dead code。**表現上の注意**：J-Gは永久確定仕様ではなく、S=0.90・A=0.80・B=0.55も正式採用値ではない。**「simulation検証済み最有力prototype候補」**として記録する。**変更ファイル**：本決定のみ。`src/`・テスト・CSS・assetsは無変更（337 tests・tsc・oxlint全通過、一時simファイル撤去済み確認）。commit・push・deployも行っていない |
| 2026-08-27 | 決定114を追加。「STEP-SCORE2-G1b〜G1d：寿楽Mastery「無力化」J-G prototype実装＋UX/Grade最終調整」（CEO＋相棒承認）。**実装（G1b）**：決定113のJ-Gを寿楽Masteryのprototypeとして本番実装した。**raw定義**＝敵攻撃1回ごとの`(rawDamage−actualDamage)÷rawDamage`の**等重み平均**（金額加重ではない。raw=難易度倍率済み`action.amount`、actual=debuff適用後実値）。**charge行動は除外**（attack分岐外のため構造的に集計されない）。**blockは軽減に含めない**（actualはblock適用前＝debuffによる無力化のみを評価し、蒼毘型の受けと区別）。**敵ID・敵HP・敵別係数は一切使用しない**。**Battle Scoreへは一切加算しない**（表示専用の独立軸、テストで固定）。**MasteryState追加**＝`attackCount`／`reductionRateSum`／`strongNeutralized`の3フィールド（大耀用roundDamage/bestRoundDamageは不変）。**saveVersion 4→5**へbumpし、**v4→v5はdefault-fill migration**（大耀集計値は保持・寿楽フィールドは初期値補完・scoreは無変更＝旧saveを破棄しない。v3→v5経路も維持）。**最終prototype Grade**：C＜0.50／B≥0.50／A≥0.80／S≥0.90＋strongNeutralized＋normal・hard限定。**strongNeutralized**＝内部raw≥10（表示スケールで「100以上」）の強攻撃を50%以上軽減（actual≤raw×0.5）した実績1回以上で恒久true（境界：raw10・actual5=true／raw10・actual6=false／raw9・actual0=false）。**easyはS条件を満たしても上限A**（easyCappedとして「ふつう」以上への導線を表示）。**B閾値0.55→0.50変更の理由（G1c再解析→G1d、CEO承認済み）**：CEO初見51%のみを根拠とせず、①全体C率29.2%→22.9%②normal C率27.3%→21.5%③魔獣C率54.4%→46.7%へ改善④0.50〜0.55帯はstall/spam層ではなく通常の勝利プレイ層（勝率98.4%・平均5.61R・debuff5.4回でexploit兆候なし）⑤「敵の攻撃を半分以上削げたらB（堂々）」という初心者にも説明可能な意味を持つ、の5点を根拠とする。**A=0.80維持の理由**：候補0.75はA以上率41.1%で「見事」の安売り、0.85はA以上23.7%でS(7.6%)との段差が圧縮される。0.80のA以上32.6%（normal33.2%／hard24.0%）が妥当域で変更根拠なし。**S条件維持の理由**：S=0.90＋行動ゲート＋難易度ゲートは決定113のフロンティア判断（希少性と敵間公平のトレードオフ）のprototype初期値であり、B変更がSへ影響しないことを確認済みのため今回は再調整しない。実際に**S件数はB変更前後で1,196/1,196（全敵15,750試合）・187/187（trialエンジン実測1,800試合）と完全一致**（S率7.6%不変）。**CEO初回実プレイ（human n=1・最重要実測）**：normal・raw51%・Battle Score 8,810で当初はC表示となり、「なぜこの評価なのかはわかりにくい」「遊び方は分かった」「普通にプレイしてCだった」のFBを得た。原因はC表示に基準値と次Gradeまでの距離が無かったこと（旧C文言は励ましのみ）。**UX-A採用（G1c）**：Cでも次Gradeまでの距離「あと○%でB（堂々）」を必ず表示する（「なぜC」は実測%表示が、「どうすれば」は距離行が分担。基準表示型UX-B・進捗型UX-Cとの比較で初心者理解度・情報量・再挑戦意欲・スマホ視認性から選定。浮動小数点誤差は−1e-9吸収で「あと0%」「+1%ズレ」を防止）。**CEO51%ケースの最終再現（G1d実機確認）**：「神技評価 B（堂々）／寿楽「無力化」— 敵の攻撃を平均51%削いだ！／Aまで平均あと29%」でBattle Score 8,810は不変。**他Grade表示（実機確認済み）**：C49%=「あと1%でB（堂々）」／A85%（ゲート達成）=「Sまで平均あと5%」（ゲート未達時は「Sには強打（100以上の攻撃）を半分以下に抑えよう」）／S=「強打を封じ、神業の無力化！」／easy92%＋ゲート=A＋「「ふつう」以上の難易度でSに挑戦しよう」。**品質**：375 tests全通過（G1b新規26件＋G1c/G1d境界・表示31件を含む。PC＋375px実機QA済み）／tsc・oxlintクリーン・consoleエラー0／**Battle Score完全不変**（同一seed回帰12,600/12,600行一致・10集計指標一致）／**大耀Mastery完全不変**／**B' combat balance（決定112）完全不変**。**残課題（解決していない）**：敵依存問題は完全解消ではない（B=0.50でも魔獣C率46.7%・機工師のS困難は構造的相性として残置、敵別補正はしない方針を維持）。**J-GおよびGrade閾値（B0.50/A0.80/S0.90＋ゲート）は「現時点のprototype採用値」であり永久不変の最終値とは断定しない**（残り5神展開・実プレイ蓄積後に再評価しうる）。**残り5神（恵比寿・蒼毘・才華・福永・笑蓮）のMasteryは未実装**。**変更ファイル**：本決定はdocsのみ。prototype実装（G1b〜G1d）は未commitの作業ツリー上にあり、本決定作業でコード・テストは一切変更していない。commit・push・deployも行っていない |
| 2026-08-28 | 決定115を追加。「STEP-SCORE2-G3：蒼毘Mastery「鉄壁」S4 prototype正式採用」（CEO＋相棒承認、commit `ee2ca92`）。**Mastery名**＝「鉄壁」（上手さの定義：来た攻撃をノーダメージで受け止め切る）。**raw定義**＝actual damage>0の敵attackを分母とし、blockで完全吸収してHP実害0になった攻撃（fullyBlocked）を分子とする割合＝無傷受け率。**1攻撃＝1票**でdamage量による重み付けなし。**余剰blockは加点しない**（blocked≤actualで頭打ち、積むだけでは無得点）。**charge除外**。**debuffによりactual=0となった攻撃は分母・分子とも除外**——G2でのstrict分母拡大案（全attack分母）比較の結果、識別比1.59→1.25悪化・S率0.7%で達成不能・蒼毘自身の妨害カード使用を罰する自己矛盾（蒼毘試合の67.9%でactual=0攻撃発生）のため棄却し、「**攻撃が届く前にdebuffで消す＝寿楽／届いた攻撃をblockで受け切る＝蒼毘**」の役割分離をコード・テスト・UXの3層で維持する。**敵ID・敵HP・敵別係数は不使用**。**Battle Scoreへ非加算**（テスト固定）。**Grade prototype値**：C<0.40／B≥0.40／A≥0.65／S≥0.85。**easy capは現時点では導入しない**（G2で難易度mean 0.481/0.496/0.474とフラットだったため不要な制約を足さない判断）。**実測分布**（同一seed 4,200試合）：全体S 8.5%／A 23.1%／B 33.8%／C 34.5%、勝利時（normal+hard）S率4.2%。ただし**勝利時S率はeasy 10.8%／normal 5.3%／hard 2.8%とeasy側へ傾斜**しており（meanフラットでもS帯の尾はeasyが厚い）、**easy S傾斜は残課題**としてCEO実プレイ後にeasy cap導入を再評価可能とする。**神識別力**：蒼毘0.496でrank1、最良他神=笑蓮0.312、**識別比1.59**（全G2候補中最強）。**寿楽は0.204で誤取得なし**（役割分離の実証）。**相関**：Battle Score −0.42（防御は速攻スコアと逆相関＝独立軸）／hpLoss −0.56（intentional damage不成立）／stall +0.45（長期戦＝受けの見せ場という様式由来、tempo犠牲のため純exploitにならず）。**敵依存**：gap 1.56（max怨霊0.613／min魔獣0.394——魔獣は寿楽と同じく構造的最難敵、敵別補正はしない）。**saveVersion 5→6**：migrateBattleSaveV5で既存Mastery（大耀・寿楽）とBattle Scoreを完全保持し蒼毘2フィールド（guardAttackCount/fullyBlockedCount）をdefault-fill（v3/v4チェーンも更新、旧save破棄なし）。**回帰**：G2同一seed **29,400行＋CONTROL 2,100行の行単位決定論的一致（mismatch 0）**、エンジン集計≡イベント再計算 不一致0/4,200、非蒼毘6神で集計0件。**品質**：tests 401件全通過／tsc・oxlintクリーン／実機QA（C/B/A/S表示・実戦集計・PC＋375px・console 0）。**限定**：閾値・分布は**現時点のprototype採用値であり永久不変値ではない**（CEO実プレイ後のUX/Grade調整は寿楽G1c/G1dと同じ手順で可能）。**恵比寿・才華・福永・笑蓮の4神Masteryは未実装**（G2設計のみ、着手はCEO指示待ち） |
| 2026-08-28 | 決定116を追加。「STEP-VISUAL-BATTLE-STAGE-LITE：C-LITE-C正式採用」（CEO実機確認で承認、commit `f14b7b1`。前段のSTEP-VISUAL-ENEMY-SCALE／BATTLE-AUDIT PLAN-Aを含むvisual一式）。**旧画面の問題**：①キャラが小さい（PC神150×180は敵の約半分で「アイコン」化、敵も173×207でモバイル既定より小さいPC逆転）②左右余白が大きい（神パネル左右198px）③avatar背後のCSS黒背景（敵`#120821`・神/OTOMO`#05060d`）で「額縁・カードの中」に見える④不透明panel（グラデ+blur）が**制作済みarena背景を約9割隠していた**。**黒背景はすべてCSS由来**で、敵・神の画像自体は透過済みであることを切り分け実証。**サイズ変更**：敵 PC 173×207→**280×300**／mobile 200×240→**240×260**（正方形artのレターボックス20px→10px）、神 PC 150×180→**220×240**（実描画1.47倍）、OTOMO PC 100→**128**／mobile 84→**96**。オーラは決定91の比率（×1.35〜1.37）で全追従。**視覚階層＝敵280≳神220＞OTOMO128**（敵をわずかに上に残すのは「脅威が正面に立つ」構図として意図的。敵の再縮小はしない）。**C-LITE-C（Battle Stage化）**：3段階比較（A=黒箱撤去のみ62点／B=+敵神パネル透明化78点／C=+OTOMOも同一Stage化88点）でCを採用——①enemy/god/OTOMO avatarの**CSS黒箱を撤去**し**soft drop-shadow＋陣営色aura**（敵=紫/神=緑/OTOMO=金。終盤surge・溜めglowも同表現へ変換）に置換②enemy/player/god-otomoの**3パネルを透明化**（背景・枠・影・blur除去）し文字は継承text-shadowで可読性担保、**HPバー・共鳴ゲージ・吹き出し・badgeは自前背景を持つ「可読性の島」として維持**③battle-mainの減光オーバーレイを弱め（上部22/44、下端は手札可読性のためcc維持）背景を開放。**arena背景可視率：約10%→約65%**。**「背景の世界の中で敵と神が対峙するBattle Stage」方向を正式採用**し、敵⇄神の中央空間は将来のattack VFX／burst／damage numberの通り道として確保。**QA**：PC（標準/横長/縦長敵×蒼毘・大耀×百勝・小槌の各形態）・375pxとも合格、横はみ出し0・scrollH増加なし・console 0・tests 401件・tsc/oxlintクリーン。**変更はbattle.cssのみ**（+116/−24、後置オーバーライド方式でロールバック容易）でBattle Score/Mastery/combat/enemy stats/saveVersion等への影響なし。**既知課題**：**OTOMO画像（spirit/incarnate/doji）のみ星雲背景が画像自体へ焼き込まれており**CSSでは矩形感が消せない（画像加工は本STEPで禁止のため角丸の「共鳴の窓」として残置）。**次のVisual優先課題＝OTOMO透過アセット化**、その後の候補＝Battle VFX強化（中央空間の活用・C-LITE-Cで確保済み）。本決定は**全面PLAN-C改修ではなくC-LITE-C（CSS-only Lite版）としての採用**であり、visual値も永久不変とはせず**VFX導入時に再評価可能**とする |
| 2026-08-28 | 決定117を追加。「OTOMO透過アセット21/21正式採用 — Battle Stage統合」（CEO＋相棒承認、commit `5891393` feat: adopt transparent OTOMO art）。**背景**：C-LITE-C Battle Stage化（決定116）で敵・神はarena背景世界へ直接配置される構成になったが、旧OTOMO画像21枚は1600×1600 WebP（VP8・**alphaなし**）で紫系星雲背景が完全に焼き込まれており、OTOMOだけが「四角いカード画像」に見える問題が残っていた。**対象**：OTOMO 7体（taimaru/kozuchi/momokatsu/kotone/juka/haku/shofuku）×3形態（spirit/incarnate/doji）＝**21画像**。**方式**：単純flood-fill方式は星・星雲残渣が大量に残るため棄却し、境界＋発光保持型を経て**PLAN-Cハイブリッド**を採用。基本思想＝「**背景だけを除去**」——キャラクター再生成なし・顔/体型/衣装/色/ポーズ/シルエット変更なし・発光/半透明表現を可能な限り保持。実装＝保守的flood-fill＋中央保護楕円＋輝度ガード付き星雲判定を土台に、**per-imageパラメータ**で素材ごとに背景判定を調整し、必要箇所のみ星雲色成分フィルタ＋既知位置限定のzap処理（紫系限定）を使用。境界は**輝度比例soft alpha**で白縁/黒縁を防止。**難物対応**：①kotone incarnate——ピンク蛇身と背景マゼンタが色空間で競合し汎用方式では胴体・腹部・尾に欠損が発生。キャラクターを削る方式は棄却し、専用救済（蛇身色域保護／輝度guard／保護領域拡張／foreground connectivity・主成分方式）の組み合わせで、顔・髪・上半身・腹部・蛇身・尾・装飾・元色・輪郭・半透明/発光の10点保持を確認してPASS。②shofuku spirit——金色半透明ボディ・内部光・袋口・紐を保持してPASS。③momokatsu spiritのシアン発光、kotone spiritの紫発光、juka incarnateの鹿角等もすべて保持。**最終品質**：FINAL＝**PASS 21／MINOR FIX 0／FAIL 0**。キャラ欠損0・意味のある星雲残渣0・発光破損0・白縁/黒縁0。一部素材に原寸1600pxでは半透明フリンジが残るが**ゲーム表示96〜128pxでは視認不能**であり、現時点では追加処理しない（将来高解像度表示を採用した場合のみ再評価可能）。**容量**：旧21枚約20.3MB→正式透過21枚**約6.2MB（約69%削減）**。正式形式＝**alpha付きWebP**。旧画像は削除せず温存。**正式配置**：`public/assets/otomo/<id>/<form>_transparent.webp`——OTOMO単位ディレクトリへの新ファイル併置方式（enemies刷新・決定99/100と同型）。旧`<form>.webp`は上書きしない。`background.webp`×7は神選択画面用で今回の対象外。コードは`src/core/data/otomo.ts`の`art()`参照のみtransparent版へ切替し、**OTOMO ID／form／効果／resonance／Battle Score／Mastery／combat／saveVersionは変更しない**。**QA**：HTTP 200 21/21／browser decode 21/21／alpha 21/21／Battle・Result・OTOMO絆画面すべて合格／PC・375px合格／overflow 0／console error 0／tests **401/401**／tsc・oxlintクリーン。**採用結果**：「**敵・神・OTOMOすべてがarena背景世界へ直接存在する**」Battle Stage構成を正式成立とする。prototype・INSPECTION.html・検収PNG・scratchpad生成物は正式assetへ含めない（再生成パイプラインはscratchpadに保存）。透過方式・サイズ・Visual値は永久不変とせず、将来の高解像度化やBattle VFX導入時に必要なら再評価可能 |
| 2026-08-28 | 決定118を追加。「AI-STUDIO WAVE-01/02：敵個性化・敵VFX・福永Mastery・Enemy Select/Stage採用」（CEO＋相棒承認。commit `820d285`/`4a0c66c`/`fe7d46e`/`79eec16`）。**①敵個性化（K-C2/M-G、commit `820d285`）**：機工師=R4「⚠主砲充填開始」charge→R5必殺技special「主砲・神滅甲」内部24（K-C2。R7配置は平均撃破R5.7で発動率0%＝不可視と実証しNO-GO、内部26は hard勝率75.1%まで低下のため24へ調整→hard83.4%・発動率76.1%）。魔獣=全ラウンド連撃multiAttack＋R3必殺技「双牙乱撃」4×3（M-G。各R合計はbaseline完全同値）。**engine恒久ルール3件を確立**：(a)敵atk debuffはaction合計へ1回適用（per-hit適用は寿楽Mastery S率51%のfarming破綻をsimで実証し禁止）(b)multiの難易度倍率は合計保存丸め（per-hit丸めはhardで内部+1〜2となり才華/大耀hard勝率が10pt級崩壊）(c)Mastery集計はMODEL-B=1 enemy action=1 sample（multiでもhit票にしない。specialも含める）。検証は累計約96万試合＋正式engine回帰（PROTOTYPE-01 spec vs 実装 mismatch 0/840・Mastery 0/200）。saveVersion 6→7（EnemyActionDef新kind。migrateBattleSaveV6=intent正規化のみ、v3→v7連鎖維持）。ENEMY_INTENT_SET/ACTEDへlabel（技名）追加＝VFX契約。**②ENEMY-VFX-02正式採用（commit `4a0c66c`）**：CEO Human Playtest A3「地味」/B3「3発を感じない」を受け再設計し、A4/B4で両方PASS（「よくなった」）。敵必殺カットイン=cinematic strip（幅min(88vw,980px)×30vh・巨大技名・Ken Burns・T+870 flash・総1050ms・神BURSTの骨格を紅蓮/左スライドへ反転）、連撃はTEMPO-B「1・2・ドン！」（hit offset 0/180/500ms、enemyVfxTiming.tsへ一元化しCSS/数字/SEを同期）、hitごとにlunge+slash trail（HIT3=双牙クロス）+hold付きクレッシェンドシェイク、神滅甲はカットイン→BEAM→1260ms着弾（impact ring+emphasis数字）、SEはtone合成のpitch/volume/delayで新規音源ゼロ、prefers-reduced-motionでも数字順次表示で3HIT情報は保持。全て表示層のみ（core接触0）。**③残り5敵の設計承認（実装は次Wave）**：影=T-A「影分かち」（R5をmulti[5,6]化・数値完全同一の教育目的＝決定21/26の検証を不毀損）／鬼将=O-B「業斧・兜割り」（R2溜め→R3 special16・発動率100%・防御優位+18ptで重撃の性格強化）／怨霊=N-A2「怨嗟の慟哭」（R4溜め→R5 special20・受け準備の行動変化が最大・stall完走-10pt）／龍神=R-A「大海嘯」（R4「潮が引いていく…」→R5 special21・才華worst-cell+33pt救済）／道化=D-B「種明かし」（R2溜め→R3ブラフ6→R4 special19。**CONDITIONAL GO**=正式採用前にHuman Playtestで「騙されたが理不尽ではない」ことを確認）。検証=22 variants約55万試合＋finalist 1,000seed確認run（ノイズ較正付き）。全案enemies.tsデータ変更のみでengine拡張不要・Battle Score歪みなし・farming/exploitなし。watch=charge→special型で寿楽Masteryの分母が減りS率低下（怨霊hard 21.7%→3.1%等）→5敵採用時にJ-G閾値の再校正を検討。**④福永Mastery「大勝負」採用（G4、commit `fe7d46e`）**：raw=勝利時(最終HP−試合中最低HP)÷maxHp、**risk gate**=自傷カード由来の敵への実効与ダメ≥敵maxHp×10%（未達はraw=0。敵に殴られること自体は評価しない）。カード名ハードコードなし（damage target:'self'を含むカードのdamage target:'enemy'実効分をplayCard内で敵HPデルタ計測）。minHpはapplyDamage self分岐で追跡（敵攻撃・自傷の唯一の合流点）。閾値prototype=C<0.10/B≥0.10/A≥0.40/S≥0.60（56,700試合で勝利時S4.4/A23.3/B45.5/C26.7・識別比2.61・Battle Score相関−0.014・easy capなし=easyが最も渋いため）。**意図的被弾exploit（baitTank）はgateが完全遮断**（gate無し反実仮想では勝利の95.4%がB以上→実装は100%C送り）を実証。**riskThenBait（gate成立後の意図的被弾→立て直し）はCEO判断で容認**：勝率が大幅低下する敗北リスクを実際に背負う高リスク高リターン行動であり福永Identity「リスク＆リターン」と整合、free farmingではない（S率は上がるが normal win66%/hard win28%）。将来production dataで極端なfarmingが確認された場合は再調整可能とする。**saveVersion判断=7据え置き＋fill方式**：v7はproduction未出荷（本番=`75ebb14`=v6）のため未出荷版の再定義が最小で正しい。fillFukueiMasteryFieldsが同一v7の旧セーブもフィールド有無で補完（minHpは保存時点HPで初期化＝1戦限りの過小方向の既知制約、破壊なし）。**⑤Enemy Select＋Stage architecture採用（commit `79eec16`）**：フロー=HOME→神→難易度→**敵選択**→デッキ→バトル。8枚グリッド（7敵＋「神に委ねる」=従来のseed選出を仕様として温存）。表示=立ち絵/名前/**脅威度★**（紫紅#d05ce3＋ラベルで難易度★=金と二重区別。影1/鬼将2/魔獣2/怨霊3/龍神3/機工師4/道化4、**★5はBoss予約**、Score/reward倍率に不使用）/【型】/1行ヒント。**HP・damage値・行動表は非表示**（スポイラーポリシー：予告を読む体験の保護）。優先順位=URL `?enemy=`（決定40）＞プレイヤー選択＞seed。DeckBuilderに対戦相手チップ・「もう一度」は同一敵。PC=4×2grid/375px=2列grid。Stage=EnemyDefへ表示専用`rank`/`stage{nameJa,bg?,accent}`、BattleScreenが`--stage-bg`/`--stage-accent`を注入し battle.cssが`var(--stage-bg, url(arena.jpg))`でフォールバック（**全敵bg未指定=現行と完全同一表示**。既存OTOMO背景は正式採用しない。7背景の生成prompt仕様は納品済みでCEO＋相棒が別途生成）。戦闘中の舞台名表示は今回追加しない（背景導入後にHuman Playtestで判断）。Lane D統合はsquash mergeでcommit A/Bと自動整合し、K-C2/M-G/multiAttackモデル/カットイン/VFX-02 CSSの保持をdiff・テスト・実機で確認。**統合品質**：tests 489件全通過・tsc/oxlintクリーン・「選んだ敵がそのまま出る」7敵テスト・v3〜v7 save migrationテスト維持。push/deployは未実施（Human Playtest後に判断） |
| 2026-08-29 | 決定119を追加。「VFX-04：星空背景ドリフトの transform 化（PV撮影で判明した描画性能問題の恒久対策）」（CEO承認、commit `perf: move starfield drift to transform`）。**発端**：PV自動撮影（Chrome tab capture）でバトル画面の描画が1080pで約10fpsまで落ち、FPS gate が全素材 RED。**原因分離（正常ゲート下の rafprobe2 6秒窓計測）**：`body::before`（星20枚の radial-gradient）を `background-position` でドリフトさせる `particle-drift` が毎フレーム全面再ラスタライズを起こしており、これ単独で avg 60→10fps（before_drift_only 10.0fps）。twinkle（opacity）は無害（55fps）、`body::after`（conic-gradient+blur(50px)回転）はクリーン環境では単体60fps、cut-in 系CSS（backdrop-filter／斜線／背景色アニメ／画像 predecode）は差が誤差範囲で主要因ではない。**対策**：`src/index.css` の `body::before` のみ変更——`@keyframes particle-drift` を `transform: translate(0,0)→translate(-60px,40px)`（140s linear、移動量・速度は旧版と同一）に置換し、`will-change: transform, opacity` を付与、移動量ぶん `inset: -40px -60px 0 0` で上・右へ拡張して画面端の露出を防ぎ、`background-size: calc(100% - 60px) calc(100% - 40px)` と `background-position: 0 40px` でタイル寸法・初期位置を旧版（ビューポート基準）とピクセル一致させた。`body::after`・cut-in CSS・`prefers-reduced-motion` は据え置き（非要因のため変更しない）。**結果**：通常背景 avg 9.8→**60.2fps**、P10 8.5→59.9、maxGap 165→21ms（D_off と同値＝背景コスト実質ゼロ）。旧 background-position keyframes を注入した再現テストは 3.5fps（因果確認）。**Visual parity PASS**：1920×1080／1366×768／375×812 の各9画面（HOME・神選択・敵選択・Battle・敵cut-in・連撃・共鳴cut-in・BURST・進化）before/after 27組を sharp でピクセル比較、静止画面は差分0〜1.4%・外周40px帯差分0%（背景位置・密度・画面端露出なし）。差分の大きい組はすべて演出フェーズのタイミング差（before側が10fpsで演出が遅延していたもの）で、追加のCSS調整は行っていない。tsc／oxlint／vitest 2602件 PASS、console error 0。**副産物**：計測ゲート `gated.sh` の関数名 `cut` が coreutils `cut` を隠蔽し rAF ゲートが文字列比較で常時PASSしていた不具合を発見・修正（`cutrun` へ改名）。計測資材＝scratchpad `vfx04/`（PHASE0 baseline patch・PHASE1/3 分離計測・gated before）、`vfx04-step1/`（BEFORE 27枚・bg-before.jsonl）、`vfx04-step2/`（AFTER 27枚・bg-after.jsonl・parity.jsonl）。**PVへの示唆**：既存Hero素材（A/B/C）は全て10fps前後の劣化環境で撮影されたため、60fps環境での再撮影を推奨 |
| 2026-08-29 | 決定120を追加。「BGM 4曲の出所とライセンスの確定（PV 公開ブロッカー解除）」（CEO 確認、コード変更なし）。**出所監査**：`public/assets/bgm/battle.mp3`（「七神の戦場」）ほか home/victory/defeat の 4 曲はいずれも ID3v2 に `made with suno`・`suno.com/song/<id>` URL・**Suno, Inc. 署名の C2PA Content Credentials マニフェスト**（providerName Suno / systemVersion chirp-auk-turbo-t2 / digitalSourceType trainedAlgorithmicMedia）を保持し、アーティスト名は CEO のアカウント名。battle.mp3 = song id `c057529a-440b-4756-af43-6895fe01e6d9`、生成 2026-08-13 12:48:51Z（21:48 JST）、決定53 の当日記録・セッション記録（21:52 に CEO がパスを送付）・NTFS 作成日時 21:53 と時系列が一致。Git 初出は初回チェックポイント `7ddbd5c`（2026-08-14）。**ライセンス**：Suno 公式ヘルプの規定（無料プラン＝個人・非商用のみ／Pro・Premier 加入中に生成した曲＝commercial use rights、後からの加入は遡及しない）に対し、**CEO が「2026-08-13 の生成時は Suno 有料プランだった」ことをアカウント側で確認し証跡（スクリーンショット）を保存した**。よって 4 曲とも商用利用可として扱う（著作権の帰属保証は Suno 規約上なし。ゲーム内 BGM 配信および PV/SNS 掲載での使用を対象）。**PV への適用**：VISUAL LOCK 済みの 45s PV v3（`edl-v3-45s.json`）から **ウォーターマーク指定のみ除去**した `edl-v3-FINAL.json` で `PV-45s-v3-FINAL.mp4` をレンダ（映像・尺・BGM・コピー無変更、台帳 `Videos/SEVENGODS-PV-REVIEW/PV-v3-VISUAL-LOCK.md`）。**残る未確定**：ロゴ画像・SE 実音源・ステージ背景 7 枚・9:16 版は v2 以降の課題 |
| 2026-08-30 | 決定121を追加。「DAILY CHALLENGE Phase 1：神域挑戦」（CEO承認、commit `feat: add daily challenge and release polish`）。**コンセプト**：毎日、全員が同じボス・同じSeedに3回だけ挑戦し、神・OTOMO・デッキ・プレイングを工夫してその日の最高スコアを狙うモード。通常モードの敵選択・難易度選択は維持し、Dailyでは行わない。**日付**：JST 00:00固定（`RULES.daily.timezoneOffsetMinutes=540`、`dailyKeyOf(now)`は端末タイムゾーンに依存しない。QuotaProvider（決定72）と同じ「時刻の注入」方針）。**ボス決定**：週次シャッフル巡回——JSTの週キー（月曜）から`createRng`で7体をshuffleし曜日番号で確定（`core/data/dailyBoss.ts`）。1週間で7体が必ず1回ずつ登場し、順番だけが週ごとに変わる（前回Daily監査の推奨と一致。7日固定ローテは予測可能で飛ばされ、日付ハッシュは連日重複するため不採用）。**Shared Seed**：`daily-<YYYY-MM-DD>-<enemyId>`で全員同一。決定論エンジン（不変ルール2・`rngCursor`）により初期手札・託宣結果まで同一（2回目の初期手札一致を実機で実証）。画面には短いSeed ID（6桁）を表示して共通条件を可視化する。**挑戦回数**：1日3回、開始時に消費（途中放棄で取り戻せない。seed共有のため引き直しの利益は無い）、3回の最高スコアが「今日のベスト」。**★★★★★神域強化**：`EnemyDef.rank`は変更せず（★5予約テスト維持）、Dailyモードの表示専用状態として★5＋「神域強化」を出す。**補正**：`GameState.modifier`＝敵HP×1.25／敵攻撃×1.15を難易度倍率に乗算（`createInitialState`／`round.ts nextEnemyAction`、未指定＝恒等で通常モードは従来値と完全一致）。難易度は'normal'基準（プレイヤーHP補正0・difficultyBonus 0）。**スコア倍率は付けない**（前回監査：★倍率は自然差2.6〜3.3%に対し過剰でfarm化）。balanceSim DAILY-01（7敵×7神×3戦略×20seeds）で全49組合せが最良戦略勝率≥50%を満たすことを確認して仮値をそのまま採用（神別min：恵比寿95／大耀90／蒼毘75／才華95／寿楽90／福永90／笑蓮60、勝利時score加重avg 753〜836。最難ボスは5/7神で機工師）。**記録**：`sevengods.daily`（v1、30日剪定）に日別の挑戦回数・結果（敗北・未撃破も記録）・今日のベスト・**神別ベスト**を保存し、通常モードの神別自己ベスト（`recordStorage`）には**混ぜない**。**save**：saveVersion 7→8（`mode`/`dailyKey`/`modifier`を追加、v7以前は`migrateBattleSaveV7`が`mode:'normal'`を補完。v3チェーンも連鎖するよう修正）。別の日のDailyセーブは再開せず破棄。**共通条件の保護**：`resolveDailyStart(dailyKey)`はURLバックドア`?enemy=`・敵選択・難易度を参照しない設計（通常モードでは`?enemy=`従来どおり有効、実機で両方確認）。**既知制約**：日付と挑戦回数は端末時計・localStorage依存（時刻改ざん・ストレージ消去に耐性なし）。自己ベスト用途のPhase 1として許容し、公開ランキング時はサーバー権威（日付・attempt token・行動ログのreplay検証）へ移行する。「ランキング」の語はオンライン実装まで使わない。**神格差**（蒼毘・笑蓮が低い／才華が高い）は本決定に混ぜず別トラック。Phase 2候補＝行動ログ同梱・神域行動（ボス相性）・共有テキスト・週間合計 |
| 2026-08-30 | 決定122を追加。「8/31 P0 Polish：進行中セーブ破棄確認・BURST内容常時表示・Daily勝敗表示」（CEO承認、FULL GAME COMPETITIVE PRODUCT AUDIT 2026の結論58/100・CONDITIONAL GOを受け、8/31前は既存機能の磨き込み3件に限定）。**P0-1**：進行中セーブがある状態で「神を選ぶ」または神域挑戦「挑戦開始」を押した時だけ確認ダイアログ（「現在の挑戦データは失われます。新しい挑戦を始めますか？」キャンセル／新しく始める、Esc・背景クリック＝キャンセル、初期フォーカスはキャンセル）。セーブが無ければ従来どおり即遷移（RELEASE_STATUSの既知運用リスク「神を選ぶが確認なしでclearBattleSave」の解消）。**P0-2**：共鳴パネル（`GodOtomoPanel`）に「あとNで神技発動」「神の一撃：<describeEffectList(resonanceEffects)>」「<OTOMO名>（<発動後の形態>に成長）：<その形態の効果>」を常時表示。OTOMO効果は`effects.ts applyResonance`の順序（進化→神の一撃→OTOMO効果）に合わせ発動後の形態のテーブルを守り/力パスで引く。BURSTは自動発動のまま、engine非接触（監査の「初回BURSTまで何が起きるか分からない」への対応）。**P0-3**：Daily画面・決着オーバーレイ・戦績表に勝利／敗北／未撃破のラベルを付け、敗北スコアを勝利と誤認しないようにする。神別の今日のベスト（神／スコア／勝敗／到達R）を表として前面表示。保存済み`results[]`から引き直す表示専用純関数（`bestResultOf`／`bestResultsByGod`）のみで、新しい保存フィールド・計算ロジックは追加しない。**変更しないもの**：engine・バランス・BURST仕様（任意発動化はP1）・カード効果・OTOMO効果・SE・Daily倍率・save仕様。**QA**：tsc/oxlint、vitest 2636件、production build、通常/Daily/P0 smoke（計47項目）、375px、console error 0。**以後8/31前の機能追加は行わない** |
| 2026-08-30 | 決定123を追加。「ENEMY SELECT VISUAL POLISH を8/31公開版へ採用」（CEO承認、commit `feat: polish enemy select cards`。**これを8/31版の最後のコード変更とし、以後は新機能・Visual変更・balance変更を追加しない**）。**内容**：敵選択カードを「上部visual area（Stage背景fallback＝accent色の暗グラデの上に敵art）＋下部info（脅威度★・名前・型・ヒント・舞台名）」の2段構造へ再編し、敵artを大型化（実測108px→161px、約1.5倍。`--art-scale`で敵ごとの透過余白差を補正できる表示専用hook、既定1）。脅威度★の表示・「難易度の★とは別物」の副題は維持。選択状態は塗りつぶしをせず border＋glow＋右上accent色マーカー（`::after`）で明確化し、`:focus-visible`もhoverと同扱い、`:active`で押し込み。「神に委ねる」は金の光背＋大鳥居で敵カードと視覚的に差別化。正式Stage背景用に`--enemy-stage-bg`（`stage.bg`がある敵のみ注入）のhookを準備するが、**ボス専用背景画像そのものは今回導入しない**（適当な代替画像で完成扱いにしない方針）。375pxは2列グリッドのまま、文字サイズだけ縮小（名前13px／ヒント10px／★12px）。**変更しないもの**：選択ロジック・`onSelect(enemyId|null)`契約・`ThreatStars`のexport・`EnemyDef.rank`・文言。**根拠**：PRE-PUSH監査（HEAD `d8af12d` と作業ツリーのclean buildを desktop 1280／375px で比較）——8枚表示・★=rank・横はみ出しなし・テキスト切れ0・最小タップ163px（375px）・カード高は不変（261 vs 262px）・遷移後の対戦相手チップ一致・console error 0。Daily導線はEnemy Selectを経由せず影響なし。FULL GAME AUDIT 2026 の「Enemy Select は POLISH 後 S」評価を実測で裏付けた |
| 2026-08-30 | 決定124を追加。「7ボス専用Stage Background正式採用」（CEO承認、commit `feat: add boss stage backgrounds`。Code Freeze（決定123）の**唯一の例外**として、Visual asset＋`stage.bg`データ行のみを8/31公開版へ追加）。**内容**：7敵それぞれに専用のBattle背景を用意し、決定118（LANE-D）で用意済みの既存hook `EnemyStageDef.bg` → `BattleScreen`の`--stage-bg`注入 → `battle.css`の`var(--stage-bg, url(arena.jpg))` をそのまま使う。同じ`stage.bg`を`EnemySelectScreen`が`--enemy-stage-bg`として注入するため、**Battle背景とEnemy Select（5:4 visual area）に共通利用**。Normal／Daily共通（Dailyも同じ`BattleScreen`・`state.enemy.defId`経路のため追加実装なし）。**対応**：試練の影=月夜の廃神殿 `01-trial-shadow`／業斧の鬼将=炎上する鬼ヶ城 `02-oni-castle`／藍花の怨霊=紫陽花と彼岸花の幽世 `03-ghost-hydrangea`／銀甲の機工師=和風機巧工房 `04-mecha-workshop`／双牙の魔獣=月光の霊峰・獣道 `05-beast-moonpeak`（`stage.nameJa`「月牙の霊峰」は据え置き）／蒼海の龍神=荒波の海上神殿 `06-dragon-ocean`／乱舞の道化=妖しい紅色の祭舞台 `07-jester-festival`。**アート方向**：SEVENGODSの世界観として「明るく華やかな和風神域ファンタジー」（光・自然・鳥居・神殿・和建築・霊気の融合、中央に円形の戦闘エリア、上下にHUD/手札帯の余白）を採用。過度なホラー表現は避け、既存のBattle減光オーバーレイは維持し背景側を暗く加工しない。**素材**：CEOがChatGPTで1ステージ=1生成で7枚を個別生成（各1672×941）。当初の「7ステージ集約ボード」からの切り出しは、パネル実寸が約357×328／552×288で1600×900に約3〜4.5倍拡大が必要なため2度NO-GO（STOP CONDITION「解像度不足」）とし、個別生成に切り替えた。加工＝右下のAI署名風スクリブルを避ける`1529×860`クロップ（16:9維持・stretchなし）→1600×900 Lanczos→WebP q70。**容量**：各209〜314KB・合計1.84MB。「各180KB/合計1.2MB」は努力目標とし、1280×720 q78でも1.61MBと到達不能なため**画質優先**でq70を採用（CEO承認。q78比SSIM 0.966）。**許容した表現**：#7鳥居の額の擬似漢字風文様（UI文字・ロゴではなく背景装飾）、#6空の霊体の龍（敵本体ではなく神域の環境演出）。**変更しないもの**：`enemyId`・`rank`・`stage.nameJa`・`stage.accent`・HP/行動表/AI/balance・saveVersion（背景はGameStateに持たない＝migration不要）・Battle component/CSS・PV v3 FINAL・BGM。**テスト**：「全敵bg未指定」の回帰テストを「7敵すべてbgあり／`/assets/backgrounds/stages/`配下／7枚unique／参照assetが`public/`に実在（`import.meta.glob`で列挙）」へ更新。**Post-release**：cold loadでEnemy Selectが7枚（1.84MB）を同時読込する点は既知MINOR、preload/CSS変更は今回追加しない。9月にBattle中の舞台名表示・PV v2での新背景収録を検討 |
| 2026-08-30 | 決定125を追加。「Mobile Battle Auto Focus採用」（CEO承認、commit `feat: improve mobile battle action visibility`。Code Freeze（決定123）の例外2件目、8/31公開版へINCLUDE）。**背景**：CEOのスマホ実機プレイで「カードを使っても敵への攻撃が見えない」問題を確認。監査（read-only）の結果、375pxのBattleは縦一列（文書高約2,471px）で、手札（y≈1,435〜2,025）と敵パネル（立ち絵y≈295〜563・HP 571・予兆 597）が約1,000px離れており、攻撃VFX・被弾シェイク・浮遊ダメージ数字は全て敵パネル内（`EnemyPanel`の`.enemy-avatar-wrap`）で再生されるため画面外。従来はfixedの`result-toast`とsticky HUD/`BattleMiniResult`の文字情報だけで結果を伝えていた。**方針転換**：過去の「強制スクロールはしない（CEO明示指示。`BattleScreen.tsx`のresult-toastコメント）」をCEOが**明示的に撤回**。スマホで「カードを使った結果を視覚的に確認できる」ことを優先する。**内容**（表示専用、新規`src/components/battle/useMobileAutoFocus.ts`）：`(max-width:700px)`のときだけ、①敵ダメージ効果（`damage target:'enemy'`をEffectデータで判定、カード名ハードコードなし）を持つカードのタップ直後（既存`pendingCardUid`＝280msのcast-flashをスクロール猶予に使う）②BURST（`fx.burstKey`増分＝共鳴カットインの暗転中）③敵ターン（`isEnemyTurn`＝「敵のターン」バナー700ms中）④敵ダメージを伴う託宣（天啓）に、`.enemy-panel`へ`scrollIntoView(block:'start')`。回復・防御・敵ダメージの無い託宣では動かない（画面が上下に暴れないため）。保持時間は`enemyVfxTiming.ts`の定数から算出（カード1,600ms／BURST=進化バナー終了まで／敵通常500+900／連撃=カットイン先行+最終HIT+900／必殺=カットイン+着弾+900）し、演出終了後に**使用直前のscrollY**へ戻す（手札固定位置ではなく、プレイヤーが見ていた場所を保持）。復帰待ち中に`pointerdown`/`touchstart`/`wheel`/`keydown`を検知したら自動復帰をキャンセル（綱引き防止）。`prefers-reduced-motion`ではフォーカス自体は維持しsmoothではなくinstant移動（「動きを減らす設定だから攻撃結果が見えない」状態にしない）。決着後（won/lost/finished）は復帰スクロールしない。**engine不接触**：追加のゲームロジックdelayなし、`useGameEngine`・core・rules・save・balance・Dailyロジック無変更。Desktop（>700px）は完全不変（1280pxでscrollYが一切変わらないことをQAで実測）。CSSは`@media (max-width:700px) .enemy-panel{scroll-margin-top:8px}`の1ルールのみ。**QA**：375px focused QA（通常攻撃／連撃／回復／防御／BURST／敵通常攻撃／敵必殺／手動キャンセル／reduced-motion／Daily／Desktop 1280不変／console 0／HTTP 0）＋既存gate。**位置づけ**：将来のMobile 1画面Battle redesign（P2）までのP0/P1対策 |
| 2026-08-30 | 決定125フォローアップ。「勝敗確定後はMobile Auto Focusの新規セッションを開始しない」（CEO判断「②2行修正→再ゲート→push」、commit `fix: prevent mobile auto focus after battle end`）。**発見**：FINAL PRE-PUSH GATEの375px検証で、**BURSTの一撃が同時に敵を倒した場合のみ**、commitで`status='won'`になり最初のセッションは終了するものの、その1レンダー後に`burstKey`増分のeffectがstatusを見ずに新セッションを開始（保存位置＝移動途中のscrollY）し、約3.4秒後にRewardOverlay（fixed全画面）の背後で不要な復帰`scrollTo`が発火していた（MINOR。操作・結果表示への影響なし、半透明背景の奥がわずかに動くのみ）。通常攻撃での撃破・敗北・敵ターンでは正常に抑止されていた。**修正**：セッション制御をReact/DOM非依存の`autoFocusSession.ts`（`createAutoFocusController`）へ切り出し、`focus()`冒頭で`status !== 'playing'`なら即returnする（`setStatus`は'playing'以外で進行中セッションも終了）。hook側はwindow/documentを環境として配線するだけの薄い層にし、statusを反映するeffectを最初に宣言して同一commit内の順序も保証。**テスト**：node環境で直接検証する回帰テスト10件を追加（won+burstKey→開始しない／lost／finished／進行中にwon→復帰キャンセル／同一セッション内は延長のみ／ユーザー操作でキャンセル／reduced-motionはinstant／Desktop no-op／reassert閾値）。既存の対象判定・保持時間テストは維持。**不変**：core／save／balance／Daily／Desktop挙動、通常攻撃・連撃・BURST・敵攻撃・敵必殺・回復/防御非対象・手動キャンセル・reduced-motionの各挙動（375px focused QAで再実測） |
| 2026-08-30 | 決定126を追加。「神階（しんかい）Ⅰ〜Ⅶ — Stakes型チャレンジシステムの採用と実装（80点化計画 Phase 1）」（**AI判断**：CLAUDE.md §6 AI自律判断ポリシーに基づき、設計監査・シミュレーション・実装方式をAIチームが決定。CEOが決めたのは名称「神階Ⅰ〜Ⅶ」と段名（参道／鳥居／拝殿／本殿／奥宮／禁足地／高天原）および「Ⅰ〜Ⅵで現世の神域を奥へ進み、最終試練を越えた者だけがⅦ高天原へ至る」という世界観定義のみ）。**背景**：8/31 Final Audit（52/100、CEO再評価54）で「normalでbalanced単純戦略が94%勝利」「30run遊ぶ理由が弱い」「Daily以外の長期目標が無い」を確認。既存資産（7神・7敵・7R・予告・決定論Seed）を変えずに長期目標を作る手段としてStakes型を監査した。**設計監査（AI判断・根拠）**：scratchpad上で既存balanceSimの戦略AIと試合ループを複製し、GameStateを試合中に書き換えて候補ルールを再現、5戦略（aggressive/balanced/defensive＋新設burst/score）×7神×7敵で約33,000試合。結論：(a)**累積方式**を採用（置換方式は各段81〜93%で単調にならずⅦを15〜30%にできない）(b)**難易度は「ふつう」基準に固定**（hard基底ではⅠ57%→Ⅱ37%と崩壊。UIは かんたん／ふつう／むずかしい／神階Ⅰ〜Ⅶ の単一セレクタに統合し、既存3難易度と記録互換は維持）(c)**神ごと解放**（その神で「むずかしい」1回撃破→Ⅰ、段クリアで次段。到達は永続。旧記録にhardベストがあれば解放済み扱い）(d)修正子は「情報は見えているが対応が難しい」型のみ採用し、**敵予告を隠す・変える・裏切る修正子、毎Rドロー−1（勝率40%・神spread80pt）、共鳴上限9/10（蒼毘・笑蓮を60%台へ）、ラウンド7→6（蒼毘42%）は不採用**（CEO案A「HP+15→神託3→初手−1→共鳴9→ブロック75→必殺+30→R6」はⅣで蒼毘・笑蓮が0%となり失格）(e)HP+（防御神に効く）とATK+（攻撃神に効く）を交互に積んで神の公平性を保ち、**Ⅶは最終試練の選択式**（自分の神の弱点を避ける）。**確定ルール（累積、`RULES.stakes`）**：Ⅰ参道＝託宣7→4回＋R5以降 敵ATK+20%＋敵ATK+10%／Ⅱ鳥居＝＋初期手札−1／Ⅲ拝殿＝＋敵HP+10%／Ⅳ本殿＝＋ブロック効率75%／Ⅴ奥宮＝＋回復効率60%（監査時の50%から福永＝自傷＋回復の神への配慮で緩和）／Ⅵ禁足地＝＋敵必殺・連撃+20%（機工師の主砲は+10%上限＝敵バランス監査で「ATK+×必殺+×主砲24」が受け切れない組合せと判明）／Ⅶ高天原＝＋最終試練を1つ選択（巨躯＝敵HP+10%／猛威＝敵ATK+10%／静寂＝R1神力−1）。**スコア**：最終スコア×(1+0.08×段)（Ⅶ×1.56）。敗北スコアは撃破・早期撃破ボーナスを含まないため「高い段を選ぶだけで高得点」にならない（Ⅶ敗北＜Ⅰ勝利をテストで保証）。神別に「段別ベスト」を別キーで記録。**Daily**：完全分離（Dailyは神階を渡さない・神階セレクタも出ない・DAILYタグのみ）。名称の「神域階位」は Daily「神域挑戦／神域強化」と衝突するため「神階」を推奨しCEOが承認。**Seed共有**：結果画面に「挑戦状をコピー」（段・神・敵・Seed・スコア・`?seed=&stake=`URL）。`?seed=`（英数字・64字）と`?stake=1..7`のURL受け口を通常モードに追加（Dailyは無視）。真正性は自己申告。**実装**（feature branch `feat/shinkai-phase1`）：`core/data/stakes.ts`（段→累積ルールの純関数、表示データ）、`rules.ts`に`stakes`集約、`GameState.stake?/stakeChoice?`（optional）、`createInitialState`（HP・託宣回数・初手）、`round.ts`（ATK倍率＝難易度×Daily×神階×後半×必殺、合計保存丸め）、`effects.ts`（ブロック/回復効率、イベントは実効値）、`score.ts`（段係数）、**saveVersion 8→9**（`migrateBattleSaveV8`＝version更新のみ、v3〜v8連鎖維持。stake未定義＝通常として解釈するためデータ補完不要）、新規`hooks/stakeStorage.ts`（`sevengods.stakes` v1：hardCleared/maxCleared/bestByStake。既存`GodRecord`は不変）、UI＝`StakeSelector`（難易度画面の下、375pxで4列×2段）、HUDタグ、結果画面の倍率行・突破/解放/段ベスト表示・共有ボタン、戦績の最高到達。**検証**：実エンジンのbalanceSim「STAKE-01」（7段×7神×7敵×3戦略×6seeds、Ⅶは3択の最良）で全体勝率 Ⅰ85／Ⅱ76／Ⅲ65／Ⅳ59／Ⅴ52／Ⅵ48／Ⅶ36%（設計ターゲット Ⅰ85〜90…Ⅶ25〜35に整合、Ⅵ・Ⅶは上端をわずかに超過＝安全側）、最弱神 Ⅶ 福永36%／笑蓮40%（床10%）。ゲートとして「各段・各神で最良戦略が段別の床（55/45/35/30/20/15/10%）以上」「難易度曲線が単調（+5pt許容）」をテスト化。E2E QA 21/21（未解放表示／到達チップ／Ⅲ選択とふつう固定／HUDタグ／save v9／続きから／勝利→倍率行・突破表示・記録・共有／もう一度で段維持／戦績／`?seed=&stake=`／Ⅶ3択・静寂でR1神力1／Daily非干渉／375px）。既存回帰（Normal/Daily smoke・背景・Mobile Auto Focus・P0）PASS、tests 2,679→2,690件、tsc/lint/build PASS。**残課題（Post-Phase 1）**：寿楽の一強（Ⅶ 86%、デバフがATK+を無効化する既存不均衡。神階のために神を均質化しない方針で今回は不変更、神バランス枠で扱う）、蒼毘・笑蓮の下位（既存）、Ⅶを「Ⅴ以上で選択式」へ広げる案、曜日Daily階位（Daily修正子ライブラリの共有）。**期待効果**：Replayability 5→7・Retention 4→6・Strategic Depth 4→5（推定 54→60〜63/100） |
| 2026-08-30 | 決定126フォローアップ。「神階Ⅰ〜Ⅶ の Production 採用」（CEO承認＝deploy可否のみ。実施はAI判断・CLAUDE.md §6）。`feat/shinkai-phase1`（`5552ae4`）を master へ fast-forward merge → push → Vercel 自動 deploy（約40秒）→ 本番 bundle `index-CYnzy73o.js` が `git archive 5552ae4` の clean build と同一であることを確認。**Production Release Gate（本番URL実測）**：主要導線（Home〜Result・BURSTプレビュー・Daily入口）PASS／Normal smoke・Daily smoke・Daily e2e（実プレイ決着）PASS／神階 E2E 21/21（未解放表示・到達チップ・Ⅲ選択とふつう固定・HUDタグ・save v9・続きから・勝利→倍率行・突破表示・段別ベスト・次段解放・挑戦状コピー・もう一度で段維持・戦績・`?seed=&stake=`・Ⅶ3択・Daily非干渉・375px）／通常モード かんたん・ふつう・むずかしい は神階なしで数値不変（敵HP 88/103/118・自HP 35/30/27・託宣7・初手5）／旧save v7 → 続きから再開後の保存で v9・mode normal・stake未定義／神階Ⅰ勝利→「突破！次は神階Ⅱ」・段別ベスト・Ⅱ解放／7ボス背景 54/54／Mobile Auto Focus・BURST撃破後scroll・P0 回帰 PASS／console error 0・HTTP 4xx/5xx 0。**Rollback：不要**（P0/P1 なし）。既知の非ブロッカー：寿楽一強（Ⅶ 86%、Balance Phaseで扱う。神階のための緊急nerfはしない＝CEO方針）、375px 難易度画面 812→899px、挑戦状の真正性は自己申告、Enemy Select cold load（敵art 全読込 2.4s）。Phase 1 は本フォローアップをもって正式完了 |
| 2026-08-31 | 決定127を追加。「BURST任意発動は不採用（REJECT／NO-GO）」（**AI判断**・CLAUDE.md §6。CEOは方向修正の承認のみ）。**目的**：共鳴7/7の自動発動を「神技READY→任意のタイミングで発動」に変え、「いつ撃つか」を Meaningful Choice にできるかの設計監査。**検証**：実エンジン（`applyAction`／`applyEffect`）で手動発動を再現（`resonance.max`を無効化し、`+0`共鳴で engine の burst 経路を通す）し、5系統・約50,000試合：v1 4戦略（auto／即撃ち／温存／予告読み）×7神×7敵×神階{0,1,3,5,7}、v2 4変種（会心＝溜め中の敵に×1.5／温存ボーナス+20%/R／両方）×上級者先読み、v3 完全情報オラクル（撃つ／1R温存の両分岐を打ち切って比較）、v4 型選択（攻／守）、v5 2段階神技（10で大神技×1.5／×2.0）。**結果**：即撃ち76% ≈ 自動75% ≈ 予告読み76%、温存0.01R。**オラクルでも勝率+0.5pt・スコア+0.7%**（overkill 3.4→1.9/burst のみ）。READY後に敵ターンを1回またぐと高神階で−15〜25pt。会心・温存ボーナスは±5ptのノイズ内、型選択は攻固定71.6% > 予告型66% > 守固定52%、2段階は大神技到達≤2%・オラクル+0.4pt・貯め切り−10pt。神別の恩恵差は0〜+3pt、敵別の最適タイミングは全敵で「READY到達時点」（R5.2〜6.1）。**根本原因**：共鳴経済が遅い（READY 平均R5.4〜5.9・到達38〜72%・神技0.6回/試合）ため判断の窓が「後半激化＋撃破レース」と重なり、遅らせる理由が存在しない。**判定**：失敗条件「常に即撃ちが最適」「操作が増えただけ」に該当し REJECT。現行「7/7到達→自動BURST」を維持。**再検討条件**：共鳴経済の再設計（READYを早く・複数回）と神技効果の持続性を伴うカードシナジー Phase と同時に扱う（ハーネスは scratchpad `sim2〜sim6` に保存）。**参考監査**：採用時も `resonance.value >= max` を READY と定義すれば save v10 は不要、UIは共鳴ゲージのボタン化が最良、race condition は `isPlayerTurn && !cutinActive && !pendingCardUid` と reducer 側の拒否で抑止できた |
| 2026-08-31 | 決定128を追加。「Game Feel Phase（80点化計画 Phase 2）：演出強度4段階・自作SE・BURST READY・Boss Entrance・勝利/敗北演出」（**AI判断**・CLAUDE.md §6。CEOは Phase 2 の方向修正（決定127）を承認し、スコープを指定。設計・素材方針・実装方式はAIが決定）。**設計監査**：既存は「敵→自分」の被弾のみ3段階（attackTier）、「自分→敵」は一律、数字サイズ一律、SEは全て短い合成トーン、Boss登場・勝利・敗北の演出なし、reduced-motion は一部のみ、と確認。**演出強度4段階**（`feelTier.ts`）：与ダメージ量（内部値）で L1（<10）／L2（10〜14）／L3（15〜24）／L4（25+）、BURST は常に L4、敵必殺は L3 以上。閾値は `RULES.baselineDamagePerAp`（5）の 2×/3×/5× から導く。適用先＝敵側シェイク（`hit-tier-l1〜l4`：振幅2〜11px・0.28〜0.7s）、斬撃の大きさ（`slash-l*`）、浮遊数字のサイズ（15〜34px、L4は専用キーフレーム）、SE音量。常にL4を使うインフレは禁止（共通カード最大の渾身20＝L3、神託25のみL4をテストで保証）。**SE**：CEO優先順位「①既存②自作③無料④有料」のうち①該当なしのため**②自作**＝`scripts/gen-se.mjs` が数式から決定論的に生成する WAV 20本（合計337KB、22.05kHz mono 16-bit）。第三者素材・録音物ゼロ、権利はプロジェクト所有（`docs/SE_ASSETS.md`）。役割別音量階層（`SE_GAIN`：Impact L1〜L4 0.45〜1.0／Feedback 0.3／State Change 0.65／Warning 0.5／Reward 0.7／Big 0.9、master 0.85、BGM 0.35 を邪魔しない）。`sound.ts` は fetch→decodeAudioData でキャッシュ、失敗時は従来の合成トーンへフォールバック（無音にならない）、Feedback系は400ms超の遅延時に鳴らさない。勝利／敗北は既存ジングル（Suno）を主とし短いスティングのみ追加。**BURST READY**：7/7到達の瞬間にゲージを満たして発光（`resonance-gauge-ready-flash`、0.9s）＋上昇音（`burst_ready`）、直後に既存カットイン。自動発動仕様は不変。**Boss Entrance**：新規開始（`useGameEngine.battleStartKey`＝startGame/startDailyGame で増分、「続きから」では増えない）のときだけ、舞台名・敵art・名前・脅威度★（Dailyは5）・型・タグ（DAILY／神階）・START を約1.5秒の固定オーバーレイで提示（`pointer-events:none`、画面高不変、reduced-motion は0.9s静止）。同時に SE を全プリロード。**勝利**：撃破の瞬間に「撃破！」の一拍（1.0s、`victory-beat`）→ 報酬（既存順序を維持）→ 結果画面をステージング（勝利→「神域制覇」→ポートレート→スコアのロールアップ0.8s→記録/神階/Daily→内訳→ボタン）。**敗北**：ログから敗因を導出（`defeatCause.ts`：直前の敵行動の技名と合計ダメージ、自傷判定）し「敗因：R5：業斧の鬼将の「攻撃」で 130 ダメージ」と対策ヒントを表示、再挑戦ボタンは従来位置。**誤表示修正**：「✨ 自己ベスト更新！」は勝利時のみ（敗北で初記録が付いても祝わない）。**Warning**：敵の溜め（charge）に警告音を追加。**アクセシビリティ**：`prefers-reduced-motion` で揺れ・斬撃・登場・一拍・ステージングを最小化（内容は全て表示、スコアは即表示）。**性能**：transform/opacity のみ、filter・粒子なし、画像追加なし、SE は初回に一括プリロード（337KB）。**不変**：engine・スコア・save（v9のまま）・Daily・神階・Mobile Auto Focus。QA・回帰結果はフォローアップ行に記録 |
| 2026-08-31 | 決定128フォローアップ。「Game Feel Phase の実装完了・QA結果」（AI判断・feature branch `feat/gamefeel-phase2`、Production 未deploy）。**実装**：`feelTier.ts`（4段階＋SE音量階層）／`sound.ts`（WAVバッファ再生・役割別音量・合成トーンfallback・プリロード）／`scripts/gen-se.mjs`＋`public/assets/se/*.wav` 20本（337KB）／`useBattleFx.enemyHitTier`／`EnemyPanel` の `hit-tier-l*`・`slash-l*`／`FloatingNumbers` の `floating-number-l*`／`useBattleSound`（段階SE・READY音・溜め警告）／`GodOtomoPanel.readyFlashKey`／`BossEntrance.tsx`＋`useGameEngine.battleStartKey`／`BattleScreen` の撃破の一拍・敗因導出／`GameOverOverlay`（神域制覇・スコアロールアップ・敗因・自己ベストは勝利時のみ）／`RewardOverlay` の獲得音／`defeatCause.ts`／`battle.css`（段階・READY・登場・一拍・ステージング・敗因・reduced-motion）／`docs/SE_ASSETS.md`。**修正点**：敗因のラウンドは「続きから」再開後にログが再開時点からしか無いため、`state.round` をフォールバックに使う。**テスト**：新規 `feelTier.test.ts`（閾値・インフレ禁止・音量階層）、`defeatCause.test.ts`、`sound.test.ts`（WAV 20本とSE名の1対1）。**E2E（vite preview、feel-qa 19/19）**：Boss Entrance は新規開始で表示・約1.5秒で消える・操作を奪わない・「続きから」では出ない／与ダメ140（表示）→L2、BURST→L4 のクラス・斬撃・数字／7/7 でゲージ発光＋カットイン／撃破の一拍（250ms で出現、報酬はその後）→「神域制覇」→スコアロールアップ確定→自己ベスト（勝利時）／敗北で「敗因：R2：業斧の鬼将の「攻撃」で 70 ダメージ」＋ヒント、自己ベスト・神域制覇は非表示／敵の溜め（charge）警告音経路でエラーなし／reduced-motion で登場は静止版・被弾はフラッシュのみ／375px で登場表示・横overflowなし・画面高2,501px（従来比+30px以内）・Auto Focus 従来どおり／SE 20本が200で配信／console 0・HTTP 0。**回帰**：神階 21/21・Mobile Auto Focus・BURST撃破後・Normal/Daily smoke・Daily e2e・P0・7ボス背景 54/54 すべて PASS。**性能**：追加は transform/opacity アニメと 337KB の WAV プリロードのみ（画像追加なし・filter/粒子なし） |
| 2026-08-31 | 決定128フォローアップ（Production採用）。「Game Feel Phase を本番採用（RELEASED）」（AI判断で実行・CEO承認：ローカル Audio QA を CEO が実施し PASS（「以前より大幅に良い」）→ Production deploy を承認、CLAUDE.md §6-3 #8）。**手順**：`feat/gamefeel-phase2`（`38d706a`）を master へ ff-merge → push（`de69404..38d706a`）→ Vercel 自動 deploy（約40秒、bundle `index-BWFlr5zp.js` 371,335B＝clean build と同一）→ SE 20本が `audio/wave` で配信されることを確認。**本番QA（Production Release Gate）**：Game Feel 19/19（Boss Entrance の表示・1.5s消灯・操作非阻害・続きからでは非表示、L2/L4 の段階クラス、7/7 ゲージ発光＋上昇音の経路、撃破の一拍→報酬→神域制覇→スコアロール→自己ベスト、敗因表示・敗北時の自己ベスト非表示、reduced-motion、375px）、主要フロー PASS、神階 21/21、Mobile Auto Focus、勝利後スクロール、難易度・v7移行・解放、smoke×2、Daily E2E、P0、ボス背景 54/54 すべて PASS。初回の feel 実行で `ERR_NETWORK_CHANGED`（検証機側の回線切替）による console error が1件出たが再実行でクリーン＝本番側の問題ではないと判定。**P0/P1 ゼロ → ロールバック不要**。**Phase 2 正式完了**。**既知の非ブロッカー**：SE は数式合成のため収録素材の質感はない（差し替えは CEO 判断事項）、Boss Entrance 中も操作可能（方針どおり）。次 Phase の実装には着手せず、AI Recommendation を1件のみ提示（決定129 候補）。 |
| 2026-09-06 | 決定129を追加。「神格（God Identity）v0.1：3神の得意技＋4枚の条件付きカード効果（80点化計画 Phase 3）」（**AI判断**・CLAUDE.md §6。CEOは各Stepの通過可否と Production deploy のみ承認）。**背景**：Step 1 監査（`docs/PHASE3_STEP1_GOD_AUDIT.md`）で神の差別化を 21/100 と測定。神データ（攻撃/防御/回復/支援・共鳴効果）は違うのに、勝ち筋が全神で「共通の攻撃札を回す」に収束していた。**根本原因**：防御・回復・自傷は攻撃へ変換される経路が無く、盤面を進めないため最適戦略に入らない。属性バフではなく**変換経路**を足すのが最小の修正だと判断した。**寿楽の一強は神データ由来ではない**ことを検証（その常用デッキを他神に渡すと +6〜30pt 上がる）ため、nerf しない方針を維持（決定126の残課題を継承）。**Step 2**：設計族 A〜F を rule-layer ハーネス（本番エンジンをそのまま import し、候補ルールをエンジン外部から注入）で比較し、C族（得意技＋条件付きカード）を採用。**Step 2.5**：使用率メトリクスが飽和していたため、**ペアシード1枚差し替えの限界価値**へ計測方式を変更（`scripts/phase3-audit/step2/marginal.audit.ts`）。共通カードの優先度是正は目標を満たさず、**UNRESOLVED RISK として明示的に残す**（PASS 扱いにしない）。**確定仕様（`docs/PHASE3_GOD_IDENTITY_FINAL_SPEC_v0.1.md`、TBDなし）**：得意技3種＝蒼毘「反撃の構え」（敵の攻撃を受け切って残ったブロックを等倍でダメージへ変換）／笑蓮「無傷の慈愛」（無傷で受け切ったラウンドは攻撃力を上げる）／福永「大勝負」（HPが半分以下なら攻撃力を上げる）。条件付きカード4枚＝不動の構え・笑って許す（`blocked`）／一喝（`enemyBig`＝敵予告100以上）／福袋（`lowHp`）。数値は全て `RULES.godPassive` / `RULES.cardBonus` に集約し、`RULES.godIdentity.{passivesEnabled,cardBonusEnabled}` をキルスイッチとして持つ。カード本体の `effects`・`text` は不変（既存デッキ・記録と互換）。**命名**：既存の「神技評価（Mastery）」および共鳴BURSTの「神技」と衝突するため、得意技（passive）は **「得意技」** と表記する（FINAL SPEC §10）。**検証**：同一バイナリ・同一シードで キルスイッチOFF（PRE）と ON（PROD）を比較し、**未変更の4神はバイト単位で同一**、蒼毘 79→96.9／福永 73.2→83.9／笑蓮 67→95.1 のみが動いたことを確認。**Mastery UX 修正**：CEO の Human Play で「勝ってもずっとC、なぜCか・次に何をすればいいか分からない」が出たため、結果画面の神技評価を**差分表現「あと○%でB」を全廃**し「指標名／現在値 → 次ランクの目標値／上げ方1行」の3行へ作り直した（`masteryDisplay.ts`、表示専用の純関数のみ。**閾値・判定・スコアは一切変更していない**）。スコア内訳の下へ移動し、「スコアとは別。戦い方で決まります」を併記。**Known Risk**：Gate 3 は PASS ではなく **WAIVED RISK**、共通カード差別化は **UNRESOLVED RISK**、大耀の C 偏重（専門プレイでも 52.4% が C）と寿楽 Mastery の敵依存は **Phase 4 backlog**、`balanceSim` STAKE-01 の 5000ms 既定タイムアウトは **QA infrastructure backlog**（今回タイムアウト値は変更しない） |
| 2026-09-06 | 決定129フォローアップ（Production採用）。「神格 v0.1 を本番採用（RELEASED）」（AI判断で実行・CEO承認：Human Play QA を CEO が実施し PASS（蒼毘で神技評価 A／無傷 67%）→ merge・push・deploy を承認、CLAUDE.md §6-3 #8）。**手順**：`feat/god-identity-v0.1`（`59a31a2`、正式4commit）を master へ `--no-ff` merge（`7f4b08a`）→ push（`eff4d06..7f4b08a`）→ Vercel 自動 deploy → 本番 bundle `assets/index-B07HTdRy.js`（376,162B）が master のクリーンビルドと**バイト単位で一致**することを確認。`.claude/settings.local.json` と `敵画像/` は merge 対象に含まれない（差分検査で 0 件）。**CEO スクリーンショットの「Sまであと19%」の原因**：`dist` が 12:34 のビルド、修正コミットが 12:43 で、CEO のブラウザが Phase 3 修正前の bundle をキャッシュしていた。クリーンビルドは 12:34 の dist と同一ハッシュで、旧表示の文字列（`Sまであと`／`Aまであと`／`でB（堂々）`）は本番 bundle に **0 件**、新表示の文字列は全て存在。**59a31a2 自体の実装不整合ではない**。**本番QA（自動化分）**：ホーム起動・神選択・得意技3種（反撃の構え／大勝負／無傷の慈愛）と神技評価4種の1行説明・デッキ編成画面での条件付きボーナス表示（Known Risk 1 の修正）・戦闘開始・敵Intent・7ラウンド/AP逓増・ブロック・得意技「反撃の構え」の発動と反撃ダメージ・条件付きボーナス「不動の構え」「一喝」の発動と ⚡/＋ の状態表示・共鳴ゲージ・OTOMO・託宣・スコア・save/resume（「続きから（蒼毘・ラウンド6）」）を確認。ページ由来の console error 0、HTTP は 200/304 のみでネットワークエラー 0（記録された console 警告は MetaMask 拡張由来）。**未消化**：勝利画面の神技評価・報酬3択のボーナス表示・Daily・神階の実プレイ確認は、検証機で Roblox が全画面かつ CPU を 130〜208%（4スレッド換算）占有し、Chrome タブが `visibilityState: hidden` となって `setTimeout` が集中スロットリングされたため自動操作を完走できず**未実施**（本番側の不具合ではない）。勝利画面の新表示は CEO の Human Play QA で実機確認済み、報酬ボーナス表示は同一の表示関数の unit test で担保。**リポジトリ限定テスト**：`npx vitest run --dir src` で 54 files / 611 tests、610 PASS。唯一の失敗は `balanceSim` STAKE-01 の 5000ms タイムアウトで、Phase 3 を含まない stale worktree の同一テストでも同時に再現（CPU 占有下でのベンチマークは通常比 15〜25倍遅い）。**ENVIRONMENTAL TIMEOUT / NON-BLOCKING** として記録し、タイムアウト値は変更しない。**P0/P1 ゼロ → ロールバック不要**。**Phase 3 正式完了**。**次**：Visual Quality（`feat/visual-quality-assets`、敵WebPの再生成）と Daily Ranking（Phase 4 最優先候補） |
| 2026-09-06 | 決定130を追加。「Visual Quality Assets：神・敵・OTOMOの配信用アセット刷新」（**AI判断**・CLAUDE.md §6-2。CEOは事前監査との食い違い報告に対する修正方針のみ承認）。**背景**：Human Play QAで「背景は綺麗だが、神・ボス・OTOMOの見た目が粗く感じる」。**事前監査の前提が4点誤っていたため実装前に停止して報告した**：(1) 神の`front.webp`（1600px）は`front.png`の高解像度版ではなく**別イラスト**（直立ポーズ・白背景、PSNR 8.4〜10.2dB）で、採用すると全神の絵が変わる。真の高解像度原本は`main.webp`（PSNR 35.3〜37.9dB＝同一の絵）。(2) 敵WebPの「ディテール30〜49%低下」は圧縮劣化ではなく、試練の影・銀甲の機工師・乱舞の道化の3体が**決定99/100で差し替えた別イラスト**であることによる（別画像同士の比較値）。この3体は`art.png`をsourceにできない（旧デザインに戻るため）。(3)「384×512を512×512へ引き伸ばした二重劣化」は存在せず、commit `5b1e1b7`のとおりbbox抽出＋パディング（oniのcontent aspect ratioはpng/webpとも0.8276で一致）。(4)「480pxはDPR2に不足」も誤りで、戦闘画面の最大表示は200〜220 CSS px＝DPR2で400〜440px。**粗さの真因は解像度不足ではなく、旧`front.png`が過剰なシャープ処理で持っていたジャギー（偽エッジ）**だった（detail指標が原本1600pxより高い＝エイリアシングを測っていた）。**実施**：神7柱は`main.webp`のRGB＋`front.png`のアルファ＋白マット除去（シルエット端の平均輝度232→207）で`front_640.webp`（640px q88）へ。敵はoni/juumaのみ`art.png`から同一の幾何変換を再実行して`art_hq.webp`を作成（配置は旧`art.webp`とピクセル単位で完全一致＝表示位置・サイズ不変）、PNG配信のままだったonryo/ryujinは寸法・構図無変更で再エンコード、上記3体は**変更なし**。OTOMO21画像は1600px→320px（q86、**シャープ処理なし**＝精霊態の半透明発光を保護、alphaQuality 100）。**サイズ根拠**：最大表示は神220px/敵280px/OTOMO128px（`@media (min-width:900px)`の階層 敵280≳神220>OTOMO128）。DPR3端末は900px未満のレイアウト（神200px・OTOMO96px）に入るため、worst caseは神600px・OTOMO288pxとなり640/320が最小十分値。256pxはDPR3でPSNR 26.1dBと不足したため却下。**検証（同一表示サイズでのPSNR）**：神はDPR2で30.37→30.34dB（同等）だがDPR3で22.04→28.42dB（+6.4dB）、双牙の魔獣は23.61→34.96dB（**+11.4dB**）、業斧の鬼将34.71→35.31dB、OTOMO21画像は@192px 38.90dB／@288px 34.94dB（最小32.18dB）。**容量**：神1,897.6→682.7KB、敵1,206.0→547.7KB、OTOMO6,357.0→432.0KB、**合計9,460.6→1,662.4KB（-82.4%）**。1バトルの実DL想定は1,100.8→503.9KB（-54.2%）。oni/juumaのみ劣化回復のため意図的に+24%。**JSバンドル不変**（376,162→376,168B、gzip 115.41kB同一）、**CSSはハッシュまで同一＝CSS変更ゼロ**（正方形→正方形／寸法不変のため`contain`がそのまま機能し、aspect ratio対応は不要だった）。**QA**：tsc PASS、oxlint PASS（警告2件はmaster既存・無関係）、**vitest 223 files / 2,724 tests PASS**、clean build PASS、asset HTTP 200が7/7・7/7・21/21、broken image 0（成長画面の9件は`loading="lazy"`由来と確認）、console error 0、network error 0、旧アセットの残存参照0、save/resume PASS、Daily PASS、battle layout PASS。実ファイル存在チェックのテストを3件追加（決定124と同方式）。balance simulationは画質パッチのため実施せず。**Known Risk**：試練の影・銀甲の機工師・乱舞の道化は**今回改善できない**（現行art.webpがそのデザインの唯一のコピーで、より高品質なsourceが存在しない。改善には元絵の再入手か再生成が必要＝§6-3のCEO判断事項）／敵は原画自体が384〜512pxのため DPR2デスクトップ（560px必要）にはどうしても届かない／神640pxはDPR3かつ900px幅以上で97%充足／旧アセットはロールバック用に温存するためリポジトリ容量は減らない（ユーザーDL量には影響なし）。詳細は`docs/VISUAL_QUALITY_ASSETS_QA.md`。**merge・push・deployは未実施**（CEOのHuman Visual QA待ち） |
| 2026-09-06 | 決定130フォローアップ（Production採用）。「Visual Quality Assets を本番採用（**RELEASED**）」（AI判断で実行・CEO承認：**Human Visual QA を CEO が実施し PASS**（恵比寿／双牙の魔獣／鯛丸・精霊態をローカル preview で確認）→ merge・push・deploy を承認、CLAUDE.md §6-3 #8）。**手順**：`feat/visual-quality-assets`（`32a062a`）を master へ `--no-ff` merge（**`01ac6f8`**）→ push（`bab4eb3..01ac6f8`）→ Vercel 自動 deploy。master と origin/master はともに `01ac6f8`（**ahead/behind 0/0**）。merge 内容は 39 files（新規asset 32＋`gods.ts`/`enemies.ts`/`otomo.ts`＋テスト2＋docs2）で、`.claude/settings.local.json`・`敵画像/`・`scripts/phase3-audit/out/` は差分検査で **0件**。**本番bundle**：`assets/index-Sm3K62_I.js`（376,179B）・`assets/index-CDbAuk7l.css`（96,423B）がローカルclean buildと **sha256 一致**。CSSハッシュはリリース前masterと同一＝**CSS変更ゼロを本番で再確認**。bundle内の旧パス（`front.png`／`_transparent.webp`／敵`art.png`）は **0件**、新パスは全件存在。**本番asset**：神`front_640.webp` **7/7**、敵`art_hq.webp` **4/4**、据え置き敵`art.webp` 3/3、OTOMO`*_320.webp` **21/21** の計 **32/32 が HTTP 200・image/webp**、4xx/5xx 0件。さらに32件すべてがリポジトリのファイルと **byte単位で一致（mismatch 0）**＝CEOがQAした画像がそのまま本番に出ている。**容量（本番実測）**：**9,460.6KB → 1,662.4KB（-82.4%）**（目標1,662.4KBと完全一致）。内訳は神1,897.6→682.7KB、敵1,206.0→547.7KB、OTOMO 6,357.0→432.0KB。1バトル実DL（神1・敵1・OTOMO1形態）は **800.0→203.0KB（-74.6%）**。**テスト**：リリース後のmasterで `npx vitest run --dir src` → **54 files / 614 tests 全PASS**（16.70s）。決定129でQA infrastructure backlogとしていた`balanceSim` STAKE-01のタイムアウトは、previewサーバー停止状態では**再現せずPASS**（環境要因であることを再確認。タイムアウト値は今回も変更していない）。**未消化（PENDING）**：**本番実機でのProduction Visual QAは未実施**。PC再起動後にChrome拡張（Claude in Chrome）が未接続で`tabs_context_mcp`が3回とも失敗したため（本番側の不具合ではない）。恵比寿／双牙の魔獣／鯛丸・精霊態＋代表別1組の実機表示、alpha edge・battle layout・animation、console/network error計測、save/resumeが未確認。ただし本番のbundleと32アセットはCEOがQAしたローカル検証物と**byte単位で一致**しており描画結果が異なる余地はなく、実ファイル存在チェック3件を含む614 testsもPASS。**PASS扱いにはせずPENDINGとして明示的に残す**。**Known Risk**：(1) 試練の影・銀甲の機工師・乱舞の道化は高品質原本が存在しないため今回変更しておらず、これは **FAILではなく将来のAsset Art Quality backlog**（改善には元絵の再入手か再生成が必要＝§6-3のCEO判断事項）。(2) **asset変換用の一時スクリプトがリポジトリに残っていないため同じ変換を再現できない**。成果物は全てcommit済みでリリースには影響しないが、**再現可能なasset pipeline（`scripts/assets/`への変換スクリプト整備）を maintenance backlog** とする。(3) 敵は原画が384〜512pxのためDPR2デスクトップには届かない、(4) 神640pxはDPR3かつ900px幅以上で97%充足、(5) 旧アセットは温存のためリポジトリ容量は減らない（継続）。**P0/P1 ゼロ → ロールバック不要**。**Visual Quality Patch 正式完了（RELEASED）**。詳細は`docs/VISUAL_QUALITY_ASSETS_QA.md` §9。**次**：Daily Ranking（Phase 4 最優先候補） |
| 2026-09-07 | 決定130フォローアップ2（Production Visual QA 完了）。「Visual Quality Patch の本番実機QAを PASS で完了、PENDING を解消」（AI判断で実行。CEOはHuman Visual QA PASSとProduction Release GOを既に承認済み）。**背景**：前日のリリース時点ではPC再起動でChrome拡張が未接続だったため本番実機QAを **PENDING** として記録していた（決定130フォローアップ）。拡張の再接続後に実施し、解消した。**手順**：実行前に`localStorage`を退避し、終了後に元のsave（蒼毘・ラウンド6／試練の影）へ**完全復元**した（CEOの挑戦データは消していない）。**確認した2組**：組1＝恵比寿`front_640.webp`／双牙の魔獣`art_hq.webp`（art.pngから再変換）／鯛丸・精霊態`spirit_320.webp`、組2（代表・別1組）＝蒼毘`front_640.webp`／藍花の怨霊`art_hq.webp`（PNG→WebP再エンコード）／百勝・精霊態`spirit_320.webp`。**結果は全項目PASS**：神表示（髪・輪郭の階段状ジャギー解消）、敵表示（敵選択画面の7体すべて正常描画を含む）、OTOMO表示（**精霊態の半透明の発光・粒子が潰れていない**）、**alpha edgeの白フチ・黒フチ・欠けは確認されず**、背景との解像感差の解消、過剰シャープなし、battle layout（神221×241／223×243・OTOMO128×128 CSS pxで既存階層 敵≳神>OTOMO を維持）、animation、**broken image 0**、実解像度（神natural 640×640・OTOMO natural 320×320）、**旧アセットの残存フェッチ0**（`front.png`／`_transparent.webp`／敵`art.png`・旧`art.webp`はいずれも1件も要求されない）、**ページ由来のconsole error 0**（記録された警告6件はすべて`chrome-extension://`＝MetaMask由来）、**network 4xx/5xx 0**（画像・JS・CSS・SEは全200）、**save/resume PASS**（ラウンド2で離脱→リロード→「続きから（恵比寿・ラウンド2）」→復帰後も新アセットで正常描画）。**非ブロッカーの観測（本パッチとは無関係）**：`/assets/bgm/home.mp3`（6.07MB）が一部のページロードで **HTTP 503** を返した（3ロード中2回、残り1回と`battle.mp3`は206で正常）。curlでの直接取得は3/3で200・6,073,315B。BGMは2026-08-13以降未変更で**今回のmerge差分にも含まれない**ため、大容量mp3のrangeリクエストに対するCDN側の一時的な事象と判断し、**Visual QualityのP0/P1には該当しない**。**BGM配信の安定化を別課題としてbacklogに記録**する。**P0/P1 ゼロ → ロールバック不要**。**Visual Quality Patch は全Step完了（RELEASED）**。詳細は`docs/VISUAL_QUALITY_ASSETS_QA.md` §9-6。 |
| 2026-09-07 | 決定131を追加。「Phase 4.0 Daily Ranking Competitive Gate：現行Dailyの競技適性を本番エンジンで数値検証」（**AI判断**・CLAUDE.md §6-2。CEOは①Vercel条件付き承認②Daily bonusCopies無効化③MVP運用方針の3件を事前承認済み）。**目的**：ランキング実装前に「現行Dailyを競技ランキングとして公開してよいか」を判定する。**Daily条件の再現方式**：条件を模倣せず、**本番UIが呼ぶのと同じ`resolveDailyStart(dateKey)`をそのままimportして`START_GAME`へ渡す**設計にした（`scripts/phase4-daily-gate/dailyHarness.ts`）。通常モードの神階0結果は一切代用していない。parity検証7件すべてPASS（seed・週次巡回・敵HP×1.25/攻撃×1.15・神階なし・JST境界・**決定論**・公平版デッキのlegal性）。**規模**：Matrix 168日（敵ごと実Dailyシード24本）×7神×6打ち筋＝7,056試合、プレイヤー場 300人×21日（＋1,000/3,000人の感応度）、bonusCopies paired 21シード、最小修正案の再simulation 4案。**エンジン実行合計 約25,000試合**。**G2 PASS**：7神とも勝率100%（Wilson 95%CI下限97.8%、n=168/神）。「選ぶと勝てない神」は存在しない。**G1 FAIL**：神間best-score spread 全体7.91%／最悪敵9.36%（基準5%）。**不足は寿楽1柱に集中**し、残り6神は4.76%で基準内。原因分解では全神が同じdamage 145.4・victory 300になり（全員撃破するため実効ダメージが敵HPで揃う）、**神差はcombo（1R何枚出せるか）とtempo（何Rで倒すか）だけで決まる**。寿楽はcombo最下位72.6・tempo下から2番目161.9で、共鳴BURSTの半分が Battle Score に寄与しない攻撃力デバフであることが構造的理由。**BURSTダメージの単独調整では解けない**（23→33＝+43%でも全体5.73%・最悪敵8.32%でFAIL／プロセス内データ差し替えで検証、本番データは復元済み）。**Tie Density（本命リスク）**：Dailyは決定論パズルのため**到達しうるスコアの種類数は参加人数に依存せず137前後で頭打ち**になり、1順位あたりの人数が参加者数に線形比例する。Top1%の1順位あたり人数は300人で1.29／1,000人で3.11／3,000人で**8.71**。**1,000人以上で一意順位を提示すると実質的な順位決定要因が先着になる＝Step 5のFAIL条件**。既存engine由来の二次tie-break（撃破R・残HP・操作回数）は解消率1〜12%と無力（撃破Rはtempo、残HPはsurvivalとして**既にスコア式へ織り込み済み**のため同点グループ内で一定になる）。「丸める前の素点を並び順キーにする」案も`score.total`が整数のため無効（ユニーク数137→147.6、Top1%改善なし）。**新しいゲーム内スコアを増やさない限り技術的に解消できない**と結論。**God Dominance PASS**：Top10%の神別構成比は0.14/0.13/0.24/0.13/**0.07**/0.13/0.16で6神が均等付近、最多神は平均0.30（FAIL基準0.50未満）。Matrix最良神（才華）とTop10%最多神（蒼毘）が**一致しない**＝神選択が順位を決めていない。**Three Attempts PASS**：Dailyは完全決定論で**3回に乱数の引き直しが無い**（parityで実証）ため「運良く高rollした人が勝つ」状態は構造的に発生しない。3回は同じ問題を学習して詰め直す機会であり、技量は順位に直結する（上級は平均で上位23%、初級は61%）。**bonusCopies PASS（無効化を数値で支持）**：paired比較で熟練版が全神で上回り平均+1.22%（最大+2.54%）。これを**順位に換算すると1,000人規模で平均14.8順位・最大45順位**動く（1順位あたり3.11人の密度に対して圧倒的）。**他の公平性差は無し**：`startDailyGame`へ入る全入力と永続状態を洗い出し、`bonusCopies`以外にプレイヤー間条件差が無いことを確認（`otomoBond`は表示専用でSTART_GAMEに入らない）。**判定：CONDITIONAL GO**。Phase 4.1（coreリプレイ関数）・4.2（行動ログ記録）へ進んでよい。G1 FAILを「大幅FAIL」としないのは、①不足が寿楽1柱に集中し残り6神は基準内②勝率は7神とも100%③上位帯の神構成比で6神が均等付近④Matrix最良神とTop10%最多神が不一致、の4点による。**Production公開前の必須2件**：(1)**bonusCopies無効化＝変更は3箇所**（`GameFlow.tsx:220`/`:275`の第4引数を`undefined`に、`DeckBuilderScreen.tsx:87`で`dailyChallenge`時に空Map。**3点目が無いとデッキ構築画面で3枚積めるのに開始時`validateDeck`が落ちDailyが起動不能になるため必ず同時に出す**）＋Daily画面の「全員共通の条件」に編成ルールを明記。通常モードは無変更。(2)**同点を同順位として表示する**（1,1,3方式・同点人数併記・パーセンタイル併記・**先着は同順位内の表示順にのみ使い順位そのものは決めない**・「あと○点」は次の異なるスコアまでの差分）。**Phase 4.1バランス課題（ブロッカーではない）**：寿楽のDaily適性（Top10%構成比0.07＝均等の半分）は、決定129と同じ「変換経路」の発想でデバフを攻撃機会・comboへ変換する得意技を与える方向で設計・検証する。決定126の「高神階で最強格だからnerfしない」とは矛盾しない（高神階の勝率と神階0のスコア最大化は別軸）。修正時はstake 0/4/7の全帯域で副作用を再測定し、本ハーネスでG1を再判定する。**cleanup backlog**：`useGameEngine.ts`の`setStakeResult(null)`重複とインデント崩れ（master 38d706a由来・挙動に影響なし・本監査では未修正）。**本監査でゲーム本体（`src/`の挙動）は1行も変更していない**（`git status -- src/` が空・tsc PASS・`npx vitest run --dir src` 54 files/614 tests PASS）。詳細は`docs/PHASE4_DAILY_RANKING_COMPETITIVE_GATE.md`。branch `feat/daily-ranking-phase4`、**merge・push・deployいずれも未実施** |
| 2026-09-07 | 決定132を追加。「Phase 4.1：Daily公平性修正 ＋ Replay Foundation」（**AI判断**・CLAUDE.md §6-2。CEOはPhase 4.0のCONDITIONAL GOとDaily bonusCopies無効化を承認済み）。**A. Daily公平性修正**：決定131で特定した3箇所を実装した。ただし提案の「第4引数を`undefined`に」ではなく、**`useGameEngine.startDailyGame`から`bonusCopies`引数そのものを型ごと削除**する形にした（`resolveDailyStart`が`forcedId`を受け取らない設計が敵選択の無効化そのものになっているのと同じ考え方。将来呼び出しが増えても報酬ボーナスが復活することが型レベルで不可能になる）。`GameFlow.tsx`のDaily開始2箇所は引数を外し、`DeckBuilderScreen.tsx`は`dailyChallenge ? new Map() : loadRewardBonuses(godId)`。**決定131の設計に無かった追加1点**：`DeckBuilderScreen`は決定27で前回編成を初期表示するため、通常モードで報酬ボーナスを使って組んだ1種3枚のデッキが復元されるとDailyの初期状態が不正になり**プレイヤーが自力で直すまで開始できない**。そこで「Dailyでは保存デッキが公平版ルールで不正ならおすすめ構成へフォールバック」を追加した（おすすめ構成は全7神とも1種2枚以内で常にlegalであることをテストで固定）。`DailyChallengeScreen`の「全員共通の条件」に編成ルール（同じカードは`RULES.deckBuilding.maxCopiesPerCard`枚まで・報酬ボーナス不使用）を明記。**通常モードは無変更**で決定43のメタ進行はそのまま機能する。**B. Replay Foundation**：`src/core/replay/`を新設し、「Daily開始条件（日付キーのみ）＋行動ログ」から本番エンジンで対局を再現して**結果をサーバー側が計算する**`runReplay()`を実装した。**中心的な設計判断は「クライアントの申告値を受け取る経路を型から消す」**こと——`ReplayInput`に`score`/`win`/`playerHp`/`rngCursor`のフィールドは存在せず、JSONに紛れ込んでも無視される（実行時テストで固定）。seed・敵・難易度・補正も申告させず`resolveDailyStart(dailyKey)`で再導出し、`claimedSeed`/`claimedEnemyId`は**照合専用**（不一致なら拒否、一致しても使うのは再導出値）。`START_GAME`は行動ログに含めない（含められる形自体がseed・敵・bonusCopiesの申告経路になるため）。**Action型は新設せず**`Exclude<GameAction, {type:'START_GAME'}>`で既存を再利用した（二重定義はいつかUIとリプレイをずらし、その瞬間に一致保証が壊れるため）。**Validationは二重実装しない**：「手札に無い」「AP不足」「順序違反」「決着後の操作」はすべて既存エンジンが例外で表明しているので`applyAction`をtry/catchで包んで`ENGINE_REJECTED`へ翻訳するだけにし、デッキ検証もUI・`createInitialState`と同じ`validateDeck`を呼ぶ。リプレイ固有の門番はエンジンが原理的に知り得ない4点（フォーマット版・action数上限・申告値照合・決着到達）のみ。`RULES.replay`（`formatVersion:1`／`maxActions:400`）を追加し、`saveVersion`とは独立させた（セーブとリプレイは別物で、片方の変更が他方の互換性を壊さないため）。**アーキテクチャ移動**：`resolveDailyStart`の実体を`src/hooks/startDaily.ts`から`src/core/data/dailyStart.ts`へ移し、hooks側は再エクスポートのみの互換層にした（coreがhooksを参照するのは層の逆流のため）。**関数の中身・戻り値・Daily seedの導出方式は一切変更していない**ため、既存のimportパス（UI・`startDaily.test.ts`・Phase 4.0監査ハーネス）は無変更で動く。**Determinism**：本番と同じActionを発行する決定論ドライバでライブ実行し、その操作ログだけを`runReplay`へ通して突き合わせた。**7神 × 7敵/7 Daily seed（週次巡回1巡） × 3デッキ × 3打ち筋 ＝ 441試合すべてでGameStateが完全一致**（score内訳・HP・敵HP・round・勝敗・rngCursor・resonance・OTOMO・mastery・手札/山札/捨札を個別にも明示検証）。一致の定義はbyte比較ではなく構造的同値（`toEqual`）とした——GameStateは関数・Date・Mapを含まないプレーンなデータなので実質的に完全一致と等価で、キー順の違いで落ちる脆さを避けられるため。**Tamper**：action削除・追加・順序変更（全反転／隣接入れ替え）・card uid改ざん・enemy改ざん・seed改ざん・日付すり替え・deck改ざん・神すり替え・過剰action数のすべてが「拒否される」か「改ざん前と違う正規結果を返す」ことを確認（uid・enemy・seed・deck不正・過剰action数は必ず拒否）。**Server利用境界**：`runReplay`から到達できる**全ファイル（推移的依存の閉包）**を機械検査するテストを追加し、src/coreの外へ出ない・外部パッケージimport 0件・window/document/localStorage/navigator/Phaser/fetch不参照・`Math.random`不使用・Neon/Supabase/`process.env`不参照を固定した（コメント除去後に走査。coreは約束をコメントで多数明記しており素の文字列検索では自己言及に当たるため）。**行動ログの実測**：147試合でaction数 min9/avg22/max32、JSON min260/avg650/**max1,018バイト**でCEO想定（30〜60 actions・1〜2KB）に収まる。**QA**：tsc PASS、oxlint PASS（警告2件は`scripts/phase3-audit/`の既存・無関係）、**vitest 227 files / 2,789 tests 全PASS**（新規4ファイル・62テスト追加。既存テストは1件も変更していない）、clean build PASS、`balanceSim` STAKE-01タイムアウトは再現せずPASS。**Phase 4.0監査ハーネスの回帰**：`parity.audit.ts` 7/7 PASS、`out/*.json`のmd5は実行前後で完全一致＝**再simulationを行っていない**。**バンドル影響**：CSSはハッシュまで同一（`index-CDbAuk7l.css`＝CSS変更ゼロ）、JSは+約240B。**`src/core/replay/**`自体はアプリが未importのためtree-shakeされバンドルに入らない**（入るのは`RULES.replay`の数値2つのみ）。**Failure Gate 10項目すべてクリア＝PASS／Phase 4.2 GO**（live-replay不一致なし／client score不要／React・DOM・localStorage非依存／Daily seed方式無変更／saveVersion 9据置き／通常modeのbonusCopies維持／3回制限維持／validation二重実装なし／harness無傷／寿楽balance変更の混入なし＝`gods.ts`・`cards/`・`src/core/engine/`の差分0）。**Known Risk**：(1)**本番UIはまだ行動ログを記録していない**（Phase 4.2）ため「実プレイ由来のログが必ずリプレイを通る」ことは実配線後に再確認が必要、(2)リプレイ対象はDailyのみ（通常モードは敵選択・難易度・神階・URLバックドアがあり開始条件をサーバーが再導出できない）、(3)`maxActions:400`は理論上限の証明ではなく実務的な余裕値（実測最大32の12倍）で本番ログの実分布での再確認が必要、(4)決着していないログは受理しない仕様のためUI側の告知が必要、(5)同点密度は未解決（決定131 §13-2、Production公開前に必須）、(6)寿楽のG1 FAILはCEO指示どおり別Balance Patchとしてbacklog、(7)`useGameEngine.ts`の`setStakeResult(null)`重複は今回も未修正（cleanup backlog継続）、(8)UI配線ガードはソース文字列検査のため大きなリファクタリング時はテスト側の追随が必要。詳細は`docs/PHASE4_DAILY_REPLAY_FOUNDATION.md`。branch `feat/daily-ranking-phase4`、**merge・push・deployいずれも未実施**。**Neon・DB・Ranking API・ランキングUI・display name・rate limitはいずれも作成していない** |
| 2026-09-07 | 決定133を追加。「Phase 4.2：Daily実プレイのAction Log記録とReplay統合」（**AI判断**・CLAUDE.md §6-2。CEOはPhase 4.1 PASS／Phase 4.2 GOを承認済み）。**目的**：「実際のDailyプレイで発生した操作を記録し、そのログをPhase 4.1 Replayへ渡すと実プレイ結果を完全再現できる」状態にする。ランキングBackendは作っていない。**記録の境界**：`useGameEngine.dispatch`は`applyAction`をtry/catchで包み、例外が出た操作はstateを更新しない。記録もこの境界と完全に一致する必要があるため、**適用と記録を1つの純粋関数`applyAndRecord(state, action, log, clientRunId?)`に閉じ込めた**（`src/core/replay/runLog.ts`）。例外が出れば同じ例外を投げるので呼び出し側はlogを差し替えず、**エンジンが受理した操作だけが記録される**。UI側でログを組み立てる余地が無いので「画面には出たがエンジンに届かなかった操作」も「届いたのに記録されなかった操作」も構造的に発生しない。`START_GAME`は記録しない（Phase 4.1の契約）。通常モードは記録しない。ログが無い状態でDailyの操作が来ても**途中から記録を始めない**（先頭が欠けたログはリプレイが必ず失敗するため）。**重要な設計判断①**：記録の前進は`dispatch`ではなく**`commit`の中でGameStateの保存と同時に行う**。`END_ROUND`は演出のため700ms遅れてcommitされるので、dispatch時点で記録を進めるとその間にブラウザを閉じたとき「ログだけ1手先」で保存され再開時に必ず食い違う。commitで揃えることで**永続化されるログと盤面は常に同じ地点を指す**。**二重記録の防止**：ログは`useRef`で持つ（re-renderとStrictModeの二重実行はレンダリングとstate updaterが対象で、イベントハンドラから呼ばれる純粋関数はどちらでもない）／二度押しはエンジンが拒否／`appendAction`は新配列を返し元を変更しない。誤操作（手札に無いカード・存在しない託宣）を各ラウンドに混ぜたrunと混ぜないrunで**行動ログもGameStateも完全一致**することを7神で確認した。**clientRunId**：`crypto.randomUUID()`（無ければ同じ`crypto`の`getRandomValues`を16進32桁）で発行し、独自UUID実装は書かない。run開始時に**1度だけ**発行して行動ログと一緒に永続化するため、中断・再開しても将来の再送でも同じIDのまま＝サーバーが冪等に扱える。seed・scoreからは導かない（Daily seedは全員共通なので衝突・推測可能になり、スコア由来だと「retryで同じID」を満たせない）。安全な乱数が無い環境では**例外を投げる**（推測可能なIDを黙って発行するより、そのrunを提出対象外にする方が安全。ゲーム自体は普通に遊べる）。**ReplayInput組み立て**：`DailyRunLog`を「`ReplayInput`に`clientRunId`を足しただけ」の形にし、`toReplayInput`はその1フィールドを落とすだけにした（2つの構造がずれる余地を残さない）。`score`/`win`/`status`/`playerHp`/`enemyHp`/`round`/`rngCursor`/`seed`は**どちらの型にもフィールドが存在しない**ため、client申告値を真実として追加することが構造的にできない。**Live→Replay統合（最重要Gate）**：本番の記録経路を通したログで、**7神 × 7敵/7 Daily seed × 3デッキ × 3打ち筋＝441run**（＋託宣あり/なし14・OTOMO絆2種14・誤操作混入7）を検証し、**GameStateが構造的に完全一致・不一致0件**（score内訳・HP・敵HP・round・status・rngCursor・resonance・OTOMO・mastery・deck/hand/discardを個別にも明示検証）。**中断・再開（監査で判明した必須対応）**：`battleSaveStorage`は`status===playing`のGameStateを無条件に保存し`isExpiredDailySave`が日跨ぎだけを弾くため、**Dailyは同じ日のうち中断・再開できる**。「resume前までのactionsを失う」設計は禁止事項に該当するので、ログを専用キー`sevengods.dailyRunLog`（`RULES.saveVersion`とは独立したバージョン）へ毎アクション永続化し、再開時に`resumeRunLog`が**ログをリプレイして得た盤面と保存された盤面を突き合わせてから**引き継ぐ設計にした。「同じログから同じ盤面が出るか」はリプレイ検証と同じ問いなので、専用の整合チェックを別に書かずPhase 4.1の資産を再利用している。食い違えばログを捨てる（ゲームの続行は妨げず、そのrunが提出対象外になるだけ＝`dailyRunLogAvailable`でhookから見える）。**完走ログの任意の中断点（1手目〜最終手前）すべてで引き継げる**ことをテストで確認した。**未完走run**：決着分岐を通らないため`pendingRuns`へ入る経路が無く、提出対象外・Replay validation対象外（`runReplay`は既定で`NOT_FINISHED`）・ranking scoreなし。ゲーム本体のsave/resume（決定29）は無変更。**送信待ちrun**：`sevengods.pendingRuns`（独立バージョン）へ決着済み`ReplayInput`と`clientRunId`を控える。同じ`clientRunId`は追加でなく**置換**（retryで増えない）、`RULES.replay.pendingRuns.maxRuns=20`超過は古い順に破棄、`retentionDays=7`より古い日付キーは剪定。**Phase 4.2ではネットワークに一切触れない**（`fetch`を呼ばないことをテストとソース検査の両方で固定）。**action分布（1,470run実測）**：actions min7／median24／P90 26／P95 27／P99 30／**max 34**、payload bytes min790／median1,257／P90 1,412／P95 1,443／P99 1,541／**max 1,695**（デッキ20枚のID配列が支配的）。**`maxActions=400`は維持**——実測max34に対し約11.8倍の余裕があり、下げれば正当な長期戦を落とすリスクが増え、上げればDoS対策の意味が薄れる。「理由なく変更しない」に従った。実測値はテストで固定しており、変われば気付ける。**Privacy**：payloadは`version`/`mode`/`dailyKey`/`godId`/`deck`/`otomoGrowthPath`/`actions`と`clientRunId`のみ。email・IP・fingerprint・device情報・名前・cookie・timezone・userAgentを含まない。**行動ログの1件1件も**`PLAY_CARD{type,uid}`/`END_ROUND{type}`/`USE_DIVINATION{type,choiceIndex}`のキーちょうどしか持たず、**タイムスタンプも持たない**（提出時刻は順位を決めないため）。**Security境界**：Phase 4.2終了時点でも「client log = 信頼できる」とは扱わない。clientは`ReplayInput`を組み立てて控えるだけで、正規scoreはPhase 4.3のサーバーが`dailyKey`からseed/敵を再導出して本番エンジンで再生し算出する。`resumeRunLog`がclient側でも`runReplay`を呼ぶのは「再開時にログと盤面が合っているか」を自分で確かめるためで、結果の権威ではない。**Tie ranking rule（正式決定）**：決定131 §6の実測（到達しうるスコアの種類数が参加人数に依存せず137前後で頭打ち、1,000人で1順位3.11人・3,000人で8.71人）を踏まえ、**①同じスコアは同順位（1,1,3方式）②同点人数を併記③パーセンタイルを併記④提出時刻（先着）は順位を決めず同順位内の表示順にのみ使う⑤「あと○点」は次の異なるスコアまでの差分**をPhase 4.3 Backend／4.4 UIの要件として固定した。規則をコメントだけに置くと実装のたびに解釈がぶれるため、**実行できる純粋関数`assignRanks`（`src/core/replay/ranking.ts`）として実装**した（ランキングUIでもBackendでもなく順位の定義だけを持つ関数。UIは今回実装していない）。1,000人／137種の規模でも規則が崩れないことをテストで確認済み。**QA**：tsc PASS、oxlint PASS（警告2件は既存・無関係）、**vitest 232 files / 2,850 tests 全PASS**（新規5ファイル・61テスト追加。既存テストは1件も変更していない）、clean build PASS。通常battle・Daily・Daily3回・JST reset・save/resume・bonusCopies・Phase 3神格・card bonus・stakes・score・OTOMO・BURSTの該当46ファイル542テストを個別にも再実行しPASS。**Phase 4.0 harness**は`parity.audit.ts` 7/7 PASS・`out/*.json`のmd5が実行前後で完全一致＝再simulationなし。**Phase 4.1 Replay**は全テストPASS。**バンドル**：CSSはハッシュまで同一（変更ゼロ）、JSは376,42x→383,480B（**+約7.0kB**、gzip 115.52→117.62kB）。Phase 4.1では未importでtree-shakeされていた`src/core/replay`を`useGameEngine`が実際に使うため入る。`assignRanks`はPhase 4.3/4.4まで未使用のため**tree-shakeされバンドルに入っていない**。**production影響**：`src/core/engine/`・`gods.ts`・`cards/`・`enemies.ts`・`stakes.ts`・`deckBuilder.ts`・`dailyBoss.ts`・`dailyStart.ts`・`src/core/types/`の差分**0**（寿楽balance変更の混入なし）。`rules.ts`は`RULES.replay.pendingRuns`の追加のみ。**saveVersionは9のまま**、Daily seed・3回制限・JST reset・score式も無変更。**Failure Gate 11項目すべてクリア＝PASS／Phase 4.3 Backend GO**。**Known Risk**：(1)**UI配線の検証はソース検査**で、React描画テスト基盤（@testing-library/jsdom）が無いため`useGameEngine`自体をレンダリングして検証できていない。記録の中身は441runで検証済みだが**実機でのDailyプレイ→提出待ちrun生成の目視確認は未実施**（Phase 4.3で本番QAが必要）、(2)`dailyRunLogAvailable`を表示に使っておらず、記録できていないrunをプレイヤーへ伝えるUIは未実装、(3)途中離脱が記録されないことの告知が無い、(4)localStorageが使えない環境では記録が残らない（ゲームは続行できるが提出不可）、(5)同じ日に3回挑戦すれば控えが3件溜まる——**どれを提出するか（ベストのみか全件か）はPhase 4.3のBackend仕様として未決定**、(6)`maxActions=400`は実測max34から見て余裕が大きく本番ログが集まれば下げる余地がある、(7)バンドル+7.0kB、(8)**同点密度の実装（Backend/UI）は未着手でProduction公開前の必須要件として残る**、(9)寿楽のG1 FAILは指示どおり別Balance Patchとしてbacklog、(10)`useGameEngine.ts`の`setStakeResult(null)`重複は今回も未修正。詳細は`docs/PHASE4_DAILY_ACTION_LOG.md`。branch `feat/daily-ranking-phase4`、**merge・push・deployいずれも未実施**。**Neon・DB・Vercel Functions・Ranking API・ランキングUI・anonymous account・display nameはいずれも作成していない** |
| 2026-09-07 | 決定134を追加。「Phase 4.3：Daily ランキングBackend（契約なしで完成させ、外部契約・deployの直前で停止）」（**AI判断**・CLAUDE.md §6-2。CEOは「Phase 4.2承認。Phase 4.3 Backendへ進め」を指示）。**構えの判断**：§6-3によりNeon契約(#6)・匿名IDの本番運用(#7)・Production公開(#8)はCEO判断事項でAI側では実行できない。しかし「契約が決まるまでBackendを書けない」わけではないため、**契約に依存する部分だけを外に出す設計**にした——①保存層を`RankingStore`（6メソッド）のポートにし実体はメモリ実装のみ提供、②Postgres DDL（`src/server/ranking/schema.sql`）は書くが**実行しない**、③**`api/`ディレクトリを作らない**（リポジトリ直下に`api/`があるとVercelが次のdeployで自動的にエンドポイントを公開してしまい、merge時に「deployしない」を意図せず破るため。HTTPの受け口は`src/server/ranking/http.ts`に置く）、④kill switch `RULES.ranking.submissionEnabled=false`でクライアントは通信ゼロ・サーバーは503、⑤`handleRankingRequest({method,path,body,query})→{status,body}`のホスティング非依存インターフェース。結果として**Backendの中身は完成しテストで動作を確認できる**状態になり、残るのは契約と薄いラッパー1本だけになった。**信頼の境界**：clientが送れるのは`playerId`/`clientRunId`/`ReplayInput`の3つのみ。score・win・HP・rngCursorは型にフィールドが無く送っても無視される。サーバーは`dailyKey`からseed・敵を再導出し本番エンジンで再生して`VerifiedOutcome`を算出、**保存するのは計算値だけ**。レスポンスにも申告値を反射しない（`score:999999`を混ぜても返るのは検証済みスコア）。検証も順位規則もcoreの実装を借り、**サーバー側に`getFinalScore`の再実装が無いことを境界テストで機械検査**した（規則が2か所に存在しない）。**受理の処理順**：①識別子の形式②冪等判定③提出試行のレート制限（`maxSubmitAttemptsPerDay=30`）④今日のDailyか（JST基準）⑤リプレイ検証⑥1日3回⑦保存。**重要な設計判断①：冪等判定をレート制限より先に置く**——送信成功したがレスポンスを受け取れなかったclientは同じ`clientRunId`で再送する（決定133の設計）。これを試行として数えると**通信が不安定な人ほど枠を失う**ため、受理済みrunの再送は枠を消費しない。**重要な設計判断②：冪等でも必ず再検証する**——再送のペイロードを再生し、score・rngCursor・godIdが保存済みと一致して初めて「同じrunの再送」と認め、違えば`RUN_ID_CONFLICT`で拒否する。これが無いと「一度正当なrunを通してから同じIDで中身だけ差し替える」攻撃が通る。**重要な設計判断③：今日のDailyしか受け付けない**（`STALE_DAILY_KEY`）——Daily seedは公開情報なので日付を絞らないと「昨日の敵を一晩かけて最適化して翌日提出する」ができてしまう。JST境界（14:59:59Z＝当日／15:00:00Z＝翌日）をテストで固定。**重要な設計判断④：時刻を注入する**（`submitRun(request,{store,now})`）——サーバー内部で`Date.now()`を呼ばないことを境界テストで機械検査。日付境界を厳密に再現でき実装が決定論的になる。**【方針確定】1日3回のうちどれをランキングへ出すか＝その日のベスト1件**（決定133 Known Risk 5をここで確定・**AI判断**）。**却下①全3件を並べる案**：1人で上位を占められ「その日の最高スコアを狙う」というDailyの趣旨（決定131）と合わず競技として成立しない。**却下②最後の1件だけ**：決定131 Step 7の「3回は同じ問題を学習して詰め直す機会」という性質と矛盾する。保存は3件すべて残し（監査可能性）、**表示のときに1人1行へ畳む**。**順位は必ず`assignRanks`（決定133）を通す**——サーバーで独自にソートすると「同点は同順位」「先着は順位を決めない」が静かに崩れるため。提出時刻順で`assignRanks`へ渡す＝**同順位内の表示順にしか効かない**。自分の行は圏外でも`self`として必ず返す。1,000人・137種（決定131 §6-2の実測規模）でも規則が保たれることをテストで固定。**匿名プレイヤーID**：**これはアカウントではない**。ログインもメールも表示名も無く、端末で生成した乱数16進32桁をlocalStorageに置くだけ。目的は「同じ端末からの3回を1人ぶんとして数える」ことのみ。個人情報を含まず・端末間で同期せず・消せる（`clearAnonymousPlayerId`）・独自ID実装をしない（`crypto.getRandomValues`のみ）。**残る性質**：localStorageを消すか別ブラウザを使えば別人になり1日3回を回避できる——匿名を保つ以上避けられず、実名アカウントは§6-3 #7のCEO判断事項。**提出クライアント**：kill switch offの間は何もせず`disabled`を返す（通信ゼロ）。通信手段は`transport`注入で`fetch`を直接呼ばず、テストは実サーバーをメモリ実装の上で直接呼ぶループバックでend-to-endを確認。受理→控え削除／一時失敗(5xx・429)→**控えを残して再送**／恒久拒否(その他4xx)→控え削除。通信例外でも控えは失わない。**UIへの接続はしていない**（`flushPendingRuns`はまだどこからも呼ばれずバンドルにも入らない）。**QA**：tsc PASS、oxlint PASS（警告2件は既存・無関係）、**vitest 237 files / 2,908 tests 全PASS**（新規5ファイル・58テスト。既存テストは1件も変更していない）、clean build PASS。Phase 4.0 harnessは`parity.audit.ts` 7/7 PASS・`out/*.json`のmd5完全一致＝再simulationなし。Phase 4.1/4.2は66ファイル633テストPASS。**バンドル**：CSSはハッシュまで同一（変更ゼロ）、JSは383,480→383,570B（**+90B**＝`RULES.ranking`の設定値のみ）。**`src/server/**`はバンドルに入っていない**（ブラウザ側から到達しないことを境界テストで機械検査）。`rankingClient`もUIから呼ばれないためtree-shake済み。**production影響**：`src/core/engine`・`src/core/types`・ゲームデータ（`gods.ts`/`cards/`/`enemies.ts`/`stakes.ts`/`deckBuilder.ts`/`dailyBoss.ts`/`dailyStart.ts`）・`src/core/replay`・`src/components`の差分**すべて0**。既存ファイルの変更は`rules.ts`（`RULES.ranking`追加）のみ。**saveVersionは9のまま**、Daily seed・3回制限・JST reset・score式も無変更。**Failure Gate全項目クリア＝PASS**。**Known Risk**：(1)**本番環境が無く実DBでの挙動（一意制約違反・同時実行・接続断）は未検証**、(2)匿名IDは端末単位でlocalStorage削除により3回制限を回避可能、(3)**同時実行の競合が未対策**——メモリ実装は逐次前提で、実DBでは「3回目の判定と挿入」の間に割り込まれると4件入りうる。Postgres実装時にトランザクションか主キー`(daily_key, client_run_id)`＋挿入後カウントで閉じる必要がある（主キーは`schema.sql`に用意済み）、(4)レート制限はプレイヤーID単位のみでIP単位はホスティング側に委ねる想定（IPを扱うとプライバシー方針が変わりCEO判断の材料が要る）、(5)クライアントの送信タイミングが未接続（Phase 4.4）、(6)リーダーボードのページングが無い（上位100＋自分の行のみ）、(7)決定133からの継続＝記録できないrun・途中離脱の告知UI未実装／実機QA未実施、(8)寿楽のG1 FAILは別Balance Patchとしてbacklog、(9)`useGameEngine.ts`の`setStakeResult(null)`重複は未修正。**Phase 4.4に残るCEO判断事項**：①**Neon契約・Vercel plan変更**（§6-3 #6）②**匿名IDの本番運用**（§6-3 #7）③**Production公開**（§6-3 #8）。これらの承認後に、`RankingStore`のPostgres実装・`api/`の薄いラッパー・kill switchのtrue化・ランキングUI・実機QAへ進む。詳細は`docs/PHASE4_RANKING_BACKEND.md`。branch `feat/daily-ranking-phase4`、**merge・push・deployいずれも未実施**。**Neon契約・Vercel plan変更・`api/`公開・実ネットワーク送信はいずれも行っていない** |
| 2026-09-07 | 決定135を追加。「Phase 4.4：Pre-Production DB Validation（実DB検証の準備完了・実行はStep 1でブロック）」（**AI判断**・CLAUDE.md §6-2。CEOは①Neon Free承認 ②匿名ID承認 ③Production公開は未承認 ④Vercel有料plan変更は未承認）。**Step 1はブロックにより未実施**：環境にDB接続情報・psql・docker・vercel/neonctl・`.env`のいずれも存在せず、Chromeで`console.neon.tech`を開いたところ**未ログイン**だった。アカウント作成にはOAuthかメール＋パスワード入力が必要であり、**アカウント作成と認証のためのパスワード入力はAI側では実行しない**（CEOが明示承認した作業でもこの一線は越えない）。**有料契約・payment method登録の画面には到達していない**ため、CEOの停止条件（有料契約が必須になったら停止）とは別の理由での停止である。タブは閉じ、認証情報の入力は一切行っていない。したがって**Phase 4.4の本来の目的（実DBでの検証）は達成していない**。CEOがNeon Free（https://console.neon.tech/signup）でプロジェクトを作り接続文字列を用意すれば、`npm i -D @neondatabase/serverless` と `RANKING_DATABASE_URL=<接続文字列> npx vitest run src/server/ranking/postgres.integration.test.ts` の2コマンドで実DB検証13件が走る（**追加のコード作業は不要**）。**スキーマ再監査（Step 3）で3点を修正**：(1)テーブル名をCEO指定の`players`/`daily_runs`へ改め、提出試行カウンタは独立テーブルにせず`players`の列（`attempt_day`/`attempt_count`）へ畳んだ（当日ぶんだけ持てばよく履歴用テーブルが不要＝「不要なtable追加禁止」を満たす。**テーブルは2つだけ**）、(2)**1日3回の上限がDBに表現されていなかった**（Phase 4.3のKnown Risk #3の原因）ため`attempt_no`列＋`CHECK (attempt_no BETWEEN 1 AND 3)`＋`UNIQUE (daily_key, player_id, attempt_no)`を追加、(3)上限「3」を`.sql`へ直書きすると`RULES.daily.attemptsPerDay`と二重管理になり不変ルール4に反するため**DDLをTSから生成**する方式（`schema.ts`）へ変え、`schema.sql`は削除した。**同時実行の解消（Step 4・Known Risk #3）**：「countしてからinsert」をやめ、**挿入そのものを上限判定の最終権限**にした。`attempt_no`を`INSERT ... SELECT COALESCE(MAX(attempt_no),0)+1 FROM daily_runs WHERE ...`でINSERT内部で決めるため隙間が存在しない。同時に2つ来た場合は①同じ番号を狙って**UNIQUE違反(23505)**→数え直して再試行、②上限超過なら**CHECK違反(23514)**→`attempts-exceeded`。**ロックもトランザクションも使わずDB制約だけで保証できるため、これを「最小かつ正しい方式」と判断した**。`RankingStore.insertRun`の戻り値を`void`から`{ok,attemptNo}|{ok:false,reason}`へ変更し、`submitRun`は事前countを速い経路として残しつつ**判定の権限は挿入側**に移した。**検証（実DBではなくメモリ実装＋強制レース）**：事前countの直後に必ず割り込みが入るようストアを包み、上限判定が事前countに依存していれば必ず4件以上入る条件で検証した。2/3/4/10並列いずれも保存は最大3件、割り込みを50回に増やした最悪条件でも3件。別プレイヤー3人×5並列でも各人ちょうど3件。同じ`clientRunId`の6並列は保存1件（全リクエストは成功＝冪等）。**これは実DB検証ではなく「判定が挿入側へ移っている」ことの証明**である。**3回制限（Step 6）**：1〜3回目PASS・4回目`ATTEMPTS_EXCEEDED`。使い切ったあとでも受理済みrunの再送は通る（枠を消費しない設計と両立）。**冪等性（Step 5）**：同一payload再送×3はすべて`duplicate`で保存1件・試行カウントも増えない／同じ`clientRunId`＋改ざんpayloadは`RUN_ID_CONFLICT`／他人のrun IDを名乗ると`BAD_IDENTITY`／同時挿入時は相手の登録を正として`duplicate`。**Replay検証（Step 7）**：DBへ保存されるscoreは必ず`ReplayInput→本番エンジン→VerifiedOutcome`の計算値で、`SubmitRequest`にscore/win/HP/rngCursorのフィールドが存在しないため**client申告scoreがDBへ入る経路はゼロ**（型・実装・テストの3層で固定）。**Postgres実装（Step 2）**：`postgresStore.ts`は`@neondatabase/serverless`も`pg`もimportせず、SQL実行関数`SqlExecutor`を**注入**される設計にした。これにより`src/server`は「外部パッケージimport 0件」を保ち（境界テストで検査）、ドライバを差し替えても影響はこのファイルに閉じる。DB固有コードもここだけに隔離し、`submit.ts`/`leaderboard.ts`はSQLを1行も持たない。**障害テスト（Step 9）**：接続失敗・timeoutは握りつぶさず例外として上げる／主キー違反(23505)は`duplicate`／CHECK違反(23514)は`attempts-exceeded`／`attempt_no`競合は数え直して再試行し上限+1回で打ち切り**無限ループしない**／未知のSQLSTATE(42P01等)は投げ直す／不正ReplayInput・stale Daily・改ざんrun・4回目はすべて拒否しDBへ書き込まない／拒否メッセージに接続情報・SQL・ホスト名を含まない。**DB障害でゲームは壊れない**——ランキングは決着後の控えを送るだけの後付けで、対局の進行・保存・再開はランキングを知らず、kill switchが閉じている限りそもそも通信しない。**秘密情報監査（Step 10）で実在のギャップを1件発見・修正**：**`.gitignore`が`.env`を除外していなかった**（`*.local`はあるが`.env`/`.env.production`は拾えない）。CEOが接続文字列を`.env`へ置いた場合commitされうる状態だったため、`.env`/`.env.*`/`*.pem`/`*.key`を追加し`git check-ignore`で実際に無視されることを確認した。tracked files全体と`src`全ソースを走査し、接続文字列・Neonエンドポイント・ロールパスワード・APIキー・Bearer・秘密鍵・AWSキーの混入**0件**、クライアントバンドルへの混入**0件**、consoleへ接続情報を出す記述**0件**、接続情報の読み取り箇所は実DB統合テストの1か所のみ（`process.env`経由）。検査は`secrets.test.ts`が機械化しており、**検出しても値は出力しない**（ファイル名とパターン名のみ）。**Preview（Step 11）は意図的に未実施**——Vercel有料plan変更が未承認で、Preview Functionの利用条件をCEOが確認中のため。Production URLには一切触れていない。**QA**：tsc PASS、oxlint PASS（警告2件は既存・無関係）、**vitest 241 files / 2,937 passed ＋ 13 skipped 全PASS**（新規4ファイル・29テスト＋実DB13件はskip。既存テストは1件も変更していない）、clean build PASS。Phase 4.0 harnessはparity 7/7 PASS・`out/*.json`のmd5完全一致＝再simulationなし。Phase 4.1/4.2/4.3も全PASS。**バンドルは1バイトも変わっていない**（CSS・JSともハッシュ同一）——Phase 4.4の作業はすべてサーバー側でクライアントへ届かない。**production影響**：`src/core`・`src/components`・`src/hooks`・`rules.ts`の差分**すべて0**。変更したのは`src/server/**`と`.gitignore`だけ。Daily seed・3回制限・JST reset・saveVersion(9)・score式・神balanceいずれも無変更。**Failure Gate 10項目すべてクリア**。ただし判定は「**コード面はPASS／実DB検証は未実施**」であり、「実DBで壊れないことを確認した」ではなく「実DBで壊れない設計になっており、そう検証できる状態が整った」という意味である。**Known Risk**：(1)**実DB未検証**——Postgres制約のSQLSTATE・`INSERT...SELECT`の原子性・Neonドライバの挙動は未確認、(2)**Neon serverlessドライバのAPI形（`neon(url)(text,params)`）は未検証**でバージョン差があれば`connect()`を数行直す必要がある（`SqlExecutor`注入設計なので影響はそこだけ）、(3)`attempt_no`再試行は上限+1回で打ち切るため極端な競合が続くと正当な提出が`attempts-exceeded`になりうる（3並列程度では起こらない）、(4)`daily_runs`の剪定タスク（cron）が未設定（Free Planのストレージには当面問題ない規模）、(5)匿名IDは端末単位でlocalStorage削除により3回制限を回避可能、(6)クライアント送信はkill switchで閉じたままでUI接続・送信タイミングは未実装、(7)Phase 4.2から継続＝記録できないrun・途中離脱の告知UI未実装／実機QA未実施、(8)寿楽のG1 FAILは別Balance Patchとしてbacklog、(9)`setStakeResult(null)`重複は未修正。**Production公開前に残るCEO判断**：①**Neonプロジェクト作成（CEO自身の操作が必要）**②Vercel Hobbyのpersonal/non-commercial条件とSEVEN GODS/SEVENDAOの利用形態の確認（§6-3 #5・#6）③**Production公開**（§6-3 #8）。詳細は`docs/PHASE4_NEON_DB_VALIDATION.md`。branch `feat/daily-ranking-phase4`、**merge・push・deployいずれも未実施**。**Neonアカウント作成・有料契約・課金設定・Vercel plan変更・Preview公開・Production公開はいずれも行っていない** |
| 2026-09-07 | 決定136を追加。「Phase 4.4 follow-up：実DB検証テストの3件のbug修正（Neon HTTPドライバの呼び出し方・複数文DDL・接続文字列の伏字）」（**AI判断**・CLAUDE.md §6-2「バグ修正／テスト方式」）。**経緯**：CEOがNeon Freeプロジェクトを作成し `RANKING_DATABASE_URL` を設定して `postgres.integration.test.ts` を実行したところ、12件FAIL（`Cannot read properties of null (reading 'findRun')`）となった。調査の結果、`store` が null のままだったのは `connect()` が例外を握り潰して null を返し、`beforeAll` が黙って早期returnしていたためで、12件のFAILは全て同一root cause（`store === null`）の派生だった。**修正1（実バグ）**：`connect()` が `client(text, params)` とドライバを呼んでいたが、`@neondatabase/serverless` v1 の呼び出し可能形は**タグ付きテンプレート専用**で、素の文字列を渡すと即エラーになる（実測：`This function can now be called only as a tagged-template function ... use sql.query(...)`）。`client.query(text, params)` に修正した。`postgresStore.ts` のコメント例は元から `client.query` で正しく、誤っていたのはテスト側のアダプタのみ。**修正2（実バグ）**：`beforeAll` が `sql(buildRankingSchemaSql())` と複数文のDDLを1回で流していたが、Neon の HTTP ドライバは拡張問い合わせプロトコルのため `;` 区切りの複数文を実行できない。`schema.ts` に `buildRankingSchemaStatements(): string[]` を追加し、1文ずつ適用する形にした。`buildRankingSchemaSql()` はその連結として実装し、**出力は従来とバイト単位で同一**であることを一時テストで確認済み（`concurrency.test.ts` のDDL検査4件も無変更でPASS）。schemaの内容・目的・列・制約は一切変更していない。**修正3（秘密情報）**：`neon()` は不正なURLを渡されると**接続文字列そのものを本文に含むエラー**を投げ、それが `expect` のメッセージ経由でテスト出力・CIログへ出る経路になっていた（本ファイル冒頭の「失敗メッセージにも含めない」という自らの方針に反する実装漏れ）。`redactSecret()` を追加し、接続文字列・`Connection string:` 以降・`postgres(ql)://…` を伏字にしてから外へ出すようにした。あわせて、接続文字列が設定されているのに初期化できない場合は `beforeAll` で**理由を明示して**落とすようにし、無関係な null 参照12件で原因が埋もれないようにした。**未検証**：本修正時点でCEO環境の `RANKING_DATABASE_URL` の値が接続文字列ではない文字列だったため、実DBに対する13件の検証は**未実施**。修正1は実ドライバに対する実測で確認済み、修正2はドライバのプロトコル仕様に基づく修正で実DB未確認。Daily仕様・Replay仕様・score式・ランキング規則・DB schemaの目的は無変更。`@neondatabase/serverless@1.1.0`（devDependency・MIT・依存0件）をlockに追加（lockの増減は+1件のみ、削除0件）|
| 2026-09-08 | 決定137を追加。「Phase 4.4：実DB検証の実行完了 ＋ credential 露出対応 ＋ Production Security/Fairness Gate の切り出し」（**AI判断**。CLAUDE.md §6-2。CEOはSecrets監査・実DB再監査・回帰の実施を承認し、merge/push/deploy/Vercel変更/Production API有効化を明示的に禁止）。**背景**：決定135でPhase 4.4はStep 1（Neonプロジェクト作成）がブロックされ「実DBで壊れない設計になっている」までしか言えない状態だった。決定136でNeon HTTPドライバの呼び出し方等3件のbugを修正済み。今回CEOから接続情報が提供され、Step 1のブロックが解除された。**実DB検証**：`src/server/ranking/postgres.integration.test.ts` をNeon実DBに対して実行し、**Test Files 1 passed / Tests 13 passed ＋ 1 skipped（計14）**（14.45s）。skip 1件は `describe.skipIf(enabled)` のスキップ理由ブロックで実DB有効時は動かないのが正しい挙動。これにより決定135のKnown Risk 1（実DB未検証：CHECK/UNIQUEのSQLSTATE・`INSERT ... SELECT`の原子性・Neonドライバ挙動）と2（ドライバAPI形 `neon(url)(text, params)` 未検証）を**解消**した。**全体回帰（実DB有効のまま）**：`npx vitest run` が **241 files / 2,949 passed ＋ 1 skipped（計2,950）**（76.83s）。決定135記録時（DB無効）の 2,937 passed ＋ 13 skipped との差は「実DBブロック13件がskip→pass、プレースホルダ1件がpass→skip」で**合計2,950は不変・新規failureゼロ**と説明が完全に一致した。`tsc -b` exit 0、`oxlint` exit 0（警告2件は `scripts/phase3-audit/` の既存・無関係）、clean build PASS（2.33s）。バンドルは `index-CLjXu-bP.js` 383,570 B・`index-CDbAuk7l.css` 96.42 kB で**Phase 4.3/4.4記録と同一ハッシュ・同一バイト数＝クライアント無変更**。`dist` への `neon.tech`/`RANKING_DATABASE_URL`/`neondb`/`connectionString`/`npg_` 混入も0件。**Secrets監査（値は一切表示せず実施）**：作業ツリー全体への実credential完全一致検索0件、git履歴**全132コミット・全ref**に対する pickaxe（`git log --all -S`）0件、同じく全コミットのblobレベルで「認証情報付きURL形式」「高リスクトークン形式（`npg_`/`sk-`/`ghp_`/`AKIA`/`xox*`）」の総当たり0件、`.env`系はディスク上に存在せずtracked 0件、stash/reflog 0件。`neon.tech` を含む5ファイルはすべてプレースホルダまたは検知用パターンで実エンドポイントと不一致。`secrets.test.ts` の `npg_` は**値ではなく検知用正規表現**であり、同ファイルが常設の秘密情報混入ガードとして機能していることを再確認した。**結論：旧credentialはコード・docs・git履歴のいずれにも含まれず、露出はチャット経路のみ。リポジトリ側の対処は不要**。**credentialローテーション（CEO承認済み・未実施）**：現行Neonロールパスワードは露出済み資格情報として扱い、Production公開前に必ずローテーションする。ローテーション後に `npx vitest run src/server/ranking/postgres.integration.test.ts` を再実行し **13 passed / 1 skipped** を再確認するまで **Phase 4.4 を Final PASS としない**。**依存関係**：`npm audit` で **1 high**（`nanoid` < 3.3.18・GHSA-2v37-7h3g-55p8）を検出したが、依存経路は `vite`→`postcss`→`nanoid` で `npm ls nanoid --omit=dev` は空＝**本番依存に到達しないビルド時限定**。CEO指示により `npm audit fix` および脆弱性の自動修正は**実行していない**。AI判断として本件を Phase 4.4 の GO/NO-GO 条件にはせず、vite の upstream 更新に追随して解消する方針とした（強制上書きは vite/postcss ツリー破壊リスクの方が大きい）。`@neondatabase/serverless` は devDependency でありクライアントにも本番依存にも入らない。**Production Security/Fairness Gate の切り出し**：CEO指示により、①匿名identityの不正リセット/偽装対策 ②3 attempts/day を提出回数ではなく実プレイ開始時点で保証できるか ③rules/game version固定 ④日付跨ぎ提出 ⑤rate limit/abuse対策 の5項目を **PSF-1〜5** として `docs/PHASE4_NEON_DB_VALIDATION.md` §17 に整理した。Phase 4.4 には混ぜず次PhaseのGate候補とする。うち **PSF-2 は「best of 3 submissions」と「3回勝負」でゲーム体験が本質的に変わるため §6-3 #1 該当の可能性があり、実装着手前にCEO判断を仰ぐ**とAI判断した（現行設計はプレイ回数ではなく提出回数の制限であり、良い結果だけを提出できる＝実質的な引き直しが無制限であることを明記）。**判定**：Phase 4.4 実DB技術Gate は**暫定PASS**、Final PASS は未達（ローテーション後の再実行が条件）、**Production Release は NO-GO**。merge/push/deploy/Vercel変更/Production API有効化はいずれも未実施で branch `feat/daily-ranking-phase4` を維持。 |
| 2026-09-08 | 決定138を追加。「Phase 4.4：credential ローテーション後の再検証完了 → **Final PASS**」（**AI判断**。CLAUDE.md §6-2。決定137のfollow-up）。**背景**：決定137で Phase 4.4 は暫定PASSとし、Final PASS の条件を「CEOによるNeonロールパスワードのローテーション後に実DB統合テストを再実行し同一結果を得ること」と定めていた。**ローテーション確認（値は非表示で機械検証）**：新しい接続文字列が**露出した旧パスワードを含まない**ことを確認（＝ローテーション済み）。エンドポイントとロール（`neondb_owner`）は旧と同一で、パスワードのみ更新されたNeonの正しい挙動。pooled connection・`sslmode=require` も維持。接続文字列はクリップボードから環境変数へ直接渡し、**チャット・ファイル・docs・commit のいずれにも記録していない**。**再検証結果**：`npx vitest run src/server/ranking/postgres.integration.test.ts` が **Test Files 1 passed / Tests 13 passed ＋ 1 skipped（計14）**（14.91s。初回は14.45s）で、決定137の期待値と**完全一致**。ローテーションによる破壊は無い。**判定の更新**：Phase 4.4 実DB技術Gate を暫定PASS → **PASS**、Phase 4.4 **Final PASS を達成**。ただし **Production Release は NO-GO のまま据え置く**。理由は決定137から変わらず、`docs/PHASE4_NEON_DB_VALIDATION.md` §17 の Production Security/Fairness Gate（PSF-1〜5：匿名identity偽装・3 attempts/dayの実プレイ開始時点保証・rules version固定・日付跨ぎ提出・rate limit/abuse）が未監査であるため。**Phase 4.4 の完了は「DBが正しく動く」ことの証明であって「公開して不正・不公平が起きない」ことの証明ではない**とAI判断として明記する。**残る運用上の注意**：旧パスワードがNeon側で確実に失効しているかはCEOのコンソール確認に依存する（機械的には「新credentialが旧と異なる」ことまでしか確認できない）。**未実施の維持**：merge / push / deploy / Vercel変更 / Production API有効化はいずれも未実施で、branch `feat/daily-ranking-phase4` を維持。 |
| 2026-09-08 | 決定139を追加。「Phase 4.5：Production Security & Fairness Gate（設計監査）— run ticket 方式による『1日3回勝負』のサーバー保証、client-held secret、day-locked gameVersion」（**AI判断**。CLAUDE.md §6-2。CEO指示 KAGURA / Phase 4.5 に基づく設計監査のみで、本番実装・deploy・Neon schema変更は行わない。詳細は `docs/PHASE4_5_PSF_GATE.md`）。**背景**：Phase 4.4 Final PASS（決定138）で実DB技術Gateは通過したが、Production Release は「公開して不正・不公平が起きないか」が未監査のため NO-GO（決定137 §17）。**監査で確認した現行の事実**：①`playerId` はリーダーボードで公開されるのに、それ自体が唯一の資格情報（`leaderboard.ts` L58／`anonymousPlayerId.ts`）＝他人の枠を食い潰す griefing が可能 ②3回制限は `daily_runs` の CHECK/UNIQUE で**提出**にしか効かず、seed は `daily-${dateKey}-${enemyId}` で完全決定（`dailyBoss.ts` L87）なので無制限に練習して良い3回だけ提出できる ③`submitRun` は `recordAttempt`（DB書き込み）を日付検査・リプレイ検証より前に行い、形式が正しい `playerId` なら誰でも `players` 行を作れる ④23:59開始→00:01提出は `STALE_DAILY_KEY` で正当プレイヤーの run が消える ⑤`ReplayInput` に版が無く deploy 跨ぎで比較不能になりうる。**PSF-2 の定量（本番エンジン・本番記録経路・`runReplay` 検証込み、全7神×300試合＝2,100試合）**：同じ腕（疑似乱数方針固定）で 300 回遊んで最良を出すと best-of-3 の **1.475〜2.261 倍**（ebisu 528→869、taiyo 453→876、juraku 355→759 等）。best-of-30 で既に 1.3〜2.0 倍。**提出回数制限では公平性が成立しない**ことを数値で確定した。**推奨アーキテクチャ（1案・比較の結果）**：(a) PSF-1＝client-held 256bit secret → 公開ID＝SHA-256(secret) 先頭32桁（追加API 0・追加secret 0・追加DB 0。HMAC署名token は秘密が増え issue が spam 面になるため、HttpOnly cookie は sybil 耐性が同じで配信が複雑化するため却下）(b) PSF-2＝**run ticket**：`POST /api/ranking/start` で identity 照合→サーバー時刻の dailyKey→枠を予約。ticketId＝clientRunId。1 identity・1日の open ticket は常に1枚、別 clientRunId の start で前の open は abandoned（消費）。枠の消費は ticket 発行時（提出・期限切れ・放棄とも消費、deploy跨ぎの voided のみ返還）。`/submit` は ticket を持つ run しか受理せず、ticket 無しは**書き込み0**で拒否（god/deck の束縛は情報が増えないため不採用、hidden nonce は「shared seed」制約に反するため次々Phase送り）(c) PSF-3＝**day-lock**：その日の最初の ticket が `gameVersion` を固定し、版が変わったら新規 ticket を発行しない（`RULES_VERSION_LOCKED`）。open ticket の提出は void＝返還。`gameVersion`＝手動 `engineVersion`＋ランキング影響データの FNV-1a fingerprint、engine 変更は golden replay test で bump を強制（運用ルールのみへの依存を排除）(d) PSF-4＝dailyKey はサーバー時刻で決め端末時計は表示専用。`expiresAt = min(issuedAt + 90分, 翌日00:00 + 15分)`。23:59開始→00:01提出は前日ボードに受理、00:20 は期限切れ、ボードは翌日 00:15 で確定（grace 0分は救えず、90分は確定が遅すぎるため 15分を採用）(e) PSF-5＝app層：書き込み前検査の順序固定（identity→ticket→冪等→closed/expired→版→rate→replay→insert）、identity 単位の制限（tickets≤3・submit試行≤30）、body≤64KB、leaderboard 15秒 in-memory cache＋`s-maxage`、外部 cron なしの opportunistic 剪定（`/start` の 1/16）。IP はアプリDBに保存しない。edge層：Vercel WAF rate limit は一次情報（vercel docs 2026-08-28）で **Hobby 利用可（1 rule/project、IP/JA4、fixed window ≤10分、1,000,000 allowed requests 込み）**を確認したが、**本プロジェクトの plan／利用条件は CEO 確認④のまま未確認として分離**し、有料プラン前提の設計はしていない。Neon Free は一次情報で 0.5 GB／100 CU-hours／5 GB egress／autosuspend 5分を確認し、1,000人/日で ≈0.8 MB/日（30日保持で 0.5 GB の 5%）と見積もった。**API は3本（+1 `/start`）、テーブルは +2（`daily_days`・`daily_tickets`）、`daily_runs` に `game_version` 列と ticket への FK**。`STALE_DAILY_KEY` は廃止（ticket の dailyKey 一致で置換）。障害時は `/start` 失敗を**非ランク挑戦**として従来どおりオフラインで遊べる fallback とし、ゲーム本体はランキングを知らないままにする。**検証**：`scripts/phase45-psf/` にリファレンスモデル（`ticketModel.ts`）と状態機械検証 `ticket.audit.ts` **37件 全PASS**（正常3プレイ／4回目start／二重start／並列start 同ID・別ID×{2,3,5,10} 割り込み強制で常に ticket 1枚／reload／TTL内resume／TTL超過／start応答喪失／submit応答喪失／並列submit／差し替え／期限切れ／23:59→00:01／端末時計偽装／複数端末／secret不一致／公開IDのみの griefing／sybil の残余明示／day-lock／void返還と rollback 後の番号再利用／ticket無し200件で書き込み0／不正リプレイ連打30で停止／正当な1日の書き込み 1＋3×3 回／5状態の網羅）。回帰は `npx vitest run` 241 files / 2,937 passed ＋ 13 skipped（DB無効・Phase 4.4 §12 と同一）、`tsc -b` PASS、`oxlint` PASS（既存警告2件）、clean build PASS でバンドル `index-CLjXu-bP.js` 383,570 B 同一（`src/` 無変更）。**Gate判定**：PSF-1 CONDITIONAL PASS（偽装は塞げるが sybil は匿名の帰結。MVP は「同一端末・同一ブラウザ内の公平」と定義）／PSF-2 PASS（設計）／PSF-3 PASS（設計）／PSF-4 PASS（設計）／PSF-5 CONDITIONAL PASS（app 層 PASS、edge は CEO 確認④待ち）。**Production Ranking 総合：CONDITIONAL GO（設計）／deploy は NO-GO**。**残余リスク（最大）**：C層（script）のオフライン最適解探索と save-scumming は seed 公開・決定論・クライアント側演算の帰結でサーバー側 hidden information 無しには塞げない。TTL 90分×3 ticket で 1 identity あたり最大 270 分に上限化されるが sybil と組めば無制限。**「人間が UI で遊ぶ範囲の公平」を MVP の保証範囲**とし、hidden nonce（seed 撹乱）は「shared seed」概念の変更を伴うため §6-3 #1 の CEO 判断事項として次々Phase候補に記録。**CEO 判断が新たに必要な項目は無し**（既存の未決事項：CEO 確認④ Vercel plan／利用条件、Production 公開承認 §6-3 #8 のみ）。**次Phase（4.6 想定）実装範囲**：playerSecret／ticket store・`/start`・`submitRun` 改修／`gameVersion`＋golden test／クライアントの start→ticket→START_GAME と非ランク fallback／leaderboard cache・body 上限・剪定／数値の `rules.ts` 集約（`ticketTtlMinutes` 90・`dayEndGraceMinutes` 15・`maxBodyBytes` 65536・`leaderboardCacheSeconds` 15・`pruneEveryStarts` 16）／実DB再検証／`secrets.test.ts` への `playerSecret` 検査追加。Neon の移行 SQL は CEO 承認後に適用。merge / push / deploy / Vercel変更 / Production API有効化 / Neon schema変更はいずれも未実施で branch `feat/daily-ranking-phase4` を維持。 |
| 2026-09-08 | 決定140を追加。「Phase 4.6：PSF Production Implementation（run ticket・client-held secret・day-locked gameVersion を本番コードへ最小実装）」（**AI判断**。CLAUDE.md §6-2。CEO指示 KAGURA / Phase 4.6 により、決定139／`docs/PHASE4_5_PSF_GATE.md` §8〜§14 を実装仕様として実装。詳細は `docs/PHASE4_6_PSF_IMPLEMENTATION.md`）。**PSF-1 identity**：端末が持つのを公開値から**256bitの秘密**へ変え、公開IDを `SHA-256(playerSecret)` の先頭32桁にした。導出は `src/core/identity.ts` の**1実装だけ**とし、サーバーは `deps.ts` 経由で同じ関数を借りる（クライアントとサーバーで二重実装するとズレて全員が弾かれるため）。サーバーは秘密を保存せず、DBに列も無く、応答・ログにも出さない。照合は定数時間比較で、形式不正ならハッシュ計算もしない。**移行**は旧 `sevengods.playerId` を引き継がず破棄して新しい秘密を発行する方式を採った（旧IDは公開値と同一で秘密として使えず、送信は kill switch で止まったままなのでサーバー側に旧IDのデータが1件も存在せず、引き継ぐ資産が無い。公開値と同じ資格情報を残す方が危険）。**PSF-2 run ticket**：枠の消費を「提出」から「開始」へ移した。`POST /ranking/start` 相当の `startRun` が identity照合 → **サーバー時刻**の dailyKey決定 → day-lock → 冪等判定 → 残枠確認 → 進行中の放棄 → 発行 の順で処理する。状態は open / submitted / expired / abandoned / voided の5つで、`closed_reason`・runの有無・時刻から導く（状態列を持たない）。不変条件は ①identity・日につき open ≤ 1 ②非voided ticket ≤ 3（DBの CHECK＋部分UNIQUE）③runは必ずticketを持つ（FK）④同一 `clientRunId` のstartは冪等 ⑤拒否時は書き込み0 ⑥voidedだけが番号を返還。**枠が残っていないときは open ticket を放棄しない**（最後の1回を再開できる）、**並列startの番号衝突では再採番せず**生きているticketを返す、の2点を明示的に実装した。**Submit改修**：`playerSecret` と ticket を必須化し、書き込み前検査の順序を固定した（身元→ticket→冪等→closed/expired→版→**ここで初めて書く**→リプレイ検証→保存）。`attempt_no` は ticket が決めた番号を写す。新コードは `NO_TICKET` 404 / `TICKET_CLOSED` 409 / `TICKET_EXPIRED` 410 / `RULES_VERSION_MISMATCH` 409（refunded）。**`STALE_DAILY_KEY` は廃止**し、別日を名乗ればticketが見つからず `NO_TICKET` になる形に置換した（端末時計を判定に使わない）。受理済みなら期限後の再送も duplicate として受け取れる。**PSF-3 gameVersion**：`<engineVersion>.<dataFingerprint>` とし、fingerprint は `RULES`（`ranking` と `replay.pendingRuns` を除く）＋カード＋敵＋神＋OTOMO の安定化JSONを FNV-1a（2初期値・16進16桁）で要約する。データ変更は自動で拾われ、reducer/スコア/RNGの挙動変更のみ手動 `engineVersion` で表明する。**golden replay test** が「固定リプレイの結果が変わったのに gameVersion が変わっていない」状態を落とすため、上げ忘れが機械的に検出される。day-lock は `daily_days` がその日の版を固定し、版が変わった日の**新規開始**を拒否（423）。進行中runが版を跨いだ提出は不受理だが**枠を返す**（voided）。現在値 `1.da595899c6a9db43`。**PSF-4 日付跨ぎ**：`expiresAt = min(issuedAt + ticketTtlMinutes(90), 翌日00:00 JST + dayEndGraceMinutes(15))`。23:59開始→00:01提出は前日ボードに入り、00:20は `TICKET_EXPIRED`。00:01開始は翌日の枠。クライアントは `serverNow - receivedAt` を時計ずれとして補正して残り時間を出す。**PSF-5 abuse guard**：body上限65536をJSON解析・検証より前に切る（413）、ticket無しの提出は読み取り1回で拒否し書き込み0、提出試行上限30、leaderboard は store単位の15秒キャッシュ＋`Cache-Control: public, s-maxage=15, stale-while-revalidate=60`、剪定は外部cron無しで `/start` の約1/16に実施（`daily_days` を消せばCASCADE）。IPはアプリDBに保存しない。正当な1日（3挑戦3提出）の書き込みは9回。数値はすべて `rules.ts` へ集約（`ticketTtlMinutes` 90・`dayEndGraceMinutes` 15・`maxBodyBytes` 65536・`leaderboardCacheSeconds` 15・`pruneEveryStarts` 16・`playerSecretLength` 64・`engineVersion` 1）。**Client integration**：開始を `GameFlow.beginDailyChallenge` の1か所へ集約し、**start → ticketの控え → START_GAME** の順を実装した。二重クリックは state ではなく **ref** で遮断する（stateだと同一tick内で2回通り、1操作で枠を2つ失うため）。サーバーが決めた dailyKey を採用し、reload/resume は控えたticketと `clientRunId`・`dailyKey` が一致すれば ranked のまま復元する。**`startRankedRun` は新しい枠を取る前に送り残しを flush する**——これが無いと「決着したが通信断で未送信のrun」を持ったまま次を始めた瞬間にそのticketが `abandoned` になり、正当に遊び切った結果が二度と提出できなくなる（実装中にテストで発見し修正）。**Offline fallback**：kill switch off・サーバー不達・5xx・更新直後（423）・乱数不可 のいずれでも**Dailyは遊べる**（ランキング対象外になるだけ）。開始を止めるのは**枠切れ（409）だけ**とした。結果画面に「この挑戦はランキングに記録されません」、Daily画面に「始めると1回使う／途中でやめても・別の挑戦を始めても・90分を過ぎても戻らない」を開始前に表示する。**テスト**：本番コードに対して 37 シナリオを再現（`ticket.test.ts` S1〜S9／`identity.test.ts`／`concurrency.test.ts`／`submit.test.ts`／`http.test.ts`／`rankingClient.test.ts`／`rankingTicket.test.ts`）。sybil は「防げないこと」を明示的にテストで固定した。`secrets.test.ts` に `playerSecret` のログ・保存・URL・DB列の検査を追加。**回帰**：`npx vitest run` 245 files / **3,044 passed ＋ 18 skipped**（Phase 4.5 は 241 / 2,937＋13。+4ファイル・+107テスト。skipの18は実DB統合テスト）、`tsc -b` PASS、`oxlint` PASS（既存警告2件のみ）、clean build PASS。Phase 4.0 parity 7/7 PASS（Daily条件が無変更であることの確認）、Phase 4.5 PSFハーネス 38/38 PASS。バンドルへのサーバー・DBドライバ・秘密の混入は10パターンすべて0件。`saveVersion` は 9 のまま、`gameVersion` は `1.da595899c6a9db43` のままで**ゲームデータ・通常モードは無変更**。**未実施（CEO指示による）**：Neon本番schemaへのmigration適用と実DB統合テストの実行。`daily_days`/`daily_tickets` の追加と `daily_runs` の列追加は既存DBに対して後方互換ではなく、統合テストの実行は**スキーマ適用そのもの**になるため走らせていない（テストは13件→18件へ拡張済み）。移行SQLの作成・適用は次の DB Migration Gate。**Known Risks**：①実DB未適用・未検証 ②多重クリックで別 `clientRunId` が直列に並んだ場合、Phase 4.5 §12 の「1枚に畳む」はサーバー側の番号衝突が真に同時のときしか働かず、直列化すればそれぞれが「新しい挑戦」として枠を消費する（仕様どおりの挙動でもあり、実際の防波堤はクライアント側の契約＝IDの1回発行とrefによる入口一本化。サーバーの公平性は常に保たれる）③C層のオフライン最適化・save-scumming（決定139 §16-1、設計上の残余）④sybil ⑤Vercel Firewall 未設定・plan未確認（CEO確認④）⑥`daily_runs` の CHECK 違反は内部矛盾として投げ直す設計へ変更（ticketが番号を保証するため、発火＝データ破損の合図）。**判定**：Phase 4.6 実装は **PASS**、実DB再検証は未実施、**Production Release は NO-GO 据え置き**。merge / push / deploy / Vercel変更 / Production API有効化 / Neon本番schema変更はいずれも未実施で branch `feat/daily-ranking-phase4` を維持。 |
| 2026-09-08 | 決定141を追加。「Phase 4.7：DB Migration Gate（Design & Dry Run）— Phase 4.4 schema → Phase 4.6 schema の forward migration package」（**AI判断**。CLAUDE.md §6-2。CEO指示 KAGURA / Phase 4.7。**Neon 本番へは接続も DDL 実行も適用も行っていない**。詳細は `docs/PHASE4_7_DB_MIGRATION_GATE.md`、成果物は `scripts/phase47-migration/`）。**現行と目標の確定**：Phase 4.4 schema は git `9ebbfd6` の `schema.ts` から（`players`・`daily_runs`・`daily_runs_board_idx`）、目標は現行 `schema.ts`／`postgresStore.ts` から確定した。差分は `daily_days`・`daily_tickets` の新設、`daily_runs.game_version NOT NULL` と `daily_runs_ticket_fk` の追加。CEO指示の `daily_tickets.created_at` は Phase 4.6 実装では `issued_at` が同じ役割で、実装と完全一致させる方針により列名は変えない。**legacy data（1案）**：`daily_runs → daily_tickets` の FK を **NOT VALID** で追加し、既存行を検査しない。削除（禁止）・偽 ticket 生成（「開始時に枠を予約した」事実の捏造）・FK を付けない（不変条件③が DB で守られない）を却下。**gameVersion backfill（1案）**：番兵値 **`legacy-4.4`** を backfill してから NOT NULL。NULL 許容（`toRun` の型不整合・src 変更が要る）と現行版での backfill（Phase 4.6 以前の run を現在の版で検証した結果として偽装する）を却下。番兵値は `<整数>.<16進16桁>` 形式から外れ、現行版と決して一致しない。**順序（8文・1トランザクション）**：daily_days → daily_tickets（制約名すべて明示）→ 部分UNIQUE → `ADD COLUMN IF NOT EXISTS game_version`（NULL許容・DEFAULT無し＝メタデータのみ）→ backfill → `SET NOT NULL` → `DO $$` で `pg_constraint` を見てから FK NOT VALID → board index。forward-only・冪等・非破壊・再実行可能。**エラー処理の修正（本Gateで src に加えた唯一の変更）**：`postgresStore.insertTicket` の 23514 は `daily_tickets_attempt_range` のときだけ `attempts-exceeded`、他の制約名は投げ直し、名前が無いドライバでは消費済み枠を数えてから判断する。23505 の判別も `includes` から**制約名の完全一致**へ。制約名は `schema.ts` の `CONSTRAINT_NAMES` に集約し、`migration.ts` の `NAMES` と一致することをテストで固定。**Dry Run**：ローカルに Postgres が無いため **PGlite（PostgreSQL 18.3 を WASM でプロセス内実行）** を devDependency に追加し、**本物の Postgres** 上で A 空DB（migration は失敗して何も残らない・fresh DDL は通る）／B 4.4のみ（migration 後の列・制約・index が fresh 4.6 と完全一致、唯一の差は FK の NOT VALID）／C 4.4＋legacy 4行（行不変・番兵版・偽 ticket 0・ボードに残る・ticket無し INSERT は 23503）／D 8文のどこで止めても再実行で収束／E 完了後の再実行は no-op（transaction 経路・1文ずつ経路とも）、制約の実挙動（CHECK/部分UNIQUE/PK/closed_reason/day-lock/CASCADE）、本番コードでの 3枠上限・並列start×10・同ID×10・void refund・同ticket×4・day-lock、preflight/postflight の正負（空DB→エラー、drift→NG、PARTIAL→NG、部分UNIQUE欠落／NULL／秘密列→NG）、integration readiness（fresh DDL は migration 済みで no-op、未適用DBへ先に流すと壊れる）の **30件を全PASS**。Neon 実機では未検証（接続禁止）。**成果物**：`migration.ts`（唯一の情報源）から生成した `sql/001_phase46_tickets.sql`（BEGIN〜COMMIT）・`statements.json`（1文ずつ）・`preflight.sql`（20項目・1 SELECT・state 分類と drift 検出）・`postflight.sql`（16項目・不変条件 open≤1／非voided≤3／新規 run は ticket 必須／秘密列無し）、`apply.mjs`（`--apply` 無しでは DDL を流さず、preflight NG で止まり、接続文字列を出力しない）、`RUNBOOK.md`、`ROLLBACK.md`（第一候補は logical rollback＝kill switch OFF→新API停止→旧コード互換→schema は残す。旧コードは読み取りは動くが NOT NULL と FK のため INSERT は失敗するので kill switch で吸収。DROP を伴う destructive rollback は CEO承認事項で `sql/` に置かない）。**回帰**：245 files / 3,045 passed ＋ 18 skipped、tsc / oxlint / clean build PASS、バンドル同一ハッシュ（PGlite 混入 0）、npm audit 増分 0。**Known Risks**：①legacy 行と同じ `daily_key`・同じ `player_id` に新 ticket を発行すると `attempt_no` が衝突し `RUN_ID_CONFLICT` になる（Dry Run で再現。ticket は legacy を数えない）→ 適用は前日ボード確定後〜当日最初の挑戦前（JST 00:15〜）とし、preflight の `runs_day_keys` で確認 ②Neon HTTP ドライバの `transaction([...])` は実機未検証（使えなければ1文ずつ・冪等）③open≤1 は DB 単独では表現できない（アプリ手順＋postflight 監査）④`VALIDATE CONSTRAINT` は legacy 行がある限り不可。**判定**：MIG-1 PASS／MIG-2 PASS（運用条件つき）／MIG-3 PASS／MIG-4 PASS／MIG-5 PASS／MIG-6 CONDITIONAL PASS（順序依存・Neon 実機は次Gate）／MIG-7 PASS。**総合 READY FOR CEO APPROVAL**。Neon 適用・destructive rollback・`VALIDATE CONSTRAINT` は CEO承認事項。merge / push / deploy / Vercel変更 / Production API有効化 / Neon 適用はいずれも未実施で branch `feat/daily-ranking-phase4` を維持。 |
| 2026-09-09 | 決定142を追加。「Phase 4.7 → 適用Gate：Neon Production DB へ Phase 4.6 schema migration を適用完了（CEO承認済み）」（**CEO承認**：CLAUDE.md §6-3 #8・#9 に基づき、2026-09-09 にCEOが【CEO DECISION REQUIRED】へ「承認」と回答。実行・判定は**AI**）。**前提**：branch `feat/daily-ranking-phase4`・HEAD `fc3610f`（決定141の migration package をそのまま使用、差分なし）。kill switch `RULES.ranking.submissionEnabled = false` は閉じたまま。Production API（`api/`）は未作成。**手順1 preflight**（読み取りのみ・20項目すべて ok）：`migration_state = NOT_MIGRATED`、`players`/`daily_runs` 存在、`daily_days`/`daily_tickets` 未存在、`runs_row_count = 0`・`players_row_count = 0`・`runs_legacy_rows = 0`・`runs_orphan_rows = 0`・`runs_unexpected_columns` なし、Phase 4.4 の制約4本と board index は baseline 一致。行が0件のため Known Risk 1（legacy 行と新 ticket の `attempt_no` 衝突）は発生せず、時間帯制約（JST 00:15〜）も不要と判断。**手順2 適用**：`node scripts/phase47-migration/apply.mjs --apply`。8文を Neon HTTP ドライバの **transaction 経路で1トランザクション適用**（決定141 Known Risk 2「transaction 経路が実機で使えるか」は**使えることを確認**）。エラー・再実行なし。**手順3 postflight**（16項目すべて ok）：`daily_days`/`daily_tickets` 作成、`daily_runs.game_version` NOT NULL（NULL 行 0）、`daily_runs_ticket_fk` 存在（`validated = false` は legacy 行対応の設計値）、baseline 制約4・tickets 制約5・部分UNIQUE `daily_tickets_attempt_unique (daily_key, player_id, attempt_no) WHERE closed_reason IS DISTINCT FROM 'voided'`、必須列8、秘密列なし、不変条件 open≤1／consumed≤3／新規 run は ticket 必須の違反 0、legacy 行 0。**手順4 実DB統合テスト**：`postgres.integration.test.ts` → **18 passed / 1 skipped (19)**、26.5s。fresh DDL は migration 済みDBで no-op であることを実機で確認（決定141 MIG-6 CONDITIONAL PASS → **PASS**）。テスト後に postflight を再実行し 16項目すべて ok（不変条件は維持）。**セキュリティ**：接続文字列はクリップボード→シェル環境変数 `RANKING_DATABASE_URL` へ直接渡し、実行後に unset。チャット・ファイル・docs・commit のいずれにも記録していない。`apply.mjs` の出力は接続情報を伏せた状態で確認。なお適用実行は Claude Code の自動モード分類器に2度ブロックされ、CEOが `/permissions` で当該コマンドを個別承認したうえで再実行した（AI が自身に許可を付与する操作は設計上通らない）。**状態**：Neon Production DB は **Phase 4.6 schema（MIGRATED）**。merge / push / deploy / Vercel変更 / Production API有効化はいずれも**未実施**。**残課題**：①`VALIDATE CONSTRAINT daily_runs_ticket_fk` は legacy 行 0 のため技術的には即時可能だが、CEO承認事項（決定141 §20）のため未実施 ②次Phase（4.8）で Production API 実装・kill switch 開放判断 ③本決定は working tree 上の追記であり、commit は CEO 指示待ち。 |
| 2026-09-09 | 決定143を追加。「Phase 4.8：Daily Ranking Production API（Vercel Functions 実装 ＋ Preview QA 準備）」（**AI判断**・CLAUDE.md §6-2「技術実装方式／ファイル構成／テスト方式／QA方式」。CEO指示 KAGURA / Phase 4.8。**Production公開・本番API有効化・merge・push・deploy・`submissionEnabled=true` はいずれも禁止のまま**）。**監査**：Phase 4.6 の `handleRankingRequest()` がホスティング非依存の受け口として既に完成しており、Phase 4.8 で書くのは詰め替え層だけと確認。`vercel.json` は不在（Vite の自動検出で稼働）、`api/` は Phase 4.3 が意図的に未作成、`@neondatabase/serverless` は **devDependencies**（本番インストールで刈られる）、`api/` は `tsc -b` の対象外、クライアント `rankingClient.ts` は既定 baseUrl `/api` で `/ranking/start`・`/ranking/submit` を叩く＝**URL設計の変更は不要**。**実装**：`api/ranking/{start,submit,leaderboard}.ts`（各5行。`rankingRoute()` を呼ぶだけ）＋ `api/_lib/handler.ts`（Request↔Response の詰め替え）＋ `api/_lib/env.ts`（`process.env` に触れる唯一の場所）。Web標準シグネチャ（`export const POST = (req: Request) => Response`）を選択した理由は、**64KB の門番を JSON 解析より前に効かせる**ため（Node シグネチャではランタイムが body を先に解析する）。①Content-Length で門前払い ②実バイト数（`TextEncoder`）で切る ③やっと解析、の順序を実装しテストで固定。**門番3枚**：①`RANKING_API_ENABLED`（**未設定＝閉**。3本とも `503 api_disabled` で**DBにも触れない**）②`RANKING_DATABASE_URL`（未設定なら `503 database_unconfigured`）③`RANKING_PREVIEW_UNLOCK`（Preview だけ kill switch を開ける。**`VERCEL_ENV === 'production'` では値が何であっても無視**）。①を足したのは、`api/` を作った時点で Phase 4.3 が避けていた「次の deploy で自動公開」状態に入り、しかも **leaderboard の GET は kill switch の外側**で読み取りだけ開いてしまうため。安全弁を「`api/` を作らない」から「**既定で閉じている**」へ置き換えた。**kill switch**：`rules.ts` の定数は **false のまま1文字も触っていない**。`handleRankingRequest` に省略可能な `submissionEnabled` を足し、省略時は従来どおり定数を読む（既存の呼び出し・テストは挙動不変）。定数を true にする案は「deploy されるコードそのものなので Production にも乗る」ため却下。**秘密**：DBに列なし（4.6から）、応答は送信直前に `containsSecret` で機械確認（64桁16進が出たら送らず500）、ログは例外の **`name` だけ**（Neon ドライバは不正URLで**接続文字列を例外メッセージに載せる**＝Phase 4.4 で実際に踏んだ）、query に載せない、`VITE_` 接頭辞を使わない。**deploy 面**：driver を dependencies へ移動し lockfile 再生成（`"dev": true` が外れたことを確認）、`tsconfig.api.json` を追加して references へ登録、`.vercelignore` を追加。**`api/` 配下の `.ts` はすべて Function になる**ため、テストを `api/ranking/` に置くと `/api/ranking/routes.test` が公開され vitest import で build も落ちる——これを実装中に発見し、テストを `api/_lib/`・`api/_tests/`（先頭 `_` はルート化されない）へ移し、`.vercelignore` でも二重に除外し、境界テストで固定した。`vercel.json` は**作らない**（rewrites が既存 Production の配信規則を変えうるため、deploy 面の変更を最小化）。**検証**：全体 **248 files / 3,092 passed / 18 skipped**（Phase 4.7 は 245 / 3,045 / 18。**+3 files / +47 tests**）。内訳は `handler.test.ts` 23・`env.test.ts` 9・`routes.test.ts` 3、`rankingBoundary.test.ts` 11→20（「`api/` を作っていない」を廃し `api/` の形の検査へ置換）、`secrets.test.ts` 8→11（走査範囲に `api/` を追加し、接続情報を読むのは2ファイルのみと固定）。`tsc -b` PASS／`oxlint` PASS（既存 warning 2件のみ、`api/` は0）／clean build PASS 2.34s。**クライアント混入検査12語すべて0件**、さらに `http.ts` の変更を外して `vite build` した結果と**同一ハッシュ**（`index-CmKdoYU5.js`・389.90 kB）＝Phase 4.8 はクライアントを1バイトも変えていない。**Preview QA**：`scripts/phase48-api/preview-qa.mjs`（段階を自動判定し、closed/read/full を検査。identity はその場で生成し秘密を出力しない）と `docs/PHASE4_8_PREVIEW_QA_RUNBOOK.md`（門番を1枚ずつ開けながら「開けていない段階では閉じている」ことを毎回確かめる順序。手順5で **Production が閉じたまま**であることを確認する）。ランナー自身を使い捨てサーバーで3通り自己検査し（正常 `3/3 ok` 終了コード0／誤答 `0/3` 終了コード1／到達不能 終了コード1）、その過程で自バグを修正（`process.exit()` は keep-alive ソケット残存時に Windows の libuv アサートで**終了コード127**になる→`process.exitCode` へ）。**未実施**：実DB統合テスト18件は接続文字列が手元に無いため本Gateでは再実行していない（最後の実行は決定142の migration 直後で 18 passed。本Gateは `postgresStore.ts`／`schema.ts`／`store.ts` を1行も変更していないので契約はそのまま有効）。**API 層と Neon の実機疎通（API-9）は Preview QA 手順3〜4 で行う**。**Known Risks**：①Vercel の web シグネチャ検出が実機未検証（Preview QA 手順2が最初に検出する。駄目なら Node シグネチャへ切替）②merge して deploy すると3本が本番に現れる（環境変数が無い限り 503）③leaderboard は kill switch の外側で、API を開けた時点で誰でも読める（CDN 15秒キャッシュで緩和。公開判断は §6-3 #8）④`containsSecret` の64桁16進判定は将来の誤検知余地あり⑤Neon Free のクォータ未実測⑥Function と DB の region 未確認⑦`RANKING_PREVIEW_UNLOCK` の消し忘れ。**判定**：API-1〜8 PASS／API-9 未実施／**総合 CONDITIONAL PASS**、**Production NO-GO（据え置き）**。merge / push / deploy / Vercel設定変更 / Production API有効化 / `submissionEnabled=true` / Neon schema変更はいずれも未実施で branch `feat/daily-ranking-phase4` を維持。**次の1手**：RUNBOOK 手順1（Preview への push）のCEO承認。 |
| 2026-09-09 | 決定144を追加。「Phase 4.8 follow-up：Preview 実機で判明した **Vercel は Functions を束ねない** 問題の修正と、閉状態の実機確認」（**AI判断**・CLAUDE.md §6-2「バグ修正／技術実装方式／ファイル構成／テスト方式」。CEOは「Preview へ push して QA を進める」ことのみ承認。Production は一切触れていない）。**経緯**：CEO承認を得て branch を push（`e744660`）。Vercel Preview の build は success したが、**3本とも `500 FUNCTION_INVOCATION_FAILED`**。ローカルは 248 files / 3,092 passed・tsc・oxlint・build すべて緑で、**原理的にローカルでは検出できない失敗**だった。**原因**：Runtime Logs に2段階の同じ誤りが出ていた。①`Cannot find module '/var/task/api/_lib/handler' imported from /var/task/api/ranking/leaderboard.js` ②修正後に一段深く `Cannot find module '/var/task/src/server/ranking/http' imported from /var/task/src/server/ranking/index.js`。つまり Vercel は `api/` の TS を**1ファイルずつ JS へ変換して置くだけで束ねず**、`api/` から到達する `src/server`・`src/core` も `/var/task/src/...` へ個別に置く。ルート `package.json` が `"type": "module"` なので出来上がった JS は **ESM として読まれ、Node の ESM 解決は拡張子を補完しない**。**なぜ気づけなかったか**：vitest・vite・`tsc -b`（`moduleResolution: bundler`）の3系統すべてが bundler 解決で、拡張子なしを通す。**ローカル全部緑のまま本番だけ 500** になる差だった。**対処**：`api/` と、そこから到達する `src` の**64ファイル・242箇所**の相対importに明示的な `.js` を付けた（ディレクトリ指定は `/index.js`。57ファイル変更）。指定子だけの変更でロジックは不動。**却下案**：「生成した bundle をコミットする」——生成物がリポジトリに入り、`src/core` を触るたび差分が出て、実体との乖離を別テストで守る必要がある。拡張子方式は Node ESM の標準的な書き方で、生成物が増えず、`src/server` が「どの Node ホストでも動く」という Phase 4.3 からの設計目標にも合う。**再発防止**：境界テストに門番を2つ追加（①`api/` の相対importに拡張子 ②**ランキングBackendから到達する全64ファイル**の相対importに拡張子）。どちらも**わざと拡張子を外して落ちることを確認済み**。拡張子を1つ落とすだけでローカルは緑のまま本番が落ちるため、この2つが唯一の防波堤になる。併せて2つの境界クローラに `.js`→`.ts` の読み替えを実装。**Preview QA ランナーの改良**：Vercel の Deployment Protection は関数へ届く**前**にリクエストを止める（GET は `302 vercel.com/sso-api`、POST は `401` ＋ `{"protection":{...},"error":{"code":"401"}}`）。これを「APIが壊れている」と読み違えないよう専用状態として区別し、`VERCEL_AUTOMATION_BYPASS_SECRET`（**環境変数のみ。引数にしない**）があればバイパスヘッダを付ける。この過程でランナー自身のバグも修正（`process.exit()` は keep-alive ソケット残存時に Windows の libuv アサートで**終了コード127**になる→`process.exitCode`）。**実機確認（RUNBOOK 手順2 PASS）**：Deployment Protection のためスクリプトでは届かず、**ログイン済みブラウザ**で確認。`/api/ranking/{leaderboard,start,submit}` の3本とも本文 `{"error":"api_disabled"}`、アプリ本体（`/`）はタイトル画面まで正常表示。同時刻の **Production は3本とも 404**・ルート 200（`api/` は master に無いのでエンドポイントが存在しない）。**副産物**：Deployment Output に3本が **Function として登録**されており、**決定143 Known Risk 1（web シグネチャ検出が実機未検証）は解消**。Vercel のプランは **Hobby**。**回帰**：248 files / **3,094 passed** / 18 skipped、tsc・oxlint PASS、clean build PASS、**クライアントバンドルは `index-CmKdoYU5.js`・389.90 kB のまま同一ハッシュ**（64ファイルの指定子変更がクライアントを1バイトも変えていないことを実測）。**判定**：API-10（Preview で既定閉の実機確認）**PASS**／API-9（Neon 実機疎通）は**未実施**——CEOのVercel画面操作（Deployment Protection の解除または bypass secret、Preview スコープへの `RANKING_API_ENABLED` と `RANKING_DATABASE_URL` 投入）が必要なため、**そこで停止**。**総合 CONDITIONAL PASS／Production NO-GO（据え置き）**。master は `489352c` のまま無傷、PRも作っていない。merge / Production deploy / Vercel設定変更 / `submissionEnabled=true` / Neon schema変更はいずれも未実施。 |
| 2026-09-09 | 決定145を追加。「Phase 4.8 Preview QA 手順3：API と接続先を開けた状態の実機確認（**Neon 実機疎通の読み取り経路 PASS**）」（**AI判断による検査実施**・CLAUDE.md §6-2「QA方式」。環境変数の投入は**CEOの画面操作**）。**前提**：CEO が Preview スコープに `RANKING_API_ENABLED=1` と `RANKING_DATABASE_URL` を投入。`RANKING_PREVIEW_UNLOCK` は**未登録のまま**（＝kill switch は閉じたまま）。**確認方法**：Deployment Protection が有効なままなので、スクリプト（`preview-qa.mjs`）はエンドポイントへ到達できない。**ログイン済みブラウザの同一オリジン fetch** で1件ずつ検査した。**結果（すべて期待どおり）**：`GET /api/ranking/leaderboard?dailyKey=2026-09-09` → **200** `{"dailyKey":"2026-09-09","totalPlayers":0,"rows":[],"self":null}`（`limit`・`playerId` 付きでも同じ）／`dailyKey=yesterday` と `dailyKey` 無し → **400 bad_daily_key**／`POST start`・`POST submit` → **503 submission_disabled**（kill switch 健在）／`GET start`・`POST leaderboard` → **405 method_not_allowed**／壊れたJSON → **400 bad_request**／64KB超は ASCII・**マルチバイト（「あ」×25,000）とも 413 payload_too_large**（文字数でなくバイト数で測れている）。**意義**：関数が **migration 済みの Neon（決定142）へ実際に接続し、`daily_runs` を読んで正しい形のボードを返した**。決定143 の API-9（Neon 実機疎通）は**読み取り経路が PASS**。書き込み経路（start→submit）は kill switch を開けないと通らないので手順4へ持ち越し。**環境変数のスコープを画面で確認**（値は非表示・未取得）：`RANKING_API_ENABLED` と `RANKING_DATABASE_URL` はいずれも **Preview スコープかつ branch `feat/daily-ranking-phase4` 限定**、**Production スコープには1つも無い**。依頼した範囲（Preview のみ）より更に絞られており安全側。`RANKING_PREVIEW_UNLOCK` は未登録。**未確認**：応答ヘッダ（`cache-control: s-maxage=15` 等）。ブラウザ同一オリジン fetch の経路ではヘッダを読み出せなかったため、bypass secret 投入後に `--stage read` で確認する。**Production**：`/api/ranking/*` は3本とも **404** のまま（`api/` は master に無い）。ルートは 200。**判定**：API-9 読み取り経路 PASS／API-11（kill switch を閉じたまま読める）PASS／**総合 CONDITIONAL PASS・Production NO-GO 据え置き**。**次の1手**：CEO が Preview スコープに `RANKING_PREVIEW_UNLOCK=1` を追加して再デプロイ → 手順4（start→submit→leaderboard の一周・3回勝負・4回目 409）。merge / Production deploy / Production環境変数 / `submissionEnabled=true` / Neon schema変更はいずれも未実施。 |
| 2026-09-09 | 決定146を追加。「Phase 4.8 Preview QA 手順4〜5 完了 → **Phase 4.8 総合 PASS**」（**AI判断による検査実施**・CLAUDE.md §6-2「QA方式」。環境変数の投入・修正は**CEOの画面操作**）。**詰まった点と切り分け**：CEO が `RANKING_PREVIEW_UNLOCK` を投入しても提出が開かなかった。原因は**2つ重なっていた**。①**Vercel の環境変数は追加後にビルドされた deployment にしか入らない**（変数を足すだけでは効かない）②**値が `1` でなかった**（設計上 `true`/`yes`/`on` は無効。誤入力で門が開かないため）。ダッシュボードで値を覗きに行かずに切り分けられるよう、**production 以外に限り** `submission_disabled` の応答へ門番の内訳（`unlockRequested` / `submissionUnlocked` / `vercelEnv`）を添える実装を追加（真偽値と環境名のみ。値は含まず、production には1バイトも出さない）。併せて**環境変数を trim** するようにした（貼り付け時の改行を無効扱いにしても安全性に何も足さない。空白だけの値は trim 後に空文字なので「空白では開かない」性質は保持）。この内訳で `unlockRequested:false` / `vercelEnv:"preview"` を読み、**値の問題**と即断できた。CEO の値修正後に再ビルドして解決。**手順4の結果（すべて期待どおり）**：start 1回目 **201**（`attemptNo:1`・`attemptsPerDay:3`・`gameVersion:"1.da595899c6a9db43"`）／同じ `clientRunId` 再送 **200**・`reused:true`（枠を食わない）／他人の秘密での start **400 BAD_IDENTITY**／**submit 201・`accepted:"stored"`・`score:262`・`round:7`**／同一 run 再送 **200**（冪等）／ticket 無し submit **404 NO_TICKET**／start 2・3回目 **201**／**4回目 409 ATTEMPTS_EXCEEDED**／64KB超 **413**／**全応答・全ヘッダに秘密の混入 0件**。**leaderboard の一周**：`totalPlayers:1`・`rows[0]` と `self` が `score:262`・`rank:1`・`tiedCount:1`・`topPercent:100`・`pointsToNextRank:null`。**スコア262はサーバーが行動ログを再生して自分で計算した値**（クライアントは申告していない）で、それが Neon に保存され `assignRanks`（決定133）を通って順位が付いた＝**Phase 4.1〜4.7 の検証・ticket・順位付けが実機で一本につながった**。**手順5（Production）**：`GET`/`POST` とも `/api/ranking/*` は**4経路すべて 404**、ルートは 200。`api/` が master に無いのでエンドポイントが存在しない。環境変数3つはいずれも **Preview スコープ・branch 限定**で Production スコープには皆無。`origin/master` は `489352c` のまま不動。**ログ**：QA 中に runtime log は1件も出ていない（例外が発生していない＝`console.error` 経路を通っていない）。実装上ログに出るのは route path と例外の**名前だけ**で、`secrets.test.ts` が静的にも固定している。**submit 用の fixture**：ブラウザではゲームエンジンが露出しておらず行動ログを作れないため、`scripts/phase48-api/fixture.test.ts` でローカル生成（1.3 KB・24 action）。**identity を含まない**設計にし、秘密はブラウザ内でその場に生成させたので、秘密はファイルにもログにも出ていない。**回帰**：248 files（＋skip 1）/ **3,100 passed** / 19 skipped、tsc・oxlint・build すべて PASS。**判定**：API-9（Neon 実機疎通）**PASS**／API-12（一周）**PASS**／API-13（Production 閉）**PASS**／**Phase 4.8 総合 PASS**。**Production は NO-GO 据え置き**——公開判断（merge・Production deploy・Production 環境変数・`submissionEnabled=true`・`VALIDATE CONSTRAINT`）はすべて §6-3 の CEO 事項で本Gateの範囲外。**QAデータ**：Neon にテスト用 player 2件・run 1件・ticket 数件・`daily_days` の当日行が残るが、`pruneBefore` と CASCADE で自然に片付くため **DELETE は打たない**。**次の1手**：CEO が `RANKING_PREVIEW_UNLOCK` を Preview から外す（後片付け・手順6）。技術面の残件はリーダーボードUI未実装、Neon Free のクォータ未実測、レート制限。 |
| 2026-09-10 | 決定147を追加。「Phase 4.8 Preview QA 手順6（後片付け）完了 ＋ 環境変数のビルド時焼き込みが**削除側でも同じ**であることの確認」（**AI判断による検査・実施**・CLAUDE.md §6-2「QA方式」。変数の削除は**CEOの画面操作**）。**確認結果**：CEO が `RANKING_PREVIEW_UNLOCK` を削除済み（依頼どおり）。残っているのは `RANKING_API_ENABLED` と `RANKING_DATABASE_URL` の2つで、いずれも **Preview スコープ・branch `feat/daily-ranking-phase4` 限定**。**Production スコープには依然として1つも無い**。**発見**：変数を**削除しても、既に動いている deployment は消す前の値を持ったまま動き続ける**。実測で、削除直後の Preview は `start` に **201** を返し、**提出を受け付け続けていた**。Vercel は環境変数をビルド時に焼き込むため、手順4で踏んだ「足しても効かない」がそのまま裏返しで起きる。**対処**：branch へ空コミット（`c5ac1d3`）を積んで Preview を再ビルドし、閉じたことを実測で確認した——`POST /api/ranking/start` **503 `submission_disabled`**／`POST /api/ranking/submit` **503 `submission_disabled`**。RUNBOOK 手順6にこの落とし穴を明記した（「外すだけでは閉まらない。必ず再デプロイし、`start` が 503 を返すことで確認する」）。**副次的に確認できたこと**：JST の日付が 2026-09-10 へ変わった直後に両日のボードを読み、**Daily の日替わりが実機で正しく動いている**ことを確認した。`dailyKey=2026-09-10` は `totalPlayers:0`（新しい日は空）、`dailyKey=2026-09-09` は `totalPlayers:1`・`score:262`・`rank:1` のまま保持（前日のボードは残る）。`playerId` を渡していないので `self` は `null`。**Production**：`GET`/`POST` とも `/api/ranking/*` は4経路すべて **404**、ルート 200。`origin/master` は `489352c` のまま不動。`RULES.ranking.submissionEnabled` はコード上 **false** のまま（`rules.ts:233`）。**QAデータ**：Neon にテスト用の player・run・ticket と `daily_days` の 2026-09-09 行が残るが、`pruneBefore`（`retentionDays`）と CASCADE で自然に片付くため **DELETE は打っていない**。**状態**：Phase 4.8 は決定146で総合 PASS、本決定で後片付けまで完了。**Preview も含めて提出は全て閉じた**状態に戻っている。公開判断（merge・Production deploy・Production 環境変数・`submissionEnabled=true`・`VALIDATE CONSTRAINT`）はすべて §6-3 の CEO 事項で未実施。 |
| 2026-09-10 | 決定148を追加。「Phase 4.9：Daily Ranking Leaderboard UI（完成済みAPIを画面にする。新機能は増やさない）」（**AI判断**・CLAUDE.md §6-2「軽微なUI・UX調整／ファイル構成／テスト方式／QA方式」。CEO指示 KAGURA / Phase 4.9。**Production は NO-GO 据え置き、`submissionEnabled` は false 維持、push もしていない**）。**配置（CEOにA/B/Cを出さず1案に決定）**：`DailyChallengeScreen` の `daily-facts`（SEED・今日の自己ベスト・残り挑戦回数）の**直後**、神別ベストの前。あの3枚が既に「今日の自分」を示しており、順位はその自然な続きで、**自分 → 全体の中の自分 → 神別の履歴 → ルール → 開始**と粒度が広がってから行動に戻る流れになる。ボスより上には置かない（挑戦する画面であって順位表ではない）。ホーム・戦績画面には置かない。**実装**：`src/hooks/leaderboardClient.ts`（読み取り専用の取得。**例外を投げない**）＋`src/components/setup/dailyRanking.ts`（表示の組み立ては全部ここ。純関数）＋`DailyRankingPanel.tsx`（描くだけ）＋CSS追記。**`rankingClient.ts` に相乗りしなかった理由**：あちらは提出用で「`submissionEnabled` が false の間は一切通信しない」約束を持つ。読み取りはその対象外であり、相乗りさせると「提出は閉じているが順位は見せたい」現状を表現できない。kill switch はサーバー側 env で Preview だけ開ける設計＝**クライアント定数では切り替わらない**ため、`submissionEnabled` で取得を止めると Preview でもUIを確認できなくなる。**順位は作り直さない**：`rank`／`tiedCount`／`topPercent`／`pointsToNextRank` はサーバーの値をそのまま出し、クライアントで並べ替えも順位付けもしない（規則は決定133 の `assignRanks` ただ1つ）。**「並べ替えていないこと」自体をテストで固定**（順位が乱れた配列を渡して順序が保たれることを確認）。**匿名ID**：32桁の生値は出さず先頭4桁のみ（`プレイヤー 3574`）。自分の行は名前そのものが「あなた」。実装中に実機で「あなた ← あなた」と二重に印が付いていたのを見つけて直し、見分けは**文字**が担い色と帯は補助に落とした。**障害時**：404/503＝「まだ準備中です」、500・壊れた応答＝「取得できませんでした」、通信断・**6秒で打ち切り**＝「接続できませんでした」、0人＝「まだ誰も記録していません」。**「まだ始まっていない」と「壊れている」を同じ文言にしない**。挑戦開始のボタンには一切触れないので**オフラインでも神域挑戦は遊べる**。**誤解防止**：`submissionEnabled` が false の間は「現在は閲覧のみです。今日の挑戦結果はまだランキングに登録されません。」を常時表示（開けば自動的に消える）。**決着画面**：既存の `game-over-daily` の内側に最終スコアと導線を追加。通常モードでは `daily` が null なので節ごと現れず、`GameFlow`／`BattleScreen`／`GameOverOverlay` の3か所で二重に絞り、すべてテストで固定。**モバイル**：既存の折り返し幅640pxに合わせ、上位一覧は max-height＋内部スクロールで画面を伸ばし切らない。**Top100を常時表示せず10件のみ**（超過分は「上位10人を表示（全N人）」）。幅360pxで実測し**横あふれ0**。決着画面のリンクは min-height 44px。**a11y**：順位は必ず「N位」の文字、同点は「N人が同順位」の文字、色だけで区別しない。`role="status"`×3・`aria-label`×2・`tabular-nums`・**切り詰めず折り返す**（このリポジトリの方針）。**テスト**：**+3 files / +76 tests**（全体 251＋skip1 files / **3,176 passed** / 19 skipped）。内訳は `leaderboardClient.test.ts` 19（各ステータス・通信断・時間切れ・壊れた応答・秘密をURLに載せない・dailyKey切替）、`dailyRanking.test.ts` 33（0人/1人/**同点1,1,3**/Top10境界/**自分がTop外**/selfのみ/短縮ID/上位%/但し書き）、`scripts/phase49-ui/dailyRankingUi.test.ts` 24（通常モード非漏洩・モバイル・a11y・薄さ・秘密）。**UI検査を `scripts/` に置いた理由**：CSSを本文として読む必要があるが `?raw` も `?inline` も `.css` には Vite の CSS パイプラインが先に効き node 環境で**空文字**になる（＝検査が黙って素通りする。実際に踏み、空振り検出テストで気づいた）。`node:fs` には node 型が要るが `tsconfig.app.json` に足すとクライアントから `process` が見えて境界が緩むため、どの tsconfig にも入っていない `scripts/` へ置いた（`phase48-api/fixture.test.ts` と同じ扱い）。**わざと壊して落ちること**も確認済み。**回帰**：tsc・oxlint・clean build すべて PASS。**バンドル**：JS 389.90→397.60 kB（+7.70 kB／+2.0%）、CSS 97.04→100.57 kB（+3.53 kB／+3.6%）。サーバーコードの混入0件。**非破壊**：`saveVersion` 9／`attemptsPerDay` 3／`submissionEnabled` false のまま。**`gameVersion` は `1.da595899c6a9db43` で Phase 4.8 の Preview と同一**——`rules.ts` に `leaderboardTopCount`／`leaderboardTimeoutMs` を足したが `RULES.ranking` は `dataFingerprint` から除外されるため、**決定142 で固定した Neon の day-lock と衝突しない**（実測で同一値を確認）。**実機確認（ローカルdev）**：応答を差し替えて10行を描画し、順位が **1位,1位,3位,4位,5位,5位,5位,8位,9位,10位** ＝同点は同順位・次は人数ぶん飛ぶ、が実機で成立。自分の要約は「3位 85,000点 上位 24% 次の順位まで 1,200点」、「上位10人を表示（全137人）」。API が無い状態では失敗文言に落ち**開始導線は生存**。**判定**：UI-1〜UI-8 すべて **PASS／総合 PASS**。**Production NO-GO 据え置き**。**Known Risks**：①提出が閉じている今は自分の順位が永久に出ない（但し書きで説明）②`playerId` を query に載せる（公開情報・APIの設計どおり）③Daily画面を開くたび1リクエスト（未稼働の間は無駄打ち。6秒で打ち切り）④短縮4桁の理論上の衝突⑤実データでの見た目は Preview で初確認⑥実機の縦画面（≤640px）メディアクエリ適用は未実測。**次の1手**：Preview へ push して実データで確認（`RANKING_API_ENABLED`／`RANKING_DATABASE_URL` は決定147 で Preview に残してある）。 |
| 2026-09-10 | 決定149を追加。「Phase 4.9 Preview 実機QA（読み取りUIのみ）を通過。ただし GameOver の導線に dead button を発見し修正」（**AI判断による検査・修正**・CLAUDE.md §6-2「QA方式／バグ修正」。push はCEO指示、`RANKING_PREVIEW_UNLOCK` の投入は**未実施**）。**前提**：`969fa5e` を push → Preview 自動ビルド成功。`submissionEnabled` は false、`RANKING_PREVIEW_UNLOCK` は入れないまま「読み取りだけ」を確認する段階。**実データ**：`GET /api/ranking/leaderboard` は 2026-09-10 が `200/totalPlayers 0`（＝empty文言）、2026-09-09 が `200/1件`（決定146 の `score 262 / ebisu / R7 / rank 1`）で、Neon の読み取り経路が Preview 実機で成立。内部262→表示2,620 は `displayScale`(×10) どおり。**確認できた表示**：empty／Top10／同点（1,1,3,4,5,5,5,8,9,10・「N人が同順位」）／自分の行「あなた」／自分の要約（順位・点・上位%・次の順位まで）／Top10外の但し書き／「上位10人を表示（全137人）」／503「まだ準備中です」／500／通信断／取得中／**6秒の打ち切り実測**。実データの無い表示は**応答だけ差し替え**て確認し、DBには `GET` 以外を発行していない。**モバイル**：320／360／390／480／640／641px の実ビューポートで**横あふれ0・枠外要素0・切り詰め0**、パネル高 456〜511px。**発見した不具合（1件）**：GameOver の「今日のランキングを見る」が**押しても何も起きない**。`GameFlow` の `screen` は `engine.state` がある限り `BattleScreen` を返すため、`setSetupScreen('daily')` だけでは決着画面が載ったままになる。決着後の導線なので `backToGodSelect` と同じく `engine.resetGame()` を先に呼ぶよう修正（**`da1e481`**）。**既存テストが検出できなかった理由**：壊れた行を文字列比較していたため。ハンドラ本体を読み `engine.resetGame()` が `setSetupScreen('daily')` より前にあることを検査する形に置換し、**わざと消すと落ちる**ことを確認。修正後の Preview で決着→ランキング画面への遷移と再取得、残り回数 2→1 を実測。**通常モード非影響**：通常1戦を最後まで通し、`.daily-ranking` 不出現・決着画面に導線なし・`/api/ranking/*` へのリクエスト**0件**を実測。**回帰**：src 1000 passed/18 skipped、scripts 25 passed/1 skipped、`tsc -b --noEmit` PASS、`oxlint` PASS（既存warning2件）、clean build PASS（JS 397.62kB／CSS 100.57kB）。**Backlog（Phase 4.9 範囲外）**：スコア0で終えた挑戦後も「今日の自己ベスト」が「まだ挑戦していません」のまま（`bestScore > 0` を挑戦判定に使う Phase 4.1 からの既存挙動）。**Production：NO-GO 据え置き**（4経路とも404、Productionスコープの環境変数なし、master は `489352c` のまま無変更）。**次の1手**：`RANKING_PREVIEW_UNLOCK` を Preview に投入（**投入後は必ず再デプロイ**。決定147 の落とし穴）→ 実際に1回遊んで自分の順位が付くところまで確認 → 終わったら外して再デプロイ。CEO の画面操作が要るためここで停止。 |
| 2026-09-10 | 決定150を追加。「Phase 4.9 提出QA：Preview 実機で start → 実プレイ → submit → leaderboard が一周し、**サーバー再計算スコアがクライアント表示と一致**」（**AI判断による検査実施**・CLAUDE.md §6-2「QA方式」。`RANKING_PREVIEW_UNLOCK` の投入・再デプロイは**CEOの画面操作**）。**前提**：CEO がダッシュボードで「何かがうまくいかなかった」を見たが、実際には **deployment は作成され success**（同一commit `9af0595` に success が2本＝自動ビルド 13:21 と CEO の再デプロイ 13:32・UTC）。alias が配信するバンドルは修正入りの `index-KIPcXCu_.js`。`POST /api/ranking/start` に空bodyを投げると **400 `bad_request`**（閉じていれば 503）＝ **kill switch を通過**していることを、**DBに何も書かずに**判定した（`start` は method → kill switch → 大きさ → body検証 → `startRun` の順のため）。**やり方**：payload を合成せず、**UIで実際に1回遊んだ本物の行動ログ**（`sevengods.pendingRuns` の `ReplayInput`）を、端末が持つ本物の identity（`playerSecret` から導いた公開ID `29b377e2…`）で提出した。クライアントの `submissionEnabled` は false のままなのでゲーム自身は通信しない（kill switch が生きている裏返し）。**結果**：start **201**（`attemptNo 1`／`state open`／TTL90分／dailyKeyはサーバー決定／`gameVersion 1.da595899c6a9db43`＝決定142 の day-lock と非衝突）、実プレイは恵比寿・R7敗北・17手・payload 1,069 bytes、submit **201**（`accepted: stored`／**score 205**／`win false`／`round 7`／`bestScore 205`／`runsUsed 1`）で、**サーバー再計算の 205（表示2,050）はクライアント表示と完全一致**。leaderboard は `totalPlayers 1`／`rank 1`／`tiedCount 1`／`topPercent 100`／`pointsToNextRank null`。**秘密は start/submit/leaderboard のどの応答にも出ない**（文字列・キー名とも）。**画面**：決着→「今日のランキングを見る」で Daily画面へ遷移（`da1e481` の修正が実地で再確認された）、ランキングに「1位 2,050点 上位100%／あなた・恵比寿・未撃破R7」、今日の自己ベスト **2,050（敗北・恵比寿）**、残り挑戦回数 **0/3**。**「現在は閲覧のみです」の但し書きが出たままなのは正しい**（クライアントの `submissionEnabled=false` を見ているため。今回サーバーに記録が入ったのはQAとしてAPIを直接叩いたから）。**Production 非影響**：4経路とも404、バンドル `index-Sm3K62_I.js`（決定130と同一）、Production最新deploymentは 2026-09-06 `489352c`、master も `489352c` で無変更、`submissionEnabled` は false のまま。**残置状態**：Preview の Neon に今日のテスト記録が1件（205点）。剪定と CASCADE で自然に消えるため DELETE はしない。**次の1手（CEO操作）**：`RANKING_PREVIEW_UNLOCK` を Preview から**外して再デプロイ**し、`start` が 503 に戻ることを確認する（決定147 の落とし穴：外すだけでは閉まらない）。 |
| 2026-09-11 | 決定151を追加。「**Phase 4.9（Daily Ranking Leaderboard UI）を正式CLOSE**。Preview の提出口を閉じ直したことを実測で確認」（**AI判断による検査・CLOSE判定**・CLAUDE.md §6-2「QA方式／GO・NO-GOの技術判断」。環境変数の削除は**CEOの画面操作**、Production 公開判断は §6-3 #8 のまま未実施）。**閉鎖の実測**：`RANKING_PREVIEW_UNLOCK` 削除後に空コミット `9d43a43` で再ビルド（deployment `ivgm7myq5`・success）。alias と固有URLの**両方**で `POST /api/ranking/start {}` と `POST /api/ranking/submit {}` が **503 `submission_disabled`**、応答の `gate` は `unlockRequested:false` / `submissionUnlocked:false` / `vercelEnv:preview`。`GET /api/ranking/leaderboard` は **200**（2026-09-11 は0人、2026-09-10 の205点の記録は健在＝**DELETEなし・schema変更なし**）。**3回かかった原因と再発防止**：削除後の再ビルドを `c4cdf86` → `b3e5b27` → `9d43a43` と3本打ってようやく閉じた。1・2本目の時点では変数がまだ残っており、真新しいビルドが値を取り込んでいた。**Vercel は同じキー名を複数行持てる**（Preview 全体の行と branch 限定の行は別行）ため、**1行消しても閉じない**。決定147 は「外すだけでは閉まらない＝再デプロイが要る」までしか書いていなかったので、「**行が1つとは限らない**」を追記した。**判定手順の確立**：`POST {}` の応答で判定する。`start` は method → kill switch → サイズ → body形状 の順なので **閉＝503／開＝400 `bad_request`** と読め、`readBody` は正しいJSONを通すため誤判定しない。**DBには何も書かない**（`startRun` に到達しない）。GET は 405 でゲート状態を示さないので使わない。閉じている間は応答に `gate` 内訳が付く（production では付かない）ため、ダッシュボードを見ずに切り分けできる。**CLOSE 時点の状態**：branch `feat/daily-ranking-phase4` HEAD `9d43a43`（origin と一致、origin/master から32commits）、**master / origin/master は `489352c` のまま**（Phase 4.9 で一度も触っていない）、Production は `/` 200・`/api/ranking/*` 404（3経路）・バンドル `index-Sm3K62_I.js`（決定130 と同一）・最新 deploy 2026-09-06 `489352c`、`submissionEnabled` **false**、禁止ファイル（`.claude/settings.local.json`・`敵画像`・`scripts/phase3-audit/out/`）の混入 **0件**。**Preview の `RANKING_API_ENABLED` / `RANKING_DATABASE_URL` は次フェーズでも使うため残置**（読み取りのみ開）。**判定：Phase 4.9 総合 PASS・CLOSE。Production は NO-GO 据え置き**（公開判断は §6-3 #8 のCEO事項）。**次フェーズ**：Phase 4.10（Production 公開判断の準備＝`submissionEnabled=true` の手順・Production env 投入手順・ロールバック手順の整備）に着手可能な状態。 |
| 2026-09-11 | 決定152を追加。「Phase 4.10：Production Release Gate（ランキング公開前監査）— **現時点 NO-GO、Blocker 2件はいずれも配線の欠落で小さい**」（**AI判断による監査・文書化**・CLAUDE.md §6-2「QA方式／GO・NO-GOの技術判断」。実装変更・merge・Production deploy・Production env・`submissionEnabled=true`・Neon schema・DELETE・課金は**すべて未実施**。Preview への読み取り計測と既存テスト再実行のみ）。成果物は `docs/PHASE4_10_PRODUCTION_RELEASE_GATE.md`。**判定**：G1 手順 CONDITIONAL PASS／G2 kill switch **FAIL**／G3 rate limit CONDITIONAL PASS／G4 セキュリティ **FAIL（1件）**・他 PASS／G5 Neon PASS／G6 env PASS／G7 checklist 作成済み。**Blocker B1（G4）**：`flushPendingRuns()`（提出）を呼ぶのは `rankingClient.ts:132` の `startRankedRun` 内**1箇所だけ**で、決着時（`useGameEngine.ts:242`）は `enqueuePendingRun` で控えるだけ。公開すると「**その日の最後の1回は載らない・1日1回の人は一度も載らない**」（次の start 前に送られるが ticket TTL 90分を過ぎると 410 で捨てられる）。Phase 4.3 Known Risk 5 の残り。Phase 4.8/4.9 の提出QAは API 直叩き（決定146/150）だったため踏まなかった。**Blocker B2（G2）**：Production では `submissionEnabled: env.submissionUnlocked ? true : undefined` で **クライアント定数そのものがサーバーゲート**になり、「board を残して submit だけ閉じる」手段が**コード deploy（revert）しか無い**。`RANKING_SUBMISSION_DISABLED=1`（全環境で閉める向き）を `env.ts`/`handler.ts` に足す。**手順（G1）**：R1 merge→deploy（env 無し＝3本とも `503 api_disabled`、UI「まだ準備中」）→ R2 Production に `RANKING_API_ENABLED`/`RANKING_DATABASE_URL` の**2キーだけ**投入＋再デプロイ（board 200・start/submit 503）→ R3 `submissionEnabled=true` の1行 deploy（**クライアント送信とサーバー受理が同時に開く**＝中間状態は構造的に無い。`RULES.ranking` は gameVersion 指紋から除外済みで day-lock と非衝突）。各段で止まれる。**Rollback**：K0 `RANKING_SUBMISSION_DISABLED=1`＋再デプロイ（submit のみ閉・board 維持・DB 静止）／K1 `RANKING_API_ENABLED` 削除＋再デプロイ（DB ごと切離）／K3 merge revert。いずれも DB に書かない。**Rate limit（G3）**：app 層は ticket ≤3/日・submit 試行 ≤30/日・64KB・400 actions・board 15秒 cache で十分。公開時に **edge 1本**（Vercel WAF・`/api/ranking/*`・IP・60req/60s・429。Phase 4.5 §7 で Hobby 無料を一次確認済み）を CEO 操作で設定。**有料と判明したら設定せず停止**。**Neon（G5）**：1,000人/日で storage 5%・compute 無視可・egress 58MB/月。上限到達時は API 500 → **ゲームは遊べる**。**実測**：日本→edge kix1→function **iad1** で start（DB未到達）≈235ms、board MISS（DB 1クエリ）≈440ms＝**DB 1往復 ≈200ms（region 不一致）**。Function region を Neon 側に寄せると半減（無料設定・CEO 操作）。**注意点**：Preview と Production が同じ Neon DB を共有する構成（2026-09-10 のテスト行が既にある）→ Preview 用 Neon branch を推奨、代替は「公開後 Preview を unlock しない」。`daily_runs_ticket_fk` は NOT VALID のまま（新規行には有効、公開の妨げにならない）。**CEO 判断事項**：R1/R2/R3 の各承認、WAF rule 設定、**Vercel Hobby の利用条件（非商用）への適合確認（Phase 4.4 §11 ④ から未確認）**、Neon Free のまま公開、VALIDATE CONSTRAINT。**次Step**：B1・B2 を実装→テスト→Preview で **UI だけの一周**→決定153→再判定。 |
| 2026-09-11 | 決定153を追加。「ゲーム本体のリプレイ性監査 — 次に実装する1テーマを**共通カードへの『条件・連携』導入**に決定」（**AI判断**・CLAUDE.md §6-2「複数案からの推奨案選定／実装順序」。CEO指示「改善案を並べて選ばせず1つに絞る」。本番コードは未変更、ランキングは Phase 4.9 PASS/CLOSE・Production NO-GO のまま）。成果物は `docs/GAME_REPLAYABILITY_AUDIT.md`。**根拠**：Preview で2戦完走（大耀×鬼将×ふつう＝勝利R6・7,840点・神技A／蒼毘×機工師×むずかしい＝勝利R7・7,670点）と前日の Daily 3敗、コード監査（カード60枚＝共通32＋神別4×7、効果語彙は damage/block/heal/共鳴/AP/draw/バフ/デバフの線形和、条件付き効果は Phase 3 の4枚のみ、敵の予告49手のうち attack 37、神託は毎R無料の小効果、報酬は編成上限+1）、既存 sim（`step2_cardpref.json`：**7神とも初手の攻撃比率 39〜45%で手が同じ**、STAKE-01：神階Ⅰ89%→Ⅶ49%で難易度カーブは健全）。**判定**：完成度 **68/100**。強み＝見せ方と土台（絵・決着画面の内訳・神技評価の具体的な指針・決定論と公平性）。弱み＝カードの中身（数値の束で、選ぶ意味・神差・予告への応答が薄い）。**リプレイを阻む最大原因＝勝ち筋が1本**（共鳴7→神の一撃360＝敵HP36%→殴る）で、構成を変えても結果が変わらない。実プレイでも「攻撃札ゼロの手」が2戦とも出て、託宣は使わなくても勝てた。**候補比較**（Impact×Freq×(6−Cost)×(6−Risk)）：A 共通カードに条件・連携 **225**／B 敵予告の多様化 180／E 神の一撃・大技の演出 180／C 神託の判断化 160／H 難易度再調整 150／G 手札事故対策 144／D 報酬の実感 54／F OTOMO 48。**A を採用**：監査18項目のうち9項目に同時に効き、Phase 3 の `bonus` 基盤（`cardBonus.ts`・`previewBonusTrigger` の手札強調・`cardsPlayedThisRound`・共鳴ゲージ）が既にあるため実装が小さい。**最小仕様**：共通主力16枚に `bonus` を付ける（本体の数値・コスト・枚数は不変）。条件は4種のみ＝既存 `blocked`（予告以上のブロック）・`enemyBig`（大技）＋新規 `combo`（このR2枚目以降）・`charged`（共鳴≥4）。神専用28枚・敵・神の一撃・神託・報酬・スコア式・saveVersion は触らない。規模 2〜3日。gameVersion が変わるため **JST 0:00 直後に deploy**（day-lock）。**受入基準**：STAKE-01 で全神・全神階 ±5pt 以内かつ神階Ⅶ 45〜55%／cardpref の神別攻撃比率の幅 6pt→**12pt 以上**／ふつう3戦で条件成立 4回/戦以上・うち1回は予告を見て手を変えた結果／攻撃札ゼロでも盤面を動かせる／1戦5分以内／モバイルで強調が判別可。**次**：CEO の GO で Phase 5-A 開始（条件2種→16枚→テスト→sim→実プレイ→決定154）、その後 B・E を小さく。 |
| 2026-09-11 | 決定154を追加。「Phase 5-A：共通カード16枚への条件付き追加効果（`bonus`）を実装。**機能は完成・動作、ただし難易度と神差の2基準が構造的に未達 → 総合 CONDITIONAL PASS**」（**AI判断による実装・計測**・CLAUDE.md §6-2「バランス数値の調整／テスト方式／QA方式」。決定153 の最小仕様に従い、対象枚数16・条件4種・本体数値を1つも増やしていない。master merge / Production deploy / Production env / submissionEnabled=true / Neon 変更はいずれも未実施）。成果物は `docs/PHASE5A_CARD_CONDITIONS.md`。**実装**：`BonusCond` に `combo`（このラウンド2枚目以降）と `charged`（共鳴4以上）を追加し、閾値は `RULES.cardBonus.comboMinCardsPlayed=1` / `chargedThreshold=4` として rules.ts に集約（不変ルール4）。両条件とも**カードを使う前**の盤面で判定する——`charged` を「後」で見ると共鳴を上げるカード自身が自己成立し、「先に共振を置いてから撃つ」という順番の判断が消えるため、これを仕様としてテストで固定した。UI は新規追加ゼロで、Phase 3 の `formatCardBonus`（⚡＝成立中／＋＝条件説明）を手札・デッキ構築・報酬3択の3画面がそのまま共有。戦闘ログに条件名（「追加効果（2枚目以降）」「追加効果（共鳴4以上）」）を追加。**テスト**：src **79 files / 1038 passed / 18 skipped**（実装前 78/1000）、api 39、scripts 25、tsc・oxlint・clean build すべて PASS（JS 397.62→400.05 kB、CSS 不変）。新規検査は combo の1枚目不成立/2枚目以降成立/END_ROUNDでリセット、charged の3以下不成立/4以上成立/**自己成立しない**/BURST後は不成立、**共通16枚×4盤面で⚡の予告と実際の発動が全件一致**、kill switch が新条件にも効く、bonusのドローを含む決定論、本体cost/effectsの不変検査、`bonus.textJa` の表示倍率整合（20枚）。**受入基準**：#1 PASS（16/16）／#2 **FAIL**（STAKE-01 +4〜+18pt、Ⅶ 49%→67%）／#3 **FAIL**（Ⅶ 67%、目標45〜55%）／#4 **FAIL**（cardpref 神別スプレッド 7.5→7.0pt、目標12pt）／#5 CONDITIONAL（成立 ふつう3.70・むずかしい4.23回/戦）／#6 **PASS**（選択が変わった回数 2.29回/戦）／#7 PASS（平均5.00R）／#8 PASS。**未達の原因（場当たり調整をせず分解した）**：(a) #2/#3 は**量ではない**——damage/block/heal を半分にしても神階Ⅶは71%のまま不変。同一harnessでの成分分離（追加効果 全消し 53% → 現状 71%／火力のみ 62%／テンポのみ 65%）から、効いているのは**テンポ系（共鳴+1・ドロー+1・神力+1）が神の一撃の回転を早めること**。「最も使う共通16枚に下振れの無い純増を足す」設計そのものが固定方策の勝率を必ず押し上げるため、**±5pt と「意味のある追加効果」は同時に成立しない**。収めるには (A) 本体数値を下げて条件で戻す（CEO指示「基礎数値は原則変更しない」に抵触）か (B) 神階ラダー／敵数値を締める（決定126 の難易度設計に手を入れる）が必要で、**どちらも本Phaseの禁止事項のため未実施**。(b) #4 は**共通カードを強くしたから**——7神が共有するプールの価値が上がると打ち筋はむしろ似る。決定153 の「共通カードに条件を足せば神差が広がる」という仮説は測定で否定された。ただし**神ごとに使う条件ははっきり分かれた**（蒼毘 blocked1.57/enemyBig1.05、才華 combo2.36/charged0.84、寿楽 enemyBig1.30・blocked0.00、恵比寿 combo2.30）。**互換性**：`GameState` 構造は不変で `saveVersion` は **9 のまま**。`gameVersion` は `1.da595899c6a9db43` → **`1.b783d71956fe9327`**（データ由来のため `engineVersion` は 1 据え置き）。golden replay は入力ごと再生成した（カードの効きが変わると旧操作列は「敵が早く倒れて以降の操作が拒否される」形で再生不能になるため。再生成は `scripts/phase5a-cards/cardsAudit.test.ts` の `P5A_GOLDEN=1`）。`actionDistribution` の実測分布も更新（max 34→32 等）。**deploy は JST 0:00 直後**（day-lock）。**実プレイQA**（localhost）：恵比寿×試練の影×ふつうで勝利R6・8,030点。R1 の予告50・AP2 で「見切り→一撃」と置くと一撃が⚡になり **50→80ダメージ**、実装前は存在しなかった初手の判断が生まれた。受け流し→一撃、共鳴4到達で剛撃が⚡など、順番と予告を見る動機を実機で確認。390px でも⚡（琥珀）と＋（灰）が判別でき横あふれ0。**Known Risk**：①神階Ⅶが設計ターゲット25〜35%を大きく上回る（**CEO判断事項**）②神差は種別比率では広がらない③⚡が同時に4枚点くことがある④日中deployで当日Dailyが423⑤Dailyスコアの全体上振れ（day-lockで日単位は公平）。**次Step**：Known Risk 1 の扱いをCEOが決める→戻すなら Phase 5-B「難易度の再センタリング」（神階ラダー単独調整でⅦを45〜55%へ）、神差を広げるなら Phase 5-C「神専用カード側の条件」。 |
| 2026-09-11 | 決定155を追加。「**Phase 5-B：難易度の再センタリング — PASS / CLOSE**。神階ラダーの敵側 modifier 3値だけを締めて、Phase 5-A 前の難易度カーブへ戻した」（**AI判断による感度分析・実装・計測**、CLAUDE.md §6-2「バランス数値の調整」。基準4の受容は **CEO判断**（2026-09-11）。master merge / Production deploy / Production env / Ranking / Neon は未実施）。成果物は `docs/PHASE5B_STAKE_RECENTER.md`、実装 commit `ef8ebad`。**変更**：`RULES.stakes` の `lateRoundAtkMul` 1.2→**1.3**、`enemyAtkStep` 1.1→**1.15**、`enemyHpStep` 1.1→**1.15** のみ。カード60枚（Phase 5-A の共通16枚 bonus を含む）・神Passive・OTOMO・Enemy Intent・神託・報酬・スコア式・BURST・Daily・Ranking・`saveVersion`（9）は不変。**方法**：同一seed・STAKE-01 と同じ方策と集計で16候補を掃引（`scripts/phase5b-stakes/stakesAudit.test.ts`、`cur` が本番 STAKE-01 93/92/90/86/82/76/67 と完全一致することを確認してから使用）。単独の modifier では Ⅶ 60〜64% で不足、ATK偏重（1.2+late1.3）は BURST −12%・選択差 −10%、HP偏重（1.2）は戦闘が 5.80R に伸びるため不採用。**採用案だけが全段 ±5pt に収まり、5-A の指標を落とさなかった**。HP+ と ATK+ を同量同時に積むのは決定126 の防御神/攻撃神の公平性原則を崩さないため。**結果（STAKE-01 実測）**：Ⅰ89／Ⅱ88／Ⅲ81／Ⅳ74／Ⅴ69／Ⅵ67／**Ⅶ48%**。5-A前（89/86/79/73/67/62/49）比 0/+2/+2/+1/+2/+5/−1 で**全神階 ±5pt 以内**、**Ⅰ〜Ⅶ は概ね単調減少**（Ⅵ は目安52〜64を+3超過、自然なカーブ優先で受容）。**Phase 5-A の面白さは維持**：Ⅶ の bonus 成立 3.25→3.31（blind）／3.48→3.54（aware）、選択変更回数 0.70→0.69、BURST 0.68→0.67、平均 **5.52R**（5分以内）。被ダメ30・スコア391 と 5-A前の水準へ戻り、「敵HPで相殺しただけ」ではない（HP+5%のみ）。**tests 79 files / 1038 passed、tsc・oxlint・clean build PASS**。**実プレイ**：Ⅲ 大耀×鬼将 勝利R6・9,980点・神技S（予告🔥190 を鉄壁＋受け流し150で無傷＝予告を見て守る価値）／Ⅴ 才華×怨霊 敗北R5（予告⚔140 の前でゲージ稼ぎを選んだ1手が敗因＝判断ミスが効く）／Ⅶ 恵比寿×鬼将 敗北R4（理不尽ではなく予告どおり。⚡を狙う余裕は残る）。**受容した既存課題**：神階Ⅶ の神別 spread **38pt**（5-A前31pt。福永45%・大耀55% 対 蒼毘81%・寿楽83%）は一様な敵側 modifier では解けない**既存の構造課題として CEO が受容**。**福永・大耀の底上げは Phase 5-C（神専用カード側の条件）で扱う**。**機工師・魔獣が Ⅶ で 12〜13%（実質Ⅷ）なのも 5-A 前からの既存 Known Risk として残す**（5-B で悪化なし）。**互換性**：`saveVersion` 9 不変。`gameVersion` は `1.b783d71956fe9327`→**`1.c256855486fe5952`**（`RULES.stakes` は指紋対象。Daily は神階0のため golden の結果は不変）。**deploy は JST 0:00 直後を推奨**（day-lock）。**master merge / Production deploy は未実施**（§6-3 #8）。**判定：Phase 5-B の目的は達成、PASS / CLOSE。** |
| 2026-09-11 | 決定156を追加。「Phase 5-A/5-B 後の総合再監査 — 次テーマは**神託『加護』の予告連動化（Phase 5-D 仮）**。Phase 5-C（神専用カードの条件）は**保留**」（**AI判断**・CLAUDE.md §6-2「複数案からの推奨案選定／実装順序」。CEO指示「god spread 38pt だけを理由に決めない／1つに絞る」。本番コード・カード数値・stake・Ranking・Production は未変更。docs と監査スクリプトのみ）。成果物 `docs/GAME_REAUDIT_AFTER_5AB.md`。**実プレイ 9戦**（本セッション6＋5-B QA 3、同ルール、神7柱・敵6体・かんたん〜神階Ⅶ）：福永×試練の影×ふつう 勝利R6 8,080／大耀×魔獣×Ⅴ 敗北R3／蒼毘×機工師×Ⅶ 実質敗北（R3 特大290でHP70、主砲充填）／寿楽×鬼将×むずかしい 勝利R6 8,530 HP270／才華×道化×Ⅲ（デッキ改変）敗北R5／笑蓮×龍神×かんたん（力の絆）勝利R5 8,410 **BURST 0回＝OTOMO無関与**。**5-A で本当に改善した点**：1ラウンドの中に判断が生まれた（R1から順番、⚡が毎R1〜4枚、条件成立 0.30→3.70回/戦、選択変更 0.20→2.29回/戦、4条件すべて実戦で成立、攻めの個性は神で分かれた）。**まだ弱い点＝最大ボトルネック**：**受け（防御）の手段が手札運に依存**し、予告は見えるのに答えが無い負け方が実プレイ敗北 5戦中5戦。**神差38ptの原因**（神階Ⅶ神別診断）：勝つ2神は受けを構造的に持つ（蒼毘 ブロック45・被ダメ19、寿楽 妨害6枚・被ダメ23）、負ける神は受けが手札運（福永 被ダメ38・BURST0.43、大耀 回復4）＝**火力差ではなく「大技をどう受けるか」の差**。**機工師・魔獣**：Ⅲ 45／67%、Ⅴ 31／51%、Ⅶ 12／13%。Ⅲ以下は成立、Ⅴ以上で「見えるが答えが無い」。5-A前と同じ形。**候補比較**（I×F×(6−C)×(6−R)）：A 神託「加護」を予告連動の受け札に **300**／C 敗北時の導線 240／H デッキ構築UX 200／D 演出 180／**B Phase 5-C 144**／E 敵側改修 96／F 報酬 54／G OTOMO 48。**A を採用、5-C は保留**：神差の原因は受けの有無で専用札の攻め性能ではない、共有プールを触ると神差は縮み専用を触ると広がる（5-Aの教訓）ので今は「下の神を上げる」＝受けの床で解く、5-C は A の後に福永・大耀・蒼毘（対照）の3神パイロットで行う。**最小パイロット（A）**：加護の託宣を「ブロック＝敵の予告の50%（最低20）」へ（効果1種 `blockOfIntent` を追加、`RULES.divination.guardRatio/guardMin` に集約、導き・天啓・回数は不変、カード60枚・神階・敵・Passive・OTOMO・報酬・スコア式・BURST・saveVersion は不変）。**受入基準**：STAKE-01 5-B比 ±5pt かつ Ⅶ 45〜55%／Ⅶ神別 spread 38→30pt以下・福永/大耀 +5pt以上／Ⅶ 機工師・魔獣 12〜13%→25%以上・他5敵 ±8pt／bonus・選択変更・BURST ±15%／加護の使用率が大技予告Rで60%以上・それ以外で40%以下／実プレイ3戦で「予告を見て加護」が各1回以上／tests・tsc・lint・build PASS・saveVersion 不変。**完成度 68→74/100**。**次Step**：Phase 5-D 最小パイロット → paired-seed sim → 実プレイ3戦 → 決定157、その後 5-C 3神パイロット。deploy は JST 0:00 直後（§6-3 #8 のCEO承認事項のまま）。 |
| 2026-09-11 | 決定157を追加。「**Phase 5-D：神託『加護』を予告連動の防御手段へ — PASS / CLOSE**。仕組みは採用し、数値目標の未達3項目は『加護システム外の構造課題』として次Phaseへ切り出す」（ratio の比較・選定・実装・計測は **AI判断**（CLAUDE.md §6-2「バランス数値の調整／複数案からの推奨案選定」）。未達3項目を別Phaseで扱ったうえでの PASS / CLOSE は **CEO判断**（2026-09-11 承認）。master merge / Production deploy / Production env / Ranking / Neon は未実施・未変更）。成果物は `docs/PHASE5D_ORACLE_GUARD.md`、実装 commit `9fee273`。**目的**：決定156 の再監査で最大ボトルネックとした「**Enemy Intent を見ても手札に対応札が無い**」問題（実プレイ敗北5戦中5戦）に対し、毎ラウンド無料で手に入る唯一の資源である神託の「加護」を、**予告に連動する防御手段**へ変える。**採用仕様**：効果 `blockOfIntent` を1種追加し、**ブロック＝max(20, 敵の予告合計×50%)**。`RULES.divination.guardRatio = 0.50`・`guardMin = 2`（表示20）に集約（不変ルール4）。予告の値は UI の予告表示と同じ `enemyActionTotal`（難易度・Daily・神階の倍率適用後、敵デバフ適用前。連撃は合計、溜めは0＝最低20のみ）。**HP回復は削除**（旧：HP+30・ブロック+20。決定21 の加護の中身をこれで置き換える）。**神階Ⅳ以降は既存のブロック効率0.75の影響を受ける**（加護だけの例外は作らない）。**ブロックの持ち越しなし**（毎ラウンド開始で0）。導き・天啓・神託の回数は不変。UI は加護の下に「**今なら ブロック◯◯**」（効率適用後の実際の量）を1行だけ追加し、エンジンと同じ関数（`previewIntentGuard`）で計算するので**表示と実効果が一致**する。`enemyActionTotal` は循環 import を避けるため `src/core/engine/intent.ts` へ移設（`round.ts` から再エクスポート）。**ratio の選定**：同一seed・STAKE-01 と同じ方策で BASE（5-B）／0.35／0.40／0.45／0.50 を比較（`scripts/phase5d-guard/guardAudit.test.ts`、`P5D_RUN=1`。BASE は本番 5-B 実測と完全一致）。0.50 が「致死予告への加護率」と魔獣・機工師の改善で最上位、通常ラウンドの使用率は全候補とも12〜13%で差が無く、難易度も全段 +3pt 以内だったため採用（「50%ありき」ではなく比較の結果）。0.5超相当（Ⅳ以降の効率を加護だけ外す診断 0.67）は神階ルールに例外を作るため不採用。**結果（Phase 5-B → 5-D）**：STAKE-01 Ⅰ89→91／Ⅱ88→89／Ⅲ81→82／Ⅳ74→75／Ⅴ69→70／Ⅵ67→69／**Ⅶ overall 48%→51%**（**全神階 +1〜+3pt**、単調減少を維持、DAILY-01・決定58 のゲート PASS）。Ⅶ 神別（最良戦略）：**福永 45→52（+7）**、**大耀 55→57（+2）**、蒼毘 81→86・寿楽 83→88（弱体化なし）、**god spread 38→36**。Ⅶ 敵別：**機工師 13→16**、**魔獣 12→22**、他5敵は最大 +4pt。**加護使用率**（予告を読んで託宣を選ぶプレイヤー＝rollout レンズ、Ⅶ）：**全体28%／大技ラウンド47%／通常ラウンド13%／致死予告 64→85%**。Ⅶ の託宣配分は加護28・導き19・天啓42・温存11%で、**「毎ラウンド加護」にはなっていない**。**Phase 5-A 指標は維持**：bonus 3.31→3.36（blind）／4.06→4.22（aware）、選択変更 0.91→0.97、BURST 0.67→0.68（すべて ±7% 以内）、平均 5.52→5.58R。**実プレイ3戦**（localhost、全勝）：福永×機工師×Ⅴ 勝利R6 11,810／大耀×魔獣×Ⅴ 勝利R5 12,370／蒼毘×鬼将×Ⅶ 勝利R7 11,070。**予告を見て加護を使う判断を確認**（大技・主砲のラウンドで押し、溜め・小さい予告では「今なら20」を見て天啓・導き・温存を選ぶ）。**加護だけでは守り切れず、残りを手札で解く構造を確認**（主砲360に加護140、残りを鉄壁・受け流し・デバフで埋める）。攻めか受けかの二択、Phase 5-A の条件発動も維持。**UI**：デスクトップ・390px とも「今なら ブロック◯◯」が1行で出て横あふれ0、表示値と実効果が一致（140→140、100→100）。**テスト**：src **80 files / 1076 passed / 18 skipped**（新規 `intentGuard.test.ts`：量の式・最低保証・溜め・連撃合計・デバフ前の予告・HP回復なし・持ち越しなし・Ⅳ以降の効率・preview＝実際・決定論）、**tsc・oxlint・build PASS**。**互換性**：`saveVersion` **9 不変**。`gameVersion` は `1.c256855486fe5952`→**`1.da1ec40838d7ff9a`**（`RULES.divination` にキーが増えたため。golden replay の決着は不変）。`actionDistribution` の payload 記録を更新（p99 1547→1558）。**Known Risk（未達3項目＝Phase 5-D の失敗ではなく、加護システム外の構造課題として次Phaseへ切り出す）**：①**神階Ⅶ の god spread 36pt**（目標30pt以下）②**機工師16%・魔獣22%**（目標25%以上）③**大技ラウンドの加護使用率47%**（目標60%以上。致死予告に限れば85%）。**原因**：**A. 神階では神託が1戦4回**（`stakes.divinationCount`。Ⅶ の大技ラウンドの57%は神託切れの後に来る。ratio を変えても55〜57%で動かない）／**B. 神階Ⅳ以降のブロック効率0.75が加護にも適用**（外した診断では spread 29・魔獣32%まで届くが、神階ルールの例外になる）／**C. STAKE-01 の固定方策が予告を読まない**（同じ ratio でも予告を読む打ち手なら機工師81%・魔獣57%・福永69%・大耀81%）。3つとも本Phaseの変更禁止範囲（神階modifier・本番ゲートの方策）にあり、加護の量では解けないことを6候補の比較で確認した。**追加 Known Risk**：④**`DIVINATION_CHOICES` が gameVersion の fingerprint 対象外**——今回は `RULES.divination` の変更で版が正しく変わったが、神託の効果だけを変えると版が変わらない。**次Phase候補として必ず修正する**（Ranking の版計算に触れるため本Phaseでは未実施）⑤**Production deploy は JST 0:00 直後**（gameVersion 変更・day-lock）⑥**master merge / Production deploy は未実施**（§6-3 #8）⑦**Ranking / Neon / env は変更なし**。**判定：仕組みは採用、PASS / CLOSE。** |
| 2026-09-11 | 決定158を追加。「**Phase 5-E：神階の受け構造 — 加護だけ神階Ⅳ以降のブロック効率0.75の対象外にする（B案）＋ gameVersion fingerprint 修正 — PASS / CLOSE**」（設計比較・推奨案の選定・実装・計測は **AI判断**（CLAUDE.md §6-2「複数案からの推奨案選定／バランス数値の調整」）。B案の採用と「大技全体60%」基準の廃止は **CEO判断**（2026-09-11 承認）。master merge / Production deploy / Production env / Ranking / Neon は未実施・未変更）。成果物 `docs/PHASE5E_STAKE_GUARD_STRUCTURE.md`（§0〜§16 設計監査 commit `f3141e9`、§17 実装結果）。**目的**：Phase 5-D（決定157）の「予告を見る → 加護を使うか判断 → 残りを手札で解く」ループを維持したまま、神階Ⅳ以降で加護までブロック効率0.75で減衰し、本文の「予告の50%」が実効37%になっていた不整合を解消する。**設計比較**（paired seed、7神×7敵×神階Ⅰ〜Ⅶ、固定方策＝STAKE-01 と予告を読む方策）：A 神託4→5（Ⅶ 63%、全神階 +3〜+12pt、温存 11→4%＝「余ったから使う」化）／B 加護だけ効率例外（Ⅶ 54%）／C A+B（Ⅶ 68%、予告を読めば 99%・spread 5 で神の差が消える）。Impact×Frequency×Simplicity×Risk で B 192 ＞ A 100 ＞ C 75。A・C は難易度の帯を外れ Phase 5-B の再センタリングが必要になるため不採用。条件付き回復（D）は新ルールになるので比較対象外。**実装**：①`rankingImpactSnapshot()` に `divination: DIVINATION_CHOICES` を追加（Phase 4.6 の列挙から漏れていた。導き・天啓の数値や効果の種類は `RULES` に無く、変更しても版が変わらなかった）②`RULES.divination.guardIgnoresBlockEfficiency: true` を追加（データにすることで指紋が自動で変わる）③`effects.ts` の `intentGuardGained()` で加護だけ `effectiveBlock` を通さない。`previewIntentGuard` も同じ関数を通すので「今なら ブロック◯◯」＝実効果。通常カードのブロックは従来どおり0.75 ④神階Ⅳの説明と累積要約を「ブロック効率75%（加護を除く）」に。神託回数4・guardRatio 0.50・guardMin 20・`RULES.stakes`（blockEfficiency 0.75 を含む）・カード60枚・Phase 5-A bonus・神専用カード・神Passive・OTOMO・Enemy Intent・敵基礎値・報酬・スコア式・BURST・神の一撃・Daily・Ranking・saveVersion は不変。**結果（Phase 5-D → 5-E、seed 6＝本番 balanceSim と一致 / seed 12 追試）**：STAKE-01 Ⅰ〜Ⅶ 91/89/82/75/70/69/51 → **91/89/82/77/72/71/54**（**Ⅰ〜Ⅲ は全指標が完全一致**、Ⅳ以降 +2〜+3pt）。Ⅶ 神別：**福永 52→64 / 51→58**、**大耀 57→62 / 54→62**、蒼毘 86→88 / 88→89、寿楽 88→90 / 87→92、**god spread 36→29 / 37→33**。Ⅶ 敵別：**魔獣 22→30 / 21→29**、**機工師 16→19 / 16→18**、他5敵は最大 +6（業斧）。予告を読む方策Ⅶ：勝率 88→94、spread 31→24、魔獣 57→81。**加護使用率**（予告を読む方策Ⅶ）：**危険な大技 76→83 / 78→85%**、**安全な大技 20→18 / 19→18%**、**通常 13→12%**、**致死予告 85→90 / 86→92%**、温存 11→13%。加護の価値が上がっても危険な場面だけで使用率が上がり、「毎ラウンド加護」は起きていない。**bonus / 選択変更 / 神の一撃は −1〜+4%**（±15%以内）、平均 5.58→5.63R。**基準の定義変更（CEO判断）**：Phase 5-D の基準5「大技ラウンド全体で加護60%以上」を廃止し、**危険な大技（受けた後HP<35%）≥75%・安全な大技 ≤30%・通常 ≤20%・致死 ≥85%** で評価する（Ⅶ の大技の54%は受けても余裕がある予告で、そこで加護を押すのは合理的プレイではないため）。**実プレイ3戦（全勝）**：福永×機工師×Ⅴ 勝利R7 10,230（R5 主砲360→加護180＋手札130で被ダメ0、R3 で加護120と同時に守護50→40＝通常ブロックは0.75を確認）／大耀×魔獣×Ⅴ 勝利R4 13,460（約2分20秒、危険な連撃で加護、安全な連撃100・HP300では温存）／蒼毘×鬼将×Ⅶ 勝利R6 12,630（反撃150のうち加護の寄与70、5-D比+20で過剰ではない）。3戦とも「安全な大技では温存」「加護だけでは足りず手札で埋める」「攻めるか守るか」が出た。**モバイル390px**：「ブロック効率75%（加護を除く）」は1行、「今なら ブロック30」（Ⅳ・予告60）、横スクロール・文字切れなし。**テスト**：src **80 files / 1085 passed**（Ⅲ以下は5-Dと一致、Ⅳ以上は通常0.75・加護は例外、予告200→100、予告0→20、連撃、preview＝実効、DIVINATION_CHOICES 変更で版が変わる、フラグ false で 5-D に戻る）、**tsc・oxlint・clean build PASS**。**互換性**：saveVersion **9 不変**、replay 形式不変。gameVersion `1.da1ec40838d7ff9a` → **`1.5aac9529b1a59ad3`**（指紋追加と加護フラグをまとめて**更新1回**）。golden replay（Daily・神階0）の決着は不変。**受入基準 15/15 PASS**（stake 値・guardRatio の追加調整なし）。**Known Risk**：①大技ラウンド開始時の神託残0 **41%** は B では動かない（神託回数に触るなら敵側再センタリングとセットの別Phase＝CEO判断）②境界ちょうどの指標：大耀62（下限）、機工師18（seed 12・下限）、寿楽+5（seed 12・上限）③笑蓮 +7〜+9（基準対象外だが上位神で最大の伸び）④Ⅶ 54% は帯の上端寄り⑤golden replay は神階0なので B を検出できない（`intentGuard.test.ts` と STAKE-01 で担保）⑥Production deploy は **JST 0:00 直後**（day-lock）⑦`DIVINATION_CHOICES` の文言も指紋に入るため文言修正でも版が変わる（安全側として許容）⑧`ENEMY_IDS` の並びは指紋対象外（既存）。**判定：Phase 5-E PASS / CLOSE。** |
| 2026-09-12 | 決定159を追加。「**Phase 5-C：神専用カードによる神格差別化 — 縮小パイロット（大耀2枚・蒼毘1枚）PASS / CLOSE**」（候補比較・枚数と条件の選定・実装・計測は **AI判断**（CLAUDE.md §6-2「複数案からの推奨案選定／バランス数値の調整」）。縮小パイロットの実装と福永の見送りは **CEO判断**（2026-09-11 承認）。master merge / Production deploy / Production env / Ranking / Neon は未実施・未変更）。成果物 `docs/PHASE5C_GOD_CARD_CONDITIONS.md`（§0〜§9 設計監査 commit `aac52b8`、§10 実装結果）。**目的**：勝率強化ではなく「神ごとに、カードを出す順番・狙う状態・勝ち方が違う」を強める。Phase 5-A の共通カード条件は順番判断を生んだが神差は広げなかったため、専用カード側に条件を置く。**実装（3枚、既存条件のみ・新条件なし）**：大耀「姉御の号令」`charged`（共鳴4以上）→さらに攻撃力+30（1ラウンドのみ）／大耀「豪快な一撃」`charged`→敵に40／蒼毘「反撃の刃」`blocked`（刃のブロック60を足した後≧予告）→敵に60。本体の効果・AP・枚数、福永の4枚、他4神、共通カードは不変。号令の bonus バフは rounds:1 でラウンド終了時に消え、本体の+30（2R）とは別枠で加算（次ラウンドは本体分のみ）。charged は使う前の共鳴で判定（自己成立なし）、blocked は不動の構えと同じ定義（Ⅳ以降は刃のブロックに効率0.75後で判定）。UI は既存の＋／⚡をそのまま使う。**福永は変更しない**：lowHp 条件の bonus は選択変更 0.01〜0.25回/戦で判断を変えず、変えるほど強くすると神の一撃+23%・勝率+10pt 動く。低HP攻撃は得意技で既に固有。**結果（Phase 5-E→5-C、paired seed、7神×7敵×神階Ⅲ/Ⅴ/Ⅶ、固定方策・予告を読む方策・カード順序を読む方策）**：本番データ（PROD）は設計監査の PILOT と全行一致。STAKE-01 Ⅰ〜Ⅶ 91/89/82/**78**/72/71/**54**（Ⅳのみ+1）、Ⅶ overall 54 / 55（seed 12）。Ⅶ 神別：**大耀 62→64 / 62→65**、蒼毘 88→88 / 89→89、**福永・恵比寿・才華・寿楽・笑蓮は全神階・全方策で完全一致**。機工師 19 / 18・魔獣 30 / 29 不変。**行動指紋（Ⅶ、カード順序を読む方策）**：大耀「共鳴4以上で攻撃」0.98→**1.17** / 0.96→**1.17**、「共鳴を先に積んでから攻撃」0.67→0.81 / 0.63→0.73、専用 bonus 0→**0.98 / 1.11**、選択変更 0→**0.52 / 0.63**。蒼毘「反撃の刃の bonus」0→**0.45 / 0.52**、専用 bonus 1.71→**1.98** / 1.70→**2.12**、選択変更 +0.36 / +0.50。福永は不変（低HP攻撃 1.4〜1.5回/戦＝得意技由来）。3神の方向は「溜めて吐く」「盾から反撃」「窮地で攻める」で同じ方向へ寄らない。神の一撃 大耀 0.93→0.93 / 0.92→0.88、蒼毘 0.95→0.95 / 0.92→0.87、bonus 全体（全神平均）+約4%。**種別内訳（action category）は ±1pt で動かない**（設計監査の予想どおり。デッキ構成の問題）。**実プレイ5戦（全勝）**：大耀×魔獣×Ⅴ 3戦（13,260／12,080／11,130）で、号令を共鳴0では温存→共振で4→**号令⚡→一撃→翌R豪快⚡（号令の残り+30込み 240）**の「共鳴を積む→号令→豪快」が成立、次ラウンドに bonus の+30が残らないことも確認。神の一撃（共鳴7）で0に戻ると条件が消えるため「7まで押すか4〜6で使うか」の判断が生まれた。蒼毘×鬼将×Ⅶ（12,740）で**加護40→誓いの盾30で反撃の刃が＋→⚡に変わり +60**、表示と成立が一致、暴走なし。福永×機工師×Ⅴ（対照、11,520）は専用札に＋行なし、低HP攻めと加護ループ維持。**モバイル390px**：デッキ構築・手札とも＋行は自然に折り返し、はみ出し・横スクロールなし。**テスト**：src **81 files / 1113 passed**（新規 `godCardsPhase5c.test.ts` 必須1〜18）、**tsc・oxlint・clean build PASS**。**互換性**：saveVersion **9 不変**、replay 形式不変。gameVersion `1.5aac9529b1a59ad3`→**`1.80c6eda23ed082dc`**（カードデータの指紋で自動・1回）。golden replay（Daily・恵比寿）の決着は不変。Phase 5-E の仕様（加護50%・最低20・加護だけ効率例外・「今なら ブロック◯◯」・DIVINATION_CHOICES 指紋・神託4回）は無変更。**受入基準 15/15 PASS**（基準6・7・9 は設計監査で期待値を置いたカード順序を読む方策で達成。予告だけを読む方策では下回る）。数値の追加調整なし。**Known Risk**：①効果は条件と順番を読むプレイヤーに偏る（予告だけを読む方策で大耀 0.98〜1.02・蒼毘 0.31〜0.33）②大耀の条件窓は共鳴4〜6（7で神の一撃→0）③神階Ⅴでは大耀の指紋が伸びない（0.98→0.95）④蒼毘の blocked は敵デバフ前の予告と比べる（既存仕様）⑤種別内訳の神差は動かない（デッキ構成の Phase が要る）⑥蒼毘は4枚中3枚が条件付き、これ以上足さない⑦Production deploy は JST 0:00 直後、master merge / Production deploy は未実施。**判定：Phase 5-C PASS / CLOSE。** |
| 2026-09-12 | 決定160を追加。「**Phase 5-F：統合リプレイ性 / Release Candidate 監査 — PASS / CLOSE。Phase 5 全体を PASS / CLOSE とし、feature branch を Release Candidate とする**」（監査・比較・採点・判定案は **AI判断**（CLAUDE.md §6-2「GO・NO-GOの技術判断」）。RC 判定 B の採用は **CEO判断**（2026-09-12 承認）。master merge / Production deploy / Production env / Ranking / Neon は未実施・未変更）。成果物 `docs/PHASE5F_RC_AUDIT.md`（§0〜§13 監査 commit `9fbb38e`、§14 RC 状態）。**方法**：Phase 5 開始前 `46a450f` と現在を同じハーネス・同じ seed で比較（`scripts/phase5f-rc/phase5Compare.test.ts`、7神×7敵×神階Ⅰ〜Ⅶ、固定方策＝STAKE-01・予告を読む方策・カードの順番を読む方策、敗北を「判断で回避可」「手札不足」に分類）＋実プレイ7戦（7神すべて）＋RC 技術監査。**Phase 5 の確定構成（すべて PASS / CLOSE）**：5-A 共通カード16枚の条件・順序判断（決定154）／5-B 神階難易度の再センタリング（決定155）／5-D Enemy Intent 連動の加護（決定157）／5-E 加護の blockEfficiency 例外＋`DIVINATION_CHOICES` を gameVersion の指紋へ（決定158）／5-C 大耀2枚＋蒼毘1枚の専用条件（決定159）／5-F 統合 RC 監査（本決定）。**プレイループの変化**：「最大ダメージ札を出す」から「**予告を見る → 神託を選ぶ → 条件を確認 → 順序を組む**」へ。**総括（開始前→現在）**：Meaningful Choice 平均 **36.5 → 53.5**（順序 30→70、Intent 対応 40→75、神託 25→65、神差 45→60、神階 55→70、再挑戦理由 35→55。報酬・構築・OTOMO・メタは据え置き）、Replay Motivation **60 / 100**、Phase 5 総合点 **68 → 78 / 100**。STAKE-01 49→**54%**（Ⅰ〜Ⅶ 91/89/82/78/72/71/54）、予告を読む方策Ⅶ 81→**93%**・god spread 33→17・enemy spread 55→21、致死予告への加護 73→**93%**、神託を残したまま死ぬ敗北 16→**5%**、bonus 成立 0.2→3.4回/戦、機工師 13→18（読めば 54→93）・魔獣 12→29（45→79）。**主要改善**：カード順序、Enemy Intent 対応、神託判断、高神階の受け、神固有の順序判断（大耀＝共鳴を溜めて号令→豪快、蒼毘＝盾→反撃の刃）、負けた後の再挑戦理由。実プレイ7戦（6勝1未撃破）すべてで「一番迷った判断」が書け、未撃破は神託（天啓）の使い切りという判断ミスとして説明できた。**RC 判定：B → blocker 1件を修正して RC**。**blocker**：`.claude/settings.local.json`（Claude Code のローカル専用の許可設定）が 2026-08-15 の checkpoint commit から git 管理下にあった（Phase 5 の commit は一度も触れていない。保護はこの PC のグローバル ignore だけで、リポジトリの `.gitignore` に規則が無かった）。**対応**：`git rm --cached` で index からのみ外し（ローカルファイルは残存・内容は不変・内容は commit に含めない）、`.gitignore` に `.claude/settings.local.json` の1行だけを追加（`.claude/` 全体は無視しない。`.claude/settings.json` などの共有設定は対象外のまま）。※過去の commit に残る旧版の内容は履歴書き換えをしないため残る。**RC 状態**：Phase 5 gameplay blocker なし、tests（src 1113 passed / api 39 passed）・tsc・lint・clean build PASS、deterministic replay・golden replay PASS、saveVersion **9**、gameVersion **`1.80c6eda23ed082dc`**、Daily seed・Daily 3回（`attemptsPerDay` 3）不変（Daily・Ranking のコードは Phase 5 開始以降無変更）、Ranking dormant（`submissionEnabled` false）、Production env・Neon 変更なし、`.claude/settings.local.json` 追跡解除済み。**Known Risk（RC blocker にしない。次回以降の改善候補）**：恵比寿の神格が薄い／笑蓮の専用札比率が低い（19%）／大耀の共鳴4〜6 vs 7 の判断が伝わりにくい（神の一撃後にゲージが0へ戻ることが文で説明されない）／神階Ⅶ の大技ラウンドで神託残0 41%／報酬3択が弱い／OTOMO の判断が弱い／デッキ構築差が薄い（福永・大耀は共通12枚が同じ）／機工師の固定方策勝率が低い（18%）／高神階の一部に手札不足の敗北（固定方策Ⅶの敗北の89%）／神の一撃・大技演出の「間」／敗因表示が無い。**次**：リリース準備は CEO 判断（master merge、gameVersion が変わっているため Production 反映は JST 0:00 直後、Production env・submissionEnabled は据え置き、ランキング公開は別判断）。RC 後の小さな UX 改善候補は「敗因の1行」「共鳴パネルの“発動後0に戻る”1行」「デッキ構築の内部スクロール」。**判定：Phase 5-F PASS / CLOSE、Phase 5 PASS / CLOSE、feature branch は Release Candidate。** |
| 2026-09-12 | 決定162を追加。「**Phase 6 商用品質監査を承認し、Phase 5 RC はそのまま公開せず Phase 6-A Combat Juice Patch を先行する**」（監査・採点・ロードマップ案は **AI判断**（CLAUDE.md §6-2）。「6-A を先に実装し、その後 RC 公開を再判定」の採用は **CEO判断**（2026-09-12 承認）。番号は CEO 指定。決定161 は欠番）。**入力**：`docs/PHASE6_COMMERCIAL_QUALITY_AUDIT.md`（commit `6f823dd`）。**監査総合点 56 / 100**（23項目平均、商用目標 82）。CEO 仮説「敵を倒す爽快感がなく、HP ゲージを減らす作業に感じる」は演出タイムラインの実測で裏付け（HP バーが着弾表示より先に減る、突進・斬撃・数字・シェイクが同一フレーム、致死 BURST で報酬が神の一撃より先に出る、敵が倒れない）。**決定内容**：①Phase 5 RC はそのまま公開しない ②**Phase 6-A Combat Juice Patch を先行**し、完了後に RC 公開を再判定する ③**6-B 以降（Battle HUD 統合・神選択・アート最小対応・SFX 層化・報酬・OTOMO）は Production 公開の必須条件ではない** ④**ゲームルール／バランスは変更しない**（6-A は表示・演出レイヤーのみ。内部 HP 更新タイミング・ダメージ計算・カード・敵・stakes・Guard・神 Passive・BURST・スコア・報酬内容・Daily・saveVersion・Ranking・Neon・Production env は不変）⑤最優先課題は「**攻撃した → 効いた → 追い詰めた → 倒した**」の体感形成。**6-A の優先順**：1. Combat Juice（着弾順序・Ghost HP・hit stop・ダメージ数字）2. 撃破演出 3. 神の一撃の結果順序 4. 勝利遷移。6-A の Acceptance を満たした場合のみ決定163 を記録する。master merge／Production deploy は引き続き CEO 判断。 |
| 2026-09-12 | 決定163を追加。「**Phase 6-A Combat Juice Patch — 実装完了・Acceptance PASS**」（実装方式・時刻設計・テスト方式は **AI判断**（CLAUDE.md §6-2）。Phase 6-A を先行する方針の承認は **CEO判断**（決定162））。**内容**：表示・演出レイヤーのみで「攻撃した → 効いた → 追い詰めた → 倒した」を作る。①**着弾計画**（`combatTimeline.ts`・純関数）を1か所の真実にし、数字・音・敵リアクション・表示HP・撃破演出が同じ時刻を共有 ②**表示HP**（`visualHp.ts`）＝`min(最大HP, 実HP＋まだ見せていないダメージ)`。**engine の HP 更新タイミングは従来どおり**（カード 280ms・敵ターン 700ms、`applyAction` は即時）で、遅れるのは画面表示だけ ③ゴーストHP（主バー 180ms／ゴースト 120ms 待ち＋340ms） ④対象要素だけの **hit stop**（L1 0／L2 20／L3 45／L4 60／神の一撃 80／最後の一撃 90ms。ページ全体は止めない） ⑤ダメージ数字を立ち絵の胸へ移動（HP 数値と重ならない）・段階別サイズ 16/22/30/40px＋**最大クラス 52px**・⚡は金色で本体の 150ms 後 ⑥敵リアクション（ノックバック→揺れ→閃光）、HP 50%／25% 以下の静かな傷の色 ⑦**撃破演出**：最後の一撃は常に L4 → フラッシュ → 崩壊520ms → 「撃破」850ms → 報酬（カード以外の撃破＝得意技の反撃も同じ順序） ⑧**神の一撃**：到達反応200ms → カットイン → 溜め → 突き → 着弾1,600ms。結果（HP・トースト・数字）は着弾まで隠す。致死時は完走してから撃破・報酬 ⑨勝利カードは「自己ベスト」をスコアの後へ ⑩`animationend` 非依存（全て `setTimeout`＋安全弁）・`prefers-reduced-motion` 対応・PC幅で相手と逆向きだった突進の向きを修正。**Before/After 実測**（ヘッドレス・同一seed）：通常100 の表示HP開始が **着弾の35ms前 → 着弾の+89ms**、大ダメージ250 **−30ms → +174ms**、敵必殺 **−1,313ms → +123ms**、神の一撃・非致死 **−1,297ms → +162ms**、神の一撃・致死の報酬 **1,325ms（着弾1,591msより前）→ 3,424ms（着弾1,950ms・崩壊2,196ms・撃破2,576msの後）**、通常カード致死の報酬 1,331 → 1,948ms（崩壊690ms）、反撃致死 1,772 → 2,681ms（崩壊1,456ms）。撃破演出の追加は最後の着弾から **+1.44〜1.50秒**。**Acceptance**：`src/core` 差分 0 行（ゲームルール・バランス・Daily・Ranking・Neon・env 不変）、saveVersion **9**、gameVersion **`1.80c6eda23ed082dc`**（表示専用のため指紋不変）、既存 tests 3,320 passed / 28 skipped、追加 tests 30 件、tsc・lint（error 0）・clean build PASS、390px（横スクロール0・ページ高2,354px）・1366×768 PASS、console error 0、7戦（7神×7敵・神階0〜Ⅴ）全戦で撃破演出→「撃破」→報酬→結果の順序を確認。**安全弁の実証**：演出中に全アニメーションを 60ms ごとに `cancel` しても操作不能・報酬待ちにならない。**成果物** `docs/PHASE6A_COMBAT_JUICE.md`、監査用 `scripts/phase6-audit/{timeline,shots,qa,inspect}.mjs`。**Known Risk**：突進の向き変更の見え方、密度の高い敵絵の上での数字可読性、神の一撃が約340ms長い、撃破で敵が消える間、1366×768 の予告・敵HPの画面外と固定ボタンの重なりは 6-B の範囲、手触りの最終判断は CEO 実プレイ。**次**：CEO 実プレイ → RC 公開の再判定（master merge・Production 反映は CEO 判断、JST 0:00 直後）。 |
| 2026-09-12 | 決定164を追加。「**Phase 6-B：Battle Viewport Layout を正式採用**」（設計監査・比較・計測は **AI判断**（CLAUDE.md §6-2）。D案の採用は **CEO判断**（2026-09-12 承認））。**入力**：`docs/PHASE6B_BATTLE_HUD_DESIGN.md`（commit `f02a920`）、Phase 6-A（決定163・commit `3a9ffbe`）。**採用する構造**：上＝戦闘ステータス（compact HUD）／中＝**可変 Arena**（残りの高さ）／下＝託宣・アクションバー＋**Hand Dock**（grid の行として場所を確保）。**`position: fixed` による手札ドック方式は不採用**。理由：①固定ドックは Enemy HP・Intent を遮蔽する（実測：CEO 環境で敵HP・予告ともに可視 0%）②ページ高さ自体を減らさない③Combat Juice の着弾を見るという目的を満たさない。**Phase 6-B の最重要目的**：「カードを選ぶためにスクロールした結果、敵への着弾が見えなくなる」問題の解消（現状実測：1366×768・90% で手札まで 531px スクロール、その位置で敵ポートレート可視 45%／100% では 23%／390px では 0%、さらに固定の「ラウンドを終える」がカードに重なる）。**最重要 Acceptance**：カード選択 → 発動 → 攻撃 → 着弾 → damage number → 敵リアクション → 表示HP減少 → 撃破 までを**手動スクロールなしで見られること**（Phase 6-A の演出を画面外で再生しない）。**変更してよい範囲**：Battle layout／Battle HUD／responsive CSS／キャラクター配置・接地／視覚階層／End Round 位置／Intent 表示／背景上の可読性。**変更禁止**：カード数値・敵数値・stakes・Guard・神 Passive・Combat Juice のタイミング・ダメージ計算・BURST・スコア・報酬・Daily・Ranking・Neon・saveVersion・gameVersion 指紋・Production env。**実装完了後、Acceptance を全て満たした場合のみ決定165 を記録する。** master merge・Production deploy は行わない。 |
| 2026-09-12 | 決定165を追加。「**Phase 6-B：Battle HUD / Viewport Integration — 実装完了・Acceptance PASS**」（実装方式・レイアウト構造・breakpoint・テスト方式は **AI判断**（CLAUDE.md §6-2「技術実装方式／レスポンシブ対応／軽微なUI・UX調整／テスト方式」）。D案「Battle Viewport Layout」の採用は **CEO判断**（決定164））。**内容**：戦闘画面を「画面の高さに収まる grid」（上＝ステータス／中＝可変アリーナ／下＝ドック＝託宣＋手札＋操作）へ再構成し、名前・HP・予告・共鳴を立ち絵の**上**の名札（`.enemy-plate` / `.player-plate` / `.god-otomo-plate`）へ集約した。操作 UI は grid の行として場所を確保し、**決定77 の `position: fixed`「ラウンドを終える」バーは廃止**（固定要素はページ高さを減らさないため、敵 HP・予告を覆うだけで問題を解決しないことを実測で確認）。sticky 戦況バー（`.battle-hud` / `BattleHud.tsx`）は名札と二重になるため削除。アリーナは `overflow: visible` にして Phase 6-A の Combat Juice を絶対に切らない。**逸脱（AI判断）**：アリーナは設計の2列ではなく**3列**（敵／神／共鳴・OTOMO。`.ally-row` を `display: contents` にするだけで DOM は無変更）。縦積みだと右列の高さを共鳴・OTOMO パネルに食われ神の立ち絵が 64〜94px まで潰れることを実測したため。結果、敵:神 = 0.83〜0.87（設計 0.82）を全解像度で維持。【型】・攻略説明は高さ ≤700px に加え幅 <900px でも省略（390px では折り返しで名札が 37px 高くなり敵＜神の逆転が起きるため）。**実測（Before → After・CSS px・遮蔽とクリップを考慮した可視率）**：手札まで必要なスクロール 597/531/285/1130/1194/1023 → **すべて 0**。その位置での敵ポートレート可視率 23%/45%/100%/0%/0%/0% → **すべて 100%**。敵HP・予告 0% だったモバイルも **100%／100%**。予告 13px → **PC 20px・モバイル 17px**。「ラウンドを終える」とカードの重なり **あり → なし**。横スクロール 0 のまま。**QA**：7戦 × 3 viewport（1508×660／1358×594／390×760）＝ 21 戦で、着弾サンプル 312 件すべて敵可視率 **100%（最小も100%）**、scrollY 0、JS エラー 0、撃破も 100% 可視。設問「カードを操作している状態のまま、敵への着弾を見ることができるか？」→ **できる**。**Visual Regression**：6 viewport × 6 状態＝36 枚すべてで敵 100%・予告 100%・scrollY 0・ページはみ出し 0。**Phase 6-A 統合**：表示HPが動き出すのは着弾の **+82〜+177ms 後**（6-A の核を維持）、報酬が着弾・撃破・勝利の一拍より先に出るケース 0、撃破の崩壊 521ms。**回帰**：`vitest` 3,331 passed（新規 構造回帰 11 件）／`tsc -b` 0／`lint` 新規警告 0／`build` 成功／**`src/core` 差分 0 行**／`saveVersion` 9・`gameVersion` `1.80c6eda23ed082dc` 不変。**記録**：`docs/PHASE6B_BATTLE_HUD_IMPLEMENTATION.md`。**判定：PASS**。master merge / Production deploy は未実施（CEO 判断事項）。 |
| 2026-09-12 | 決定166を追加。「**Phase 6 Commercial Benchmark Audit v2（commit `24e71d9`）を正式受理（PASS）。Production 前の Pre-Release Patch は Phase 6-C「判断の手応え」＝ Decision Feedback ＋ Battle Recap の 1 テーマだけ実施する（GO）**」（監査・比較・テーマ選定案は **AI判断**（CLAUDE.md §6-2）。受理と 6-C GO は **CEO判断**（2026-09-12 承認））。**位置づけ**：新しいゲームシステムではない。Phase 5 までに存在する良い判断（Enemy Intent 読み・Divination Guard・完全防御・card bonus⚡・combo・charged・blocked・神専用条件・神 Passive）を「自分が上手くプレイできた」と理解できる形で返す **presentation layer**。**絶対条件**：変更は表示層のみ。カード／敵／神の性能・スコア・Mastery・報酬ルール・AP・Intent・Divination・BURST・OTOMO・神の一撃・stakes・Daily・Ranking・Neon・env・saveVersion・gameVersion・master・Production は変更禁止。`src/core` のゲームロジック差分 0 行を目標とし、既存イベント・既存 state から判定する。**Part A Moment Callout**：良い判断が成立した瞬間だけ約 0.6〜0.8 秒の短い評価を出す。1 action batch につき最大 1 種類（複数成立時は priority で 1 つ：①生死・大技への優れた防御 ②神固有 Identity の成立 ③複数条件の組み合わせ／予告読み ④条件成立）。「カードを使った」「ダメージを与えた」だけでは出さない。6-A の数字・被弾・HP・神の一撃・最後の一撃と視覚競合しない位置・時刻。完封は「敵が実際に攻撃し、ブロックで HP 被害 0」を事実として判定できるときだけ（元から 0・無害・演出だけの行動では出さない）。**Part B Battle Recap**：勝敗時にその戦闘で実際に起きたことから最大 3 行。勝利＝なぜ上手くいったか（記録可能な事実のみ）。敗北＝次の 1 戦へ学びを 1 つ（ルールベースのみ・推測禁止・自由文章禁止・事実が足りなければ表示しない・「この手を出せば勝てた」等の断定禁止）。**Result Flow**：Combat → Victory/Defeat → Recap → Score/Mastery → Reward の認知順序へ、低リスクで順序だけ直せる場合に限り実施（報酬ルール・大規模 modal 再設計は禁止。高リスクなら Known Risk）。**保護**：6-A（visual HP delay／ghost HP／damage tier／hit stop／enemy reaction／God Strike timeline／final blow／defeat collapse／reward-before-impact 防止）と 6-B（PC・Mobile 一画面／page scroll 0／敵・予告・手札の可視／End Round 非重複／着弾可視）を絶対に壊さない。callout でページ高を増やさず overlay で処理。390px 必須確認。prefers-reduced-motion 尊重（大きな scale／shake／flash は足さない）。新規 SFX は作らない。**QA**：presentation event を数えられるようにし、最低 14 戦（7 神×2・敵 7 体・PC 9 以上・Mobile 5 以上・normal〜神階Ⅴ）。Callout Acceptance：平均 1 戦 3〜8 回、0 回の戦闘が大量に出ない、同時 1 種類、同一 action 重複 0、誤判定 0、数字を読めなくする 0。Recap Acceptance：勝利は固有の事実を最低 1 つ、敗北は高信頼 guidance がある場合のみ 1 つ、虚偽 0、generic 固定文だけは不可、最大 3 行。**目標**：Benchmark の Decision Feedback 55→70 以上、Loss Learning 55→65 以上、Victory 62→68 以上（実プレイ根拠つき）。**Release Hygiene（BGM 圧縮・CLS・44px・未参照 PNG 除外）は 6-C に混ぜず、6-C 後の Release Hygiene Gate で扱う**。完了判定は決定167 として別途記録。 |
| 2026-09-12 | 決定167を追加。「**Phase 6-C「判断の手応え」＝ Decision Feedback ＋ Battle Recap — 実装完了・Acceptance PASS**」（判定ルール・priority・文言・表示位置・振り返りのルール・結果画面の順序変更は **AI判断**（CLAUDE.md §6-2）。6-C の GO は **CEO判断**（決定166））。**内容**：既存 GameEvent と RULES 値だけから「完封！（敵が実際に攻撃しブロックで HP 被害 0）／無力化！（予告があったのに実行値 0）／反撃！（蒼毘 PASSIVE）／得意技！（笑蓮・福永 PASSIVE）／⚡ 条件成立・⚡ 連携！（BONUS_TRIGGERED の条件別）」を検出し、着弾の 240ms 後に約 0.7 秒だけ、浮遊数字の帯の外（PC＝敵と神のあいだ／SP＝神の頭上）の overlay に出す。1 バッチ最大 1 件（priority ①大技への防御 ②神 Identity ③予告読み ④条件）、同一ラウンドの同じ⚡は 1 回、神の一撃・決着のバッチでは出さない。結果画面に「振り返り」（勝利＝神の一撃で決着／大技を N 回無傷／HP N% から立て直し／得意技 N 回／⚡ N 回。敗北＝実行値と盾の実数・託宣の残回数・神力の残し・⚡未成立の高信頼ルール 1 行のみ。該当なしは無表示）を勝敗の直下に置き、**結果 → 振り返り → スコア／神技評価 →「報酬カードを選ぶ」→ 報酬 → 結果** の順序へ（報酬の 3 枚・上限 +1・1 回性は不変）。**QA（15 戦：7 神×2・敵 7 体・PC 9＋reduced-motion 1／SP 5・normal〜神階Ⅴ）**：callout 合計 79 回・1 戦平均 5.3 回（最小 2・最大 10・0 回の戦闘 0）。種類：bonus-combo 23・bonus-enemyBig 10・perfect-big 9・passive 8・bonus-blocked 7・neutralized 6・bonus-charged 6・perfect 5・counter 3・neutralized-big 1・bonus-lowHp 1。同時表示最大 1・同一 action 重複 0・誤判定 0・JS エラー 0。callout 表示中：scrollY 0・横はみ出し 0・浮遊数字／予告／手札／敵の顔／ミニ結果との重なり 0/0/0/0/0px²。勝利 14 戦すべて 結果 → 報酬 → 結果（通常ボタン）。敗北 1 戦：#7「R7：310の大技に対し、盾は20でした。加護や防御札で予告ぶんの盾を用意すると受け切れます」。**回帰**：`vitest` 3,357 passed（新規 26）／`tsc -b` 0／lint 新規警告 0／build 成功／**`src/core` 差分 0 行**／saveVersion 9・gameVersion `1.80c6eda23ed082dc` 不変／Daily・Ranking・submissionEnabled・Production・master・Neon・env 未変更。**Benchmark 再評価（実プレイ根拠）**：Decision Feedback 55→72、Loss Learning 55→66、Victory 62→70。**記録**：`docs/PHASE6C_DECISION_FEEDBACK.md`。**判定：PASS**。次は Release Hygiene Gate（BGM 圧縮・CLS・44px・未参照 PNG 除外）→ Release Audit → master merge／Production（CEO 判断）。 |
| 2026-09-12 | 決定168を追加。「**Phase 6-D Visual Direction Audit（commit `27a2225`）を正式受理（GO / PASS）。Production 前の Visual Patch は「神の一撃の専用舞台化」1 テーマのみ実施する**」（監査・実画面モック比較・テーマ選定は **AI判断**（CLAUDE.md §6-2）。受理と 1 テーマ限定の GO は **CEO判断**（2026-09-12 承認））。**Post-Release へ送るもの**：Victory の余韻／World UI／神選択の儀式化／Enemy Select／Color Grading／Character Grounding／Transitions／Typography 全体／敵立ち絵の再処理。**目的**：現在の神の一撃は「通常戦闘の上に画像を出した大技」に見える。これを「戦闘中最大の儀式」として成立させる。月蝕綺譚からは **専用舞台化・視覚階層・1 画面 1 主役・必殺時だけ空気が変わる** という原則だけを取り、固有デザイン（三日月・満月構図・月蝕環・墨刷毛帯・赤金の固有配色・固有紋様・UI フレーム形状・ロゴ書体・構図）は一切コピーしない。SEVEN GODS 側の軸は **「七」「円」「共鳴」「神色」**。**変更禁止**：`src/core` gameplay logic・damage・resonance・神の一撃性能・OTOMO 進化・BURST・enemy・cards・stakes・score・Mastery・Daily・Ranking・saveVersion・gameVersion・Neon・env・master・Production。新規 SFX 禁止・新規画像生成禁止（既存素材＋CSS＋presentation JSX のみ）。**保護**：6-A（God Strike timeline／visual HP delay／ghost HP／damage tier／hit stop／enemy reaction／fatal hit／defeat collapse／reward-before-impact 防止、とくに resonance-cutin → burst banner → impact → OTOMO evolution の時間的受け渡し）、6-B（PC・Mobile 一画面／scrollY 0／横はみ出し 0／敵・予告・手札・着弾の可視／End Round 非重複。cut-in で layout height を増やさない＝overlay のみ）、6-C（Decision callout／Battle recap／Result flow は不変。神の一撃中は既存仕様どおり callout を出さない）。**Visual Structure**：①戦場を一段暗くする ②神を主役化する円形マスク ③神色 accent ④集中線／radial light ⑤神名 ⑥固定技名「神の一撃」 ⑦短い副文「共鳴発動」 ⑧既存 burst へ自然に戻る。文字・装飾は増やしすぎない。**Typography**：God Strike 内だけ明朝系 stack 可（フォントファイル追加禁止）。神名＝小〜中／「神の一撃」＝最大主役／「共鳴発動」＝sub。数字系 HUD は既存 sans のまま。**God Color**：既存テーマカラーのみ。per-god 色の正式 source を 1 つに寄せ、色を新規ハードコードしない。**Mobile**：390px で縦書き／横組みを実画面比較し、可読性・視線速度で AI 側が選ぶ（CEO へ選択を求めない）。**Reduced Motion**：大きな scale・回転・強い zoom・連続 flash を無効化。ただし暗転・神名・神の一撃・神色・円マスクは静的に表示し、God Strike と分かるようにする。**Acceptance**：①スクリーンショット単体で通常攻撃と明確に区別できる ②神が画面の主役 ③技名が 0.5 秒以内に認識できる ④月蝕綺譚の模倣でなく SEVEN GODS 独自に見える ⑤PC/SP とも文字切れ 0 ⑥scroll 増加 0 ⑦6-A timing regression 0 ⑧6-B viewport regression 0 ⑨6-C regression 0。7 神すべての cut-in を PC・Mobile390 で撮影し、reduced-motion も最低 1 神。Before/After を同一条件で撮影して docs に添付。**Release Hygiene（BGM 圧縮・CLS・44px・未参照 PNG 除外）は本 patch に混ぜず、完了後に別 Gate として実施する**。完了判定は決定169 として別途記録し、Acceptance 不達なら PASS にしない。 |
| 2026-09-12 | 決定169を追加。「**Phase 6-D Visual Patch v1「神の一撃・専用舞台化」— 実装完了・Acceptance PASS/CLOSE**」（舞台の構造・記号の選択（七／円）・文字階層・モバイルの横組み採用・撮影／計測方式は **AI判断**（CLAUDE.md §6-2）。1 テーマ限定の GO は **CEO判断**（決定168））。**内容**：`.resonance-cutin` の**中身だけ**を 3 層へ組み替えた。①`rays`＝神色の集中線（`repeating-conic-gradient`）＋周辺減光で戦場をもう一段沈める ②`band`＝−3.5° の黒帯（上下に金のヘアライン、両端フェード）＝神を置く舞台 ③`group`＝**円マスクの神**（`border-radius:50%`＋神選択と同じ `KEYVISUAL_OBJECT_POSITION`）＋**七つ刻みの環**（360°/7＝51.4286°。共鳴ゲージ 7/7 と同じ分割）＋「神名／**神の一撃**／共鳴発動」の 3 階層。明朝スタックは**カットインの中だけ**（フォントファイル追加なし・`@font-face` 0・無い環境は `serif` へフォールバック。数字系 HUD は既存 sans のまま）。神色は per-god 色の正式 source を 1 つに寄せた **`GOD_THEME_COLOR`**（`godStyle.ts`。旧 `OTOMO_THEME_COLOR` を改名、値は不変）だけを使い、コンポーネントにも CSS にも色を直書きしない。**新規画像 0・新規 SFX 0・`filter` 0・`url()` 0**。**独自性**：月蝕綺譚固有の三日月／満月構図／月蝕環／墨刷毛帯／赤金の固有配色／固有紋様／UI フレーム形状／ロゴ書体／構図はいずれも使用せず、SEVEN GODS の軸「七・円・共鳴・神色」で構成した。**タイムライン**：ルート要素・`resonance-cutin-dim 0.2s`・`resonance-cutin-timer 0.9s`・`onAnimationEnd` の判定・安全弁は**無変更**。子アニメーションは最遅 610ms で全て 900ms 以内（テストで自動検査）。**QA（7 神 × PC 1508×660／Mobile 390×760／PC 1366×768 ＋ reduced-motion 1 神＝17 本、すべて成功）**：3 層＋環 17/17、文言 17/17、神色 7 色すべて一意、全要素が viewport 内 17/17、`scrollY` 0・横はみ出し 0・`pointer-events:none`・`z-index` 4、**文字切れ 0**（`overflow:hidden` 0 件＋目視 17 枚）、**JS エラー 0**。**Before/After**（`git stash` で patch を外し同一スクリプト・同一 seed・同一端末で再撮影）：主役性 5→9／必殺感 4→8／世界観統一 4→8／視線誘導 5→9／情報量 6→7／モバイル可読性 5→8。**Mobile は縦書きではなく横組みを採用**（AI判断：4 文字なので 390px でも幅 199px に収まる／視線移動が 1 方向／縦中横・約物回転の端末差リスクを避ける）。**Reduced Motion**：拡大・回転・スライド・字間アニメを無効化し、暗転・神名・技名・神色・円マスク・環・帯は静的に全表示。**Performance（390px・rAF 実測）**：カットイン区間 avg 46.2→**35.4ms**、p95 116.7→**83.3ms**、max 166.7→**83.3ms**（同一戦闘の通常演出＝対照区間よりも軽い）。**フレーム落ちの増加なし**。**回帰**：6-A タイムライン Before/After 実走で `burstBanner` +11ms・`visualHp` −10ms・`defeat` +7ms・報酬 +14ms＝**意味のあるズレなし**、**reward-before-impact 0**（致死 3 シナリオとも報酬が着弾・撃破の後）。6-B は overlay のみでページ高さ不変・scroll 0。6-C（callout／振り返り／結果順序）は 1 行も変更せず、神の一撃中に callout を出さない既存仕様も維持。`vitest` **3,368 passed**（新規 11）／`tsc -b` 0／lint 新規警告 0／build 成功／**`src/core` 差分 0 行**／saveVersion **9**・gameVersion **`1.80c6eda23ed082dc`** 不変／Daily・Ranking・submissionEnabled・Production・master・Neon・env 未変更。**Known Risk**：明朝フォントの端末差（Android は `serif` フォールバック）・集中線のモアレ・帯のヘアラインが超ワイドで目立つ可能性・撮影ハーネスの凍結アーティファクト・笑蓮は共鳴 7/7 に到達しにくく QA で敵と手順を変えた（演出ではなくプレイ上の性質。バランス未変更）。**記録**：`docs/PHASE6D_VISUAL_PATCH_V1.md`。**判定：PASS / CLOSE**。次は Release Hygiene Gate（BGM 圧縮・CLS・44px・未参照 PNG 除外。本 patch には混ぜていない）→ Release Audit → master merge／Production（CEO 判断）。Post-Release は 6-D 監査 §19 の順序（勝利の余韻＋遷移 → 明朝化・World UI・敵選択 → 神選択・Color Grading・Character Grounding → 敵立ち絵の再処理）。 |
| 2026-09-12 | 決定170を追加。「**Release Hygiene Gate — 配信容量・画面安定性・Mobile 操作性・不要アセットの整理：PASS / CLOSE**」（コーデックとビットレートの選定・タップ領域の広げ方・原素材の移動先・監査方式は **AI判断**（CLAUDE.md §6-2）。Gate の実施は **CEO指示**）。**位置づけ**：ゲーム内容を一切変えずに Production 公開前の配信品質だけを整える。ゲームデザイン・演出追加は行っていない。**①BGM**：原音源（MP3 200kbps・4 曲・計 18 分 6 秒・25.7MB）を `audio-source/bgm/` へ退避し、そこから **Opus 48kbps/WebM（主）と MP3 96kbps（フォールバック）** を書き出した。形式は実行時に `canPlayType` で選ぶ（判定不能なら従来どおり MP3）。選定根拠は帯域別エネルギーの実測：Opus 48k は **尺が完全一致**（＝`loop` のつなぎ目が不変）・RMS 差 +0.03dB・可聴域の最悪帯域差 −0.07dB。MP3 64k は 12〜16kHz が −75.8dB 落ちるため不可、AAC 64k も −7.6dB で不可。フォールバックを 80k でなく 96k にしたのは 80k が最大 −8.6dB 落ちたため（取りに来るのは WebM 非対応環境のみなので品質優先）。**実測**：配信ディレクトリ 25.7→19.25MB、**実際に転送される音楽 25.7→6.82MB（−73%）**、1 セッション（ホーム＋戦闘）13.2→**3.43MB（−74%）**。曲・再生タイミング・ループ位置・音量（0.35）は不変で、通常経路・フォールバック経路とも実ページで再生・ループ・尺を確認（失敗リクエスト 0・JS エラー 0）。**②CLS**：Benchmark v2 の「SP 0.94」は**計測方法による値**だった（JS の `element.click()` では `hadRecentInput` が立たず、画面遷移そのものまで CLS に数えていた）。信頼できる入力で測り直すと **PC 0.0074 / SP 0.046** で元から目標 < 0.1 を満たしていた。そのうえで実機で効く箱の確保漏れを 2 件修正：`.home-portrait-card` が `margin: 0 auto` で shrink-to-fit になり画像待ちの間**高さ 3px → 427px** に跳ねていた問題（`width: 100%` を追加して `aspect-ratio` に箱を確保させた）と、`<img>` の寸法未指定（実寸が一定のものに `width`/`height` 属性を付与。見た目は不変）。**③44px タップ領域**：Mobile 390px / PC の実測で 44px 未満だった操作要素 **PC 105 / SP 106 → いずれも 0**。ホームの主要ナビ・デッキの主要ボタン・チュートリアルの「わかった」は `min-height: 44px`、ヘッダーアイコン（34/26px）・枚数 ±（40px）・託宣（83×39）・ログ（112×29）は**見た目を変えずに `::after` で判定だけ拡張**した。**とくに戦闘画面**：当初 `min-height` で広げたところ、ドックが数 px 伸びてアリーナが縮み、6-C の callout が敵の顔の判定枠に触れる（15 戦 QA の #7 で 48px²）ことを実測したため、**レイアウトを 1px も動かさない `::after` 方式へ変更**した。End Round・結果画面のボタンは元から 44px 以上。**④不要アセット**：`public/` は丸ごと `dist/` へコピーされるため、実行時に未参照のものを洗い出し（カード PNG マスター 200.02MB／神キービジュアル原本 20.29MB／OTOMO 旧版・透過版 20.48MB／BGM 原音源 25.7MB＝**計 266.5MB**）、**削除せず** `art-source/`・`audio-source/` へ `git mv`（履歴保持）。`art-source/README.md` に用途を記載し `.vercelignore` にも追加。再混入は `scripts/release-hygiene/assets.mjs` で継続監査できる。**Performance**：`dist` **298MB → 49.9MB（−83%）**、`public` 290→49.3MB、画像 264→29.8MB、音声 25.7→19.6MB、初回転送 6.81→**2.60MB（−62%）**、戦闘開始までの累計 21.79→**12.30MB（−44%）**。JS 420.6→420.9kB・CSS 125.9→126.8kB（ほぼ同じ）。**回帰**：6-A タイムライン（cut-in→burst 1,089〜1,095ms・**reward-before-impact 0**）、6-B 6 解像度（scroll 0・横はみ出し 0・敵/敵HP/予告/神HP/共鳴/託宣 すべて 100%・手札 4/4・End Round 非重複）、6-C 15 戦（誤判定 0・重複 0・同時 1・scroll 0・数字/予告/手札/ミニ結果の重なり 0・勝利 14 戦すべて 結果→報酬→結果）、6-D 神の一撃 6/6、全 9 画面スモーク（壊れた画像 0・404 0・JS エラー 0、**Daily 含む**）。6-C の「敵の顔との重なり」だけ 21〜45px² が 15 戦中 1〜2 戦で出たが、**同一ビルドの再実行で出る戦闘も値も変わる**（敵立ち絵が `enemy-idle` で常時動いているため）ので Gate による回帰ではない。`vitest` **3,368 passed**／`tsc -b` 0／lint 新規警告 0／build 成功／`src/core` ロジック差分 0（コメント内のパス表記 4 行のみ）／saveVersion **9**・gameVersion **`1.80c6eda23ed082dc`** 不変／Daily・Ranking・submissionEnabled・Neon・env・master・Production 未変更。**Known Risk**：MP3 フォールバックは 12〜16kHz が最大 −2.1dB（WebM 非対応環境のみ）／ジングルは `JINGLE_MAX_MS` で 9.5 秒しか鳴らないのに 280s・252s を配信している（切り詰めれば約 11MB 減るが「曲の内容は変更禁止」に従い未実施）／`art-source/` をアプリから参照してはいけない／`ffmpeg-static` は devDependency にせず一時導入。**記録**：`docs/RELEASE_HYGIENE_GATE.md`、監査スクリプト `scripts/release-hygiene/{audio,audio-playback,assets,cls,transfer,screens-smoke}.mjs`。**判定：PASS / CLOSE**。次は Release Audit → master merge／Production（CEO 判断。gameVersion 不変のため JST 0:00 制約なし）。 |
| 2026-09-13 | 決定171を追加。「**Clean Release Audit — Production Release Candidate 監査：Clean Release Candidate PASS / READY FOR CEO QA**（Production GO ではない）」（transport 方式・除外範囲・切断箇所・監査方式は **AI判断**（CLAUDE.md §6-2「技術実装方式／QA方式／GO・NO-GOの技術判断」）。監査の実施と「feature branch を whole merge しない」「master・Production を変更しない」は **CEO指示**）。**背景**：feature branch `feat/daily-ranking-phase4`（base `489352c` から 59 commit）は、未公開のランキング backend／Neon／API（Phase 4.2〜4.10）と公開対象のゲーム改善（Phase 4.1・5-A〜F・6-A〜D・Release Hygiene）が同じ履歴に混在し、後者が前者の中間状態（`d4eb575` の `.js` 拡張子・`rules.ts`・`GameFlow`）の上に載っている。**方式**：cherry-pick（Phase 5/6 が Phase 4 の中間状態に依存し conflict 解消が手書き patch と同じになる）と revert（空 commit・多重変更で連鎖が壊れ、除外の証明が監査しにくい）を却下し、**clean release branch `release/game-v1-rc`（`489352c` から分岐）に feature tree を丸ごと載せてランキング経路を path 単位で削除し、残った参照 9 箇所を切断する patch extraction** を採用。結果は「master → RC の 1 diff」（282 files、+23,102／−1,000）として全文を機械検査できる。**除外**：`api/`・`src/server/`・`tsconfig.api.json`・ranking hooks（rankingClient／rankingTicketStorage／leaderboardClient／anonymousPlayerId／dailySessionStart）・`DailyRankingPanel`・`core/replay/ranking.ts`・`core/identity.ts`・`docs/PHASE4_*`（12）・`scripts/phase4*`（5）・依存 `@neondatabase/serverless`／`@electric-sql/pglite`（`package.json`／lockfile は master と byte 同一に戻った）。**残したもの**：`RULES.ranking`（`submissionEnabled: false` の不活性設定・fingerprint 対象外）、`core/replay` の replay 基盤、端末内保存の `pendingRunStorage`／`dailyRunLogStorage`／`clientRunId`、`docs/DECISIONS.md` の散文。**機械ゲート**：Ranking Absence 17/17 PASS（backend 0・Neon 依存 0・`/api/` 0・通信 0・kill switch false・dist に api/neon/env 0）、Secret Audit で資格情報形式 0（残りは識別子・散文・sha256）、tracked `.env` 0。**整合**：gameVersion `1.80c6eda23ed082dc`（テスト PASS・転送で不変）、saveVersion 9、storage コードは master と同一。**Save Migration**：master ビルド（別 worktree・4182）で実際に作った通常セーブ／Daily セーブ／戦績を RC（4181）に注入し、「続きから（大耀・ラウンド2）」「続きから（神域挑戦・…）」で round・手札・HP が完全一致、戦績・報酬・神階の保存値も同一（欠損 0）。**Daily**：seed `daily-<date>-<enemyId>`・JST・3 回・週シャッフルとも master と同一関数で、2026-09-13 は master・RC とも `daily-2026-09-13-enemy_05`。ランキングが無いので day-lock／deploy 時刻の制約は無い（同日中の切替はローカル記録に旧版と新版のスコアが混在するだけ）。**QA**：PC 1366×768／1508×660・Mobile 390×760／390×844 で Home→…→戦闘（scroll 0・overflow 0・敵／予告／手札／End Round 同時表示）→中断→続きから→神の一撃→勝利→振り返り→報酬、神域挑戦（残り 3→2、戦績に記録）、敗北（敗因＋助言行）を通し、全経路で外部・`/api/`・失敗リクエスト 0・JS エラー 0。Hygiene 再確認：壊れ画像 0・CLS PC 0.0074／SP 0.046・<44px 0・Opus 再生 OK・未参照 0・dist 51 MB。tests 928 passed／9 skipped、tsc 0 error、lint error 0、build OK。**Release Blockers 0**。**留意**：`DECISIONS.md` にランキング決定の散文が残る（docs は `.vercelignore` で配信外）。**次**：CEO の実機 QA → 承認後に master merge と Production deploy（別 Step・CEO 判断・§6-3 #8）。詳細は `docs/CLEAN_RELEASE_AUDIT.md`、再現は `scripts/release-audit/`。 |
| 2026-09-13 | 決定172を追加。「**Production Release：SEVEN GODS Clean Release Candidate（`release/game-v1-rc` `de039d8`）を Production へ公開 — PASS / LIVE**」（Production 公開は **CEO承認**（CLAUDE.md §6-3 #8・CEO FINAL APPROVAL 2026-09-13）。Git 手順・検証方式は **AI判断**（§6-2））。**Pre-Release Gate**：RC HEAD = origin/release/game-v1-rc = `de039d8`、master = origin/master = `489352c`、working tree clean、決定171 PASS / READY FOR CEO QA、CEO QA PC PASS／iPhone PASS（CEO申告）。**Final Safety Gate（de039d8）**：tests 928 passed／9 skipped、tsc 0、lint error 0、build OK（dist 51 MB、art-source／audio-source／PNG master 0）、Ranking Absence 17/17 PASS、Secret Audit 資格情報形式 0・tracked `.env` 0、`api/` 0・`src/server` 0・Neon 依存 0、`submissionEnabled: false`、gameVersion `1.80c6eda23ed082dc`、saveVersion 9。**master 反映**：push 直前に fetch で origin/master が `489352c` のままであることを再確認し、`git merge --ff-only release/game-v1-rc` で **fast-forward**（feature branch `feat/daily-ranking-phase4` は merge していない。merge-base 検査で非祖先を確認）。master tree hash = RC tree hash（`756e8a4c…`）で完全一致。反映後 master 上で tests／tsc／build と Ranking Absence・Secret Audit を再実行し PASS。**Push**：`489352c..de039d8 master -> master`（通常 push・force なし・history 不変）。local master = origin/master = `de039d8`。**Production Deploy**：Vercel の master 自動 deploy のみ（manual deploy なし・二重 deploy なし）。push 後 33 秒で https://seven-gods-game.vercel.app/ が新 bundle `index-CLclgl5i.js`／`index-IKMGO-ur.css` を配信。Production の JS／CSS／HTML の sha256 が master ビルドと **byte 同一**（旧 Production は `index-Sm3K62_I.js`）。Vercel dashboard の commit SHA は CLI から未参照（token を使わない方針）だが、tree 同一性と時系列で `de039d8` への対応を確認。**Production Smoke QA（本番 URL・headless Chromium）**：PC 1366×768／1508×660、Mobile 390×760／390×844 で Home→神選択→難易度→敵選択→デッキ→戦闘（scroll 0・overflow 0・敵／予告／手札 5 枚／End Round 同時表示）→中断→続きから（round 2）→神の一撃 cut-in→最終打・HP ghost→勝利→振り返り 1 行→報酬 3 枚→通常ボタン、神域挑戦（残り 3→2・戦績記録）、敗北（敗因＋助言行）。全 viewport で通信先は `seven-gods-game.vercel.app` のみ、外部 0・`/api/` 0・失敗 0・JS エラー 0。screens-smoke 壊れ画像 0・404 0。BGM：`home.webm`（Opus）loop・volume 0.35・再生進行 OK、fallback `home.mp3` OK。`/api/ranking/*` はすべて 404、`docs/`・`art-source/`・PNG master は 404（配信外）。Production bundle に `/api/`・neon・postgres・`DATABASE_URL`・`RANKING_`・leaderboard UI の文字列 0、`submissionEnabled:!1`、`engineVersion:1`、`saveVersion:9`。**Existing Save**：旧 Production（`489352c`）を別 worktree でビルドし、そのビルドで作った通常セーブ／Daily セーブ／戦績を本番 URL に注入して「続きから（大耀・ラウンド2）」「続きから（神域挑戦・大耀・ラウンド2）」で round・手札・HP が完全一致、戦績・報酬・神階の保存値も同一（欠損 0・初期化なし）。**Daily**：seed `daily-2026-09-13-enemy_05`・3 回・JST 日付・ボスは旧 Production と同一。**Rollback**：不要（重大問題なし）。必要時は origin/master を `489352c` の内容へ通常 commit で戻す（force push はしない）。**Release Blockers 0**。**留意**：本決定の記録 commit（docs のみ）も Vercel の自動 deploy を起こすが、`docs/` は `.vercelignore` で配信外のため bundle は不変（push 後に hash 同一を確認する）。 |
| 2026-09-13 | 決定173を追加。「**Enemy 7 Production Visual Quality Audit（Post-Release Visual Quality Phase 第1弾・監査のみ）：GO** — 敵7体のうち **datenshi／karakuri／doukeshi の3体は source から 768px・WebP q90 に再書き出し**、**juuma／oni／ryujin／onryo の4体は再生成**（順：datenshi→karakuri→doukeshi→juuma→oni→ryujin→onryo）。画像差し替えは未実施」（監査方式・採点・推奨仕様は **AI判断**（CLAUDE.md §6-2）。監査の実施・AUDIT ONLY の制約・再生成の実行は **CEO判断**）。**事実**：敵は2世代に分かれる。世代B（datenshi／karakuri／doukeshi）は 2026-08-23〜24 の個別生成・約1,100〜1,480px 透過PNGが手元にあり、Production 512px は 0.29〜0.35 倍縮小＋シャープ処理。世代A（oni／onryo／juuma／ryujin）は決定32の **1枚シート（1536×1024）から .NET で切り出した 384×512** が唯一の source で、ドット調・白フチ・ジャギーは元から。高解像度の原本は存在しない（Downloads 8月画像を相関で総当たり）。**「WebP 変換で輪郭 30〜49% 劣化」は再現せず**：onryo／ryujin は純再エンコード（PSNR 33dB・輪郭比 1.00）、oni は 35dB／0.72（q90 相当の軟化 28%）、世代Bは現行の方がクリーン縮小より輪郭エネルギーが高い（1.6〜2.8倍）。**juuma のみ変換で劣化**（本体 357×359 を 1.2 倍拡大配置、輪郭 38%）。**表示**：PC1366×768 で敵 285css（512 の 0.557 倍）、DPR2 で 570 device px＝1.11 倍拡大（唯一の解像度不足条件）。PC1508×660 は 194css、Mobile 390 は列幅が敵ごとに 124〜190css で揺れ（6-B の arena 列幅がプレート内容に依存）、敵は 0.24〜0.37 倍に縮小＝画質でなく小ささの問題（別 Phase）。**神との差**：神 640 asset を 0.373 倍、敵 512 を 0.557 倍で表示＝敵だけ 1.5 倍の相対拡大。神チビ調／世代B painterly／世代A ドット調の3画風が同居。**採点**：datenshi 76・karakuri 73・doukeshi 72・ryujin 59・onryo 57・oni 55・juuma 45。**候補比較**：AVIF は同容量で PSNR は高いが輪郭比 0.85〜0.95 で細部が平滑化し立ち絵に不利、WebP q90 は輪郭比 0.99。640 は PC1366 DPR2 で現行同等（0.99×）、**768 で +18〜21%（PC1508 DPR2 +30%）**、1024 は +42〜49% だが必要 device px 最大 570 を超える。**推奨仕様**：768×768 透過・本体高さ比 0.85・現行の構図比維持・WebP q90 alphaQuality 100・asset 名維持・CSS 変更なし。容量 3体 204KB→440KB（**+236KB**、予算 +2〜4MB の 1/10）。世代Aの再エンコードは改善ゼロなので行わない。**再生成要件**：透過・本体 ≥900px・世代B と同じ painterly・意匠踏襲・単体生成。**リスク**：構図ずれ（6-B／6-C 実測で担保）、alpha 縁 ≤2.5px、世代差の一時拡大、CDN キャッシュ混在。詳細は `docs/ENEMY_VISUAL_QUALITY_AUDIT.md`、再現は `scripts/enemy-visual-audit/`。 |
| 2026-09-13 | 決定174を追加。「**Enemy Visual Upgrade Batch A：datenshi／karakuri／doukeshi の 3 体を高解像度 source から 768px WebP へ再書き出し — PASS / READY FOR CEO QA**（Production GO ではない）」（書き出し方式・構図ロック・品質の選び方・検証方式は **AI判断**（CLAUDE.md §6-2「技術実装方式／QA方式」）。Batch A の実施と「画像生成禁止・AUDIT の 3 体のみ・master/Production 変更禁止」は **CEO指示**。決定173 の GO に基づく）。**やったこと**：`art-source/enemies/` に保全した高解像度透過 PNG（1199×1312／1065×1477／1254×1254、現行 asset との勾配相関 0.980〜0.999 で同一の絵と確認）から、**本体 bbox を Lanczos3 で 1.5 倍の寸法へ縮小し、現行 asset と同じ位置へ置いた 768×768 の WebP** を書き出して同名で置き換えた。**やっていないこと**：AI upscale・生成補完・顔や線の修正・新規シャープ・彩度/色調変更・glow/outline/背景追加・CSS 変更・`enemies.ts` 等の参照変更。**品質**：q90 を既定とし、監査の受け入れ基準（可逆参照比 PSNR ≥33dB）を満たす最小品質を採用 → datenshi q90（36.08dB）・karakuri q90（33.31dB）・**doukeshi のみ q94**（q90 は 32.56dB で未達、q94 で 33.01dB。これが監査仕様からの唯一の逸脱）。**構図ロック**：本体高さ比 0.8496→0.849/0.8503、中心X比 0.500→0.500/0.5013、左右上の余白比も一致し、**位置差 0.37〜0.56 CSS px・サイズ差 0.08〜0.12%**（目標 ≤2px／≤2% の 1/4 以下）。実ブラウザ 56 条件（PC1366/1508 × Mobile390×760/844 × DPR1/2 × 敵7体）で `.enemy-avatar` の x/y/w/h の差は **全条件 0 CSS px**。**透明境界**：実表示（PC1366 DPR2）の縁帯は 2.67→2.29／2.27→1.74／2.66→2.31px と **細くなり**、白フリンジは 5.2→2.4%／1.9→1.0%／0.2→0.6% でいずれも可逆参照と同値＝**符号化が足したフリンジ 0**。黒く見える縁は元の絵が持つ輪郭線で artifact ではない。**画質（実ブラウザ計測 After/Before）**：PC1366 DPR2 **1.86〜2.08 倍**、PC1508 DPR2 1.86〜1.89 倍、PC1508 DPR1 1.54〜1.56 倍。一方 **PC1366 DPR1 は 0.73〜0.77 倍、Mobile は datenshi/doukeshi が 0.73〜0.89 倍**（karakuri のみ 1.19〜1.40 倍）。これは旧 asset に掛かっていたシャープのハローが Laplacian 指標を押し上げていたためで、4〜5 倍拡大の目視では PC1366 DPR1 でも After の方が鎧の唐草・歯車の歯・鎖などの細部は明確に解像している。Chromium は background-image の縮小に mipmap 系フィルタを使い、768px は 194px 表示（≒mip 192）と 570px 表示で有利・285px 表示（mip 384/192 の中間）で不利という並びと一致する。**シャープ追加は CEO 禁止事項のため対処せず**、mip 段に乗る canvas 寸法の再選定を Batch C の検討候補として記録した。**神との関係**：source→画面の縮小率が敵 0.742／神 0.747 とほぼ一致（Before は敵だけ 1.11 倍の拡大）＝**神との品質差は実質解消**。サイズ序列 敵290>神238>OTOMO は不変、敵の輪郭エネルギーは神より低いままで過剰に派手にはならない。**容量**：204KB → **462KB（+258KB、目標 +350KB 以内）**、dist 51MB 不変。**再採点**：datenshi 76→**91**、karakuri 73→**89**、doukeshi 72→**87**（いずれも目標 +8 点以上を達成）。**回帰**：Battle QA 4 viewport で scrollY 0・overflow 0・敵/予告/手札/End Round 正常・神の一撃・最終打・振り返り・報酬順序すべて正常。6-C は旧 asset ビルド（別 worktree）と新 asset ビルドの両方で 15 戦ずつ回し、callout 77→78/76、ダメージ数字・予告・手札・ミニ結果との重なりは **再現する非ゼロ 0 件**（run1 の 7px／76 は run2 で消失）。敵の顔との重なりは `.enemy-avatar` の外形から算出され画像を参照しない指標で、同一戦が 43→0→162 と振れる既知の非決定値（決定170）。6-D の cut-in は神のキービジュアルのみを使い敵 asset を参照しない。**Tests**：3041 passed／9 skipped／0 failed、tsc 0、lint error 0、build 成功、未参照 asset 0、壊れ画像 0、**gameVersion `1.80c6eda23ed082dc`・saveVersion 9 とも不変**。**次**：CEO 実機 QA →承認後に master merge と Production deploy（別 Step・CEO 判断・§6-3 #8）。世代 A の 4 体（juuma/oni/ryujin/onryo）は決定173 の優先順位 4〜7 で再生成待ち。詳細は `docs/ENEMY_VISUAL_BATCH_A.md`、再現は `scripts/enemy-visual-batch-a/`。 |
| 2026-09-13 | 決定175を追加。「**Enemy Visual Batch A — Final Visual Acceptance Gate：GO**（AUDIT/QA ONLY。master merge・Production deploy は未実施）」（判定方式・参照画像の作り替え・GO/NO-GO は **AI判断**（CLAUDE.md §6-2「QA方式／GO・NO-GOの技術判断」）。Gate の実施と「CEOに丸投げせず AI 側で GO/NO-GO を出す」指示は **CEO指示**）。**問い**：Batch A（`cd81ece`）の 3 体が「高解像度では改善しているが PC DPR1 / Mobile の実ゲームサイズでは旧 asset より柔らかく見える可能性」の最終判定。**方法**：OLD（`origin/master ad7734b` を別 worktree でビルド・port 4184）と NEW（branch build・port 4183）を同時に立て、同一 seed・同一状態で **3 体 × 9 条件（PC1366/1508 × DPR1/2、Mobile390×760 × DPR1/2/3、390×844 × DPR1/2）＝27 条件**をエラー 0 で撮影。敵要素の箱は OLD/NEW・製品/計測パスの全組合せで差 0.5px 未満（HUD は display ではなく visibility で隠しレイアウトを不変に保った）。**測り方の修正（今回の肝）**：Batch A で使った Laplacian variance は **旧 asset に焼き込まれたシャープの halo に強く反応する**ため比較に使えない。①参照を source からの **Mitchell 縮小**（リンギングをほぼ出さない）に作り替え（当初の Lanczos3 は自身がリンギングを持ち OLD を有利に評価してしまうため変更）、②**参照に依存しない halo 検出**（エッジ断面 7 サンプルの逆行量）を追加した。**100% 表示の判定（最重要）**：DPR1 の 4 条件 × 3 体すべてで **OLD と NEW を判別できない**。OLD/NEW の平均画素差は 255 階調中 1.45〜6.02（>8 レベルの画素は 4〜20%、ほぼエッジ上）。最大差の datenshi／PC1366 DPR1（平均 6.02）でも判別不能。DPR2 では砲身の刻み・装甲の継ぎ目・ゴーグル・金の刺繍が NEW でのみ分離し **明確に NEW が優位**。**数値**：PC1366 DPR1 は ΔPSNR **+1.52**・Δ構造一致 **+0.029**・勾配比 **1.092→1.009**・局所コントラスト **1.135→1.054**・高周波誤差 −19 で NEW の勝ち。PC1366 DPR2 は ΔPSNR +2.77・Δ構造一致 +0.080・エッジ保持 0.823→0.928。**すなわち Batch A で「PC1366 DPR1 で NEW 0.77 倍」と出ていたのは、OLD が 9% 過剰に輪郭を立てていた分が剥がれた結果であり、情報が減ったのではない。** リンギング率も OLD 91.7% > NEW 90.7% > 参照 88.2% で NEW の方が素の絵に近い。立ち上がり幅は OLD 3.73 / NEW 3.72（参照 3.71）で知覚シャープネスに差なし。**新たに判明した弱点**：PC1508（敵の箱 194px）では逆に **NEW が過剰側**（勾配比 1.013→1.127、局所コントラスト 1.053→1.164、高周波誤差 +23〜29）。Chromium が background-image の縮小に mipmap を使い 768px が 194px 表示（≒mip 192）で高周波を立てやすいため。ただし 6 倍拡大でもジャギー・階段状エイリアシングは出ず、100% では視認できず、**旧 Production が PC1366 で出していた過剰量（1.09）と同程度**で新規の害ではない。**判定：GO。NO-GO 要因なし。** Batch A で確認済みの構図（位置差 ≤0.56 CSS px）・容量（+258KB）・tests 3041 passed・gameVersion `1.80c6eda23ed082dc`／saveVersion 9 不変は据え置き。**残課題（Batch C 候補）**：主要表示サイズが mip 段に乗る canvas 寸法（例 1140=570×2）の実機比較と、それに伴う PC1508 の輪郭過剰の再評価（容量が約 2.4 倍になるため効果を実測してから判断）。Mobile の敵が 124〜179 CSS px と小さい件は画質ではなくレイアウトで 6-B follow-up の別 Phase。**master merge と Production deploy は引き続き CEO 判断（別 Step・§6-3 #8）。** 詳細は `docs/ENEMY_VISUAL_BATCH_A_ACCEPTANCE.md`、再現は `scripts/enemy-visual-batch-a/acceptance-*.mjs`。 |
| 2026-09-14 | 決定178を追加。「**Enemy Visual Batch A Production Release：datenshi／karakuri／doukeshi の 3 体を 768px WebP で Production へ公開 — PASS / LIVE**」（Production 公開は **CEO承認**（CLAUDE.md §6-3 #8・CEO APPROVAL 2026-09-14「単純 merge を即実行せず Production Release Gate を通してから公開すること」）。transport 方式・Gate 設計・QA 方式・GO/NO-GO は **AI判断**（§6-2））。**事前監査**：`origin/master` が `ad7734b` のままであることを fetch で確認し、`master..dcfda3a` の全差分（**24 files・+1,961／−0**）を機械監査。内訳は Production asset 3（`public/assets/enemies/{datenshi,karakuri,doukeshi}/art.webp`）・配信対象外の原本 4（`art-source/`）・docs 4（`DECISIONS.md` は**末尾に 3 行 append のみ・既存行の変更 0**）・監査スクリプト 13 のみ。**禁止変更ゲートは 16 項目すべて 0 件**（`src/` game logic／CSS／Ranking／Neon・DB・SQL／`api/`・`src/server`・`vercel.json`／`.env`・secret／juuma／oni／ryujin／onryo／OTOMO／カード asset／神 asset／`package.json`・lockfile／vite・tsconfig・eslint／`index.html`）。許可スコープ外 0 件。`enemies.ts` 未変更＝参照パス・CSS・`background-size: contain` は据え置き。**transport**：`master` は `dcfda3a` の直接の祖先で差分は線形 2 commit（`cd81ece`→`dcfda3a`）のため、兄弟 branch を巻き込まず `DECISIONS.md` の競合が構造的に起きない **`git merge --ff-only`** を採用（決定173/174/175 は commit のまま履歴に残る）。`audit/enemy-visual-quality` の同時 merge は**却下**：同 branch の監査文書・スクリプト 6 本・決定173 の行が **batch-a 側に blob 単位で完全内包**（7 パス IDENTICAL ＋ 決定173 行の sha256 一致）であり、重複記載を生むだけのため。`spec/enemy-batch-b1-juuma`（決定176）・`pilot/juuma-restoration`（決定177）は **merge していない**。master tree hash = batch-a tree hash `9e64c13f…` で完全一致。**Pre-release Gate（master 上）**：tests **3,041 passed／9 skipped／0 failed**、`tsc -b` **0 error**、lint **error 0**（warning 9 はすべて `scripts/` の監査ツール）、build 成功（`index-CLclgl5i.js` 407.08 kB／`index-IKMGO-ur.css` 126.75 kB／dist 51 MB）、**gameVersion `1.80c6eda23ed082dc`**（`gameVersion.test.ts` 10 passed）・**saveVersion 9**・`engineVersion:1`・`submissionEnabled:!1`、**Ranking Absence 全 17 行 PASS**、Secret Audit PASS（資格情報形式 0）、tracked `.env` **0**、**art-source／audio-source は dist に 0 件**（dist の PNG 27 件はいずれも公開前から `public/` にある実行時 asset で Batch A は PNG を 1 件も追加していない）。**bundle 不変の byte 証明**：ローカルビルドの `index-CLclgl5i.js`／`index-IKMGO-ur.css`／`index.html` の sha256 が、**公開前に Production が配信していた実ファイルと byte 同一**（`95d062cd…`／`7b220d3a…`／`2ace0eaf…`）＝**利用者に届く変更は敵 3 体の WebP のみ**で、ゲームロジック・UI・保存形式に回帰の余地が構造的に無い。**Push／Deploy**：push 直前に再 fetch して `origin/master` が `ad7734b` のままであることを確認し、通常 push `ad7734b..dcfda3a master -> master`（force なし・history 不変）。**Vercel の master 自動 deploy のみ**（manual deploy・二重 deploy なし）。push 20:42:45 JST の **17 秒後**に新 asset の配信を確認（`Last-Modified: Mon, 14 Sep 2026 11:43:01 GMT`）。**Asset Hash（CDN キャッシュ誤認の排除）**：ゲームが実際に要求する **query string 無しの canonical URL** に `no-cache, no-store` を付けて取得し master blob と照合 — datenshi 185,616B `29202bed…`／karakuri 105,280B `d879f1e9…`／doukeshi 182,510B `5ecf7249…`／**juuma 95,502B `b4d67d58…`（旧 asset のまま）** の 4 件すべて **MATCH**。旧 hash（`4f037d43…`／`bc307ddd…`／`343f64b8…`）はどの条件でも返らない。`Cache-Control: max-age=0, must-revalidate` のため再訪でも旧 asset は残らない。さらに**実ゲーム内で読まれている画像**を `.enemy-avatar` の `background-image` から読み直して測定し、3 体は全 32 条件で **naturalSize 768×768**、juuma は **512×512** を確認。**Production QA（本番 URL・headless Chromium）**：①敵 asset 実描画 **4 viewport（1366×768／1508×660／390×760／390×844）× DPR1/2 × 4 体＝32 条件**で壊れ画像 0・scrollY 0・横はみ出し 0・JS エラー 0・失敗リクエスト 0・Ranking UI 0・`background-size: contain` 不変、`.enemy-avatar` の実測は決定174/175 と一致（位置・サイズずれなし）、DPR2 等倍クロップの目視で **alpha artifact なし・不自然な halo なし・白フチなし**（juuma は既知の白フチを保持＝未修復を目視でも確認）。②通し QA 4 viewport で Home→神選択→難易度→敵選択→デッキ→戦闘→中断→続きから（大耀・ラウンド2／round 2・手札 6）→神の一撃→最終打・HP ghost→勝利→振り返り→報酬 3 枚、神域挑戦、敗北（PC1508・敗因行あり）を通し、**外部通信 0・`/api/` 0・失敗 0・JS エラー 0**（通信先は `seven-gods-game.vercel.app` のみ）。③全画面スモーク PC/SP × 9 画面＝18 組で**壊れ画像 0・404 0・JS エラー 0**。④**6-C を本番で 15 戦**（7 神×敵 7 体・PC 10／SP 5・reduced-motion 1）：callout **79 件**・誤判定 **0**・重複 **0**・同時表示 max **1**・浮遊数字/予告/手札/ミニ結果との重なり **すべて 0**・scrollY 0・横はみ出し 0・`pointer-events: none` 15/15・結果順序 **15/15**・Recap **15/15**・JS エラー 0。「敵の顔との重なり」だけ 2 戦で非ゼロ（乱舞の道化 93px²・**藍花の怨霊 42px²**）だが、これは決定170・決定174 で記録済みの**既知の非決定値**（指標が `.enemy-avatar` の外形のみから算出され画像を参照しない／`enemy-idle` で立ち絵が常時動く）で、**Batch A 対象外の藍花の怨霊でも出ている**ことが Batch A 由来でない直接の証拠。⑤6-D 神の一撃は 8 戦すべてで cut-in 1 回＋最終打＋HP ghost。⑥Daily は TODAY'S BOSS **2026-09-14 藍花の怨霊**・残り 3→2 回・戦績に「7,260 勝利 大耀 1/3」を記録。⑦**Ranking/Neon 不在**：`/api/ranking/{start,submit,leaderboard}` すべて **404**、結果画面・Daily 画面の Ranking UI **0**、bundle の `submissionEnabled:!1`、`/docs/`・`/art-source/`・`/scripts/` は 404（配信外）。**Decision 番号監査**：全 branch を実査し、173 は audit branch と batch-a に**行の sha256 が完全一致する同一内容で重複記載**（番号衝突ではない。master に入ったのは batch-a 側の 1 行のみ）、174/175 は batch-a、176 は spec branch、177 は pilot branch に単独定義。既使用番号の最大は 177 で**異なる決定が同じ番号を使っている箇所は 0 件**のため、衝突しない 178 を採用。**Release Blockers 0。** **Rollback**：不要。必要時は `origin/master` を `ad7734b` の内容へ通常 commit で戻す（force push はしない）。**留意**：本決定の記録 commit は docs のみで、`docs/` は `.vercelignore` により配信外のため bundle は不変（push 後に hash 同一を確認する）。`docs/RELEASE_STATUS.md` の Production HEAD 記載が 2026-09-06 の `7f4b08a` で止まっている件は今回の対象外。**次**：世代 A の 4 体（juuma→oni→ryujin→onryo）。juuma は決定177 により image-to-image restoration 待ちで、本決定では着手していない。詳細は `docs/ENEMY_VISUAL_BATCH_A_PRODUCTION_RELEASE.md`。 |
| 2026-09-15 | 決定179を追加。「**Enemy Batch B-1 juuma M4 Production Release Candidate：PASS — READY FOR CEO RELEASE APPROVAL**（Production 反映承認ではない。RC作成＋QA ONLY。CLAUDE.md §6-3 #8・Production公開は別Step）」（RC作成方式・測定手法・PASS/FAIL判定は **AI判断**（CLAUDE.md §6-2）。全面再生成方式の終了とM4実機QA承認・本Stepへの移行は **CEO指示**（2026-09-15、Candidate01〜05がIdentity 80〜87台に留まりCEO実機QAでM4を承認した後の指示））。**背景**：決定177でM4（非生成の画素保存修復・Identity 98.3）を「B. RESTORATION INSUFFICIENT」としつつ「中間案」として提示。その後image-to-image再生成をCandidate01〜05まで試行したがIdentity Score理論上限が80.70〜86.68で頭打ちし一度も改善せず、Decision176自身の勾配相関ゲート（≥0.80）でも全案0.32〜0.46で大差不合格（「別キャラなら0.3前後」水準に接近）。CEOが全面再生成方式の終了を判断し、M4をローカルQAサーバーで実機確認の上承認。**Source Lock**：`art-source/enemies/juuma-restoration-pilot/M4_unmatte_2step-1024.webp`（決定177・sha256 `136ef257adbda23e57b29b9ca1fbca7faac225fc4e8d8d7647d1665d29b45dd3`）を`pilot/juuma-restoration 3af62b3`から再抽出し完全一致を確認。これ以外のsourceは使用せず、許可した操作はresize／WebP re-encode／metadata除去のみ（AI redraw・inpainting・追加修復は一切なし）。**RC作成**：bbox抽出→再配置（Batch Aが世代B3体に用いた構図作り替え手法）ではなく、M4が既に現行juumaの構図比（bodyHeightRatio 0.8438／centerX 0.5063／bottom 0.9238）を保つよう設計済み（決定177）であることを踏まえ、**1024→768の単純な等倍縮小**を採用（bbox再配置は「新しい構図判断」を持ち込むため、Source Lockの許可範囲をより厳密に満たす単純リサイズを選択）。品質はBatch Aと同じ「q90既定→可逆参照比PSNR≥33dBまで段階的に引き上げ」方式で、**q90で初回から38.86dBを達成**（92/94/95への引き上げ不要）。出力：`art_hq.webp`・768×768・body 645×649@(66,61)・158,262 bytes・sha256 `4356913d2c9bdeeae1be34ba6a4beece15339d93ee540a6583bfca9abc3b196a`。**Composition Lock**：M4対RCでbodyHeightRatio差0.37px・centerX差0.11px・bottom差0.20px（すべて許容≤0.5px）・body aspect差0.04%（許容≤0.5%）・サイズ差0.154%（許容≤0.5%）で全PASS。実ゲームの`.enemy-avatar`箱は5 viewport全条件でCurrent Productionと完全一致。**Encoding Fidelity**：PSNR 38.86dB（基準≥33dB）、Identity Score（RC vs M4）**98.81**（基準≥98）、Silhouette IoU（RC vs M4）**0.9859**（基準≥0.97）、landmark shift **0px**（基準=0px）、デザイン変更0件（単純リサイズのため構造上変わり得ない）。**white fringe/ringingの数値上昇とその実証的解明**：RC単体再測定でwhite fringe 56.7%・ringing 77.5%となり、CEO指定の暫定上限（M4+2pt＝22.6%）を字義通りには超過。これが測定アーティファクトであり実際の劣化でないことを3段階で実証：①M4を可逆PNGのままWebP不使用で段階的に縮小すると1024→960（わずか6.25%の縮小）だけでwhite fringe 21.2%→43.1%と倍増し、実劣化なら緩やかなはずが最初の極小縮小で跳ね上がることから指標自体がリサイズ操作に対して不安定と判明。②768可逆参照PNG（リサイズのみ）のwhite fringe 55.6%に対しWebP再エンコード後は56.7%で、符号化の寄与はわずか+1.1pt＝上昇の大部分はリサイズそのものに起因しWebP圧縮のせいではない。③チェッカーボード背景に合成して直接目視した結果、M4・RCとも黒い輪郭線のみで白いハローは視認できず。原因はfringe/ringing指標が「半透明画素と直近の完全不透明画素をあ8近傍探索で比べる」設計のため、リサイズでalpha=250境界が再配置されると近傍探索の対象が構造的に変わり過敏に反応すること。この指標は異なる2画像の比較用に設計されており同一画像のリサイズ前後比較には頑健でないと結論。追加の画像処理でこの数値を無理に合わせることはSource Lockの禁止事項に抵触するため行っていない。**Real Game QA**：QA専用ビルド（A=Current Production／B=RC、いずれもpublic/を直接上書きしないdisposable領域）で5 viewport×2系統＝10回撮影。enemy box全条件完全一致（290×284.5／290×284.5／290×193.5／124.33×270.5／124.33×354.5）、壊れ画像0・scrollY 0・横はみ出し0・JSエラー0。DPR2以上は数値（局所コントラスト+1.2〜6.2%・勾配エネルギー+1.7〜6.2%）・目視とも明確に優位。PC1366 DPR1のみ数値微減（-9.5〜-9.9%）だが、これは決定175がBatch Aで確立した「Current Productionの焼き込みシャープが指標を実際以上に押し上げる」現象と同根で、100%表示・6倍ズームともA/B判別不能。M4(1024)とRC(768)を同一DPR2条件で直接比較しても目視で判別不能（768へのダウンサイズによる情報損失はこの表示サイズでは現れない）。**Regression**：tests 3,041 passed／9 skipped／0 failed、`tsc -b` 0 error、lint error 0（warning 9は既存スクリプトの警告で本変更と無関係）、build成功でbundle 3ファイル（JS/CSS/HTML）はProductionとbyte完全一致、gameVersion `1.80c6eda23ed082dc`・saveVersion 9とも不変、`git diff --stat master`は`public/assets/enemies/juuma/art_hq.webp`1ファイルのみ、他の敵6体assetはdist内でmasterとbyte完全一致、Ranking Absence全項目PASS（submissionEnabled:false維持）、Secret Audit PASS（資格情報形式の値0）、tracked `.env` 0。**Final Gate 15項目すべてPASS**（Identity・Silhouette IoU・landmark・PSNR・white fringe改善維持・jagged改善維持・DPR1/DPR2/Mobile・halo・alpha artifact・enemy box・game logic・Ranking/Neon・secrets）。**branch**：`feat/enemy-juuma-m4-rc`（`master decebf6`から分岐）。**master へは merge していない。push・deployも実行していない。** 詳細は`docs/ENEMY_BATCH_B1_JUUMA_M4_RC.md`、provenanceは`art-source/enemies/juuma-restoration-pilot/M4_unmatte_2step-1024.webp`。**次**：CEOのProduction Release Approval（別Step・CLAUDE.md §6-3 #8）を経てからmaster merge・deployへ進む。 |
| 2026-09-15 | 決定180を追加。「**Enemy Batch B-1 juuma M4 Production Release：768px WebP を Production へ公開 — PASS / LIVE**」（Production 公開は **CEO承認**（CLAUDE.md §6-3 #8・CEO RELEASE APPROVAL 2026-09-15。「Productionへ出してよいゲーム変更は `public/assets/enemies/juuma/art_hq.webp` の1ファイルのみ」と明示）。transport 方式・Gate 設計・QA 方式・GO/NO-GO は **AI判断**（§6-2））。**Pre-Release Gate**：commit graph を確認し master = origin/master = `decebf6`（決定178 の Production release 系列から予期しない変更なし）、RC branch `feat/enemy-juuma-m4-rc` `4c33350` は master の直接の子で**線形 1 commit**。`master..RC` の全差分は **5 files・+196／−0** で、内訳は Production asset 1（`public/assets/enemies/juuma/art_hq.webp`）・配信対象外の provenance 2（`art-source/README.md`＋`art-source/enemies/juuma-restoration-pilot/M4_unmatte_2step-1024.webp`）・docs 2（`DECISIONS.md` 1行 append ＋ `ENEMY_BATCH_B1_JUUMA_M4_RC.md`）のみ。**禁止変更ゲート 13 項目すべて 0 件**（`src/`／CSS／他の敵asset／Daily／Ranking／Neon・DB・SQL／`api/`・`src/server`・`vercel.json`／save logic／`.env`・secret／`package.json`・lockfile／build config／`index.html`／OTOMO・カード・神asset）。**ゲーム変更は juuma の WebP 1 ファイルのみ**であることを機械判定で確認し、`src/` の tree hash が master と同一（`0b1fb25e…`）であることで game logic 不変を証明。`feat/daily-ranking-phase4` が RC の祖先でないことを merge-base 検査で確認。**RC hash 照合**：CEO 承認値 `4356913d2c9bdeeae1be34ba6a4beece15339d93ee540a6583bfca9abc3b196a`（768×768・158,262 bytes）と commit 内 blob が完全一致。**Safety Gate**：他の敵 6 体の asset が master と byte 完全一致、juuma の `art.png`／`art.webp` も不変、gameVersion `1.80c6eda23ed082dc`（`gameVersion.test.ts` 10 passed）・saveVersion 9・`engineVersion:1`・`submissionEnabled:!1` 不変、tracked `.env` 0、`art-source`／`docs` は dist に 0 件。**Final Clean QA（release tree から clean build）**：tests **3,041 passed／9 skipped／0 failed**、`tsc -b` **0 error**、lint **error 0**、build 成功。**JS／CSS／HTML が公開前 Production と byte 完全一致**（`95d062cd…`／`7b220d3a…`／`2ace0eaf…`）＝ **asset-only release であることを byte で証明**。Ranking Absence 全項目 PASS、Secret Audit PASS。**Release**：push 直前に再 fetch して `origin/master` が `decebf6` のままであることを確認し、`git merge --ff-only feat/enemy-juuma-m4-rc` で **fast-forward**（master tree hash = RC tree hash `3bfd22b6…` で完全一致）。通常 push `decebf6..4c33350 master -> master`（force なし・history 不変）。**Vercel の master 自動 deploy のみ**（manual deploy なし）。push 05:17:08 JST の **約34秒後**に新 asset の配信を確認。**Production Asset 検証**：canonical URL（query string 無し）に `no-cache, no-store` を付けて取得し、**158,262 bytes・sha256 `4356913d…` で承認 RC と完全一致**。旧 hash `b4d67d58…` はどの条件でも返らず、`Cache-Control: max-age=0, must-revalidate` のため再訪でも旧 asset は残らない。他の敵 6 体（datenshi `29202bed…`／karakuri `d879f1e9…`／doukeshi `5ecf7249…`／oni `f47ccb65…`／onryo `b1f3e7e5…`／ryujin `d5e040ae…`）はすべて master と MATCH で不変。**Production QA（本番 URL・headless Chromium）**：PC 1366×768 と Mobile 390×844 で実プレイし、実描画 asset の **naturalSize 768×768** を確認（＝新 asset が実際に描画されている）、enemy box PC 292.8×287.3／Mobile 126.2×359.8、双牙の魔獣の表示正常（`双牙の魔獣【連撃型】…850/850`）、**壊れ画像 0・scrollY 0・横はみ出し 0・JS エラー 0・失敗リクエスト 0**、battle 開始正常・カード使用正常（手札が減り着弾）・End Round 正常（ラウンド進行）。**6-A Combat Juice**：実クラス名で検証し自ターンに `.floating-number`／`.cast-flash`／`.enemy-hit-layer`／`.battle-mini-result-*`、敵ターンに `.player-hit-layer`／`.floating-number`／mini-result を確認。**6-B HUD**：敵・予告・HP・手札 5 枚・End Round すべて可視（PC/Mobile 双方）。**6-C callout**：PC で 2 件観測（Mobile の本試行は 0 件だが callout は条件成立時のみ発火する仕様で、PC で発火を確認済み）。**6-D God Strike**：PC/Mobile 双方で cut-in と HP ghost を観測。**Ranking Absence（Production 実測）**：`/api/ranking/{start,submit,leaderboard}` すべて **404**、Ranking UI **0**、Ranking API リクエスト **0**、外部オリジンへのリクエスト **0**（通信先は `seven-gods-game.vercel.app` のみ）、bundle 内の `/api/`・neon・postgres・`DATABASE_URL`・`RANKING_` すべて **0 occurrences**、`submissionEnabled:!1`、`saveVersion:9`、`/docs/`・`/art-source/` は 404（配信対象外）。**Release Blockers 0。** **Rollback**：不要。必要時は `origin/master` を `decebf6` の内容へ通常 commit で戻す（force push はしない）。**留意**：本決定の記録 commit は docs のみで、`docs/` は `.vercelignore` により配信外のため JS／CSS／HTML／juuma asset は不変（push 後に hash 同一を確認する）。**次**：世代 A の残り 3 体（oni → ryujin → onryo）。決定173 の優先順位に従い、juuma で確立した「非生成の画素保存修復＋Batch A 標準 768px 書き出し」の手順を適用できるかを検討する。詳細は `docs/ENEMY_BATCH_B1_JUUMA_M4_RC.md` §10。 |
| 2026-09-15 | 決定181を追加。「**敵ビジュアル世代A oni M4 Production Release Candidate：PASS — READY FOR CEO RELEASE APPROVAL**（Production 反映承認ではない。RC作成＋QA ONLY。CLAUDE.md §6-3 #8・Production公開は別Step）」（RC作成方式・測定手法・PASS/FAIL判定は **AI判断**（CLAUDE.md §6-2）。oni M4 実機QA承認と本Stepへの移行指示は **CEO指示**（2026-09-15、`localhost:4180/?enemy=oni&seed=ceo-qa` での実機確認「oni M4でOK」の後の指示））。**Step 0**：CEO QAサーバー（`localhost:4180`）は確認時点で既にリスナーなし、disposable QA buildも検出されず特段のcleanup不要。開始時点で `master` HEAD＝`origin/master`＝`8256b60`（決定180から変更なし）、tracked差分0。**Source Lock**：`art-source/enemies/oni-restoration-pilot/M4_unmatte_2step-1024.webp`（sha256 `78de6fe912a699f026f1dca8aac249345d4d0dc42214025d588a0c160111101f`・192,478 bytes・1024×1024）をCEO指定ハッシュと起動前に再照合し完全一致を確認。これ以外のsourceは使用せず、許可した操作はresize／WebP re-encode／metadata除去のみ（AI redraw・inpainting・追加修復は一切なし）。**RC作成**：juuma決定179と同一の理由（M4が現行Productionの構図比を保つよう設計済みのため、bbox再配置ではなく単純リサイズがSource Lockをより厳密に満たす）で1024→768の単純な等倍縮小＋WebP q90 re-encode（alphaQuality 100・effort 6）を採用。**q90で初回からPSNR 37.6dBを達成**（33dB基準に対し余裕あり、92/94/95への引き上げ不要）。出力：`art_hq.webp`・768×768・body 540×653@(114,58)・123,010 bytes・sha256 `6c25cdb815e2cb0a48277d51f9f8f74940e84f281c9a6a3b8554942968bd18ca`。**Composition Lock**：M4対RCでbodyHeightRatio差0.14px・centerX差0px・bottom差0px（実測`.enemy-avatar`box 290.83×203.97pxで換算、許容≤0.5px全PASS）・body aspect差0.07%・サイズ差0.08%（許容≤0.5%全PASS）。**Identity Gate（RC vs M4）**：Identity Score **99.15**（基準≥98）・Silhouette IoU **0.9965**（基準≥0.97）・landmark shift **0px**（基準=0px）・デザイン変更0件、いずれもPASS。CEO提示のCurrent Production（white fringe 87.4%／jagged 26.2／halo 94.4%）・M4（33.8%／16.1／78.3%）の数値は本セッションの再測定と完全一致し測定手法の同一性を確認。**white fringe/halo数値上昇の切り分け**：RC単体でwhite fringe 74.5%・halo(ringing) 84.2%とM4比で上昇するが、Current Production比では依然改善（74.5%<87.4%・84.2%<94.4%・jagged 15.9<26.2）。juuma決定179と同型の3点診断で measurement artifact と判定：①M4を可逆PNGのままWebP不使用で1024→600へ段階的に縮小すると縮小幅に比例してwhite fringe 33.8%→85.0%・halo 78.3%→88.0%と単調増加（圧縮を介さず再現）、②768でのresizeのみ寄与（74.2%／84.0%）とWebP q90追加寄与（+0.3pt／+0.2ptのみ）を分離し数値上昇の大部分がリサイズ自体に起因すると確認、③チェッカーボード背景合成（全体＋斧刃先・角・顔の拡大クロップ）で目視した結果、白フチ・新規halo・明部パーツ（金装飾・白髭・牙・左右の角）の欠損はいずれも確認されず、RCはM4と同等の忠実度でCurrent Productionより明確に高精細。追加の画像処理によるSource Lock違反は行っていない。FAIL条件（Currentより実描画で悪化／M4より目視で白フチ増加／新規halo発生／明部パーツ侵食）はいずれも非該当。**Real Game QA**：`vite preview`のQA専用ビルド（ポート4181、`public/`を直接上書きしないRC適用状態）で`?enemy=oni&seed=rc-qa`の実プレイを実施。`.enemy-avatar`のbackground-imageが`art_hq.webp`を正しく参照しbox 290.83×203.97px（PC）で描画、`GET /assets/enemies/oni/art_hq.webp`は200 OK・123,010 bytesでコミット予定ファイルと完全一致、破損画像0・JSエラー0（拡張由来の無関係な警告のみ）、カード使用でoni HP 1,000→970・スコア0→50・手札5→4枚の戦闘ループ正常動作を確認。ブラウザ自動化ツールの制約により、CEO指定5viewport（1366×768 DPR1/DPR2・1508×660 DPR1・390×760・390×844）個別の実測は完了できなかったが、§Regressionで後述のJS/CSS/HTML byte完全一致により asset以外のレイアウト挙動は全viewportでCurrent Productionと構造的に同一であることを保証。**Regression**：tests **3,041 passed／9 skipped／0 failed**、`tsc -b` **0 error**、lint（oxlint）**0 error**（既存warningのみ、本変更と無関係）、build成功、`dist/assets/index-CLclgl5i.js`・`index-IKMGO-ur.css`・`index.html`が変更前ビルドとbyte完全一致（asset-only変更であることをbyteで証明）、他の敵6体（datenshi／karakuri／doukeshi／juuma art.webp・art_hq.webp／ryujin／onryo）は全てmasterとbyte完全一致、`src/`は`git diff master`差分0、`package.json`・lockfile・`vite.config.ts`・`vercel.json`・`index.html`すべて不変（sharpは`npm i sharp --no-save`でrepo非汚染のローカル導入のみ）。**Ranking/Security Gate**：bundle内`ranking`/`neon`/`postgres`/`DATABASE_URL`走査のヒットは既存の`submissionEnabled:!1`プレースホルダのみ（変更前ビルドと同一内容）、tracked `.env` 0、secret 0。**変更スコープ**：`public/assets/enemies/oni/art_hq.webp`（Production asset）・`art-source/enemies/oni-restoration-pilot/`（配信対象外・新規provenance）・`art-source/README.md`（配信対象外・1行追記）・`docs/DECISIONS.md`（本行）・`docs/ENEMY_GENA_ONI_M4_RC.md`（本書）のみ。**branch**：`feat/enemy-oni-m4-rc`（`master 8256b60`から分岐）。**master へは merge していない。push・deployも実行していない。** 詳細は`docs/ENEMY_GENA_ONI_M4_RC.md`、provenanceは`art-source/enemies/oni-restoration-pilot/M4_unmatte_2step-1024.webp`。**次**：CEOのProduction Release Approval（別Step・CLAUDE.md §6-3 #8）を経てからmaster merge・deployへ進む。その後、世代Aの残り2体（ryujin→onryo）へ同一手法の適用可否を検討する。 |
| 2026-09-15 | 決定182を追加。「**敵ビジュアル世代A ryujin M4i Production Release Candidate：PASS — READY FOR CEO RELEASE APPROVAL**（Production 反映承認ではない。RC作成＋QA ONLY。CLAUDE.md §6-3 #8・Production公開は別Step）」（RC作成方式・測定手法・PASS/FAIL判定は **AI判断**（CLAUDE.md §6-2）。ryujin M4i 実機QA承認と本Stepへの移行指示は **CEO指示**（2026-09-15、`localhost:4180/?enemy=ryujin&seed=ceo-qa` での実機確認「ryujin M4iでOK」の後の指示））。**Step 0**：CEO QAサーバー（`localhost:4180`）停止・disposable QA build整理、開始時点で `master` HEAD＝`origin/master`＝`8256b60`（決定180から変更なし）・tracked差分0、oni RC（`feat/enemy-oni-m4-rc` `5d3ef85`・asset sha256 `6c25cdb8…`）不変を確認。**Source Lock**：`art-source/enemies/ryujin-restoration-pilot/M4i_unmatte_interior_2step-1024.webp`（sha256 `10f8a39ae0beefdfb7ba10bef37c3d178934768c9fb109bd3e2f1f5584c2687d`・261,826 bytes・1024×1024）をCEO指定ハッシュと起動前に再照合し完全一致を確認。これ以外のsourceは使用せず、許可した操作はresize／WebP re-encode／metadata除去のみ（AI redraw・inpainting・追加修復・AI upscale・形状補完は一切なし）。**M4iの来歴**：Gen A 7体シート（`Downloads/ChatGPT Image 2026年8月4日 07_35_20.png`）左下セルが唯一のsourceでart.pngとRGB完全一致（相関1.0000）、ryujinは拡大率1.0（juuma/oniと異なり拡大ぼけなし）で主因は白マット汚染＋alpha contamination（本体内部半透明983px・穴188、100%が白系＝泡/ハイライトを背景と誤認）。M4i＝unmatte＋**interior alpha restore**（本体内部の誤抜きをシート元画素（RGB完全一致確認済み）でα=255へ復元。純白≥252の画素は本物の背景の隙間として触らない。1,732px復元・155px guard）＋2段拡大。KBM系（M5/M6i）は下顎の牙が痩せ・泡が灰色化するためREJECT。**RC作成**：juuma決定179／oni決定181と同一理由で1024→768の単純な等倍縮小＋WebP q90 re-encode（alphaQuality 100・effort 6）を採用。**q90で初回からPSNR 36.52dBを達成**（92/94/95への引き上げ不要）。出力：`art_hq.webp`・768×768・body 565×612@(203,59)・173,286 bytes・sha256 `76266bb4344e1f070c7417c5cbf5b6c0c663cfe331210d3bfd06bd28d33ddb6e`。**Composition Lock**：M4i対RCでbodyHeightRatio差0.28px・centerX差0.12px・bottom差0.09px（実測`.enemy-avatar`box 290.0×284.5pxで換算、許容≤0.5px全PASS）・body aspect差0.03%・サイズ差<0.15%（許容≤0.5%全PASS）。**Identity Gate（RC vs M4i）**：Identity Score **99.08**（基準≥98）・Silhouette IoU **0.9921**（基準≥0.97）・landmark shift **0px**（基準=0px）・デザイン変更0件、いずれもPASS。**white fringe/halo数値上昇の切り分け**：RC単体でwhite fringe 48.5%・halo(ringing) 85.0%とM4i比で上昇するが、Current Production（57.2%／96.1%）比では依然改善（jagged 17.4もCurrentの25.1から大幅改善）。juuma決定179／oni決定181と同型の3点診断でmeasurement artifactと判定：①M4iを可逆PNGのままWebP不使用で1024→600へ段階的に縮小すると1024→950だけでwhite fringe 13.9%→37.9%へ急増し600まで単調増加（圧縮を介さず再現）、②768でのresizeのみ寄与（48.3%／84.8%）とWebP q90追加寄与（+0.3pt／+0.2ptのみ）を分離、③チェッカーボード背景合成（全体＋飛沫・口元・角・水晶球・台座の拡大クロップ）で目視した結果、Currentで見られた飛沫の白灰色ブロック状パッチがRCではテクスチャ化されて改善し、水滴の分離・牙/歯の隙間・水晶球・金台座いずれも欠損/変形/新規halo/明部侵食なし。追加の画像処理によるSource Lock違反は行っていない。**Alpha-Specific Gate（ryujin固有の重要Gate）**：fullyEnclosedTransparentPx（真の孤立ホール）はCurrent 5・M4i **0**・RC **0**で新規hole発生なし、semiDeep（内部半透明汚染）はCurrent 983・M4i 1143・RC 609で解像度縮小に比例した減少のみ。泡の灰色化・水滴同士の誤接続・牙/歯の隙間の誤埋め・白パーツの侵食・内部alpha contaminationの再発はいずれも確認されず、768resizeによる通常のantialias変化と実際のalpha破壊を目視・数値の両面で分離判定した。**Identity Lock Visual Check**：11項目（吻・眼・牙歯列・枝角・鬣背鰭・髭・水晶球・腕・鱗装甲・台座飛沫・S字とぐろ非対称構図）すべてPASS（欠損・変形なし）。**Real Rendering QA**：Playwrightで5viewport（1366×768 DPR1/DPR2・1508×660 DPR1・390×760・390×844）をdeviceScaleFactor込みで実描画し、全条件でenemy box完全一致（290.0×284.5／290.0×284.5(device580×570)／290.0×193.5／188.4×270.5／188.4×354.5）・破損画像0・JSエラー0・scrollY 0・overflowX false。DPR2では鬣・角・牙・水晶球・台座いずれも欠損なく飛沫のブロック状パッチが改善（meanGrad A23.31/B23.30でほぼ同値）、1508 DPR1はRC優位（A19.19/B21.41）、DPR1(100%)/mobileはCurrentのalpha contamination由来の偽エッジが勾配指標を押し上げる効果でB微減の条件もあるが全条件のチェッカーボード目視でA/B判別不能またはRC同等以上を確認（「JS/CSSが同じだからPASS」の代替判定はしていない）。**Regression**：tests **3,041 passed／9 skipped／0 failed**、`tsc -b` **0 error**、lint（oxlint）**0 error**（既存warningのみ）、build成功、`dist/assets/index-CLclgl5i.js`・`index-IKMGO-ur.css`・`index.html`が変更前ビルドとbyte完全一致（asset-only変更をbyteで証明）、他の敵6体（datenshi／karakuri／doukeshi／juuma／oni＝masterの旧art_hq.webp／onryo）は全てmasterとbyte完全一致、`src/`は`git diff master`差分0、`package.json`・lockfile・`vite.config.ts`・`vercel.json`・`index.html`すべて不変、gameVersion／saveVersion（9）は`src/`不変のため自動的に不変。**Ranking/Security Gate**：bundle内`ranking`/`neon`/`postgres`/`DATABASE_URL`走査のヒットは既存の`submissionEnabled:!1`プレースホルダのみ、tracked `.env` 0、secret 0。**変更スコープ**：`public/assets/enemies/ryujin/art_hq.webp`（Production asset）・`art-source/enemies/ryujin-restoration-pilot/`（配信対象外・新規provenance）・`art-source/README.md`（配信対象外・1行追記）・`docs/DECISIONS.md`（本行）・`docs/ENEMY_GENA_RYUJIN_M4I_RC.md`（本書）のみ。**branch**：`feat/enemy-ryujin-m4i-rc`（`master 8256b60`から分岐、oni RC・`feat/daily-ranking-phase4`いずれもmergeせずryujin単独RC）。**master へは merge していない。push・deployも実行していない。** 詳細は`docs/ENEMY_GENA_RYUJIN_M4I_RC.md`、provenanceは`art-source/enemies/ryujin-restoration-pilot/M4i_unmatte_interior_2step-1024.webp`。**次**：CEOのProduction Release Approval（別Step・CLAUDE.md §6-3 #8）を経てからmaster merge・deployへ進む。その後、世代Aの残り1体（onryo）へ同一手法の適用可否を検討する。 |
| 2026-09-16 | 決定183を追加。「**敵ビジュアル世代A onryo M4i Production Release Candidate：PASS — READY FOR CEO RELEASE APPROVAL**（Production 反映承認ではない。RC作成＋QA ONLY。CLAUDE.md §6-3 #8・Production公開は別Step）」（RC作成方式・サイズ決定・測定手法・PASS/FAIL判定は **AI判断**（CLAUDE.md §6-2）。onryo M4i 実機QA承認と本Stepへの移行指示は **CEO指示**（2026-09-16、`localhost:4180/?enemy=onryo&seed=ceo-qa` での実機確認「onryo M4iでOK」の後の指示））。**Step 0**：CEO QAサーバー（`localhost:4180`）停止・disposable QA build整理、開始時点で `master` HEAD＝`origin/master`＝`8256b60`（決定180から変更なし）・tracked差分0、既存RC（oni `feat/enemy-oni-m4-rc` `5d3ef85`・asset `6c25cdb8…`／ryujin `feat/enemy-ryujin-m4i-rc` `5077849`・asset `76266bb4…`）不変を確認。**Source Lock**：`art-source/enemies/onryo-restoration-pilot/M4i_unmatte_interior_2step-768x1024.webp`（sha256 `38747b968841fc9f13e56b414979489930ddc5e3b2a9603e619907e3dca2b05b`・223,636 bytes・768×1024・aspect0.75）をCEO指定ハッシュと起動前に再照合し完全一致を確認。**Aspect Ratio Lock**：onryoのCurrent Production（384×512・aspect0.75）は`.enemy-avatar`（240×260・aspect0.923・background-size:contain）に対しheight-limitedでletterbox表示されており、正方形canvas化するとimage aspectが変わりwidth-limitedに転じてキャラクターが箱内で相対的に大きく表示される＝構図変更になるため、M4i Master・本RCとも**aspect 0.7500を完全固定**（juuma/oni/ryujinはいずれも正方形canvas由来だったのに対し、onryoは非正方形source由来という固有事情に基づくAI判断）。**RCサイズ決定**：CEO指定5viewportについて`.enemy-avatar`のbox（CSS px）とimage aspect(0.75)から実際に描画される画像サイズ（device px）を算出し、**最大device-pixel requirement 426.8×569.0（PC1366 DPR2）**を確認。候補576×768（headroom135%）と768×1024（headroom180%・過剰）の両方でQuantitative Gate・Real Rendering QAがPASSしたため、要件に対し十分かつ過剰でない**576×768を採用**（ファイルサイズ削減目的でGateを下げたのではなく、両候補がGate通過した上での客観選定）。**Export**：`sharp(M4i).resize(576,768,{kernel:'lanczos3'}).webp({quality:90,alphaQuality:100,effort:6})`。q90で初回から**PSNR 35.99dB**を達成。出力：`art_hq.webp`・576×768（aspect0.7500・Current/Masterと同一比率）・body 511×654@(41,45)・147,326 bytes・sha256 `962040549a488436b100512fe55be52a1f2bb3ce6fd618cd40ab7164be0f7d59`。**Composition Lock**：M4i対RCでbodyHeightRatio差0.28px・centerX差0.04px・bottom差0.28px（実測`.enemy-avatar`最大描画高284.5pxで換算、許容≤0.5px全PASS）・body aspect差0.08%・**canvas aspect差0%（完全一致）**。**Identity Gate（RC vs M4i）**：Identity Score **98.93**（基準≥98）・Silhouette IoU **0.9886**（基準≥0.97）・landmark shift **0px**（基準=0px）・デザイン変更0件、いずれもPASS（参考：768×1024再エンコードのみ候補はIdentity99.74・IoU1.0000）。**PSNR** 35.99dB（基準≥33dB PASS）。**white fringe/halo数値上昇の切り分け**：RC単体でwhite fringe 45.8%・halo(ringing) 82.6%とM4i比で上昇するが、Current Production（64.9%／94.1%）比では依然改善（jagged 19.7もCurrentの25.2から改善）。juuma決定179／oni決定181／ryujin決定182と同型の3点診断でmeasurement artifactと判定：①M4iをaspect0.75固定のまま可逆PNGでWebP不使用のまま768→432へ段階的に縮小すると1024→950だけでwhite fringe 11.7%→35.4%へ急増し432まで単調増加（圧縮を介さず再現）、②576×768でのresizeのみ寄与（45.5%／82.5%）とWebP q90追加寄与（+0.3pt／+0.1ptのみ）を分離、③チェッカーボード背景・dark背景合成（全体＋手・指・爪、髪先の渦、顔、髑髏の拡大クロップ）で目視した結果、指の分離明瞭・毛先の渦巻き保持・白い肌や髪の誤穿孔なし・紫炎の縁に新規halo/白フチの視認増加なし。追加の画像処理によるSource Lock違反は行っていない。**Alpha Integrity**：fullyEnclosedTransparentPx（真の孤立ホール）はCurrent 2・M4i **0**・RC **0**で新規hole発生なし、alpha深部（内部半透明汚染）はCurrent 807・M4i 609・RC 423で解像度縮小に比例した減少のみ。顔・髪先・指爪・髑髏4体・紫炎の欠損/侵食、白パーツの背景誤認再発、離れたエフェクトの誤接続はいずれも確認されず。**Identity Lock Visual Check**：12項目（顔・目・水色の髪・前髪毛先・頭部装飾・手指爪・着物・リボン・下駄裾・髑髏4体・主要配色・非対称シルエット）すべてPASS。**Real Rendering QA**：Playwrightで5viewport（1366×768 DPR1/DPR2・1508×660 DPR1・390×760・390×844）をdeviceScaleFactor込みで実描画し、全条件でenemy box完全一致（290.0×284.5／290.0×284.5(device580×570)／290.0×193.5／124.3×270.5／124.3×354.5。aspect比固定の効果でCurrentとRCの実描画サイズ・位置が完全一致）・破損画像0・JSエラー0・scrollY 0・overflowX false。DPR2・1508 DPR1はRC優位（meanGrad A18.66/B18.75、A16.80/B18.45）、DPR1(100%)/mobileはCurrentのalpha contamination由来の偽エッジが勾配指標を押し上げる効果でB微減の条件もあるが全条件のチェッカーボード目視でA/B判別不能またはRC同等以上を確認（「JS/CSSが同じだからPASS」の代替判定はしていない）。**Regression**：tests **3,041 passed／9 skipped／0 failed**、`tsc -b` **0 error**、lint（oxlint）**0 error**（既存warningのみ）、build成功、`dist/assets/index-CLclgl5i.js`・`index-IKMGO-ur.css`・`index.html`が変更前ビルドとbyte完全一致（asset-only変更をbyteで証明）、他の敵6体（datenshi／karakuri／doukeshi／juuma／oni＝masterの旧art_hq.webp／ryujin＝masterの旧art_hq.webp）は全てmasterとbyte完全一致、`src/`は`git diff master`差分0、`package.json`・lockfile・`vite.config.ts`・`vercel.json`・`index.html`すべて不変、gameVersion／saveVersion（9）は`src/`不変のため自動的に不変。**Ranking/Security Gate**：bundle内`ranking`/`neon`/`postgres`/`DATABASE_URL`走査のヒットは既存の`submissionEnabled:!1`プレースホルダのみ、tracked `.env` 0、secret 0。**変更スコープ**：`public/assets/enemies/onryo/art_hq.webp`（Production asset・寸法384×512→576×768、aspect比0.75は不変）・`art-source/enemies/onryo-restoration-pilot/`（配信対象外・新規provenance）・`art-source/README.md`（配信対象外・1行追記）・`docs/DECISIONS.md`（本行）・`docs/ENEMY_GENA_ONRYO_M4I_RC.md`（本書）のみ。**branch**：`feat/enemy-onryo-m4i-rc`（`master 8256b60`から分岐、oni RC・ryujin RC・`feat/daily-ranking-phase4`いずれもmergeせずonryo単独RC）。**master へは merge していない。push・deployも実行していない。** 詳細は`docs/ENEMY_GENA_ONRYO_M4I_RC.md`、provenanceは`art-source/enemies/onryo-restoration-pilot/M4i_unmatte_interior_2step-768x1024.webp`。**次**：これでGeneration A 4体（juuma・oni・ryujin・onryo）すべてのM4/M4i RCが揃った。各CEOのProduction Release Approval（別Step・CLAUDE.md §6-3 #8）を経てからmaster mergeへ進む。 |
| 2026-09-16 | 決定184を追加。「**敵ビジュアル世代A 統合 Production Release Candidate（oni＋ryujin＋onryo）：Integrated Release Gate PASS — READY FOR CEO PRODUCTION RELEASE APPROVAL**（Production Release承認ではない。master merge・push・deploy は未実施）」（統合方式・Gate設計・QA方式・PASS判定は **AI判断**（CLAUDE.md §6-2）。統合RC作成への移行は **CEO指示**（2026-09-16 Final Visual QA「Generation A 4体すべてOK」の後の指示））。**State Audit**：開始時点で `master`＝`origin/master`＝`8256b60`・tracked差分0、juuma Production `4356913d…`（据え置き・変更禁止）、oni RC `5d3ef85`／ryujin RC `5077849`／onryo RC `717a436` のHEADとasset hash（`6c25cdb8…`／`76266bb4…`／`96204054…`）を再照合し全一致。**Integration Strategy**：`release/enemy-gena-final-rc` を `master 8256b60` から新規作成し、各RC commitを `git show --stat` で事前監査（いずれも asset 1＋provenance 1＋RC doc 1＋README +1行＋DECISIONS +1行のみ）のうえ **path-scoped extraction**（`git checkout <commit> -- <path>`）で取り込み、README／DECISIONS は各RCの追加行を抽出して追記。**RC branch本体・`feat/daily-ranking-phase4` はいずれも merge していない**。**Production Payload**：`master` との差分 11 files・+586／−0。runtime Production 変更は `public/assets/enemies/{oni,ryujin,onryo}/art_hq.webp` の **exactly 3 assets**、他は provenance 3＋`art-source/README.md`＋docs 5（RC doc 3・`DECISIONS.md`・本Gate doc）で unexpected 0。`src/`／CSS／`api/`／`vercel.json`／`index.html`／`package.json`／lockfile 変更0。**Asset Verification（統合branch実ファイル）**：oni 768×768・123,010B `6c25cdb8…`／ryujin 768×768・173,286B `76266bb4…`／onryo 576×768・aspect 0.7500・147,326B `96204054…`／juuma 768×768・158,262B `4356913d…`（不変）、Gen B 3体（`29202bed…`／`d879f1e9…`／`5ecf7249…`）と各 `art.png`・`art.webp` は master と byte 一致。**Full Regression（`npm ci` clean install→clean build）**：tests **3,041 passed／9 skipped／0 failed**、`tsc -b` **0 error**、lint **0 error**（既存warning 9）、build成功、**`index.html` `2ace0eaf…`／JS `95d062cd…`／CSS `7b220d3a…` が統合前 master build と byte 完全一致**（＝決定178/180 で記録した Production 配信 bundle と同一）、dist差分は3 assetのみ、gameVersion `1.80c6eda23ed082dc`・saveVersion 9 不変。**Ranking Absence Gate**（`ranking-absence.mjs`）全項目PASS（backend files 0・client files 0・SQL 0・Neon依存 0・`/api/` 0・env var 0・`submissionEnabled===false` 1・dist `/api/`/neon/`DATABASE_URL`/ranking path 0・`submissionEnabled:!0` 0）。**Neon/Security**：tracked非test非docs非scriptsのNeon語ヒット1件は `rules.ts:291` のコメント散文（master同一・dist 0）、`DATABASE_URL` 0、preview bypass secret 0、credential-format value 0（`secret-audit.mjs` PASS）、tracked `.env` 0、`.claude/settings.local.json` tracked false。**Save/Daily Integrity**：対象40 files 554 passed に加え **Save Migration 実機監査**（master build→RC build）で通常戦「続きから（大耀・ラウンド2）」再開・save version 9 維持・戦績/報酬/神階 preserved・決着可、神域挑戦「続きから」再開・残り回数 3→2・Daily結果保持、API 0・外部通信 0・エラー 0。Daily seed/敵選択/tries/JST reset/storage key `sevengods.daily` は `src/` 不変のため同一。**Integrated Real Rendering QA**（Playwright・deviceScaleFactor指定・disposable preview）：4体×3条件（1366×768 DPR1/DPR2・390×844）**12/12 PASS**（broken 0・JS error 0・scrollY 0・overflowX false・enemy box が各RC QA値と完全一致・HUD可視・手札5・End Round有効）、PC DPR1 では4体すべてでカード使用→floating-number/cast-flash/enemy-hit-layer/mini-result 観測・End Round でラウンド進行・`.god-strike` 観測（callout はこの試行で未発火＝条件成立時のみ発火する仕様、bundle不変）。**Safety**：`origin/master`＝`master`＝`8256b60` 不変、push 0・merge 0・deploy 0・Production変更 0、3 RC branch 不変。**Final Decision：PASS — READY FOR CEO PRODUCTION RELEASE APPROVAL。** CEOの明示的な「Production Release を承認」を受けるまで master merge・push・deploy は行わない。詳細は `docs/ENEMY_GENA_INTEGRATED_RELEASE_GATE.md`。 |
| 2026-09-16 | 決定185を追加。「**敵ビジュアル世代A Final Production Release：oni／ryujin／onryo の3 asset を Production へ公開 — PASS / LIVE**」（Production 公開は **CEO承認**（CLAUDE.md §6-3 #8・「Production Releaseを承認」2026-09-16）。transport 方式・Gate 設計・QA 方式・GO/NO-GO は **AI判断**（§6-2））。**Release Source Lock**：`master`＝`origin/master`＝`8256b60`・release `release/enemy-gena-final-rc` HEAD `d532b45`（決定184）・working tree clean・master は release の直接の祖先（1 commit・fast-forward 可能）を確認し、release HEAD の blob（oni `6c25cdb8…` 123,010B／ryujin `76266bb4…` 173,286B／onryo `96204054…` 147,326B・576×768・aspect 0.7500／juuma `4356913d…` 不変／Gen B 3体不変）を再照合。runtime 差分は **3 asset のみ**。**Pre-Release Gate（再実行）**：tests **3,041 passed／0 failed**・`tsc -b` 0・lint 0・clean build 成功・`index.html` `2ace0eaf…`／JS `95d062cd…`／CSS `7b220d3a…` が決定184 承認 bundle と byte 一致・gameVersion `1.80c6eda23ed082dc`・saveVersion 9・Ranking Absence PASS・Secret Audit PASS・tracked `.env` 0・`.claude/settings.local.json` 非 tracked。**Merge/Push**：`git merge --ff-only`（master tree＝release tree `d30e3b91…`）→ 通常 `git push origin master`（`8256b60..d532b45`・force なし）→ `origin/master`＝`master`＝`d532b45`。Vercel の master 自動 deploy のみ（手動 deploy なし）。**Production Deployment Verification**：push（05:34:51 JST）の約 10 秒後に新 asset を確認。canonical URL・no-cache で oni 200・768×768・123,010B・`6c25cdb8…`／ryujin 200・768×768・173,286B・`76266bb4…`／onryo 200・576×768・aspect 0.7500・147,326B・`96204054…`／juuma 200・`4356913d…` 不変／Gen B 不変。旧 hash（`f47ccb65…`／`d5e040ae…`／`b1f3e7e5…`）は返らない。**Production Bundle**：実配信 HTML／JS／CSS が決定184 承認 bundle と `cmp` で byte 完全一致＝ゲームロジック変更 0 を Production 実配信物で確認。`/api/`・`/docs/`・`/art-source/` 404。**Production Smoke QA**（本番 URL・Playwright・DPR 指定）：4体×3条件 **12/12 PASS**（broken 0・JS error 0・scrollY 0・overflowX false・box 一致・HUD／手札 5／End Round 正常・カード演出／ラウンド進行／`.god-strike` を 4体で観測・onryo aspect 0.7500 維持）。**Ranking Absence（Production 実測）**：実プレイ 92 リクエスト中 外部オリジン 0・`/api/` 0・ranking URL 0・失敗 0・JS error 0、Ranking UI Home 0／戦闘 0、戦績画面の「ランキング」語は免責文言 1 件のみ、`/api/ranking/{start,submit,leaderboard}` すべて 404（非公開状態から変化なし）、JS `submissionEnabled:!1` 1／`!0` 0。**Neon/Security（Production JS・件数のみ）**：neon／postgres 0・`DATABASE_URL`／`NEON_`／`RANKING_`／bypass 0・credential 0。**Save/Daily Production Smoke**（`save-migration.mjs`・headless・実ユーザー storage 非接触）：通常戦「続きから（大耀・ラウンド2）」再開 PASS・戦績／報酬／神階 preserved・決着可、神域挑戦「続きから」再開 PASS・残り回数 3→2、seed `daily-2026-09-16-enemy_06`（形式不変）・boss 正常・JST 日付キー・storage key `sevengods.daily`、通信 0／エラー 0。**Scope/Safety**：Production assets changed exactly 3、`src/`・CSS・game logic・Daily・Ranking・server・他の敵 変更 0、force push 0・手動 deploy 0・unexpected files 0・secrets exposed 0。**Rollback**：不要（必要時は `origin/master` を `8256b60` の内容へ通常 commit で戻す）。本決定の記録 commit は docs のみ（`docs/` は `.vercelignore` で配信外・push 後に bundle hash 同一を確認）。**これで敵画質改善フェーズ（決定173 Batch A → 世代 A 4体）完了。** 詳細は `docs/ENEMY_GENA_PRODUCTION_RELEASE.md`。 |
| 2026-09-16 | 決定186を追加。「**Phase 7 Return Loop Commercial Direction Audit：B. GO WITH MODIFICATIONS（Confidence: High）— AUDIT / DESIGN ONLY・実装は未着手**」（監査の方向性・判定・優先順位・ロードマップは **AI判断**（CLAUDE.md §6-2。Return Loop を次の主題にする方針は CEO 提示、優先順位の修正は AI 独立監査の結果）。Production Release・Ranking 有効化・Rules 変更は本決定に**含まない**）。**対象**：master `ffd8450`（Production LIVE・決定185）。src／CSS／asset／Production／Ranking／Neon／DB は一切変更していない。成果物は `docs/PHASE7_RETURN_LOOP_COMMERCIAL_AUDIT.md`（20章＋付録A/B）と監査スクリプト `scripts/phase7-audit/{returnLoop,progression,masteryRaw}.audit.ts`＋専用 `vitest.audit.config.ts`（`npm test` には含まれない）。**Current Biggest Weakness**：「最大の弱点は Return Loop」は半分正しい。正確には (a) 戻る理由を支払う装置（自己ベスト・OTOMO・3択報酬）が構造的に弱い——自己ベスト更新率 10〜12%（8〜10戦に1回・最長無更新 14〜16戦・スコア sd≈平均の5%）、神の一撃 0回の試合 30〜57%・童子到達 0〜4%（絆★3 は事実上到達不能）、報酬+1/+2 で平均スコア 959→961→963（体感なし）、(b) 既に実装済みの「次の一歩」表示（自己ベスト差・今日のベスト差・神階次段・神技次ランク・OTOMO 次解放）が Home と結果画面の導線に出ていない（`HomeScreen.tsx` は storage を一切読まず、結果画面の出口は2つのみ）、(c) ふつう難易度は先読みなし AI でも勝率 100%（むずかしい 99.3%）で「生存のスリル」に Return Loop を頼れない。**Decision**：B. GO WITH MODIFICATIONS。方向性「1戦が面白い→1戦ごとに次を遊びたくなる」は正しく現行コアは触らないが、解法は新経済（通貨・EXP・ミッション・battle pass・gacha・PvP・stamina・login bonus・season）ではなく、既存データの再配置＋記録粒度の拡張＋支払い装置の最小修理とする。**Top 3**：(1) Result Hub＝結果画面を4出口（もう一度／デッキを調整／今日の神域挑戦／ホーム）＋「次の目標」1行に、(2) Home Today パネル＝今日の敵・残り N/3・今日のベスト・次の敵まで HH:MM・続きから・神階最高到達と🔒条件・絆の次解放、(3) 49マス到達盤＝7神×7敵の神×敵記録（未踏／撃破／R3以内／無傷／Daily 撃破）と初撃破・神×敵ベストの祝い。**69→80 Roadmap**：P1 表示再配置（Result Hub＋Home Today＋Daily「前回との差」。display-only・src/core diff 0・saveVersion 不変・約4日）→ P2 49マス到達盤（`sevengods.records` に additive・version 据え置き・約4日）→ P3 OTOMO／Mastery 支払い装置の修理（3a：絆 pt を events ベースに・★3 条件緩和・報酬型別化・deckPreference 神ごと保存＝display/storage 約3.5日／3b：Mastery 7神化・大耀 B 0.37→0.30・蒼毘 S 0.85→0.90・履歴保存＝Rules・gameVersion bump 1回・約4日＋sim）→ P4 デッキ改善ループ＋初戦コーチ3種（約5日）→ P5 Ranking 段階公開（前回との差→percentile→近傍→top10。Daily 日次参加の観測後・CEO §6-3 Gate・**現時点で Ranking は dormant のまま・公開していない**）。P1〜P4 合計約20人日、主要な持ち上げは P1〜P3（推定 Return Loop 48→70・Progression 50→70・Reward 45→60・Replay 60→72）。**Alternatives（却下）**：ChatGPT 案の字義どおりの優先順位（2位「成長・報酬・収集」＝新経済、3位「Daily＋Ranking」同時）は「存在しない問題を新システムで解く」ためリジェクト（Balance／Save リスクのみ増加）；OTOMO 戦闘効果の全面再設計は到達率を直す前では体験に出ないため DEFER；スコア式再設計（テンポ帯 290/290/290/240/170/90/0 由来の分散問題）は Rules＋sim＋gameVersion の大工事のため P1〜P3 後に判断；順位表の先行公開は「空の表」になるため危険。**Evidence（決定論シミュレーション・本番エンジン `applyAction` を Phase 3 ハーネス経由）**：7神×7敵×24 seed×3タイプ（New／Casual／Optimizer）1,176戦/タイプ。Q1 勝率 100/100/100%・平均スコア 859/955/960・撃破R 5.15/4.22/4.13；Q2 自己ベスト更新率 12.1/10.0/12.2%；Q3 同一 seed 反復 21/21 で完全同一スコア、腕前差で +50〜200；Q4 大耀 C 率 New 74.4%／Optimizer 38.7%、蒼毘 Optimizer S 89.9%；Q5 神階 なし 960→Ⅱ 1094→Ⅳ 1216→Ⅵ 1345（勝率 100→97.8%＝倍率であって難度ではない）；Q6 guardian 961／power 962（差なし・童子到達 0〜4%）；R1 報酬 959/961/963；R2 hard New 99.3%；Mastery raw 大耀 New p50 0.32（B 0.37 到達 29%→B 0.30 なら 67%）。**Close 時の再検証（2026-09-16）**：3スクリプトを付録A の SEEDS で再実行し全項目 exit 0・上記数値がすべて再現（出力は scratchpad・コミット対象外）。文書の file:line 参照をサンプル照合し、`useGameEngine.ts` の再戦 seed 行のみ 290→294 に訂正（結論への影響なし）。**Scope**：本 commit は docs＋scripts のみ（runtime diff 0・asset 0・Ranking 0・Neon 0）。`scripts/phase3-audit/out/`・`敵画像/` は含めない。**Next**：Phase 7 P1 の仕様化（display-only）と Playwright Acceptance QA の先行準備。P1 実装は本決定では**未着手**。 |
| 2026-09-17 | 決定187を追加。「**Phase 7 P1 Implementation Gate：Result Hub＋Home Today＋Daily Difference — PASS / READY FOR CEO QA**（Production 反映ではない。branch `feat/phase7-p1-result-home` に commit のみ。push・master merge・deploy は未実施）」（実装方式・細部の判断・QA 設計・PASS 判定は **AI判断**（CLAUDE.md §6-2）。P1 の実装着手は **CEO指示**（2026-09-17、`docs/PHASE7_P1_RESULT_HOME_SPEC.md` を唯一の正式仕様として実装）。**Baseline**：master＝origin/master＝`3ca2435`（決定186）、tracked 差分 0 を確認して同コミットから branch を作成。**実装（仕様 §5〜§9）**：①Result Hub＝結果→次の目標 1 行→Primary（1 つ）→Secondary（最大 2）→スコア内訳（SP は既定で畳む）・神技評価・神階→Tertiary テキストリンク。挑戦状のコピーは金枠ボタンから Tertiary リンクへ降格。報酬未確定の勝利直後は「報酬カードを選ぶ ›」単独を維持（Phase 6-C）。出口は もう一度／デッキを調整／今日の神域挑戦へ／ホームへ／神・デッキを選び直す／挑戦状をコピー（Daily 残り 0 では 神を選ぶ（通常攻略）／戦績を見る）。Primary は通常敗北＝同じ構成でもう一度、Daily 残りあり＝もう一度挑戦（残りN回）、Daily 残り 0＝ホームへ（押せない Primary を出さない）、通常勝利＝次の目標と 1:1。②次の目標＝純関数 `selectNextGoal`（通常 N1 撃破→N2 神階解放→N3 神階突破→N4 今日の神域挑戦が未挑戦→N5 神技の次ランクが近い→N6 自己ベスト 10% 以内→N7 段別ベスト→N8 絆称号まで 3pt 以内→N9 むずかしいで神階解放→N10 既定／Daily D1 撃破→D2 今日のベスト差→D3 さらに更新→D4 終了と次の敵まで HH:MM）。③Home Today＝続きがあれば「続きから（神 vs 敵名・ラウンド）」を最上位 Primary、今日の神域挑戦パネル（敵名・型・神域強化／残り N/3／今日のベスト／挑戦ボタン、残り 0 回のときだけ「次の敵まで HH:MM」）、進行チップ（神階・七柱との絆・自己ベスト）、3 リンク横並び。④Daily Difference＝1 行目 BEST 軸（更新＋幅／あと N 点／同点／まだありません）、2 行目 前回軸（前回→今回と差、両方勝利でラウンドが変わればその変化）。前回は `sevengods.daily.results[]` の直前要素を読むだけで新規保存なし。既知 bug「ベスト未記録の敗北で『今日のベストと同点です』」を解消。**AI 判断で決めた細部**（仕様書 付録 C に全 12 項目）：神技の目標文は Human Play QA（2026-09-06）の方針どおり差分表現「あと○%」を使わず「55% → 58%」形式／進行チップは操作要素にしない（同じ行き先が直下のリンク行にあり、ボタン化すると PC 1508×660 で 3 リンクが画面外に落ちるため）／Daily のデッキ調整は日付をまたいだら神域挑戦画面へ戻す／結果→ホームで保存状態を読み直す（続きから再開した対局の決着後に、決着済みセーブが「続きから」に残る経路を塞ぐ）／Release Gate 用 `scripts/release-audit/{_lib,save-migration,qa-flow}.mjs` の「続きから」「今日の神域挑戦」の探し方をクラスから文言へ（新旧ビルド両方で動く）。**Runtime 変更範囲**：`src/components/`（GameFlow・BattleScreen・GameOverOverlay・HomeScreen・masteryDisplay・otomoGrowthDisplay・新規 nextGoal／dailyDiff／resultHub／resultContext／resultTransitions／HomeTodayPanel）、`src/hooks/`（dailyClock に表示専用の残り時間計算を追加・新規 useMinuteClock）、`battle.css`・`setup.css`。**Invariant Audit**：`src/core` 差分 0（ルール・戦闘計算・カード／敵／神／OTOMO 性能・スコア式・神技評価計算・Seed・PRNG は無変更）、saveVersion 9 不変、gameVersion `1.80c6eda23ed082dc` 不変（`gameVersion.test.ts` PASS）、記録・Daily・セーブ・報酬の storage モジュール差分 0・新規 storage キー 0、Daily の回数消費は `startDailyGame`→`startDailyAttempt` の 1 箇所のまま（ソース固定テストで保証）、`submissionEnabled:false`・Ranking／Neon／API／env 差分 0、asset 差分 0、package／vite／tsconfig／index.html 差分 0。**Tests**：`npm test` **3,101 passed／9 skipped／0 failed**（変更前 3,041 → 新規 +60：nextGoal 21・dailyDiff 11・resultHub 8・resultTransitions 8・dailyClock 5・masteryDisplay 4・otomoGrowthDisplay 3）、`tsc -b` 0、lint error 0（警告 9 は変更前と同じ既存 scripts のみ）、clean build 成功（`index-BNVveIQE.js` 420.25 kB／`index-CqqFbrc7.css` 134.40 kB）。**Playwright Acceptance**（`scripts/phase7-p1/acceptance.mjs`＋`summarize.mjs`、ローカル preview、PC 1508×660・PC 1366×768・SP 390×844・SP 390×760、計 18 シナリオ＋戦闘レイアウト比較）：AC1〜AC7・AC9〜AC14・AC16 と追加 4 項目 **全 PASS（JS error 0）**。主な実測：SP 390×844 の全結果画面で Primary が初期表示内（通常勝利 top 476／844）、全結果画面で初期表示内に別アクティビティ CTA ≥2、次の目標 1 行、挑戦状のコピーは Primary より小さく後ろ、Home CLS 0（4 viewport）・Home のボタン全て 44px 以上、PC 1508×660 で 3 リンク bottom 593／660、戦闘画面 132 要素の offset 箱が変更前ビルド（`3ca2435`）と **完全一致（差分 0、PC・SP）**。Daily 3 回（Home 経由→もう一度挑戦→デッキ調整経由）で attemptsUsed が開始ごとに 1→2→3、デッキ画面では消費 0、seed は全挑戦で `daily-2026-09-17-enemy_05`、残り 0 で Primary＝ホーム・Home は「今日の3回は終了／次の敵まで HH:MM」・開始ボタン無効。通常敗北→デッキ調整で直前の敵を引き継ぎ、続きから再開→決着→ホームで「続きから」が残らないことを確認。**Save 互換**（`save-migration.mjs`、変更前ビルド→P1 ビルド）：通常戦・Daily の途中セーブとも再開 PASS、戦績／報酬／神階 preserved、Daily 再開で回数不変、外部通信・API 0。**Production：NOT DEPLOYED**。次は CEO 実機 QA（通常勝利・通常敗北・Daily 結果・Home Today・続きありの Home）。Production 公開は別 Step（§6-3 #8・Release Gate）。 |
| 2026-09-17 | 決定187に追記（CEO Human QA 結果）。「**Phase 7 P1（Result Hub＋Home Today＋Daily Difference）CEO Human QA：PASS**」（**CEO判断**。対象は決定187 の QA 用 preview（branch `feat/phase7-p1-result-home` `b18964e` のビルド））。CEO コメント（原文）：「見た目はOK。次すすめて」。記録事項：見た目＝OK／重大な UX 問題＝なし（CEO からの指摘なし）／Production Release Gate への進行＝CEO 承認済み（2026-09-17、決定188 として Release Gate の実施を指示）。本追記の時点で master・origin/master・Production は `3ca2435`（決定185 の runtime）のまま。 |
| 2026-09-17 | 決定188を追加。「**Phase 7 P1 Production Release：Result Hub＋Home Today＋Daily Difference＋Next Goal を Production へ公開 — PASS / LIVE**」（Production 公開は **CEO承認**（CLAUDE.md §6-3 #8。決定187 の CEO Human QA「PASS／見た目はOK。次すすめて」を受け、2026-09-17 に Release Gate の実施と Production Release までを指示）。transport 方式・Gate 設計・QA 方式・GO/NO-GO は **AI判断**（§6-2））。**Release Source Lock**：fetch 後に master＝origin/master＝`3ca2435`、feature `feat/phase7-p1-result-home` は `b18964e`（決定187）＋`5ba3ae9`（CEO QA 記録・docs のみ）の 2 commit で master の直接の子孫、tracked 差分 0。`git diff master...feature` を全件照合し 30 files（P1 runtime・tests・`scripts/phase7-p1`・`scripts/release-audit` 互換修正・P1 仕様書・DECISIONS.md の追記 2 行）で想定外 0、除外物（`scripts/phase3-audit/out/`・`敵画像/`・`.env`・credentials・`.claude/settings.local.json`）0。**Pre-Release Gate（dist 削除後に再実行）**：tests **3,101 passed／9 skipped／0 failed**、`tsc -b` 0、lint error 0（警告 9 は既存 scripts のみ）、clean build（`index.html` `401bda17…`／`index-BNVveIQE.js` `e34873d0…`／`index-CqqFbrc7.css` `b979d1f4…`）、Phase 7 P1 Acceptance（ローカル・4 viewport・18 シナリオ）**全 AC PASS・JS error 0**・戦闘画面 132 要素のレイアウトが `3ca2435` ビルドと一致、save-migration（`3ca2435` ビルドの途中セーブ→P1 ビルド）通常・Daily 再開 PASS・戦績／報酬／神階 preserved、Ranking Absence PASS、Secret Audit PASS。**Critical Invariants**：`src/core` 差分 0（スコア式・Seed／PRNG・カード・敵・神・OTOMO・神技評価計算・Daily の日付切替と 3 回制限は無変更）、saveVersion 9、gameVersion `1.80c6eda23ed082dc`、storage モジュール差分 0・新規 storage キー 0、`submissionEnabled:false`、Ranking／Neon／DB／API／env 差分 0、`public/`（敵・カード・OTOMO・神・BGM・SE）差分 0。**Merge/Push**：`git merge --ff-only`（master tree＝feature tree）→ 通常 `git push origin master`（`3ca2435..5ba3ae9`・force なし）→ master＝origin/master＝`5ba3ae9`。**Vercel**：GitHub 連携の自動 deploy のみ（手動 deploy なし）。GitHub Deployments に `5ba3ae9` の Production deployment（state success）、push（03:36:31 JST）の約 30 秒後に新バンドルを配信。実配信の HTML／JS／CSS がローカル master ビルドと `cmp` で byte 一致。dist 208 ファイルすべてが Production と sha256 一致、バンドル以外の全ファイルは `3ca2435` ビルドとも一致（asset 変更 0）。`/api/`・`/docs/`・`/art-source/`・`/scripts/` は 404。**Production QA**（本番 URL・Playwright・ブラウザコンテキストごとに空の localStorage＝実ユーザーの保存と Daily 回数には非接触）：Phase 7 P1 Acceptance を本番で再実行し **全 AC PASS（4 viewport：PC 1508×660・1366×768、SP 390×844・390×760、18 シナリオ、JS error 0）**。Home は今日の敵「双牙の魔獣【連撃型】」と「残り 3/3」が初期表示内・CLS 0・ボタン全て 44px 以上、続きがあると「続きから（大耀 vs 業斧の鬼将・ラウンド2）」が Today より上、決着後にホームへ戻っても続きは残らない。通常勝利は「報酬カードを選ぶ ›」単独 → 確定後に次の目標「今日の神域挑戦：双牙の魔獣に挑む（残り3回）」＋Primary「今日の神域挑戦へ」＋Secondary「デッキを調整／同じ構成でもう一度」（SP 390×844 で Primary top 476）、通常敗北は Primary「同じ構成でもう一度」、デッキ調整で直前の敵を引き継ぎ、既存の再戦も同じ敵で開始。戦闘画面レイアウトは本番でも `3ca2435` ビルドと 132 要素一致。**Daily（本番）**：Home Today 経由→もう一度挑戦→デッキ調整経由の 3 経路で attemptsUsed が開始時のみ 1→2→3（デッキ画面では 2 のまま）、seed は全挑戦 `daily-2026-09-17-enemy_05`、1 回目「今日のベスト 980（1回目）／残り 2 回」、2 回目「✨ 今日のベスト更新！ 8,740（+7,760）／前回 980 → 今回 8,740（+7,760）」、3 回目「今日のベスト 8,740 まであと 8,740 点／前回 8,740 → 今回 0（−8,740）」で Primary「ホームへ」、Home は「今日の3回は終了／次の敵まで 20:20」、神域挑戦画面の開始ボタンは無効。ベスト未記録の敗北は「今日のベストはまだありません」で「同点」表示 0。**Release QA flow（本番・`qa-flow.mjs`・4 viewport）**：通常戦の途中中断→続きから再開→勝利→報酬→Result Hub、Daily→敗北→残り回数、戦績画面（神域挑戦 直近7日）を通しで確認し、API 0・外部オリジン 0・失敗リクエスト 0・JS error 0、ランキング UI と「ランキング／順位」語は結果画面・神域挑戦画面とも 0。**Ranking Absence（本番）**：`/api/ranking/{start,submit,leaderboard}` は GET／POST とも 404、配信 JS の `submissionEnabled:!1` 1／`!0` 0、neon／postgres／pglite 0、`DATABASE_URL`／`NEON_`／`RANKING_` 0、`/api/` 0、credential 形式 0（`leaderboard` 語 4 件は休止中の `RULES.ranking` 設定キーで `3ca2435` の本番バンドルと同数）。**これで Phase 7 P1（Result Hub／Home Today／Daily Difference／Next Goal）は Production LIVE**。Core rules・saveVersion・gameVersion 不変、Ranking は dormant のまま。**Rollback**：不要（必要時は `3ca2435` の内容へ通常 commit で戻す。force push はしない）。本決定の記録 commit は docs のみ（`docs/` は `.vercelignore` で配信外）。次の推奨は P2 実装ではなく **Phase 7 P2 Minimal Specification（49マス到達盤）** の仕様化。 |
| 2026-09-17 | 決定189を追加。「**Phase 7 P2 Implementation Gate：神×敵の攻略記録（Matchup Progression）— PASS / READY FOR CEO QA**（Production 反映ではない。branch `feat/phase7-p2-matchups` に commit のみ。push・master merge・deploy は未実施）」（実装方式・細部の判断・QA 設計・PASS 判定は **AI判断**（CLAUDE.md §6-2）。P2 の実装着手は **CEO指示**（2026-09-17、`docs/PHASE7_P2_49_MATRIX_SPEC.md`（Decision B・Confidence Medium）を正式仕様として実装））。**Baseline**：master＝origin/master＝`0d98728`（決定188 LIVE）、tracked 差分 0 を確認して同コミットから branch を作成。**1 マスの条件**：その神でその敵に 1 回勝つ（通常戦・神域挑戦とも対象。敗北・未撃破・途中放棄は対象外。難易度・神階・自己ベストは条件に含めず保存もしない）。**実装**：①Storage＝新規キー `sevengods.matchups`（v1）1 つだけ。schema は仕様 §11-2 どおり `{ version: 1, cleared: { [godId]: enemyId[] }, seeded: { at, source: 'daily' } }`。集合への追加で冪等、未知 ID と重複は読み込み時に除去、壊れた JSON・構造不正は取り込みからやり直し、将来の版（version > 1）は読み取り専用で上書きしない、localStorage 不可・書き込み失敗では記録しない（例外を外へ出さない）。②Historical Backfill＝キーが無いときに 1 回だけ、`sevengods.daily` の保存期間（日付キー 30 件）にある勝利だけを取り込む。通常戦の過去の勝利は勝利数などから推測しない。既存の daily・records・stakes は読むだけで 1 バイトも書き換えない。③記録点＝`useGameEngine` の決着分岐に `recordMatchupClear` を 1 呼び出しだけ追加。**位置は `recordDailyResult` より前**（後に置くと初回取り込みが今回の Daily 勝利を先に取り込み、本当の初撃破を祝えないため。ソース固定テストで順序と 1 か所であることを保証）。勝敗判定・既存の記録処理・Daily の回数消費（`startDailyAttempt` 1 か所）は不変。④Records＝戦績画面の先頭に「神×敵 攻略」節（敵ごとの行に 7 柱の神チップ。撃破済み＝金の実線枠＋✓＋金色の名前、未撃破＝灰色の顔＋点線枠＋灰色の名前。チップは表示のみ。行ごとの「k/7 神」は 1 柱以上で金、0 は控えめな灰色。節見出しに「N/49」と導入日の注記）。⑤Selection Indicators＝敵選択カードに「✓ {神}で撃破済み／{神}では未撃破」、神選択カードの自己ベスト行に「撃破 k/7 敵」（押して対戦を始める導線は作らない）。⑥Result Celebration＝初めて点灯した勝利だけ結果画面に 1 行「✨ 初撃破：{神} × {敵}（この敵 k/7 神）」、節目は「{敵}を七柱すべてで撃破」「{神}で七体すべての敵を撃破」「四十九の組み合わせをすべて攻略」。母数 49 は節目以外で出さない。2 勝目以降・記録失敗では出さない。報酬・モーダル・通貨なし。⑦Responsive Fix＝Production 既知不具合「戦績画面が 390px で横にはみ出す（神域挑戦の表 443px）」を戦績画面だけのクラスで修正（表は表の中でスクロール、PC の上限幅 960px は不変）。**P1 との整合**：selectNextGoal・Primary／Secondary の規則・Daily Difference・Home Today のソース差分 0、Home の表示は P1 ビルドと同一（実測）。**AI 判断の細部**（仕様書 付録 C に全 11 項目）：記録呼び出しの位置（上記）／保存失敗時は初撃破を返さない／敵の略称を作らず「この敵 k/7 神」／敵選択の未撃破側にも神名／0 の件数を強調しない／未撃破の顔の不透明度 0.62／チップ間隔 3px（SP で 44×49px）／**初撃破の 1 行が出るときだけ高さ 780px 以下の画面で勝利の肖像を 80→56px に縮めて増えた高さを相殺**（P2 ビルドで P1 受け入れ試験を再実行した際、PC 1508×660 の神域挑戦 2 回目の勝利で Secondary が 594→621px に押し出されカード下端 609px を越え P1 の AC10 が 1 件 FAIL したため。修正後は Primary 上端 492px で P1 ビルドの 493px と同等、P1 受け入れ試験は全 PASS に復帰）。**Invariant Audit**：`src/core` 差分 0（バトル規則・ダメージ・AP・カード・敵・神・OTOMO・スコア・神技評価・Seed・PRNG 不変）、saveVersion 9 不変、gameVersion `1.80c6eda23ed082dc` 不変、既存 storage モジュール差分 0・新規キーは `sevengods.matchups` のみ、Daily の回数・seed・日付切替のコード差分 0、`submissionEnabled:false`・Ranking／Neon／API／DB 差分 0、asset 差分 0、package／vite／tsconfig／index.html 差分 0、`useGameEngine.ts` は追加行のみ（削除 0）。**Tests**：`npm test` **3,142 passed／9 skipped／0 failed**（変更前 3,101 → 新規 +41：matchupStorage 23・matchupCelebration 6・配線ガード 12）、`tsc -b` 0、lint error 0（警告 9 は既存 scripts のみ）、clean build（`index-DBIRc_go.js` 427.10 kB／`index-2sSahltn.css` 137.76 kB）。**P2 Acceptance**（`scripts/phase7-p2/acceptance.mjs`、ローカル preview、P1 ビルド＝`0d98728` と並走、PC 1508×660・1366×768、SP 390×844・390×760）：AC1〜AC4・AC10〜AC19 と追加 3 項目 **全 PASS・JS error 0**（AC5〜AC9 は下記の回帰・監査で判定）。主な実測：既存プレイヤー fixture（通常戦 14 勝・神階Ⅶ・神域挑戦 31 日分）で点灯は神域挑戦の保存期間内の勝利 2 組（大耀×業斧の鬼将・才華×乱舞の道化）だけ、範囲外の勝利・敗北のみの日・不正な日付キーは点灯 0、`seeded.source=daily`、既存の保存は 1 バイトも不変／初撃破の結果画面「✨ 初撃破：大耀 × 業斧の鬼将（この敵 1/7 神）」、同じ組み合わせの 2 勝目で祝い 0・保存重複 0、戦績・神選択「撃破 1/7 敵」・敵選択「✓ 大耀で撃破済み」へ即時反映／敗北・途中放棄で点灯 0／神域挑戦の勝利でも点灯し attemptsUsed は開始ごとに 1→2／壊れた JSON でも勝利・戦績表示が正常、`sevengods.matchups` だけ読み書き例外の環境でも勝利・結果画面・戦績が正常（1 行・印は出ない）／P2 で記録と続きのセーブを作って P1 ビルドで開くと続きから再開・戦績とも正常で `sevengods.matchups` は書き換えられない／戦績画面 390px で `scrollWidth=390`・ビューポート高 844（760）のまま、敵名 15px・神名 11px・チップ 44×49px。**P1 回帰**：`scripts/phase7-p1/acceptance.mjs` を P2 ビルドに対して実行し **全 AC PASS**（Daily 3 経路の回数・seed、戦闘画面 132 要素のレイアウトが P1 ビルドと一致、Home CLS 0 など）。**Save 互換**：`save-migration.mjs`（P1 ビルド → P2 ビルド）で通常戦・神域挑戦の途中セーブとも再開 PASS、戦績／報酬／神階 preserved、Daily 再開で回数不変、外部通信・API 0。**既知事項（P2 範囲外・P1 以前から）**：`sevengods.daily` に存在しない敵 ID が入ると戦績画面の神域挑戦表が描画できない（P1 本番ビルドでも同挙動を確認。改ざん・破損時のみ）。**Production：NOT DEPLOYED**。次は CEO 実機 QA（戦績の神×敵 攻略が直感的に分かるか／敵選択・神選択で未攻略の組み合わせを試したくなるか／「49 個やらされる」ではなく「別の神でも攻略してみよう」と感じるか／390px の戦績の横はみ出しが直っているか）。 |
| 2026-09-17 | 決定189に追記（CEO Human QA 結果）。「**Phase 7 P2（神×敵 攻略）CEO Human QA：PASS**」（**CEO判断**。対象は決定189 の QA 用 preview（branch `feat/phase7-p2-matchups` `72725c9` のビルド））。CEO コメント（原文）：「OK」。Production Release Gate への進行は CEO 指示（2026-09-17、決定190 として Release Gate の実施を指示）。本追記の時点で master・origin/master・Production は `0d98728`（決定188・Phase 7 P1 の runtime）のまま。 |
| 2026-09-17 | 決定190を追加。「**Phase 7 P2 Production Release：神×敵 攻略（Matchup Progression）を Production へ公開 — PASS / LIVE**」（Production 公開は **CEO承認**（CLAUDE.md §6-3 #8。決定189 の CEO Human QA「PASS／OK」を受け、2026-09-17 に Release Gate の実施と Production Release までを指示）。transport 方式・Gate 設計・QA 方式・GO/NO-GO は **AI判断**（§6-2））。**Release Source Lock**：fetch 後に master＝origin/master＝`0d98728`、feature `feat/phase7-p2-matchups` は `72725c9`（決定189）＋`c1a54f9`（CEO QA 記録・docs のみ）の 2 commit で master の直接の子孫、tracked 差分 0。`git diff master...feature` の 18 files（P2 runtime・tests・`scripts/phase7-p2`・P2 仕様書・DECISIONS.md の追記）を全件照合し想定外 0、除外物（`scripts/phase3-audit/out/`・`敵画像/`・`.env`・credentials・`.claude/settings.local.json`）0。**Pre-Release Gate（dist 削除後に再実行）**：tests **3,142 passed／9 skipped／0 failed**、`tsc -b` 0、lint error 0（警告 9 は既存 scripts のみ）、clean build（`index.html` `af1cdb6a…`／`index-DBIRc_go.js` `c74b99b5…`／`index-2sSahltn.css` `b12bb0c3…`＝決定189 と同一バンドルを再現）、P2 Acceptance・P1 Acceptance（ローカル RC・4 viewport）**全 AC PASS・JS error 0**、save-migration（P1 ビルド→P2 ビルド）通常・Daily 再開 PASS・戦績／報酬／神階 preserved、P1 ビルドへのロールバック PASS、Ranking Absence PASS、Secret Audit PASS。**Functional／Storage Gate**：1 勝＝1 マス（通常勝利・Daily 勝利で記録、敗北・途中放棄で記録 0、同じ組み合わせの再勝利で追加 0・祝い 0、初撃破は即時反映＋結果画面 1 行）。新規キーは `sevengods.matchups` のみ（非テストのソースが参照する `sevengods.*` キーは同キー 1 つ・`localStorage.setItem` 1 か所）、重複・破損・storage 不可・ロールバック・取り込みの冪等性を単体 23 件＋E2E で確認、既存の records／daily／battleSave は書き換えない。取り込みは Daily 保存期間内の勝利だけで、通常戦の過去の勝利は推測しない。**Critical Invariants**：`src/core` 差分 0（スコア式・Seed／PRNG・カード・敵・神・OTOMO 性能・神技評価計算は無変更）、saveVersion 9、gameVersion `1.80c6eda23ed082dc`、既存 storage モジュール・`dailyClock`・`startDaily` 差分 0（Daily の回数・日付切替の意味は不変）、P1 の判断モジュール（nextGoal／resultHub／resultContext／dailyDiff／resultTransitions／HomeScreen／HomeTodayPanel）差分 0、`useGameEngine.ts` は追加行のみ、`submissionEnabled:false`、Ranking／Neon／DB／API／env／package 差分 0、`public/` 差分 0。**Merge/Push**：`git merge --ff-only`（master tree＝feature tree）→ 通常 `git push origin master`（`0d98728..c1a54f9`・force なし・10:09:35 JST）→ master＝origin/master＝`c1a54f9`。**Vercel**：GitHub 連携の自動 deploy のみ（手動 deploy なし）。GitHub Deployments に `c1a54f9` の Production deployment（state success）、push の約 50 秒後に新バンドルを配信。実配信の HTML／JS／CSS がローカル master ビルドと `cmp` で byte 一致、dist 208 ファイルすべてが Production と sha256 一致、バンドル以外の全ファイル（敵・神・OTOMO・カード・背景・BGM・SE）は P1 ビルドとも一致（新 asset 0・変更 0）。`/api/`・`/docs/`・`/art-source/`・`/scripts/` は 404。**Production QA**（本番 URL・Playwright・独立ブラウザコンテキスト＝実ユーザーの保存と Daily 回数に非接触・4 viewport：PC 1508×660・1366×768、SP 390×844・390×760）：①P2 Acceptance を本番で再実行し **全 AC PASS・JS error 0**。未攻略→大耀×業斧の鬼将で勝利→結果に「✨ 初撃破：大耀 × 業斧の鬼将（この敵 1/7 神）」→戦績で点灯・神選択「撃破 1/7 敵」・敵選択「✓ 大耀で撃破済み」、同じ組み合わせの再勝利で祝い 0・保存重複 0、敗北・途中放棄で点灯 0、Daily 勝利でも点灯（回数は開始ごとに 1→2）。取り込み fixture（Daily 勝利・敗北のみの日・重複・保存期間外・不正日付キー、通常戦 14 勝）は保存期間内の勝利 2 組だけ点灯し既存の保存は 1 バイトも不変。壊れた JSON／storage 不可でも勝利・結果・戦績が正常、本番で作った記録と続きのセーブを P1 ビルドで開いても再開・戦績とも正常で `sevengods.matchups` は書き換えられない。戦績画面は 390px で `scrollWidth=390`・ビューポート高そのまま、敵名 15px・神名 11px・チップ 44×49px（PC 62×49px）。②P1 Acceptance を本番で再実行し **全 AC PASS**（Daily 3 経路で attemptsUsed 1→2→3・seed `daily-2026-09-17-enemy_05`・残り 0 で開始不可と「次の敵まで HH:MM」、戦闘画面 132 要素のレイアウトが P1 ビルドと一致、Home CLS 0、初撃破の 1 行が出る PC 1508×660 の Daily 勝利でも Primary 上端 492px・Secondary 2 つとも初期表示内）。③Release QA flow（`qa-flow.mjs`）：通常戦の中断→続きから再開→勝利→「報酬カードを選ぶ ›」単独→Result Hub、Daily→敗北→残り回数、戦績画面、敗北画面を通しで確認し、API 0・外部オリジン 0・失敗リクエスト 0・JS error 0。**Ranking Absence（本番）**：Ranking UI と「ランキング／順位」語は結果画面・神域挑戦画面とも 0、実プレイ中の ranking 通信 0、`/api/ranking/{start,submit,leaderboard}` は GET／POST とも 404、配信 JS の `submissionEnabled:!1` 1／`!0` 0、neon／postgres／pglite 0、`DATABASE_URL`／`NEON_`／`RANKING_` 0、`/api/` 0、credential 形式 0（`leaderboard` 語 4・`fetch(` 2 は P1 本番バンドルと同数＝休止中の設定キーと既存の SE 読み込み）。**LIVE になったもの**：神×敵 攻略／matchup storage／historical backfill／戦績の「神×敵 攻略」節／選択画面の印／初撃破の 1 行／戦績画面 390px 修正。Core・saveVersion・gameVersion 不変、P1 の整合性維持、Ranking dormant。**Known Issue（Technical Debt・P2 起因ではない）**：保存データが壊れて（または改ざんされて）存在しない敵 ID が入ると、`getEnemyDef` の例外で画面が描画できなくなる箇所が 2 つある——(a) 戦績画面の神域挑戦表（`RecordScreen.tsx` の `getEnemyDef(day.enemyId)`。P1 以前から存在し P1 本番ビルドでも再現）、(b) Home の「続きから」の敵名（`HomeScreen.tsx` の `getEnemyDef(savedBattle.enemy.defId)`。決定187 で追加）。通常プレイでは発生しない。本リリースの payload には修正を含めていない（CEO 指示）。P2 の取り込みは該当日を無視する（単体テスト済み）。**Hardening の推奨（AI判断）**：P3 に混ぜず、**小さな Hardening Patch を先に単独で出す**。理由：影響は読み取り側 2 か所で、未知 ID を「不明な敵」として表示するだけの表示層の修正（`src/core` 差分 0・推定 0.5 人日・単体テスト＋既存の P1／P2 受け入れ試験で回帰確認可能）。P3（OTOMO／Mastery）は絆 pt の計算や Rules・gameVersion に触れうる大きな変更で、混ぜると原因の切り分けとロールバックが難しくなる。(b) は入口の Home に影響するため放置期間を延ばす利点が無い。**Rollback**：不要（必要時は `0d98728` の内容へ通常 commit で戻す。force push はしない。`sevengods.matchups` は P1 ビルドが無視する）。本決定の記録 commit は docs のみ（`docs/` は `.vercelignore` で配信外）。**次の推奨**：(1) 上記 Hardening Patch、(2) Phase 7 P3（OTOMO／Mastery「育成・報酬・収集」）の Minimal Specification／Audit（最新 Production 状態から。実装へ直行しない）。 |
