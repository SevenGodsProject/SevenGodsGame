# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Secrets（Phase 4.4）
# ランキングBackendのDB接続文字列などは絶対にcommitしない。
# `*.local` は既にあるが、`.env` / `.env.production` 等は拾えないため明示する。
.env
.env.*
!.env.example
*.pem
*.key

# Claude Code のローカル専用設定（個人の許可設定。リポジトリでは共有しない。Phase 5-F・決定160）
.claude/settings.local.json
