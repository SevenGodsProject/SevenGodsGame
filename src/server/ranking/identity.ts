/**
 * Phase 4.6（決定139 §3）：匿名identityの照合。
 *
 * ★実装は `src/core/identity.ts` にあり、ここは借りてくるだけ。
 * 端末は秘密を持ち、サーバーは秘密から公開IDを導き直して照合する。
 * 導出をクライアントとサーバーで別々に書くと、いつかずれて全員が弾かれる。
 * だから実装は1か所にしか置かない（`deps.ts` 経由で借りる方針も同じ理由）。
 *
 * ★何を塞ぎ、何を塞がないか
 * 塞ぐ：`playerId` はリーダーボードで**公開**されるので、Phase 4.5 まではそれを
 * 名乗るだけで他人の提出枠とレート制限を食い潰せた（決定139 T1）。秘密の照合でこれを止める。
 * 塞がない：identity の量産（sybil, T3）。匿名を保つ以上、原理的に防げない。
 * 緩和はedge側のレート制限で、アプリ側では止められない（決定139 §3-3）。
 *
 * ★秘密は保存しない
 * 照合に使って捨てる。DBに列は無く（`schema.ts`）、ログにも応答にも出さない
 * （`secrets.test.ts` が機械検査している）。
 */
export { derivePlayerId, verifyIdentity, isPlayerId, isPlayerSecret } from './deps'
